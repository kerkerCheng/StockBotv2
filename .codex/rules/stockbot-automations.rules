# Codex 無人值守 allowlist——**2026-09-24 起為 0 條**（Phase 1 Step 1.3；C1／C4／C6）。
#
# 無人值守只剩一條路：Windows 工作 StockBotv2-Daily → crons/daily_task.py（封閉步驟清單，程式寫死，
# 由 tests/test_daily_task.py 逐項守）。它不經 Codex，所以這份 rules 對它不適用；daily 裡的 LLM 步驟
# 是 `claude -p` 零工具、只回 JSON、每次檢查 init 能力欄位，寫入由程式驗證後做。
# **Codex 不在任何無人值守步驟裡**；兩個 Codex automation（Daily Brief、Weekly Scan）由使用者停用。
#
# 為什麼清為 0 而不是留著：P0 plan review 第 2、3 輪（plan §0.7 X1、R3-1、R3-2）實測 Codex 的唯讀證明不了
# ——workspace-write 下能改 .env 與 .venv、exec 預設載入使用者層的外掛與 `mcp_servers` 設定、`[windows] sandbox` 住在
# 使用者層 config。留著任何一條 allow，就是留一個不再有人需要、卻仍放行的權限（AGENTS：不得用 broad
# permission 掩蓋整合缺口）。
#
# 退役前的 12 條 exact prefix（歷史，**現在逐條都不是 allow**，由 tests/test_codex_daily_permissions.py
# 用 Codex 自己的 execpolicy parser 證明）：crons\harvest_leads.py、engine_c\etl_yfinance.py、
# scripts\alpha_purity_snapshot.py、scripts\daily_beta_snapshot.py、-m engine_b.cli list、
# scripts\catalyst_watch.py、scripts\outcome_if_settled_today.py、-m engine_b.todo sync、
# -m engine_b.todo standing-go、-m webapp materialize、scripts\publish_daily_brief.py、
# scripts\backfill_fiscal_year_results.py。它們各自的 justification（連哪些主機、有哪道收緊）搬到
# docs/OPERATIONS.md「Sandbox impact review 結論（2026-09-24，Phase 1 Step 1.2a）」；
# 原文見 git 歷史（本檔 2026-09-24 之前的版本）。
#
# 將來若有人想把任何無人值守步驟交回 Codex：先讀 plan §0.7 第 2、3 輪，再做一次 sandbox impact review
# 五步（docs/OPERATIONS.md），同一個 change 改 permission contract test。
