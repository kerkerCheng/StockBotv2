# 抽取 review — `nvda_8k_ex991_20260826_q2fy27_pr.json`

- **reviewer：** Claude Code session 2026-08-27
- **判準依據：** AGENTS.md L4（屬性三分）／L6 Gap 4（類別詞不得推斷具體實體）／L8（sole_source 需客戶端或第三方）／L11（措辭精度）／L15（先解析身分再查權限）；co:* ID 唯一權威為 config/company_identity.json，節點去重對照 Neo4j 現況。

> 本檔記錄自動抽取（`extract.py`，model=claude-sonnet-4-6）產出後，人工 review 移除或修正了什麼。
> `loader/validate.py` 對以下每一項都回報通過——它驗的是 schema 形狀，不是 claim 是否有原文支持（L15）。

## 移除／修正

- 邊 co:nvidia -invests_in-> co:sk_hynix 改為 partnership_with：原文逐字為 multiyear technology partnership，未提任何投資。investment 與 partnership 的責任主體與強度不同（L11）。圖中既有的 co:nvidia -INVESTS_IN-> co:lumentum／co:coherent 另有出處，不受本次影響。
- 節點 prod:vera_rubin 改用既有 prod:vera_verarubin：圖中已有同一平台節點，另建會產生 L6 所述重複命名空間。
- 節點 co:google_cloud 改用 registry 既有 co:google，Google Cloud 併為 alias。
- 節點 co:microsoft_azure／co:oracle_cloud／co:spacexai 原因 registry 缺漏而移除，**2026-08-27 經使用者核准 onboard 後已加回**，並改用 registry canonical ID：`co:microsoft`（Microsoft Azure 併為 alias）、`co:oracle`（Oracle Cloud Infrastructure 併為 alias）、`co:spacexai`。SpaceXAI 的身分已查證：運營 Grok 平台、規劃 Starmind AI 衛星，NVIDIA 官方新聞稿 2026-08-24 具名，私有公司故 research_ticker 明確為 None。⚠ SpaceXAI 的 deploys 目標使用既有 `prod:vera_verarubin`（Vera / VeraRubin CPU Platform）而非另建 `prod:vera_cpu`：原文寫的是 Vera CPUs，屬該平台範圍內，另建會產生重複節點。原文對五家 rack 夥伴用現在式（running），對 SpaceXAI 用未來式（will deploy），差異已寫入 cl4。
- 節點屬性 ramp_difficulty_intrinsic（prod:vera_rubin=4、tech:cpo=5、tech:next_gen_memory=4）全部移除：原文無支持；且 tech:cpo 是既有節點，單份抽取不得覆寫其既有屬性。
- 節點 tech:next_gen_memory 與其 develops／enables 邊移除：與既有 tech:hbm／tech:dram 語意重疊，且 issuer 只寫 next-generation memory 類別詞。
- 邊 tech:cpo -enables-> tech:ai_compute_buildout 移除：原文未陳述此因果。
- co:spacexai 另有疑點：原文字面為 SpaceXAI，與既知 SpaceX／xAI 的關係未經查證，不得逕自解析（L15）。

## 新增（非原抽取產物）

- 邊 tech:pluggable_optics -competes_with-> tech:cpo（confidence 0.6）：issuer 未明述競爭關係，係由「同一 Spectrum-6 產品同時支援兩種方案」推導；兩個技術名皆在 quote 中逐字出現，故非實體幻覺，推導性質已寫入 cl1。此 relation 屬 schema/vocab.json 的 counter_path_relation，會影響 Engine D 的 counter_path_missing 判定，故刻意顯性建立而非略過。

## 入圖後發現並修正（2026-08-27）

- **`tech:pluggable_optics` 是我自己造成的重複節點。** 圖中早已有 `tech:pluggable_transceiver`
  （name「Pluggable optical transceiver」，aliases 含 OSFP／pluggables，同為 module_subsystem），
  且已帶 `tech:cpo -COMPETES_WITH-> tech:pluggable_transceiver`。
  成因與我在本檔上方指出的 `prod:vera_rubin` 重複是**同一個**：入圖前查既有節點時，
  我搜的是 optic／cpo／laser，而該節點的關鍵詞是 **transceiver**——關鍵字沒中不等於節點不存在（L11 第 5 點）。
  已改用既有節點，並移除我新增的重複 `competes_with` 邊（原 e5）；`prod:spectrum_6 -depends_on->`
  改指向既有節點。**這也說明為什麼節點去重不能靠關鍵字搜尋，需要確定性的相似度比對。**

## 狀態

- 已通過 `python -m loader.validate`。
- **尚未入圖**：graph admission 需使用者明確核准（AGENTS.md 人工 gate）。
