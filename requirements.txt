anthropic
neo4j
python-dotenv
jsonschema
# Engine C
yfinance
psycopg2-binary
# Source Fetcher
requests

# 非美股 filing 多半只有 PDF：台股 MOPS 電子書年報、日股 TDnet 決算短信
# （fetchers/mops.py）。美股 EDGAR 走 HTML 故 edgar.py 不需要這個。
pypdf

# pypdf 按內容流順序吐字，遇到**雙欄排版就會把兩欄交錯**——IQE FY2025 年報的
# 查核報告那段抽出來長這樣：「IQIQE EA Annnnuuaal lR Reeppoorrtt」，逐字引用因此
# 不可用（2026-09-10 實測，[486] 卡在這裡）。pdfminer 的 LAParams 會先還原版面
# 再取文字，是雙欄 filing 的唯一解。純 Python，不需要 poppler 那種系統二進位。
# 兩者並存不是重複：pypdf 快且夠用，pdfminer 只在版面出問題時才動用。
pdfminer.six

# Local-first audio transcription for source tracing. Model weights are cached
# under ignored library/private/ on first use.
faster-whisper==1.2.1

# Google Sheets (personal portfolio — fetchers/gsheets.py, build_dashboard.py)
google-auth
google-auth-httplib2
google-api-python-client
# Drive 備份用 OAuth user credentials（scripts/backup_private.py auth）；
# service account 已實測無 storage quota，個人帳號只能走這條。
google-auth-oauthlib

# Graph MCP server. Stable v1 only; v2 is a deliberate breaking migration.
mcp>=1.28,<2
