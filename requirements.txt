anthropic
neo4j
python-dotenv
jsonschema
# Engine C
yfinance
psycopg2-binary
# Source Fetcher
requests

# Local-first audio transcription for source tracing. Model weights are cached
# under ignored library/private/ on first use.
faster-whisper==1.2.1

# Google Sheets (personal portfolio — fetchers/gsheets.py, build_dashboard.py)
google-auth
google-auth-httplib2
google-api-python-client

# Graph MCP server. Stable v1 only; v2 is a deliberate breaking migration.
mcp>=1.28,<2
