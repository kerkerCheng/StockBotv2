# 方向重審決定紀錄：圖是中心，排序退役（2026-09-22）

> **性質：決定紀錄，不是 plan。** 2026-09-22 一次互動 session 的 brainstorm 收斂結果，使用者逐條定案。
> 之後的文件整理（`AGENTS.md`、`skills/development-flow/SKILL.md`、`docs/ROADMAP.md`）與給便宜模型的實作 plan
> **全部從本檔導出**；本檔與它們衝突時，以本檔為準，並回頭修本檔或提 amendment。
>
> **前置素材：** [`2026-09-21-graph-consumption-vs-ranking.md`](2026-09-21-graph-consumption-vs-ranking.md)
> 是待否證素材；本檔把其中被使用者採納的部分定案，未採納的留在原檔不動。
>
> ⚠ **第 1 節所有數字是 2026-09-22 的實測快照，附查證命令；判準不隨數字腐壞，數字會。**
> 引用前先跑（`AGENTS.md`「現況數字會過期，判準不會」）。

---

## 0. 使用者原話（逐字，不改寫）

- 方向：「把圖建好建更完整 > 把圖讀懂，不是單純撈公司然後一跳然後看邊的 substitutability 就濾除 >
  最終再用財務數字『輔助』，給我們一個進場退場時間點」
- 前提：「請不要被現在的系統限制 只有 graph 本身的存在是必要的 所有 schema、agent.md、lesson learn 都可以改
  不要把他們當圭臬」
- 對現況的判斷：「現在還是看到了最終我們的 schema 還是圍繞著市值 eps 倍率 盈利率等等 最終只會得到跟市場一樣的結果」
  「整個系統架在後面一大坨財務上，最終圖的資訊全被淡化掉了」
  「為了初期的三四檔，就建了一套特地 match 他們的情況的架構。但這種單一架構符合各股或各產業的需求這種事情本身就不存在吧」
- 文件：「我還是很在意 roadmap 與 agent 的精練程度⋯⋯這裡面的幾句話就會對整個開發影響很大
  像是我們之前把排序這件事看太重也是這個原因？」
- Lesson：「一條都不刪 但不合時宜的敘述就要改 也可以移動」→ 同日修正：「lesson 也不是絕對不能刪
  若你覺得改不動 實在衝突太大 也可以考慮刪除 但要慎重」
- 等待：「我看起來有很多東西是需要後日再來 trigger 或之後放入 waiting，但感覺我們等的東西越來越複雜，
  我希望這部分不要讓他直接消失，要做好且正確，才能在消息出來的時候用我們的心跳馬上跟上；
  心跳部分現在是 non AI 跟 AI 並行，我們可以利用這一點讓他們不靜默」
- 收斂方式：「先不寫 plan 單純 brainstorm 來回 最終確定一個方向再重新整理 roadmap agentmd 再出一個完整 plan
  我會給較便宜的 model 從頭到尾實作」

## 1. 診斷（實測）

### 1.1 圖是公司中心的，不是層中心的

| 事實 | 數字 |
|---|---|
| 技術／產品／材料節點 | 186 |
| 其中圖上只有**一家**供應商的 | **121（65%）** |
| 供應商 0 家 | 30 |
| 供應商 ≥3 家 | 10 |
| 公司節點（非需求錨）走不到任何需求錨的 | 23／79 |
| `substitutability` 覆蓋率（09-21 量） | 17% |
| laser 類技術節點 | 33 個，多數只有 1 家供應商、0～1 條向下邊 |

**判讀：** 65% 單供應商不是世界有 65% 零件獨家，是圖從「某家公司的文件」出發抽它供應什麼長出來的。
任何在這張圖上算「獨家程度」的東西，量到的是**我們讀了誰的文件**。粒度分裂（`cw_dfb_laser` 6 家、
`dwdm_laser_array` 1 家、`wdm_laser_16ch` 1 家）讓同一家公司在三個粒度上競爭地位相反。

```cypher
// 單供應商節點數
MATCH (t) WHERE t.type IN ['TechNode','Product','Material']
OPTIONAL MATCH (c)-[:SUPPLIES_TO|DEVELOPS]->(t) WHERE c.type='Company'
WITH t, count(DISTINCT c) AS ns RETURN ns, count(*) ORDER BY ns
```

### 1.2 注意力被財務層吃掉

| pq2 歷來 650 個編號的來源 | 數字 |
|---|---|
| `decision_lab`（估值／決策層自動鑄） | 257 |
| `engine_c`（財務觀測） | 82 |
| **小計：財務／決策層** | **339（52%）** |
| `research_action`（入圖核准） | 136 |
| `manual` | 144 |

```bash
python -c "import json,collections;d=json.load(open('library/leads/todo_pool.json',encoding='utf-8'));it=d.get('items') or d;it=list(it.values()) if isinstance(it,dict) else it;print(collections.Counter(i.get('source') for i in it).most_common())"
```

### 1.3 排序把公司壓成一條邊

Sivers 在圖裡 **61 條邊**、三條業務線（mmWave、光子、合作夥伴），排序只看到 `tech:cw_dfb_laser` 那條 sub=2。
排序只讀「公司→向下邊」，過濾 sub≥4，`None` 被 `or 0` 壓成 0。

### 1.4 文件的幾句話造成排序中心

| `AGENTS.md` | 值 |
|---|---|
| 字元／行 | 36,385／807 |
| 「必須」「不得」「唯一」「永遠」「一律」 | 49／40／20／15／12 |
| 指名函數、檔案或門檻值的句子 | 213／1,243 |
| Lessons 一節占全檔 | 34% |

直接造成排序中心的四句：「唯一排序權威仍是 `rank_bottlenecks()`」、「alpha 排序必須消費它，不得自建第二套」、
「必須輸出有序清單與明確的首選」、「首選＝filter 不是分數」。ROADMAP 硬約束第 4 條又抄一次。
接著 2026-09-15 事發段寫「七家未填 sub 被當 0 過濾」→ Phase 1 驗收變成「可投資排序要出現台股」→
研究火力去補 sub 讓特定公司過 filter。**這是一條 Goodhart 鏈。**

**機制：** 檔案混了三種內容、同一種語氣。①目標與邊界（五條分離、六 invariant、四 gate、資本硬擋），換掉任何做法都成立；
②當期手段（排序權威、漏斗六條、A/B 表、兩個桿、首選義務），換個做法就變假；③lesson 事發故事。
Agent 讀到「唯一」「不得」時分不出①②。檔案自己有判別法（「換掉 Neo4j 之後還成立嗎」）但只套在憲法節，呈現契約整章免檢。

```bash
python -c "import re;t=open('AGENTS.md',encoding='utf-8').read();s=re.split(r'[。\n]',t);print(len(t),sum('\`' in x for x in s),len(s))"
```

### 1.5 等待機制：registry 存在且沒漏，缺的是語意條件

| Event Watch（`library/leads/event_watches.json`） | 值 |
|---|---|
| 總筆數 | 95 |
| `related_entity_signal`／`entity_filing_signal`／`fact_verification`／`date` | 57／27／9（4 已消化）／2 |
| 過期但未關閉 | **0** |
| 圖上帶 `disproof_condition` 的 claim | 411／411 |
| 反證有消費者的 | **0** |
| 結構讀圖 ledger | 8 筆（2 個節點） |
| thesis lifecycle | 3 筆 |

registry 只認三種機械條件（實體被提到、日期到、財務數字落地）。411 條反證全是散文，需要有人讀新文件才判得出來，沒有機制在讀。
心跳段 2 印狀態不印 diff（尚無昨天快照）。

## 2. 新管線

```
來源選擇：公司的文件 → 層的文件                         【改選源規則】
    ↓
圖：節點／邊／逐字／provenance ＋ 入圖 gate                【不變】
    ↓
走圖（機械，零 LLM）：圖報洞 ＋ lead 點名 → 研究佇列       【建；拆掉排序當驅動】
    ↓
讀圖（LLM，append-only）：層讀圖 ＋ 插槽讀圖              【已有，加一種 kind】
    ↓                                  反證寫下 ──→ 鑄 watch
個股敘事（LLM，append-only）：哪幾層、占多少營收、誰想殺它  【已有，末行加候選狀態】
    ↓                                  缺 X／等回落 ──→ 鑄 watch
三個是非題：會死嗎／已定價嗎／出現在數字裡了嗎             【估值層停跑，換這三題】
    ↓
人決定買不買、買多少；出場認反證；量測 power-law           【不變；量測加主題籃子與圖預測】

        等待 registry（已有，加「語意條件」kind）
        心跳：零 LLM 印 diff ｜ AI 層只讀 T0 篩過的文件 ｜「未檢 N」必印
```

**圖到人之間沒有任何一段算分數。** 今天圖到人之間有十層（排序、結構讀圖、籃子 filter、thesis、估值、多年橋、賭注 overlay、
decision_lab、歸零旗標、敘事、APP 三層）；新管線剩四層（走圖、讀圖、敘事、三題）。

## 3. 決定（G1–G12，使用者 2026-09-22 定案）

| # | 決定 | 理由 | 取代了什麼 |
|---|---|---|---|
| **G1** | **首選與全域排序退役。** 排序拆成兩題：「下一個研究誰」（機械，走圖）與「該買誰」（人讀敘事）。 | 全域全序資料撐不住；「首選」把 UI 便利寫成研究目標 | `AGENTS.md`「唯一排序權威」「必須輸出有序清單與明確首選」；ROADMAP 硬約束 4 |
| **G2** | **研究佇列由圖報洞與 lead 並行驅動。** 優先序由 lead 時間與使用者點名決定，不由分數。 | 有優先分數就是換名字的排序 | 排序驅動研究 |
| **G3** | **財務層收成三個是非題：會死嗎／已定價嗎／出現在數字裡了嗎。** 估值（FY+1 × 倍數）、多年反向橋、賭注四個價格、decision_lab 自動鑄號**在 ROADMAP Phase 0 刪除，git 歷史即封存**（2026-09-22 使用者補充：「拿掉的東西都要拿乾淨」；原寫「停跑、code 留」已改）。 | 倍率的 edge 不在估值精度；停跑而不刪會讓它繼續產 blocker 與 missing，總有一天被升回來；驗收是殭屍 grep 歸零 | 估值層當 gate、`payoff_not_positive`、隱含報酬兩欄拆解 |
| **G4** | **來源策略改層中心。** 抽取從「客戶 filing 的供應商名單、產業報告、規格書、teardown」出發；預算從深挖大公司移到列舉薄層的供應商集合。需求錨仍抽，一季一份法說即可。 | 圖現在是公司中心（1.1）；邊最多的 12 家有一半是不會當 alpha 買的大公司 | 公司年報為主的選源 |
| **G5** | **讀圖分兩種 kind：層讀圖（技術節點＋變體）與插槽讀圖（客戶產品／專案 × 零件）。賭注宣告騎在哪一種上：護城河賭注騎插槽，量的賭注騎層。賭注型別字彙留鬆，A/B 只是前兩個詞。** | 粒度問題不能靠選「唯一正確的層」解；兩種讀圖各答一題 | 單一節點讀圖；A/B 二選一 |
| **G6** | **敘事末行是候選狀態，封閉字彙五個：可開／缺 X／已定價等回落／不要／已持有。** 「缺 X」「等回落」各必指向一筆 watch（有到期）。「不要」附理由、append-only。「可開」＝層或插槽讀過＋三題答過＋無反證觸發，是每檔各自過的 filter，可同時多檔。心跳印各狀態檔數與最老滯留天數。 | 沒有排序後的對稱風險是永遠不收斂；把「為什麼還沒買」變成永遠答得出來的問題，並把等待與收斂綁成同一個物件 | 跨檔首選 |
| **G7** | **等待只有一個 registry（既有 Event Watch），加一種 kind「語意條件」。** 反證與確認事件寫下時自動鑄 watch。AI 層只讀被 T0 實體比對篩過的文件，每日硬上限，命中只標旗不判定；失敗心跳照發，「未檢 N」必印。心跳段 2 改印 diff。到期＝進 pq2 重問，不是丟。客戶節點出新文件自動喚醒它所買的層的讀圖。 | 1.5：registry 沒漏，漏的是語意條件；利用心跳 non-AI／AI 並行 | 反證散文無消費者；「三套併入 Event Watch」的否決**不重開**（那是合併其他機制，本條是加 kind） |
| **G8** | **反證核對從 backlog 升為新 ROADMAP 前幾個 Phase。** 411 條舊反證不全登記，只登記被候選讀圖或敘事引用到的；心跳印「反證在盯 N／未盯 M」。 | 出場只認反證，反證核對就是承重牆 | backlog 一行 |
| **G9** | **量測加主題等權籃子當基準；並量圖自己的預測當快迴路**（讀圖說「只有一家」，半年後客戶 filing 列出第二家＝讀圖錯）。 | power-law 統計量要二三十筆跑一兩年；主題整體漲三倍時一檔兩倍是輸 | 只對 QQQ／SOXX |
| **G10** | **`AGENTS.md` 只寫目標與邊界，不寫手段；ROADMAP 驗收數字只准數圖、讀圖、敘事、等待 registry、追蹤表裡的東西。** 兩條都可機械查（見第 8 節）。 | 1.4 | 手段句與驗收數字綁排序 |
| **G12** | **舊 Decision Store 凍結唯讀；收據跟著成交走（2026-09-22 使用者定案）。** `scripts/record_trade.py` 的成交事件內嵌敘事 digest、讀圖 digest、候選狀態、三題答案、在盯的 watch id、使用者一句理由；5% 單筆與 ETF 槓桿 cap 的硬擋搬到寫 Sheet 之前，超過 fail closed、override 須附理由。decision_lab 目錄只剩唯讀查詢、schema、models、bootstrap、adapters。 | live 收據掛在退役中的 cohort→context→coverage 鏈上，研究側刪掉後舊店沒有寫入入口；實際在用的 Sheet 路徑沒有硬擋也不寫收據，歷來 live_choices 只有 1 筆——煞車要住在真的有人走的路上 | decision_lab「切一半」；「Decision Store 只留可選 receipt」 |
| **G11** | **Lesson 判準句保留；不合時宜的敘述改寫；事發故事可搬出自動載入；真改不動、衝突太大者可刪，但每筆刪除必附理由列在 triage 表供審。** | 使用者原話 | 「lesson 一條都不刪」的絕對版 |

## 4. 三題的正解

### 4.1 收斂 → G6
補充：買不買、買多少仍是使用者。系統只保證「可開」的集合看得到、「缺 X」的每一個 X 有人在等、「不要」有理由。

### 4.2 「已定價」對誰比
- **主參照＝自己：** 現在 EV/S（虧損期用 P/S）落在自己三年區間的第幾百分位，回答「重評價發生過了沒」。
  這是目標真正怕的事（09-15 量到 12／22 檔在漲完之後才入圖）。
- **次參照＝主題等權籃子的中位數，只印當脈絡不當錨。** 2026-09-11 量過「需求同源不代表估值可比」，結論仍成立。
- **第三行：** 近 30／90 天相對籃子的漲幅。
- 三行印在稽核區；敘事的「已定價」一句**必須引用它們**（型別層強制，同首屏「數字由 authority 填 placeholder」）；**不設門檻**，
  要機械化先量測（INV-5）。
- 剛上市或剛轉型者自己的歷史不可比 → 印「無法量：歷史不可比」（`absence_kind`）。
- 籃子定義與 G9 量測基準**共用同一個**。

### 4.3 讀圖單位 → G5
插槽讀圖的五個角度改成：這個客戶的這個產品還有誰在供、各自 `qualification_status`、客戶端逐字怎麼說、
它需要的上游、有沒有反向路徑。Sivers 的答案：層讀圖說 CW DFB 是競爭供應層（B）；插槽讀圖去讀 Ayar Labs 的
`prod:supernova`，Sivers 在那裡是 qualifying，還有誰、Ayar 自己說了什麼，**那才是它的賭注所在**。不改 schema，只在
`READING_KINDS` 加一個。

## 5. 對標（外部參照）

| 參照 | 對應到本系統的哪一段 |
|---|---|
| Cohen & Frazzini 2008《Economic Links and Predictable Returns》：供應鏈連結的消息傳導有滯後 | 「結構出現了但價格還沒動」那個窗的學術根據；G7 客戶節點出新文件喚醒層讀圖 |
| VC power-law 組合：不做 DCF、寫「什麼必須為真」、管出手次數與加碼贏家 | G3 停估值；G6 讓「可開」集合看得到；系統可印「結構確認事件」（合格狀態變 designed_in、客戶 filing 首次具名），加不加碼仍是人 |
| GraphRAG community summary：每子圖預寫摘要、子圖變了才重寫 | 結構讀圖＋digest staleness 同形；社群當單位是 G5 粒度題的一個備選 |
| Fisher scuttlebutt：問客戶與競爭者，不問公司自己 | G4 層中心來源＝它的文件版；L8 是它的縮寫 |
| 量測基準要主題籃子，不只大盤 | G9 |

## 6. 已知問題（寫進 plan 時要盯）

1. **走圖的洞不得變成第二個排序，也不得恆亮。** 「單一供應商」對 121 個節點成立，是恆亮；問句要窄到像
   「需求側說繞不過，但供給側只有一家，且那一家的證據全是自報」。
2. **三個是非題各自宣告資料源與「無法量」出口**，否則某天會長回估值模型。「出現在數字裡」對 Sivers 有分部營收、對台股只有月營收。
3. **誰寫讀圖：** 便宜模型實作管線；讀圖與敘事是判斷，留在互動 session 或有 budget cap 的強模型排程。
4. **AI 層失敗心跳照發**（L13）；「未檢 N」與「未 triage N」同一條規則。
5. **候選狀態「可開」若恆為 0 或恆非 0，都是壞訊號**（L14-4 恆亮／不會滅）。
6. **層讀圖與插槽讀圖的 staleness 要各自算**，插槽的 digest 要含 `qualification_status`。
7. **主題籃子的成分是一個判斷**，寫下時附理由與日期，變動 append-only。

## 7. 拿掉／留下／停跑

| 拿掉（含文件裡的句子） | 留下（不變） | 刪除（git 歷史即封存） |
|---|---|---|
| 排序當唯一權威、首選義務 | 圖、逐字、provenance、入圖 gate | FY+1 估值與隱含報酬 |
| 籃子在排序上套漏斗六條 | 四個人工 gate、五條分離、六 invariant | 多年反向橋當主流程 |
| payoff／隱含報酬當 gate | 結構讀圖 ledger 與 staleness | 賭注四個價格 |
| decision_lab 自動鑄 pq2、sheet_only_holding | thesis／disproof 三件套（L7） | decision_lab 研究側（舊店凍結唯讀） |
| 驗收數字綁「幾檔通過 filter」 | 歸零旗標（三題之一） | |
| | Sheet 是部位真相、不給尺寸、不下單 | |
| | Event Watch registry、心跳三層 | |
| | 量測層（換主統計量與基準） | |

## 8. 文件精練規則與順序

**兩條可機械查的規則：**

1. **`AGENTS.md` 只寫目標與邊界，不寫手段。** 查法：這句話指名了函數、表、門檻值或 Phase 嗎？指名了就是手段，
   搬去 `ARCHITECTURE.md`（做了的）或 `ROADMAP.md`（要做的）。
2. **ROADMAP 驗收只准數圖、讀圖、敘事、等待 registry、追蹤表裡的東西。** 例：薄層列舉完幾層、讀圖幾份、幾檔有敘事加三題、
   watch 幾筆在盯、追蹤表幾筆。**不准數「幾檔通過某個 filter」。** 這一條當初就會擋住 Phase 1 的驗收行。
   development-flow 八欄的 Acceptance status 多一格「數的是哪一層的東西」，違反直接 NO_GO。

**順序（每一步只能從上一步導出）：**

| 步 | 做什麼 | 產出形式 | 為什麼在這個位置 |
|---|---|---|---|
| 1 | 決定紀錄 | 本檔 | 之後全部從它導出 |
| 2 | `AGENTS.md` 逐句 triage | 一句一列的表：留／搬去哪／改寫成什麼／刪＋理由。目標三分之一以下。被取代句照 09-16 做法整段封存 archive，不在原地劃線堆積 | ROADMAP 的驗收要被它的「什麼算進展」評判 |
| 3 | `skills/development-flow/SKILL.md`（窄改） | ①驗收規則變欄位；②事發故事搬出、砍與 `AGENT_WORKFLOW.md` 重複的部分，只留四問加三檔；③便宜模型實作時 Z1 以上預設 R1 | ROADMAP 驗收行要寫成它會檢查的形狀 |
| 4 | `ROADMAP.md` 重建 | 只放 Phase 與四欄；amendment 與決定日誌搬到 dated decisions 檔；已撤回的診斷搬 lessons；backlog 對新方向重驗 | 從 1 導出，用 3 的規則寫驗收 |
| 5 | 給便宜模型的完整 plan | 只展開 ROADMAP 第一個 Phase | |

研究 skill（daily-brief、research-drain、alpha-status）裡的首選、籃子、payoff 句子**跟著機制走**：機制在 ROADMAP 前幾個 Phase
被拆時同一個 commit 改，不另開文件 triage。

## 9. 硬邊界（本次不放寬任何一條）

四個人工 gate；五條 authority separation；六條 hard invariant；不重建 Neo4j（資產是 EdgeAssertion 的 provenance）；
系統不給 alpha 部位尺寸、不下單、不連 broker；Sheet 是部位真相；beta 開發凍結；L8 不放寬；
「三套等待機制併入 Event Watch」「peer valuation」等已量測否決項不重開（G7、4.2 都沒有重開它們）。

## 10. 本檔自己的 disproof（L7 套在自己身上）

三個月後（2026-12-22 前後）任一成立，代表方向錯或沒落地，要回來重審而不是加機制：

- 結構讀圖 ledger 份數沒有明顯增加（查：`ls library/private/alpha/structure_readings/ | wc -l`）。
- 候選狀態「可開」恆為 0 或恆非 0。
- pq2 新鑄編號仍有半數以上來自財務／決策層（查：1.2 的命令，只看 `added_at` 在本檔日期之後的）。
- 圖上單供應商節點比例沒有下降（查：1.1 的 Cypher）。
