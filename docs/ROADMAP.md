# StockBotv2 — Roadmap

> **本檔只放 active future work。** 判準與契約在 [`AGENTS.md`](../AGENTS.md)；
> 指令與程序在 [`OPERATIONS.md`](OPERATIONS.md)。
>
> **2026-09-16 轉向：** 前一版路線圖（Alpha Research Refactor Phase 0–8、研究閉環 P0–P7、賭注 V0–V4、
> Post-MVP A–H、開放 backlog 約 90 列）已**整份逐字封存**於
> [`docs/archive/roadmap-pre-alpha-edge.md`](archive/roadmap-pre-alpha-edge.md)（照 2026-09-03 先例）。
> 每個標題與每一列未結案項的去向見
> [`docs/refactor/alpha-edge-step0-migration.md`](refactor/alpha-edge-step0-migration.md)。
> 轉向的決定紀錄（使用者原話、D0–D15、驗收、紅隊）是
> [`brainstorms/2026-09-16-alpha-edge-discovery-requirements.md`](brainstorms/2026-09-16-alpha-edge-discovery-requirements.md)。

---

## ⚠ 開發項只住這裡，不進 pq2（2026-08-31 使用者定案）

**本檔是系統開發項的唯一載體。** 改的是程式、config、schema 或呈現邏輯，而不是圖／
Engine C／thesis／資本裡的任何一筆事實 → 它是開發項，寫進本檔，**不鑄 pq2 編號**。

判準：**`go` 之後改變的是「我知道什麼」還是「系統怎麼運作」？** 前者是研究（pq2 編號），
後者是開發（本檔）。例如「補某條邊的 substitutability」改變圖裡的事實＝研究；
「改 `rank_bottlenecks` 的排序鍵」改變系統行為＝開發。

理由是**兩種東西的決策資訊完全不同**：研究項要的是「證據夠不夠、授權到哪」，一行決策行
就夠；開發項要的是「這會讓哪個數字變、驗收條件、與其他開發項的相對優先序」（L14 第 5 點），
而那些只有在本檔的表格裡排得出來。

系統主動提出的開發構想寫進本檔待排程，**不主動要求 `go`**。開發項落地後若要動圖或
authority，那是另一個 pq2 編號。判準全文見 [`AGENTS.md`](../AGENTS.md)「授權載體唯一」。

**每項強制四欄：做什麼／為什麼／驗收條件（哪個數字會變）／前置。**
沒有驗收條件的不准進佇列（L14 第 1 條）。


---

# 現行路線圖：Alpha Edge——邊緣小公司的 power-law 漏斗

**North Star：** 系統要能回答
**「哪些邊緣小公司會因為集中需求（今天是 AI capex）卡在它們身上而被放量；我們多早、憑什麼、從哪個管道知道；
事後哪個管道與哪些特徵真的產出了贏家？」**

使用者原話（2026-09-15～16）：「我們要往 AXTI 或像台股之前上銓或其他光通的邊邊走，搭配光通現在的集中度，
這樣才有機會被放量，這確實才是我最初想像的目標，現在確實花太多力氣在 beta 上。」
「10 倍當然只是目標，2、3、5 倍我都很 OK，但主要是想表達是這種等級的倍率，而不是 beta 波動。」
「alpha 部位結構不變就一直抱。」

```
發現（誰值得進佇列） → 篩選（它是不是倍率候選） → 表達（怎麼買、怎麼抱、怎麼砍） → 量測（哪個管道與特徵產出贏家）
```

**為什麼轉向（2026-09-15 blind-spot-audit 的三個 🔴，數字為當時實測）：**
①**儀器對不上目標**——FY+1 EPS × 目標倍數找 20% 錯價是高覆蓋大型股的工具；COHR 首屏「賭對了值 244、現價 266」
是這個錯位的直接產物（thesis 講 FY28 產能倍增，模型只有 FY27 一格）。②**想去的地方排序看不到**——上詮、聯亞、華星光、
全新、IET-KY、Sivers、Aehr 在圖裡都有邊、也接得到需求錨，但 `rank_bottlenecks` 把未填 `substitutability` 的邊當 0
過濾（門檻 4），可投資排序 37 列一檔都沒有；整張圖覆蓋 15%，填了的全是大公司。③**研究火力與資本落點錯位**——alpha 格
實際 1.6%（目標 10%）；22 檔追蹤等權 −3.0% vs QQQ；12/22 在漲完之後才入圖；單一帳號兩個月 492 則：65% no-go、
31% parked、4.5% 進圖。

## 四層漏斗：已有／缺（架構骨架；解法留給各 Phase 的 PLAN_PROPOSAL）

| 層 | 要回答的問題 | 已有 | 缺 |
|---|---|---|---|
| 發現 | 誰值得進佇列 | `crons/harvest_leads.py`（X／RSS／EDGAR，零 LLM）、lead-intake、source-trace、圖的傳播、MOPS／MFN／RNS fetcher | 帳號登記表與計分表、來源標籤跟著 lead 走到 outcome、傳播到小公司（「誰供應這個供應商」）、MOPS 重訊與月營收 watcher |
| 篩選 | 它是不是倍率候選 | `rank_bottlenecks`、L8、五項核驗清單、`analyst_count` 快照 | 機械條件（覆蓋家數上限、市值上限、瓶頸業務占營收下限、至少一條外部印證的瓶頸邊、入圖前 30 天漲幅上限、12 個月內指名假設的催化劑）；INV-3 逐檔報 input／accepted／filtered／reasons |
| 表達 | 怎麼買、怎麼抱、怎麼砍 | thesis lifecycle、反證三件套、5% 單筆硬擋、`alpha/reverse` 反向橋 | 多年反向橋（「五倍要什麼為真」）取代 FY+1 EPS；賭注與判斷錯的對稱 overlay；歸零旗標；entry criterion 降為 optional 中的 optional |
| 量測 | 哪個管道與特徵產出贏家 | 22 檔追蹤表、chasing 計數、`hypotheses.py`、`lead_trace_status` 封閉字彙 | 來源歸因欄、帳號計分表、power-law 統計量、回溯評分 |

**篩選是 filter 不是分數**（沿用 2026-09-15 D2）。籃子頁「首選」的三個條件換成漏斗條件；沒有一檔通過就沒有首選，
**不得為了讓籃子非空而放寬條件**——讓它非空的路是研究。

## Phase 表（每項四欄：做什麼／為什麼／驗收哪個數字會變／前置）

> 順序是決定紀錄的建議；Step 0 核准後才出 Step 1 的 PLAN_PROPOSAL，順序可在 plan 裡改，但要說明理由。
> 每個 Step 交回 HUMAN SUMMARY ＋ 八欄；~~Verdict 不是 GO 或有待決問題就停——本路線圖屬 **Z3** 且動到判準句，
> 是常規推進授權的例外 ④（[`AGENT_WORKFLOW.md`](AGENT_WORKFLOW.md) §5）。~~
> **2026-09-16 使用者定案（Phase 1 Step 1.0 交回後）：Verdict 為 GO 且沒有待使用者決定的問題時，直接合併 master 並接續下一個 Step，
> 不逐 Step 請核准。** 仍要停：Verdict 非 GO、有待決問題、動到四個人工 gate／資本／append-only authority、要改 `AGENTS.md` 判準句
> （後者仍須先給五欄 amendment）。四個人工 gate 不因此放寬：pq2 的圖寫入與 Engine C 判讀寫入仍逐筆核准。
> Phase 1 核准的計畫與工單見 [`brainstorms/2026-09-16-alpha-edge-phase1-plan.md`](brainstorms/2026-09-16-alpha-edge-phase1-plan.md)。
> 標記：▶ 進行中｜○ 未開工｜✅ 完成（回填實測 before → after）。研究項（pq2）不占本表，本表只追它的驗收數字。

| Phase | 漏斗層 | 做什麼 | 為什麼 | 驗收（哪個數字會變） | 前置 |
|---|---|---|---|---|---|
| **0** ✅ | — | **文件整併**（2026-09-16 交付並合併）：`AGENTS.md` 呈現契約整章重寫（憲法／六條 invariant／四個 gate／L1–L17／協作邊界**一字不動**）；舊 ROADMAP 整份 archive、換成本表；`ARCHITECTURE`／`OPERATIONS`／`CONCEPTS` 與新目標衝突的段落劃線加註不刪；daily-brief／alpha-status 頂端 scope note；blind-spot-audit 加「瓶頸壽命／擁擠度／週期位置」lens；跑 `scripts/sync_agent_skills.py` | 舊契約每個 session 完整載入，會把新 session 拉回舊目標（D0） | `AGENTS.md` 字元數必須降且內容驗收全過（使用者 2026-09-16 定案，取代「< 34,836／減半」，理由見 migration doc §0）：**36,075 → 34,624** ✅；`grep -c "^### L" AGENTS.md` = 17（**當時值**；L18 於 2026-09-18 新增後為 18，Phase 0 的驗收不因此失效）；`python -m audit invariants` FAIL 0；`python -m pytest tests/test_codex_daily_permissions.py` 綠；每句被移除的舊契約列得出去向 | — |
| **1** ▶ | 篩選（可見性） | **讓邊緣公司浮上排序。** (a) 研究項——已在圖裡的邊緣公司補齊**可替代性、外部印證、瓶頸業務占營收比例**三格（D8；研究走 pq2）；(b) 開發項——D7 初始三檔各測一條 filing 管道：**SIVE.ST**（瑞典 MFN）、**3081.TWO 聯亞**（MOPS）、**IQE.L**（英國 RNS，帶 going-concern 待辦，先測歸零旗標）。<br>**2026-09-16 PLAN 核准拆四個 Step：** **1.0** ✅ 公司名稱解析歸位——排序的證據分級改讀登記表真有的 `display_name`（原讀不存在的 `name`，100 家 0 家解析得到）、比對前去尾端括號註解與法律型態尾綴、補 23 家登記名稱（各自財報封面印的字串）；**1.1** ✅ `fetchers/mfn.py`＋`fetchers/rns.py`（與 `mops.py` 同構；互動式入口，不進無人值守）、`config/source_routes.json` 登記 `.ST`／`.L` 兩階、三條管道各 smoke 一份文件入圖（MOPS 抓聯亞 mtype=A ~~合併財報~~ **個別財報**——2026-09-17 實測聯亞無子公司、財報區合併財報 0 份；MFN 抓 Sivers Q2 2026 期中報告；RNS 抓 IQE FY2025 年度業績全文，含 going concern 附註）；三份 RA 已 prepare 為 pq2 [579][580][581]，~~入圖待使用者批次 go~~（2026-09-17 使用者批次 `go`，六筆全部 apply → push → `complete-ra` 結案）；**1.2** ✅ 研究項（pq2）——七家補三格：可替代性（addendum → `ra_admission`）、外部印證（客戶端／對手方文件；第三方媒體維持「待判定」，決策 C1）、瓶頸業務占營收比例（新增觀測欄位「產品線營收占比」，mechanical；決策 D2）。全新、IET-KY 沒有任何瓶頸邊，是**補邊**不是補格；Aehr 另補一條到需求錨的邊；華星光 sub=2 只重看、不得為進榜改判（**2026-09-17 實測未動，仍是 2**）；**1.3** ✅ 收尾回填（產出即本列驗收欄 ＋ 下方「Phase 1 completion gate 逐項核對」） | 想去的地方在排序裡看不見，不是資料不足，是格沒填**或邊沒有**；三條管道從沒有可重用的抓取器（既有入庫文件全是 session 手抓） | ~~可投資排序（`python -m query.bottleneck`）出現幾檔 TPEx／STO／L（2026-09-16 實測 0）；三條管道各有一份帶 `published_at` 的 SourceDoc 入庫~~（2026-09-16 PLAN 核准更正：L 當時已有 1 檔、三條管道當時已各有手抓的定日文件——照原句什麼都不做就達標，L14）。改為——**1.0** ✅ 證據分級改判 39 條邊（升 31／降 8）、待判定 90 → 53、解析不到的（邊，來源）131 → 83、IQE.L 由第 28 升第 12（雙方聯合）、排序仍 37 列、`python -m audit invariants` FAIL 0（查證：`python -m query.bottleneck --top-n 60`）；**1.1** 由抓取器產出、meta 帶 `published_at`、經 RA 入圖的文件 MOPS 2 → 3、MFN 0 → 1、RNS 0 → 1（2026-09-17：三份已由抓取器產出且 meta 帶 `published_at`＋`published_at_basis`——`mops_3081_separate_financial_statement_202602` 2026-08-12、`mfn_sivers_semiconductors_6543505f_att1` 2026-08-27、`rns_iqe_9588930` 2026-05-28；~~圖中計數在 [579][580][581] go 後才變~~ → **2026-09-17 入圖後實測達標：MOPS 2 → 3 ✅（`mops_3363_annual_report_2025` 2026-05-07／`mops_4979_annual_report_2025` 2026-05-31／`mops_3081_separate_financial_statement_202602` 2026-08-12）、MFN 0 → 1 ✅、RNS 0 → 1 ✅**，查證：`python -c "...MATCH (d:SourceDoc) WHERE d.id STARTS WITH 'mops_' OR d.id STARTS WITH 'mfn_' OR d.id STARTS WITH 'rns_' RETURN d.id"`）；`.ST`／`.L` 各多一階路由 ✅ 4 → 5 條、rung2 `mfn`／`rns` 皆 `verified=true`（查證：`python -m sourcing.routes SIVE.ST`）；IQE going concern 段落由「一手不支持」變可逐字引用 ✅（`library/raw/rns_iqe_9588930.txt` 附註 2.2 全段）；fixed entry 仍 20 條且 permission test 斷言 `fetchers\mfn.py`／`fetchers
ns.py` 不在 allowlist ✅；**1.2**（2026-09-17 全部現跑重量，不抄計畫檔）①~~可投資排序中 TW／TWO／ST 後綴檔數 0 → ≥1（目標 3）~~ **實測 0 → 0，未達**——但這是答案不是資料缺漏：七家的向下邊**全部走得到需求錨**，唯一卡點是 `substitutability` 低於門檻 4（逐檔 reasons 見下方「Phase 1 completion gate 逐項核對」第 5 項）；②`substitutability` 覆蓋 80 → **85／529（16%），未達 ≥86，差 1 條**；③八家「產品線營收占比」4 → ~~**7（AEHR 缺）**~~ → **8／8 ✅ 達標**（2026-09-17 [589] 補上 AEHR：FY2026 10-K『revenues by product category』表，Systems 57.34%／Contactors 29.77%／Services 12.89%，`mo_5a56be9daaef6ad94fd42bab4ee9093a`；**不需要 XBRL companyfacts，那張表本來就在 10-K 正文裡**。查證：`python -m engine_c.set_manual_field --list AEHR`）；④非驗收但實際發生的變化：IQE.L 由第 12 升 **第 9**（Tower 自家公告把該邊由雙方聯合升為外部印證）、公司→向下邊 218 → 222 條、外部印證 28 → 29／待判定 53 → 52／解析不到（邊，來源）83 → 82。查證：`python -m query.bottleneck --top-n 60`、`python -m loader.edge_resolution project --dry-run`、`python -m engine_c.set_manual_field --list <TICKER>`；**1.3** ✅ 收尾實測（2026-09-17）：`python -m audit invariants` **FAIL 0｜PASS 13｜檢查 4,087 筆**；全套 pytest **2,481 passed／1 skipped**；canonical 邊 **529**、materialized 屬性 **358**；`.ST`／`.L` 各 **5** 條路由、`mfn`／`rns` rung2 `verified=true`；`.codex/rules` fixed entry **20** 條；`python -m webapp status` 的 ranking artifact **current**（可行動 37／純結構 37）；待辦池無本 Phase 未決編號（[579]–[585] 全部結案）。**⚠ Phase 1 維持 ▶ 不標 ✅**——驗收行明訂「TW／TWO／ST 仍為 0 就不得標完成」，現況是**研究已做、證據不足以進榜**；缺哪份文件逐項列在下方 completion gate 第 5 項，**不得為了讓籃子非空而放寬門檻 4** | Phase 0 |
| **2** ▶ | 發現／量測 | **心跳＋分類（D12）**：純 Python 排程、固定五段、`config/daily_routine.json` 的 `drain_limit_per_run` 歸零、Codex fixed entry 與 `tests/test_codex_daily_permissions.py` 同一 change 對齊；weekly 同一套 | 研究火力被無人值守 drain 吃掉；心跳被 brief 淹沒；LLM 失敗時整份不發（沒發生與沒看到同形，L13） | 連續 3 天心跳**零 LLM** 成功發出，且每天印出「未 triage N」；`drain_limit_per_run` 由現值 → 0（查證命令見 OPERATIONS「Daily / pq1 / 待辦池的參數」）<br>**2026-09-17 進度（Step 2.1＋2.2 已交付，使用者選 A：獨立 Windows 排程）：** ①`drain_limit_per_run` **5 → 0 ✅**（查證：`python -c "import json;print(json.load(open('config/daily_routine.json'))['pq1']['drain_limit_per_run'])"`）；②`.codex/rules` fixed entry **20 → 15 ✅**（移除五條研究層專用 entry，純收緊；permission test 同 change 對齊，並由 Codex 自己的 execpolicy parser 證明那五條真的不再被允許）；③心跳排程 `StockBotv2-Heartbeat` 已註冊、每日 07:00、`Status=Ready`（查證：`schtasks /Query /TN StockBotv2-Heartbeat /FO LIST /V`）；④以 `schtasks /Run` 走真正的排程路徑實跑一次：`Last Result: 0`、publisher 回 `sent` 2/2 段。<br>**❌ 尚未達標的那一條：「連續 3 天心跳零 LLM 成功發出」——手動觸發成功不算數**（L13-1：驗收條件是產出出現在下游消費者手上）。**最早 2026-09-20 才驗得完**，在那之前 Phase 2 不標完成。<br>**連續 3 天的計數（每天實測一次，不靠記憶）：****第 1 天 ✅ 2026-09-18 07:00:01 自動觸發**——`Last Result 0`、publisher 回 `sent` 2/2 段、心跳檔 `heartbeat_2026-09-18.md`（2,843 bytes）落檔，且本輪新增的兩行（台股月營收新鮮度、無到期的等待）都出現在自動產出的那一份裡。**這是第一次真正的自動觸發**（09-17 09:45 那次是 `schtasks /Run` 手動走排程路徑）。**第 2 天 ✅ 2026-09-19 07:00:01 自動觸發**——`heartbeat exit=0`、publisher 回 `sent` 2/2 段、心跳檔 `heartbeat_2026-09-19.md`（2,972 bytes）落檔。**第 3 天＝09-20，那天驗完才可標完成。**查證：`schtasks /Query /TN StockBotv2-Heartbeat /FO LIST /V` ＋ `library/private/heartbeat/heartbeat_task.log` 逐日一段。<br>⑤Step 2.3（分類層每日硬上限）未開工。計畫與 sandbox impact review 見 [`brainstorms/2026-09-17-alpha-edge-phase2-plan.md`](brainstorms/2026-09-17-alpha-edge-phase2-plan.md)。 | Phase 0；sandbox impact review 五步 |
| **3** ✅ | 發現 | **D5 帳號登記表（封閉清單，tier `probation`／`measured`／`trusted`）、每則貼文蓋章、每週計分表五欄、計分表 materialize 進 APP**；決定紀錄 §6 的回溯評分先用 492 則既有資料跑一版（三段停損；`harvest_config` 加每月 X 總花費上限，超過即停並在心跳印出） | 單一帳號兩個月 492 則只有 4.5% 進圖；沒有量測就不能有第二個帳號；計分表半年才有意義，回溯先補一版 | 計分表有 ≥1 個帳號、~~5 欄有值~~（2026-09-17 使用者核准 A 案改寫，五欄 amendment 見下）**5 欄各自宣告狀態、且缺席種類正確**——有值的有值；等時間的宣告 `insufficient_sample` **並帶到期日**；能力不存在的宣告 `capability_absent` **並指出要建什麼**——、印量測起始日與 n；三個偏差（倖存者／後見之明／單邊上漲）印在表上；`harvest_config` 有每月花費上限；**外加一條有到期的：90 天欄位到 2026-09-27 必須由 `insufficient_sample` 變成有值**，否則是機制壞了不是時間沒到<br>**2026-09-17 交付（計畫與 sandbox review 五步見 [`brainstorms/2026-09-17-alpha-edge-phase3-plan.md`](brainstorms/2026-09-17-alpha-edge-phase3-plan.md)）：**①登記表 `config/signal_sources.json` 升 schema v2，tier 三級封閉字彙＋唯一 loader `engine_b/signal_source_registry.py`（查證：`python -m engine_b.signal_source_registry`）——**1 個帳號，tier 一律 `probation`**（含已有 495 則樣本的那一個；升等走一季一次的 pq2 manual，本模組刻意沒有寫入函式，測試斷言之）；②計分表 `engine_b/account_scorecard.py`＋`python -m webapp materialize --scorecard`（新 state kind `account_scorecard`）；③心跳第 5 段接上（weekly），第 1 段新增「本月 X 花費 / 上限」與預算停抓列；④`monthly_spend_cap_usd = 10` **必填**，載入時驗證，每個 handle 抓前重算本月已花（查證：`python -c "import json;print(json.load(open('crons/harvest_config.json'))['x_accounts']['monthly_spend_cap_usd'])"`）。<br>**實測（x:aleabitoreddit，量測 2026-06-29 → 2026-09-16，具名點名 913 則／40 檔；貼文 495、可計分 335、濾掉 160）：** 30 天 vs QQQ **−2.03%**（n=617）／vs SOXX **+3.06%**（n=617）；**每檔只算最早一次**：vs QQQ **−9.14%**、vs SOXX **+2.14%**（n=38）；點名前 30 天漲幅 **−2.28%**（去重 −5.61%）——**不是追高**；追源成功率 **37.59%**（n=439）；no-go 率 **64.63%**（n=492）。⚠ **兩個基準給出相反符號**，第一次量測就發生——只印 QQQ 會得到「這帳號沒用」，只印 SOXX 會得到「這帳號有 alpha」，兩個都是錯的。<br>~~**❌ 維持 ▶ 不標 ✅——「5 欄有值」今天有兩欄結構上不可能有值**~~（2026-09-17 使用者核准五欄 amendment 的 A 案，驗收行已改寫；下列實測留原地，它是這條 amendment 的依據）：①**點名後 90 天**：最早的點名是 2026-06-29，到 09-17 才 80 天，一次都沒走完（宣告 `insufficient_sample`，**不填 0**）——那是等時間不是等工作，最早 2026-09-27 才驗得完；②**假設命中率**：需要 filing 裁決紀錄，本系統沒有（宣告 `capability_absent`）。<br>**五欄 amendment（2026-09-17 核准 A）：****原 roadmap**「5 欄有值」｜**新觀察**：三欄的「沒有值」性質完全不同——假設命中率＝要建能力（§6 說對了）、90 天＝等時間（最早 2026-09-27）、而 §6 說「只能抽樣」的**追源成功率其實算得回來**（37.59%，n=439，靠 lead 既有的 `trace_status`，不必重跑 trace）；「5 欄有值」把三種下一步完全相反的狀況壓成同一個門檻（L12：一個表示承載兩種語意）｜**proposed change**：改為「5 欄各自宣告狀態、缺席種類正確」，外加 90 天欄位到 2026-09-27 必須有值｜**why**：比原句**更嚴**（原句只要「有值」，新句要「說得出為什麼沒有、什麼時候會有」），且與 `AGENTS.md`「缺席不得被壓成一句無資料、`absence_kind` 由產生它的程式自己宣告」一致——程式已經這樣做了，是驗收行落後於實作｜**impact**：Phase 3 可標 ✅；「要不要建 filing 裁決紀錄」成為獨立的 ROADMAP 項，不再卡住整個 Phase。<br>**2026-09-17 同 change 補上的收緊（放行與收緊必須同時發生，L15）：**`Metric` 新增 `revisit_after`——`insufficient_sample` 必須答得出「哪一天會有值」（實測 90 天兩格皆為 **2026-09-27**＝最早那則點名 2026-06-29 加 90 天），而 `capability_absent` **刻意沒有**這一格且 reason 必須指出要建什麼。守門測試 `tests/test_account_scorecard.py::test_time_bound_absence_carries_a_revisit_date_and_capability_absence_does_not`。查證：`python -m webapp materialize --scorecard` 後讀 `state/account_scorecard.json` 的 `excess_90d_vs_QQQ.revisit_after`。<br>**✅ Phase 3 完成**（驗收逐條：帳號 1 個 ✅｜5 欄狀態齊全且缺席種類正確 ✅｜量測起始日 2026-06-29 與 n 已印 ✅｜三個偏差在表上 ✅｜`monthly_spend_cap_usd = 10` ✅）。 | Phase 2（weekly 算計分表） |
| **4a／4b** ▶ | 篩選 | **D11 篩選層機械條件**（覆蓋家數上限、市值上限、瓶頸業務占營收下限、至少一條外部印證的瓶頸邊、入圖前 30 天漲幅上限、12 個月內指名假設的催化劑）＋籃子首選換成這組條件；**＋Q2：籃子每一列強制「有賭注 或 Abstention」，不准空白**；**＋Q1：籃子宇宙擴到被門檻擋下、但已研究過的那 26 條，分兩個分頁**（2026-09-17 使用者核准 A，五欄 amendment 見 [`brainstorms/2026-09-17-no-evidence-case-zoom-out.md`](brainstorms/2026-09-17-no-evidence-case-zoom-out.md) §7） | 首選是 filter 不是分數；今天的三條是賭注條件，不是倍率候選的條件；需求側節點與供給側候選今天被當成同一種東西（舊 backlog 2026-09-11 列） | filter 報表印 input／accepted／filtered 與 reasons 計數（INV-3）；`top_pick` 的判定條件由三條變六條；純需求側節點從候選集合裡被明確 filtered 而不是消失；**Q2：每一列的 `bet_state` ∈ {bet, abstained, unanswered}，`unanswered` 逐檔計數並出現在心跳第 4 段**<br>**2026-09-17 Q2 已交付（Phase 4 的第一件）：** 落地點是「沒有 variant 假設」那個分支——它先前硬編 `not_yet_recorded`，把「已研究、結論是不下注」和「沒人看過這一檔」壓成同一格（L12），而同一個檔案早就在讀 Abstention ledger（L16：分類有 SSOT 卻沒跟著資料走到需要它的地方）。新增 abstention layer `bet`／subject `variant.overlay`（寫入端零改動，沿用既有 `python -m alpha abstention <T> --add`）。**before → after 實測**（查證：`python -m webapp materialize --basket` 後讀 `basket.json` 的 `bet_ledger`，或 `python crons/heartbeat.py` 看第 4 段）：籃子 filter 理由由 3 種 → 4 種（`no_bet` 拆出 `bet_abstained`）；賭注帳 **有賭注 1｜刻意不主張 0｜欠一個答案 15**（16 檔）——**這 15 才是籃子空的原因，沒有一檔是被 substitutability 或證據強度擋下的**。⚠ 刻意**沒有**自動把估值層的 abstention 當成賭注層的：實測 16 檔有 5 檔（SOI.PA／IQE.L／MP／POET／6324.T）已宣告 `valuation/target_pe`，若頂替會把 5 筆待辦冒充成答案（`SETTLED_ABSENCE_KINDS`）。守門測試 `tests/test_bet_endgame.py`（10 條，含空跑檢查）<br>**2026-09-17 Q1 已交付：** ranking artifact 補上 `filtered_rows`／`filter`（籃子是純函式、只吃 artifact，先前讀不到第二個宇宙）；籃子多一頁「量的候選」，條件四條可機械驗證：**已在出貨**（`qualification_status ∈ {qualified, designed_in}`）、需求錨可達、不是已在護城河籃子裡的、外加 Q2 的賭注契約。**before → after 實測**（查證：`python -m webapp materialize --ranking --basket` 後讀 `basket.json` 的 `volume_filter`）：宇宙由 37 條邊 → **多了 26 條已研究但低於門檻的**（`unfilled` 的 159 條刻意不進來——那是研究缺口，去 pq1）；去重後 **12 家，通過 0 家**；理由分佈 `not_shipping_yet 8｜already_in_moat_basket 3｜no_bet 11`。**真正新出現、已在出貨、有需求錨的只有 2 家：3081.TWO 聯亞與 SHA0.DE**——而它們唯一被擋的理由就是 `no_bet`，**與 Q2 的量測完全一致：擴大宇宙之後，擋路的還是「沒人寫賭注」**。<br>⚠ **量的候選刻意沒有排序、沒有首選**：這些邊是被排序權威濾掉的，它們沒有名次；而 `rank_bottlenecks()` 的排序鍵是為護城河問題設計的（sub 是第二鍵），套到「量」上就是自建第二套評分（AGENTS 明禁）。依 ticker 字母序列出並在頁面上寫明「那是字母序不是排名」。<br>⚠ **D11 六條裡有兩條今天沒有資料源**，已在頁面與 artifact 逐條寫出（`volume_filter.missing_criteria`），不假裝已經全上：①覆蓋厚薄（`analyst_count`、市值）——`alpha_purity_snapshot` 有算，但還沒進 overview；②瓶頸業務占營收——Engine C 的 `product_line_revenue_share` 八家有值，同樣還沒進 overview。③第三條「入圖前 30 天漲幅上限」的資料源缺口見 zoom-out §6b（`technical_observations` 只有 14 檔 beta 商品）。守門測試 `tests/test_webapp_basket.py`（8 條新增，第一條驗**護城河那份逐位未動**）<br>**2026-09-17 併入（使用者定案，Phase 1 §2e 第 4 項）：六個 tech 節點走不到任何需求錨。**~~這是圖的結構缺口，補邊即可。~~ ⚠ **2026-09-17 [589] 實測推翻了這個診斷——它不是一種形狀，是四種，而且其中兩種根本不是研究問題：**<br>①**`constrained_by` 沒有被走訪**（`tech:cpo_full_stack_test`、`tech:inp_dfb_laser`）：圖裡**已經有** `tech:cpo --constrained_by--> tech:cpo_full_stack_test` 與 `co:coherent --constrained_by--> tech:inp_dfb_laser`，但 `query/bottleneck.py` 的 `UPSTREAM_RELATIONS = ("enables", "is_component_of")` 不含它，`build_upward_index` 因此跳過。**這是開發項不是研究項**（改 `rank_bottlenecks` 的行為＝開發）。⚠ **不得用「再補一條 relation 不同但語意相同的邊」去繞過 traversal**——那是用資料操弄排序，比改程式更糟。<br>②**`enables` 在圖中承載兩種語意**（元件使能 vs 需求驅動，L12 一表兩義）：`tech:ai_compute_buildout --enables--> tech:cloud_transceiver` 走的是 `upward[enabler].add(enabled)`，對「需求驅動」型的 `enables` 方向是反的。修法是先分開再各自定規則，不是調參數。<br>③**真的沒有任何需求方邊**（`tech:photodiode`）：只有三家公司 `supplies_to` 它、外加一條它自己的 `depends_on`——沒有人記錄過「誰需要光二極體」。**這一個才是真研究缺口。**<br>⚠ **2026-09-18 量到成因③比原本記的大得多，而且它不會自己變紅：**對每條 accepted 邊比較「從公司走」與「從瓶頸節點走」的需求錨，**accepted 37 列中有 6 條的瓶頸節點走不到任何錨**（`tech:isolator`／`tech:eml`／`tech:ocs`／`tech:inp_eml`／`prod:ph18da`／`co:lumilens`），filtered 185 列中另有 **83 條**。它們**照樣有錨**（`demand_anchor` 是從公司側走的），所以排序上看不出來——這正是為什麼它需要一個編號而不是一個偵測器。已鑄 **pq2 [606]**（研究：找一手文件回答「誰在買它、為了做什麼」，答得出就補一條需求方邊，答不出就記下來——那本身是結論）。⚠ **不得把它做成排序欄位**：「瓶頸節點接不接得到錢」與「這家公司的產出有沒有人在花錢買」是兩個問題，而今天只有後者有消費端。讀法已寫進 `render_markdown` 的表後註（commit `f21732b`）。<br>④**鏈長／中繼節點也到不了錨**（`tech:pluggable_transceiver`：`upward={prod:spectrum_6}`，但 `prod:spectrum_6` 自己也走不到錨）。<br>驗收多兩條：**這六個節點必須在 filter 報表裡以明確 reason 出現、且四種成因要分得開**（不得壓成一句「走不到錨」）；**`constrained_by` 要不要進 `UPSTREAM_RELATIONS` 必須先量「加了之後有幾列真的變了」再決定**（L14-3：先量測後放閘）。查證：`python -m query.bottleneck --top-n 60`（末列的🔴走不到錨提示）<br>**2026-09-18 ①已交付（量測 → 放閘，同一輪）：** ⚠ **量測推翻了「加進 `UPSTREAM_RELATIONS`」這個字面提案——那個方向是反的。** `UPSTREAM_RELATIONS` 的分支是 `upward[src].add(dst)`＝「A 被 B 需要」；而 `A constrained_by B` 要說的是「B 被 A 需要」，與 `depends_on` 同形。照字面加會靜默改掉 `co:iqe` 那條 accepted 列的鏈路（5 節點 → 4 節點，語意變成「IQE 被 InP 基板需要」），而它想解的兩個節點一個都沒解到。**方向的判準不是自由心證**：三份獨立登記處都把 dst 當被需要的一端——`DOWNSTREAM_RELATIONS` 已含 `constrained_by`、`schema/vocab.json` 的 `sole_source_beneficiary_end` 給它 `"dst"`（與 `depends_on` 同格）、圖裡 8 條邊逐條讀都是「A 受限於 B」。**它與 `depends_on` 的差別在證據性質（counter path vs technical dependency，`prompts/extract_system.md` 逐字），不在方向**——所以它同時留在 Engine D 的 `counter_path_relation` 裡，兩個身分不衝突。<br>**before → after 實測**（查證：`python -m query.bottleneck --top-n 60`、`python -m query.structure tech:inp_dfb_laser`）：**accepted 37 列排序位移 0 列、鏈路改變 0 列**（排序權威的既有輸出一字未動，`filter` 的理由計數也未動：`unfilled 156`／`below_threshold 29`）；**filtered 185 列中 2 列由「走不到錨」變成走得到**——`co:aehr_test_systems → tech:cpo_full_stack_test`（錨 `tech:ai_switch`，3 跳）與 `co:macom → tech:inp_dfb_laser`（4 跳），正是本格點名的那兩個節點。⚠ **今天 0 列排名真的變，所以這不算「讓籃子非空」的進展**；它改變的是**那兩個研究工單值不值得做**：假設補上 `substitutability=4`（what-if，不寫圖），放閘前 AEHR 落在無錨區第 **37** 名，放閘後是第 **27** 名。<br>**落地是把分類收斂成一份 SSOT，不是在四個地方各加一個字串**（L16／L17-3 的對稱面）：新增 `query/bottleneck.py::DEPENDENCY_RELATIONS = ("depends_on", "constrained_by")`（語意：src 需要 dst），四個消費端全部改吃它——`build_upward_index`、`query/structure.py` 的 `_DEMAND_INBOUND` 與 `next_layer`、`alpha/providers/graph_neo4j.py::get_supply_exposure`（第四處原本把 `constrained_by` 歸成**下游**曝險，方向反了）。⚠ **第四處今天實際影響 0 筆，而且要說出來**：它只讀 accepted `rows`，而 4 條公司側 `constrained_by` 邊的 `substitutability` 全部未填、全被門檻擋在 `filtered` 裡（查證：`accepted` 的 relation 分佈是 `supplies_to 30／depends_on 7`，`constrained_by` 0）。**它是先修對、等資料到位才會生效的那一類**——補任何一條 `constrained_by` 的 sub 到 4，個股頁的曝險行就會從 `downstream` 變 `upstream`（`briefing/alpha_view/render.py` 逐字印 `direction`）。⚠ **`enables` 刻意不一起處理**：它的官方定義是需求驅動（方向相反），但圖裡也被當元件使能用——那是本格 ②記在案的未解 L12，在它有答案之前把整張方向表泛化出來，等於憑空填一格（L17-4）。**第三個變了的數字是機制自己抓到的，不是我設計的**（L13-2：要驗就驗那個會因為「真的成功」而改變的東西）：Q5 的讀圖 staleness 立刻把 `mat:inp_substrate` 那份讀圖從 `current` 翻成 **`stale`／該重讀**，變化類型 `demand_edges`——因為那份讀圖當初的需求側是 15 條，沒看到 `co:iqe --constrained_by--> mat:inp_substrate`（現在 16 條）。`tech:cw_dfb_laser` 那份維持 `current`（它周邊沒有 `constrained_by` 邊），所以這不是「全部一起變 stale」的恆亮（L14-4）。**下游消費端因此多一筆待辦**：心跳第 2 段的「該重讀」與 pq1 段的 `stale_structure_readings`。⚠ 重讀是研究、只在互動 session（D12），不在本 Step 內完成。<br>守門測試 `tests/test_bottleneck_ranking.py::test_constrained_by_carries_demand_upward_in_the_depends_on_direction`（鎖**方向**，不只鎖「有沒有被走訪」）＋`::test_dependency_relations_stay_consistent_with_the_other_two_direction_tables`（三條不變式：⊆`DOWNSTREAM_RELATIONS`、與 `UPSTREAM_RELATIONS` 不相交、與 vocab 的 `sole_source_beneficiary_end` 一致）＋`tests/test_structure_reading.py::test_constrained_by_answers_demand_side_and_next_layer_not_only_counter_path`。<br>**2026-09-18 下午：驗收條件「四種成因要分得開」已交付（②③④本身仍未解，本項交付的是把它們分開數出來）。** 新增封閉字彙 `ANCHOR_GAP_CAUSES` ＋純函式 `classify_anchor_gaps()`，報表與 ranking artifact 各多一段。⚠ **第①種刻意不在字彙裡**：它 09:10 修掉之後**結構上不可能再被觀測**（那些節點現在走得到上游，根本不進母體），留一個恆為 0 的格子就是 L14-4 的「不會滅＝那是牆不是閘門」；迴歸由既有的方向測試守著。**before → after 實測**（查證：`python -m query.bottleneck --top-n 60` 的「瓶頸節點走不到需求錨」段，或讀 `ranking.json` 的 `anchor_gaps`）：**瓶頸節點母體 159，走不到錨 87（55%）——先前這 87 個沒有任何地方數過它們**，`demand_anchor` 只在表格裡呈現成一格「🔴 無」，那是呈現不是計數；成因分佈 **`no_demand_edge` 51｜`upstream_dead_end` 19｜`enables_direction_unresolved` 17**。⚠ **不變的那一半逐位驗過**：accepted **37 列位移 0**、`demand_anchor` **0 列變**、filter 理由計數未動（`unfilled 156`／`below_threshold 29`）、filtered 仍 185——診斷是新增輸出不是新的排序（守門測試 `test_anchor_gap_diagnosis_does_not_touch_the_ranking`）。<br>⚠⚠ **分開之後立刻推翻了 pq2 [606] 的 scope，而這正是「不得壓成一句」的實測代價**：[606] 點名的 6 個 accepted 節點裡**只有 3 個是真研究缺口**（`tech:isolator`／`tech:inp_eml`／`co:lumilens`）；**`tech:ocs` 是成因②——圖裡已經有 `tech:ai_compute_buildout enables tech:ocs` 與 `tech:optical_circulator enables tech:ocs` 兩條邊，缺的是走訪方向，那是開發項，對它去找一手文件是白做的**；`tech:eml` 與 `prod:ph18da` 是成因④（被 `cloud_transceiver` 與 `co:newphotonics` 需要，斷點在上游）。[606] 的 hint 已補上這段更正，**編號本身不動、仍逐筆核准**。<br>⚠ **成因②同時確認了一件比它更硬的事：`enables` 的走訪方向與抽取定義相反。** `prompts/extract_system.md` 逐字是 `enables` — A's adoption drives demand for B（＝**B 被 A 需要**），而 `build_upward_index` 的 docstring 與程式都寫「`A enables B` ⇒ A 被 B 需要」（`upward[src].add(dst)`）。**兩者相反，而圖裡兩種用法並存**（`tech:poet_optical_interposer enables tech:light_source_chip_to_chip` 是元件使能、`tech:ai_compute_buildout enables tech:cloud_transceiver` 是需求驅動）——L12 實錘。17 個受影響節點裡有 **13 個**的 inbound 是同一個 `tech:ai_compute_buildout`，**不是散亂的**。⚠ **今天不改方向**：全域翻轉會動到 82 條 `enables` 邊與 accepted 37 列的鏈路，而「哪一條是哪一種語意」今天沒有 SSOT；憑空造一個分類就是在沒有事實支撐的地方泛化（L17-4）。修法必須是「先分開再各自定規則」，**且要先量「翻了之後有幾列真的變」**（L14-3），那是下一個 Step。 <br>**R1 自查抓到兩件，已在同一個 change 修掉／寫明：** ①條件原本寫 `if without_anchor:`，於是**修好全部缺口的那一天整段會安靜消失**，與「查詢壞了」同形（L13-2）——改成以母體判斷，零缺口時明講「檢查跑過了」（守門測試 `test_anchor_gap_section_prints_even_when_nothing_is_missing`）。②`no_demand_edge` 的 51 個裡有 **14 個是 `co:` 節點，而其中有一批是需求終點**（`co:apple`／`co:google`／`co:meta`——有人供應它們，但「誰需要 Apple」不會有人記，它賣給消費者、不在錨的字彙裡），對它們做研究永遠不會有答案；另一批（`co:ayar_labs`／`co:lumilens`）卻是真缺口。**今天沒有任何登記處分得出這兩種，所以刻意不發明過濾器**（L17-4），改成把 `by_id_prefix` 報出來讓它看得見。⚠ **這是本項已知的誤報來源，數字 51 要這樣讀。** <br>**2026-09-18 下午：成因②的 what-if 已量完（ROADMAP 自己要求的「先量翻了之後有幾列真的變」，L14-3）——結論是全域翻轉不是解法，下一個 Step 不得照字面做。** 量法：把 `enables` 的走訪改成抽取定義那一邊（`A enables B` ⇒ B 被 A 需要），其餘三族不動，**純 what-if 不寫任何東西**。**三個數字**：①**accepted 37 列鏈路變了 0 列**（失去錨 0、獲得錨 0）——對今天的排序零影響，所以它不是排序問題，是研究缺口記帳問題；②走不到錨 **87 → 74**，但成分是`enables_direction_unresolved` **17 → 0** 的同時 **`no_demand_edge` 51 → 67（+16）**、`upstream_dead_end` 19 → 7——**翻掉一種語意就打壞另一種**（例：`tech:deposition_etch_clean` 原本靠 `enables` 往上走得到 `dram_manufacturing`／`nand`，翻了之後變成「沒有人需要它」）；③filtered 185 列有 **31 列**鏈路變動。⚠ **這正是 L12 的判準訊號「放寬與收緊都能舉出災難」**——它不是參數沒調好，是兩件事被壓在一起，所以修法只能是「先分開再各自定規則」，而**分開需要一個今天不存在的登記處**：哪一條 `enables` 是元件使能、哪一條是需求驅動。⚠ 那是**動封閉字彙／schema**（新 relation 或新邊屬性），~~依 L17-2 是 Z2 且有真的要使用者選的問題（選項至少三個：新增 relation `drives_demand_for`／加一個邊屬性／靠節點登記表分類）~~ **（2026-09-18 下午作廢：使用者要求「走正解不要短解」後把資料讀完，三個選項全部不需要，而「節點登記表」那個是錯的）**。<br>**正解（2026-09-18 定案，證據在下面）：這是「拿掉一個歧義」，不是「加一個判別器」。** 被誤用的那個意思（A 是 B 的必要投入）**本來就有家**——`depends_on`（A cannot function without B）與 `is_component_of`（A is a part inside B）。所以不新增 relation、不加邊屬性、不建登記表，**把誤植的邊放回它本來的家**，`enables` 只留抽取定義那一個意思。<br>⚠ **「節點登記表」被資料直接否證**：`tech:ai_switch` 同時是兩種語意的來源，而且**同一份文件 `coherent_q2fy26_cpo` 就產出了兩個方向**（`ai_switch enables cpo` 與 `cpo enables ai_switch`），同時 `cpo is_component_of ai_switch` 早就在圖裡——同一對節點三條邊、其中一條純重複。**分類不是節點的屬性、也不是文件的屬性，是每一條 assertion 自己的屬性**，只能逐條對來源逐字重判。<br>**三段，順序不可換：** **A 資料歸位**（82 條逐條重判 → prepared RA → **pq2 [607]**，逐筆核准）→ **B 程式歸位**（`enables` 由 `UPSTREAM_RELATIONS` 移到需求方向，**本表的開發項，等 A**）→ **C 防呆**（已於 2026-09-18 交付，見下）。**A 必須在 B 之前**：先改程式會打壞被誤植的那些邊（實測 `no_demand_edge` 51 → 67）。<br>**影響上界（實測）：** 改判後排序母體 **222 → 224（+2 列）**、**accepted 37 列 0 列變**；全改成 `is_component_of` 則母體一列不變——**對排序幾乎沒有風險**，它改的是研究缺口記帳的正確性。<br>**C 已交付（2026-09-18，不需核准的那一段）：** ①`loader/validate.py` 新增方向感知的硬擋——`A enables B` 與（`B depends_on A`／`A is_component_of B`／`B enables A`）同時出現即拒收該文件。**實測鑑別力：239 份既有抽取命中 2 份（0.8%），兩份都是真錯，0 誤傷**——合法共存的 `ai_switch enables cpo` ＋ `cpo is_component_of ai_switch` 沒有被抓（守門測試 `tests/test_enables_direction.py::test_the_legitimate_coexistence_is_not_flagged`）。②既有那 2 份寫進**例外清單並降級成 WARN**，WARN 逐字帶著 `[607]`——**清單只能縮不能長**（要加一筆得改那個檔），[607] 做完應清空，**它就是那個常駐計數器**（L14）。③`prompts/extract_system.md` 的 `enables` 補上 DIRECTION TRAP 正反例——先前三條定義並列時 A／B 角色相反卻沒有任何對照提示。<br>⚠ **順帶記一個自己踩到的**：第一版掃描腳本用 `e.get("src")`（實際欄位是 `src_id`），於是每條邊都變成 `(None, rel, None)`，掃出「239 份命中 34 份」——**成功與失敗在同一個訊號上同形**（L13-2）。若不是順手把命中明細印出來看，那個假數字會直接進判斷。 <br>**✅ 2026-09-18 晚：成因②`enables` 一表兩義**——**資料歸位（pq2 [607]→[608][609][610]）＋程式歸位（本表的開發項）全部完成。走不到錨 87 → 63（−24，−28%）、`enables_direction_unresolved` 17 → 0、accepted 37 列逐位不變。**<br>**A 資料歸位**（migration `loader/migrate_enables_rejudge_20260918.py`，先例是 `migrate_nand_merge_20260910.py`：pq2 核准 → migration，不走 RA）：改 22 份抽取檔（各留 superseded 副本）、**29 條改判＋4 條移除**、重載 → 刪 4 條 assertion ＋ 1 個併走節點 ＋ **30 條孤兒 canonical 邊** → 重投影 187 個 edge_key。⚠ **`sources[].quote` 一個字都沒動**——改的是 relation，那是抽取當下的判讀不是逐字。<br>**圖的 before → after**：`ENABLES` **82 → 63**（−2 刪、−17 改型別）、`IS_COMPONENT_OF` **68 → 82**（+14）、`DEPENDS_ON` **49 → 51**、`SUPPLIES_TO` **206 → 205**、節點 698 → 697、assertion 680 → 676、孤兒邊 0 → 0。⚠ **兩個與預期不符的數字都查過，都是節點合併的正確行為**：`DEPENDS_ON` 只 +2 而非 +3，因為 `tech:inp_eml depends_on mat:inp_substrate` 併進既有的 `tech:eml depends_on …`；`SUPPLIES_TO` −1 同理（`co:coherent supplies_to tech:inp_eml` 併進 `→ tech:eml`，那條邊現在有 2 條 assertion）。**排序 37 列只有一列變，正是那一列**（名次不動）。<br>**B 程式歸位**：`enables` 移出 `UPSTREAM_RELATIONS`，新增 `DEMAND_PULL_RELATIONS`（自成一表而不併進 `DEPENDENCY_RELATIONS`——兩者**證據性質不同**：一個是「沒有它不能運作」，一個是「它的採用把需求拉過來」，合併會讓兩種主張在下游分不開）；`query/structure.py` 的 `_DEMAND_INBOUND` 同步納入、outbound 移除，**兩邊都從同一處讀**（L16）。<br>⚠ **`enables_direction_unresolved` 這個成因格子隨之退場**——修好之後它結構上不可能非零，留著就是 L14-4 的「不會滅＝那是牆不是閘門」。這是本表第二個退場的成因（第一個是 `constrained_by`），兩個都各留一條**方向測試**守迴歸：`test_constrained_by_carries_demand_upward_in_the_depends_on_direction` 與`test_enables_carries_demand_from_the_adopter_to_what_it_pulls_in`。**剩下兩種成因：`no_demand_edge` 57／`upstream_dead_end` 6。**<br>⚠ **`loader/validate.py` 的例外清單同步歸零**：設立時有 2 筆，[607]／[608] 改掉那些邊之後全庫 239 份掃描命中 **0 條**。清單存在的意義就是走到這一格——**它只能縮不能長**，現在起任何文件犯同樣的錯都是硬擋、沒有豁免（守門測試 `test_the_grandfather_list_is_empty_and_the_corpus_is_clean`）。<br>⚠ **兩份結構讀圖都轉 stale**（`mat:inp_substrate`／`tech:cw_dfb_laser`）——走訪改了、它們的判讀基礎就變了，那是 staleness 偵測正常運作。重讀是研究，只在互動 session（D12）。 | Phase 1<br>**⚠ 2026-09-19 使用者核准 amendment：本 Phase 拆成 4a／4b。****4a＝五條不依賴估值儀器的機械條件**（覆蓋家數上限、市值上限、瓶頸業務占營收下限、至少一條外部印證的瓶頸邊、入圖前 30 天漲幅上限、12 個月內指名假設的催化劑）接進 `webapp/basket.py` 的 filter，**照原序做**；**4b＝「payoff 為正」那一條，移到 Phase 7 之後**——它是六條裡唯一依賴估值儀器的，而實測 D11 通過的 5 檔有 4 檔算不出 payoff（80%）。⚠ **明確排除的第三條路**：把「payoff 為正」從首選條件裡拿掉——那會讓籃子立刻非空，正是 AGENTS.md 禁止的「為了讓籃子非空而放寬篩選條件」。amendment 五欄見下方小節。<br>**✅ 4a 的前兩條已接進 filter（2026-09-19）：市值上限與覆蓋家數上限。**正規化與取數搬到 `alpha/providers/market_normalization.py`，`webapp/basket.py` 的 filter 與 `scripts/alpha_screen_check.py` 現在用**同一支**（L16：兩個消費端各留一份必然偏離；重構後量測腳本逐檔數字與判定一字不差）。⚠ **取數在 `materialize` 做、不在純函式裡**：正規化要打 FX，而 `build_basket_artifact` 是純函式、也是測試用假資料驗的那一支。**materialize 不是 request path**，所以打外部不違反 APP 呈現契約。⚠ 取數失敗時那兩條**完全不套**（不是當成通過）——「這一輪沒有量」與「量過而且合格」是兩件事。<br>**before → after 實測**（守門測試 `tests/test_basket_screen_thresholds.py` 6 條）：籃子 **通過 1（LITE）→ 通過 0、首選無**；新增理由 `market_cap_above_max 10`／`analyst_count_above_max 9`／兩個 `unknown` 都是 0。**⚠ 這是合法結果，不是退步**：LITE 市值 **83.5B**、覆蓋 **25 位**——它從來就不是「邊緣小公司」。`scripts/alpha_screen_check.py` 早在同日上午就預告了（「套下去會讓目前唯一的 top_pick 被擋、籃子變空。**那是合法結果**」）。`top_pick_absent_reason` 現在逐條列出八種理由與各自的檔數。<br>**🔶 4a 還剩三條沒接**：瓶頸業務占營收下限、至少一條外部印證的瓶頸邊、入圖前 30 天漲幅上限。（第六條「12 個月內指名假設的催化劑」**已在 filter 裡**，並已於同日拆成四種形狀。）<br>**✅ 4a 第三條也接上了（2026-09-19）：至少一條外部印證的瓶頸邊。**⚠ 看的是該公司在排序裡的**所有**列，不是籃子留下的那一列——「至少一條」的定義就是如此。`self_reported_costly`（自報但寫進 filing）**不算**：付出代價只提高可信度，仍是當事人陳述（L8 要的是客戶端或第三方）。**before → after**：新增理由 `no_externally_corroborated_edge` **7 檔**（GFS／MU／POET／LYC.AX／5802.T／6324.T／000660.KS）；9／16 通過這一條。L14-4 三測試：**不恆亮**（44% 被擋）、**會滅**（補到一條外部印證就滅）、**講得出因果**（只有供應商自報的瓶頸主張不知真假）。<br>⚠⚠ **這一條的註解第一版被我自己寫錯，當場查出來並更正**：原本寫「實測 AVGO／COHR／LITE 都是最前那列自報、別的列才有印證」——**實測是 0 檔有差異**，最前那列就是最強的那條（排序本來就把證據強的排前面）。用全列仍然正確（那是語意），但理由不是我寫的那個。**查一次是 30 秒的事**（L11-6）。守門測試因此照**語意**寫，不照今天的巧合寫。<br>**🔶 4a 還剩兩條**：瓶頸業務占營收下限（`segment_revenue_share` 15／16 有值，但**缺「哪個分部對應哪條瓶頸邊」的對應關係**——那是實質設計不是接線）、入圖前 30 天漲幅上限（價格 16／16 有，入圖日要從追蹤表的 chasing 計數取）。<br>**🔴 第四條「入圖前 30 天漲幅上限」今天結構上做不到——缺的是「入圖日」這個欄位，而它從來沒被記錄過。** 2026-09-19 查證：①`loader/load_to_neo4j.py` **不記載入時間**；②`SourceDoc` 只有 `published_at`（文件發表）與 `retrieved_at`（我們抓到文件）——**兩者都不是「這家公司進入圖的那一天」**；③追蹤表的 `anchor_date` 是 `shadow['as_of']`＝**決策日**，不是入圖日（拿它替代就是 L12：一個表示兩種語意）。<br>**兩個選項，都要有人決定，因為它們量的是不同的東西：****(a)** 用「該公司最早的 `SourceDoc.retrieved_at`」當代理——語意是「**我們開始研究它的時間**」，而那其實比入圖日更貼近 D11 的意圖（不要追已經漲上去的）；**(b)** 在 loader 記下真正的入圖時間戳——正確但只對**未來**入圖的公司有效，現有 16 檔仍然沒有（而且回填等於猜）。⚠ 這與 `multiple_horizon` 是同一個形狀：**要用的分類不存在，而不是取不到值**。 |
| **5** ▶ | 表達／量測 | **D2 對稱 overlay（判斷錯了值多少＝反證觸發後的假設套同一條橋）、歸零旗標（現金跑道／負債／稀釋／going concern，紅黃綠）、alpha 全歸零淨值少幾 %；D3 `realized` 降為提醒不觸發出場；D15 追蹤表三個 power-law 統計量**（12／24 個月內達 2 倍的比例、最大單檔貢獻、籃子總報酬）；舊 backlog「賭注 V4：variant 收斂納入 outcome」併入本 Phase | 尺寸由使用者決定，系統只給三件；出場只認反證；等權中位數量不到 power-law | COHR 與三檔初始標的各有這些格；`library/private/decision_lab/outcome_aggregate.json` 多三個統計量<br>**2026-09-18 交付 D15 三量（本 Phase 的第一件；D2／D3／歸零旗標仍未做）：** 落地在 `scripts/outcome_if_settled_today.py::power_law_aggregate()`（純函式，與 `equal_weight_aggregate` 並列），四個消費端同時接上——腳本 markdown（`render_power_law`）、`outcome_aggregate.json` 的 `power_law` 鍵、brief 首屏一行（`alpha/brief.py::_render_power_law_line`）、APP 部位頁新區塊「有沒有抓到那一檔」（`webapp/static/app.js` ④b ＋ artifact 的 `power_law`）。查證：`python scripts/outcome_if_settled_today.py`、`python -m webapp materialize --positions` 後讀 `positions.json` 的 `power_law`。<br>**真實資料一跑就顯出 power-law 的形狀**（2026-09-18 實測，22 檔）：籃子總報酬 **+0.02%**；**最大單檔 AXTI 等權貢獻 +2.29%、其餘 21 檔合計 −2.27%**；**曾達 2 倍 1/22、現價仍在 2 倍以上 0**（AXTI 期間高點 +124.4%，已回吐到 +50.4%）。等權平均 +0.02% 這個數字**結構上看不到這件事**——那正是 D15 要換主統計量的理由。<br>⚠ **首版的「佔籃子總報酬比例」被真實資料當場推翻**：籃子總報酬接近 0，比例印出 **15636%**。門檻式修法（小於 X% 就回 `None`）會引入一個憑空參數（INV-5），所以改成恆等式 `籃子總報酬 ＝ 最大單檔 ＋ 其餘`——**減法不可能爆**，而且更直接說出「一檔扛了多少、其餘拖了多少」。artifact 實測恆等式誤差 6.5e-19，且 `basket_total_return` 與既有 `aggregate.absolute` **逐位相同**（D15 三個統計量裡「籃子總報酬」本來就在，這裡沒有另算一份）。<br>⚠ **三個刻意分開、不得壓成一個數字的地方**：①`maturity` 的分母是**已滿 12／24 個月的檔數**，今天是 **0**，`share` 回 `null` 不是 `0.0`——「還沒有一檔滿一年」與「滿了但沒翻倍」是相反的結論（L12）；②`reached_2x_ever`（期間高點）與 `reached_2x_now`（現價）兩個都印——D3 定案出場只認反證，抱著回吐是預期內的；③三個已知偏差跟著數字走（AGENTS：計分表必印量測起始日與樣本數）。**尾句由資料決定、不寫死現況**：有檔滿 12 個月的那天 brief 那行會自己改口（守門測試 `test_power_law_render_never_prints_a_hardcoded_current_state`）。<br>⚠ `build_positions_artifact` 的 `power_law` 是**必要參數而非預設 `None`**：預設值會讓「呼叫端忘了傳」與「這次真的沒算」同形。守門測試 `tests/test_opinion_stance.py` 四條（恆等式不得換回比例、分母 0 不得回假的 0%、期間高點與現價要分開、render 不得寫死現況）。<br>**2026-09-18 交付 D2 對稱 overlay「判斷錯了值多少」（本 Phase 的第二件）：** `ASSUMPTION_SCENARIOS` 加第三個值 `downside`，`OVERLAY_SCENARIOS = (variant, downside)`；**型別層規則與賭注完全同一組**——只能是核心 driver、必須 `independent`、必須至少一條 supporting 證據（營運假設與估值假設兩邊都套）。⚠ **第三條就是「這不是 bear case」的閘門**：bear case 的毛病不是它悲觀，是它指不出根據；擋住它的是證據要求，不是命名。<br>**對稱落在程式碼結構上，不只落在文件裡**（這是驗收的一部分）：`_payoff_section(copy=...)` **一個函式兩個實例**、`_overlay_panel()` **一組 lines 兩個 panel**、`_overlay_lines()` **一段 markdown 兩個 scenario**、APP 的 `betBlock(view, cfg)` **一個區塊兩份文案**。兩份各自手寫的渲染會在某次改動後悄悄長出不同的格，而使用者要把這兩個數字**並排讀**。守門測試 `tests/test_downside_overlay.py::test_both_scenarios_go_through_the_same_section_builder`。<br>**read model 的 `downside` 由 `NotModeledSection` 換成 `PayoffScenarioSection`**：它的 status 因此由 `not_modeled`（沒這個能力）變成 `missing`＋`not_yet_recorded`（有能力、這一檔還沒人寫）——**這兩者的下一步完全不同**，`not_modeled` 沒有人該去補，`missing` 有。舊那句「系統不產生下檔估計：沒有 bear case 的數值、沒有最大回撤模型」在 D2 定案後已經是假的，**連同那段程式碼一起移除**，不留一個會被下一個人讀成現況的註解。<br>⚠ **`PayoffScenarioSection` 的欄位名改成中性的 `scenario_internal_eps`／`scenario_fair_value`**：序列化用的是 dataclass 欄位名，若讓 `variant_fair_value` 這個 key 同時裝兩個 scenario 的值，下游只能二選一而兩邊都錯（L12）。**欄位名是結構、`Datum.key` 才是身分**，兩者刻意脫鉤——所以 analyst view 的 line key（`variant_fair_value`／`payoff_return`…）與 APP **一個字都沒動**。<br>⚠ **缺席分兩種且不得互相頂替**：新增 abstention subject `bet/downside.overlay`＋`downside_absence()`（與 `variant_absence()` 並列）。宣告「沒有可辯護的賭注」**不等於**「說不出可辯護的下檔」——前者是不下注，後者是連認錯的門檻都畫不出來；互相頂替會把一筆待辦冒充成答案，那正是 Q2 在估值層踩過的形狀。<br>⚠ **刻意不強制 downside 假設指名某一條 disproof**：反證住在 `AlphaSignal.disproof_conditions`（A3，可重算、沒有穩定 id），從 append-only 的假設 ledger 指過去會造出一個會斷的跨 authority 引用，而那正是 A3／A5 分離要防的。要求「指得出什麼在支撐這個值」的閘門由 `supporting_refs` 承擔。<br>**首屏那把尺多一端**（AGENTS「2026-09-16 起那把尺多一端」）：`brief_scale` 由三個數變五個（多 `downside_target`／`downside_return`），APP 的尺多一個刻度、結論卡多一塊、稽核區多一張逐格表。⚠ **沒寫就不畫、缺席一律 `None` 不是 0**——畫一個 0% 的下檔等於替使用者做一個沒有人做過的主張。<br>⚠⚠ **今天 0 筆真實 downside 假設，所以每一檔畫面上都是「還沒寫」。** 這不是 bug，是binding constraint 的同一個形狀：機制到位了，缺的是有人寫下判斷。**不得把「多了一個空格」當成進展。**查證：`python -m alpha assumptions <T> --list`（有 downside 假設會標 `〔downside〕`）、`python -m briefing alpha <T>` 的 13b 段。守門測試 `tests/test_downside_overlay.py` 八條。<br>**2026-09-18 交付歸零旗標＋alpha 全歸零淨值（本 Phase 的第三、四件）：** 判色規則住 `alpha/wipeout.py`（純函式）；取數住 `engine_c/checklist.py::get_wipeout_inputs`；型別住 `briefing/alpha_view` 的 `WipeoutFlagsSection`；四個消費端——alpha-card 13f 段、analyst-view markdown、APP 個股頁（`wipeout` panel）、心跳第 4 段。<br>**零憑空門檻**：只有符號比較加兩個指得出出處的常數——12 個月（IFRS／UK going-concern 最短評估期）與 365 天（一個完整會計年度）。<br>**實測（2026-09-18，籃子 16 檔 × 4 盞 = 64 盞）：紅 2｜黃 8｜綠 20｜灰（沒量到）34**，有紅燈的是 **COHR 與 IQE.L**（都是負債那盞：淨負債且在燒錢）。查證：`python crons/heartbeat.py` 第 4 段、`python -c "import json;print(json.load(open('library/private/app/state/basket.json',encoding='utf-8'))['wipeout_ledger'])"`。<br>**alpha 全歸零淨值少 1.46%**——它就是 `beta` artifact 的 `risk.snapshot.alpha_total_weight`，**不另算一份**；⚠ 別與同段「alpha 占**已投入非現金** 1.57%」混用，分母不同（NAV 含現金）。<br>⚠ **稀釋那盞的首版被真實資料當場推翻**（第二次了，形狀與 D15 那次相同）：第一版用「窗內有沒有增加」×「燒不燒錢」判紅黃，一跑 16 檔就看到 **COHR +0.10%（54 天，員工股酬等級）與 LITE +15.30%（53 天）拿到同一組規則的顏色**，IQE.L 更是靠 **+0.0044%** 亮紅。那是 L12：`change > 0` 這一個表示同時承載「員工股酬雜訊」與「靠發股補現金缺口」。**修法不是設量級門檻**（憑空參數，INV-5），是把判不出來的那一半留白——窗滿一個完整會計年度才判色，量級問題交給稽核層的數字，而「是不是靠外部資金活著」本來就由現金跑道與負債那兩盞回答了。今天 16 檔的窗是 18–72 天，所以稀釋那盞**全灰且各自帶到期日**（最早 2027-07-08，INV-2：每個等待都必須有到期）。<br>⚠⚠ **going concern 那盞今天結構上點不亮**（`capability_absent`，不是 `missing`）：逐字的審計意見是 `judgment` 欄位，機械比對不得從自由文字推顏色（L15-2）。D7 指定的第一個案例 IQE.L 的 going-concern 逐字**已經在庫裡**，但它躺在 `debt_maturity_and_covenants` 這個鄰居欄位——**分類有 SSOT 卻沒跟著資料走到需要它的地方**（L16）。要讓它變色需要一個結構化欄位，已列進下方 backlog。<br>⚠ **順手修掉一個每天都在說假話的機制**：心跳的 `PENDING_PHASE`（「還沒建的能力指到哪個 Phase」）三筆裡有兩筆已交付（`power_law_stats` 當天 09:31 才交付、`zero_out_flags` 本輪交付），最後一筆指向已完成的 Phase 3，**而且整張表一個 consumer 都沒有**。兩個毛病同一個形狀：不會壞、不會報錯、測試不會紅（L17）。整張表已移除，去向改寫在印那一行的地方（INV-4）。<br>**D3「`realized` 降為提醒不觸發出場」實測已於 2026-09-15 交付**（commit `7e7012f`），本輪只做驗證與一句措辭對齊：`thesis/pending_lifecycle.py` 的註解寫於 D3 定案**之前**，說的是「『對了』也要有出場觸發」。三條守門測試早就在：`tests/test_thesis_realized.py`（含「repo 裡不得有任何程式把 status 寫成 realized」的全檔掃描）。查證：`python -m pytest tests/test_thesis_realized.py -q` → 3 passed。<br>守門測試 `tests/test_wipeout_flags.py` **16 條**，其中三條守的是同一句話：**綠燈只能由「量到了而且沒事」產生**——輸入缺席時四盞燈不得有任何一盞是綠的；灰刻意**不在** `FLAG_COLOURS` 裡（型別層讓「這盞是綠的」與「這盞沒點亮」不可能同形）；排序權威與籃子 `filter_reasons` 都不得讀到它。 | Phase 1 |
| **6** ✅ | 發現／量測 | **D15 台股每月營收納入 Engine C 一手 datum、MOPS 重訊 watcher、parked lead 超過 60 天自動 `expired` 並計數（不刪）** | 台股月營收比季報早；重訊是最早的一手；parked lead 沒有到期就是 INV-2「等待沒有到期」 | 3081.TWO 有月營收序列；`expired` 計數出現在心跳第 3 段<br>**2026-09-17 交付前兩項（三件事裡的第三件已掛成待使用者決定，見下）：**<br>①**月營收**（`engine_c/monthly_revenue.py`＋`fetchers/mops_open_data.py`＋migration`20260917_add_monthly_revenue.sql`）：registry 的 **7 檔台股各 24 個月**入庫（0 → 175 列），**3081.TWO 序列達標 ✅**（2024-09 ～ 2026-08；2026-08 營收 520,469 千元、YoY **+180.90%**，備註逐字「數據中心產品出貨量較同期增加」，且該備註**連續 19 個月**都在）。查證：`python -m engine_c.monthly_revenue --ticker 3081.TWO`。⚠ 兩個來源（當期 opendata 與歷史頁）在 2026-08 交叉比對**逐位一致、衝突 0**；數字不同時列進 `conflicts` 不靜默取一個。<br>②**重訊 watcher**（`crons/harvest_leads.py::harvest_mops` ＋ `crons/harvest_config.json` 的 `mops_watch`）：harvest 來源 **29 → 36 個**（7 檔台股各一個 `mops:` source），首跑抓到 **3105.TWO 穩懋「本公司訂購廠務工程公告」**並正確解析出 `co:win_semiconductor`。查證：`python -c "import json;d=json.load(open('library/leads/pending_leads.json',encoding='utf-8'));print(sum(1 for v in d['leads'].values() if str(v.get('source','')).startswith('mops:')))"`。<br>③**心跳第 1 段多一行**「台股月營收 N/M 檔｜最新 YYYY-MM｜已跟上公告期限／⚠ 落後 N 個月」——**月營收刻意不進無人值守**（歷史頁永久可查、漏抓補得回來，L10），所以不新增 allowlist entry；該補的時候由這一行自己說話（L14：會自己出現的計數器）。<br>⚠ **誠實記下今天的消費端有多窄**（L13-1：驗收是「產出出現在下游消費者手上」；Phase 4c 那個 🔴 記過的就是這件事）：月營收今天只到達**心跳的新鮮度行**與 `python -m engine_c.monthly_revenue --ticker`，**沒有進 APP、沒有進 alpha 的隱含報酬橋、沒有進任何排序**。也就是說使用者今天看得到「有沒有跟上」，看不到「聯亞 8 月 YoY +180.90%」。要讓它改變任何判斷，還需要一個消費端——但**那是呈現契約的決定**（要印哪些數字、會不會變成訊號），不在 Phase 6 的驗收行裡，刻意不順手加。<br>~~**❌ 第三件「parked lead 60 天自動 expired」本輪不做**~~（2026-09-17 使用者核准五欄 amendment 的 **A 案**：縮小成「非終局且沒有 watch 的 parked lead 必須有到期」，**不新增 lead 狀態、不動 `ALLOWED_TRANSITIONS`**。交付見本欄末。）下列實測留原地，它是這條 amendment 的依據：三個實測數字：④池子裡 **479 筆 parked**，`first_seen` **全部在 60 天內**（最早的 lead 是 2026-07-22，整個池子只有 57 天歷史）——**今天套用會讓 0 筆變 expired**；⑤更關鍵的是 `parked` 一格承載兩種東西：**413 筆是 terminal `trace_status`**（追源已完成／不追了／矛盾，多為 Form 4 內部人交易，`research_outcome` 逐字寫著 `no_graph_delta`）——**它們是歸檔不是等待**，標成 `expired` 是語意錯（L12 一表兩義）；⑥真正「非終局且沒有到期」的只有 **4 筆**（非終局 66 筆中 62 筆 park 當下已自動建 Event Watch，[321] 那條修法已經在守）。⚠ 另外 `expired` 若要成為 lead 的新狀態，會動到 `ALLOWED_TRANSITIONS` 這個封閉字彙。五欄 amendment 已備妥，等使用者決定。查證：`python -m engine_b.event_watch counters`、`python -c "...Counter(v['status'] for v in leads.values())"`<br>**2026-09-17 A 案交付（第三件）：**⑦新增 `engine_b.leads.parked_without_expiry()`——對**所有** parked lead 問一個機械問題「有沒有任何東西會讓它回來？」，四條判準全部可機械驗證：有非 consumed 的 watch／有 `pq2_ref`／terminal `trace_status`／以上皆無＝黑洞。⚠ **`parked_reason` 刻意不採信**——它是自由文字，一筆寫著「只是治理公告、無 thesis 內容」的 lead 讀起來像終局但機器讀不到散文（L16-3：自由字串不得決定去留）。<br>⑧**它補的是 `trace_backlog` 看不到的那一塊**：`trace_backlog` 的入口條件是「有 `trace_status` 或 `parked_reason` 含 trace」，park 時沒留下追源標記的連進都進不去——所以 `ensure_trace_watch` 的 docstring 逐字寫的「沒有具名標的就不假裝在等，**由 wake_state=unwatched 現形**」，對這一批是走不到的（L13-1：管子只接了一頭）。實測：`trace_backlog` 回報 `unwatched` **0**，而真實黑洞是 **3**。<br>⑨**before → after 實測（L14：現有資料有幾筆真的變了）：黑洞 3 → 0**。三筆各補一個 Event Watch（`ew_0091`／`ew_0092`／`ew_0093`，到期 2027-01-15），note 逐字寫明「**補的是期限，不是判定它還值得追**」——到期後由使用者決定續等或放棄。<br>⑩心跳第 3 段新增常駐計數器「無到期的等待 N」（0 也印）。查證：`python crons/heartbeat.py` 第 3 段、`python -c "from engine_b import leads;print(len(leads.parked_without_expiry(leads.load())))"`。守門測試 `tests/test_engine_b_leads.py::test_parked_without_expiry_finds_the_holes_trace_backlog_cannot_see`（四條判準各驗一次）＋`::test_parked_without_expiry_ignores_free_text_parked_reason`＋`tests/test_heartbeat.py::test_queue_section_surfaces_waits_without_an_expiry`。 | Phase 2 |
| **7** ▶ | 表達 | **多年反向橋（「五倍要什麼為真」）取代 FY+1 主流程；entry criterion 降級**（~~最後做：最大的契約變更，前六個 Phase 不依賴它~~ **2026-09-19 使用者核准 amendment 後提前到 Phase 4a 之後**——「前六個 Phase 不依賴它」在 D11 落地後不再成立：Phase 4 的首選判定就依賴它，而它對目標區沒有鑑別力） | 現行儀器是給大型股 20% 錯價的；thesis 講 FY28，模型只有 FY27 一格 | 個股頁的「賭對了值多少」由 FY+1 單格變成多年橋；COHR 首屏 244 vs 266 的錯位消失；舊 backlog 裡橋契約的四個未結案（口徑、另類資產、共識核實、多筆生效）在此一併重看 | Phase 5<br>**2026-09-19 起工。核心引擎已經在了**：`alpha/reverse/build_reverse_bridge()` 早就是「現價 ÷ 目標倍數 → 市場隱含 EPS → 二分反解每個 driver」，而且走的是真正的 `build_bridge`（任何一步的缺席與口徑衝突用跟正向完全一樣的規則現形），不單調時回 `no_sign_change` 不猜。**「五倍要什麼為真」與它的差別只有起點：把 `current_price` 換成 `current_price × 倍率`。**⚠ 倍率**刻意不寫死**（AGENTS.md「刻意不寫死比值」）——它是參數，可以一次印多個。<br>**✅ Step 7.1 交付（2026-09-19）：把倍率接成一個參數。** `build_reverse_bridge()` 新增 `target_return_multiple`（預設 `1.0`＝原本的「市場隱含」問法，**既有消費者零改動、17 條既有測試全綠**）；`required_eps ＝ 現價 × 倍率 ÷ 目標倍數`。⚠ **`market_implied_eps` 刻意不跟著動**——「市場比我們樂觀多少」與「要漲 N 倍還差多少」是兩個問題，壓成一格就是 L12。四級階梯 `RETURN_MULTIPLE_LADDER=(2,3,5,10)` 接進 view 的 `reverse_bridge.return_ladder`，**每一級都照抄，包括解不出來的那些**（INV-3：問「五倍要什麼為真」時 `no_sign_change` 才是答案）。<br>**before → after 實測**（守門測試 `tests/test_reverse_bridge_multiple.py` 7 條；查證：讀 view 的 `expectation_gap.reverse_bridge.value['return_ladder']`）：能回答「N 倍要什麼為真」的機制 **0 個 → 1 個**；3 檔實跑：**AXTI 2x `available`（兩個 driver 都解得出來）、3x 以上做不到**；**LITE 2x 就有一個 driver 做不到（營益率）、3x 以上全部做不到**；**COHR 2x 就有一個做不到（營收成長）**。<br>⚠⚠ **這一跑就講出現行儀器講不出的話，而且方向相反：** 現行儀器說 AXTI 的 payoff 是 **−32.9%**（賭注對了反而比現價低），反向橋說 **AXTI 是三檔裡唯一在目標年度內 2 倍機械可達的**。兩者不矛盾——**payoff 問「照我們寫下的賭注值多少」，階梯問「這個結構允不允許 2 倍」**。後者不是預測，是**可行性**，而那正是目標要的（2–10 倍的空間，不是 20% 的錯價）。<br>⚠ **要誠實標的限制：** `no_sign_change` 的強度取決於 `ASSUMPTION_DRIVERS` 宣告的合法上下限，而那些上下限當初是為了「正向假設的合理範圍」設的，**不是為了倍率可行性**。所以「拉到極限也做不到」是**相對於那組上下限**的結論，Step 7.2 要重看它。<br>**Step 7.2（多年）尚未開工，且有一個要使用者選的設計問題**——見本列下方或收尾摘要。<br>**✅ Step 7.2 機制交付（2026-09-19，使用者核准走法 (b)+(ii)：複合跳＋終點年份跟著各檔自己的 thesis）。**⚠ **兩個量測在動手前就改變了這一步的內容：**①**63 檔的假設 ledger 裡，有兩個以上目標年度的是 0 檔**——所以 (a) 逐年鏈是從零開始，且要新增約 252 組**沒有共識可校準**的假設；(b) 是純增量，不壞任何現有資料。②**(ii) 的前提今天不成立**：62 個 judgment 的 `expected_horizon` 全部落在「1 quarter」到「4-8 quarters」（最長兩年），而**散文裡一個年份都沒有**——連 COHR 都找不到「FY28」，那句話**只存在於 `AGENTS.md`**。`expected_horizon` 測的是**多久會被驗證**，不是**倍率在哪一年實現**（L12：兩件事被壓成一個欄位）。<br>**所以 7.2 的第一件事不是「讀 thesis 的年份」，是「讓它寫得下來」：**①`AlphaSignal` 新增 `multiple_horizon`（會計年度結束日；`None`＝**還沒有人寫下來**，不是「沒有多年主張」——缺席時多年橋誠實 missing，**不套統一年期、不從 `expected_horizon` 換算**）；②`build_bridge` 自己算出 `span_years` 並在 > 1 時把語意寫進 warnings——⚠ **橋在算術上一直支援跨年**（`base × (1+growth)` 是一次乘法），危險就在這裡：`revenue_growth=0.5` 在 span 1 是「成長 50%」、在 span 5 是「**五年累積**成長 50%」，而今天之前**沒有任何地方說出這件事**。至今所有 span 都是 1，所以這個兩義從來沒現形過。⚠ **橋刻意不替人換算**——換算等於替使用者決定那個 0.5 是年化還是累積。<br>**before → after 實測**（守門測試 `tests/test_bridge_span_years.py` 3 條）：能表達「倍率在哪一年實現」的欄位 **0 個 → 1 個**；**而填了它的 judgment 是 0／62**——**機制交付而下游真實資料變 0 筆，binding constraint 在研究端**（L14-1；這正是 V1 的 `Catalyst.resolves` 那個警訊的形狀，這次在交付當天就講出來）。已鑄 **[630]** 請求核准填第一檔（COHR）。<br>**✅ [630] 已核准並執行（2026-09-19）：第一條多年橋跑通。**①`multiple_horizon = 2030-06-30`，一手依據是 **10-Q（tier 1 法定文件）MD&A 逐字**：「a multi-year capacity agreement … to support future production volumes **through 2030**」（raw 命中 1 次）。**選 FY2030 不選 FY2031 的理由**：COHR 會計年度結束在 6/30，FY2030（2029-07～2030-06）整段落在協議期間內，FY2031 有一半在 2030 之後——**取完全被涵蓋的那一個，不取需要加假設的那一個**。⚠ 這個欄位主張的是**射程不是預言**：逐字只說協議支持到 2030。②FY2030 七條**錨點**假設（`derivation=carried_forward`，rationale 逐字寫明「**這不是預測，是反解的錨點**」）——本檔對 FY2030 的營收**沒有任何一手依據**（公司沒給長期財測、purchase commitment 只寫 multi-billion 沒有金額、六吋 InP「一年翻四倍」講的是產能不是營收）。**硬寫成長率就是把習慣冒充證據**；而反解用二分法在合法區間找根，**錨點的值不影響答案**，只提供 key 與上下限。<br>**第一條多年橋的答案（FY2030，span 4 年）：****2 倍需要 Datacenter & Communications 分部四年累積成長 4.65 倍**（營益率那一格已經做不到）；**3 倍以上所有 driver 拉到極限都做不到**。⚠⚠ **這是系統第一次說出一句可以去查證的話**——「資料中心分部要成長 4.65 倍」可以拿 NVIDIA 的 CPO 出貨量、六吋 InP 產能、份額去對；而「被低估 23%」對不了任何東西。**問題從「值多少」變成「要什麼為真」。**<br>**L11-6 實測（加 FY2030 假設會壞什麼）：0 筆。** 既有機制**已經**認得出它們屬於別的目標期間——`briefing refresh COHR` 把那七條標成「**針對 FY2030，不是目前目標期間；本視角不評估**」，FY2027 的 view 一個數都沒變（`target = actuals.period.shifted(1)`）。<br>**🔶 剩下的機制缺口（Step 7.3）：** 現有 view 路徑**到不了 FY2030**——`build_fundamental_model` 的 target 永遠是基期+1，多年橋今天只能由腳本明確傳 `target_period` 才跑得起來。要讓它出現在 alpha-card／APP，需要一個「**多年視角**」的入口，而那不得污染 FY+1 的主 view。<br>**✅ Step 7.3 交付（2026-09-19）：`python -m briefing multi-year <TICKER>`。**⚠ **它是另一條路，不是主 view 多一格**——`build_fundamental_model` 的目標期間永遠是`actuals.period.shifted(1)`，**而那是對的**：FY+1 那條鏈要對分析師共識，共識只到 FY+1／+2；多年橋**沒有共識可對**，混進同一張表會讓兩個不同的問題共用一個答案欄。兩條路**共用同一條橋、同一套二分法、同一組合法上下限**，不共用的是「要對誰」。<br>**三件刻意誠實的事，各有一條守門測試：**①`multiple_horizon` 沒填就 missing，**不從 `expected_horizon` 換算**（那測的是「多久會被驗證」，實測 62 檔全在 1–8 季）；②目標倍數**沿用 FY+1 的那一條而且說出來**——沒有人知道四年後市場付幾倍，沿用等於假設「那時市場付一樣的倍數」，**那是假設不是觀測**；③整張表第一句就寫「**這不是預測**」，否則「需要 EPS 25.39」會被讀成目標價。<br>**實跑**（查證：`python -m briefing multi-year COHR`）：COHR 印出 FY2030 四級階梯＋跨 4 年的累積值警告＋倍數沿用聲明；**LITE 誠實回「還沒有人寫下這個 thesis 主張的倍率在哪一年實現」**。<br>⚠ 順帶抽出 `NO_HORIZON_REASON` 常數：守門測試第一次跑就抓到**我自己的 fixture 把那句話寫錯**——實作與測試各寫一份字串就會各自腐壞（L16 的小尺度版本）。<br>**🔶 還沒做**：APP 頁面沒有這一頁（今天只有 CLI）；其餘 61 檔的 `multiple_horizon` 仍是空的。 |

## ✅ Phase 排序 amendment（2026-09-19 提出，**同日使用者核准並生效**）

使用者原話：「**採納 做到有需要我同意事項為止 不然就繼續直接做完**」。
Phase 表已照此更新：**Phase 4 拆成 4a／4b、Phase 7 提前到 4a 之後**。
以下五欄保留為決定紀錄。

| 欄 | 內容 |
|---|---|
| **原 roadmap** | Phase 4（篩選）＝D11 六條機械條件＋籃子首選換成這組條件；Phase 7（多年反向橋）**排最後**，理由逐字是「最大的契約變更，**前六個 Phase 不依賴它**」 |
| **新觀察** | ①D11 兩條門檻落地後實測：通過的 5 檔有 **4 檔「儀器算不出 payoff」（80%）**，被擋下的 11 檔只有 1 檔（9%）。②L11-6 逐筆重讀那 4 筆 Abstention：**四筆全部該 abstain**，且**三筆的 `revisit_when` 逐字指名「估值層新增 mid-cycle／multi-year／虧損期 method」**。③全 ledger **11 檔**宣告不主張目標倍數，理由同型；AAOI 那筆逐字寫著「**那是開發項，載體是 ROADMAP 不是本 ledger**」。④**Phase 4 的六條裡只有一條依賴估值儀器**——「payoff 為正」；其餘五條（覆蓋家數、市值、瓶頸業務占營收、外部印證的瓶頸邊、30 天漲幅、催化劑）**與估值無關** |
| **proposed change** | **(1) Phase 4 拆成 4a／4b。** 4a＝五條不依賴儀器的機械條件，接進 `webapp/basket.py` 的 filter，**照原序做**；4b＝「payoff 為正」那一條，**移到 Phase 7 之後**。**(2) Phase 7（多年反向橋）由「最後做」提前到 4a 之後。** |
| **why** | 「前六個 Phase 不依賴它」這句話在 D11 落地後**不再成立**——Phase 4 的首選判定依賴它，而它對目標區沒有鑑別力（80% 算不出）。不提前的話，Phase 4 做完得到的是一個**必然空的籃子**，而空的原因是**儀器不適用**，不是篩選條件正確運作。而需求規格研究層已經寫好了（11 筆 Abstention 的 `revisit_when`） |
| **impact** | 4a 的驗收不變（filter 報 input／accepted／filtered／reasons，INV-3）。**4b 的驗收要重寫**：從「payoff 為正」變成「多年橋算得出倍率與它的前提鏈，且每一格配 disproof」。⚠ **兩個要誠實講的代價**：①Phase 7 是最大的契約變更，提前會讓 Phase 4 完成得更晚；②**反向橋不會讓籃子非空**——它把「為什麼空」換成「這幾檔各自要什麼為真、哪一格最先會破」。不影響 Phase 1／2／5／6 |

**⚠ 明確排除的第三條路：** 「把『payoff 為正』從首選條件裡拿掉」**不是選項**——
那會讓籃子立刻變非空，正是 `AGENTS.md` 明文禁止的「為了讓籃子非空而放寬篩選條件」。

**~~不採納也是一個合法答案~~**（2026-09-19 使用者採納，此路未走）。

### ⚠ 核准當天補上的一處更正（它改變了優先序的理由，但沒改變結論）

提案時寫「11 筆 Abstention 各自獨立寫下、卻指向同一件事」——**那不精確，它們指向兩件事**：

| 要的是什麼 | 哪幾檔 | 它解決的問題 |
|---|---|---|
| **虧損期估值法**（營收倍數取代盈餘倍數） | IQE.L／POET／AAOI／CRWV／3363.TWO | 「這家**現在**虧錢的公司**今天**值多少」 |
| **多年期預測／mid-cycle** | 6324.T／SOI.PA | 「**幾年後**的樣子值多少」 |
| 靠 rollover 自己解 | MP（FY2027 共識已轉正 0.89562） | — |

⚠ **這個區別會影響排序，而且方向與直覺相反：** 虧損期估值法**看起來能解更多筆**（5 檔 vs 2 檔），
但它**仍然是一年期的錯價儀器**——只是把分母從盈餘換成營收，回答的還是「今天被低估幾 %」。
**照「解得多」排優先序會選到對不上目標的那一個。**
更糟的是：先做它，籃子會因為「虧損公司終於算得出 payoff」而變非空——
**那是用一把對不上目標的尺把籃子填滿，比空籃子危險得多。**

---

## Phase 表外的交付：結構讀圖（2026-09-17 Q4＋Q5，使用者核准 A）

**它不屬於某一個 Phase，因為它是 Phase 1（讓邊緣公司看得見）與 Phase 4（篩選）之間缺的那塊中間物：**
圖給一堆單邊事實，賭注要一個結構判斷。設計與決定見
[`brainstorms/2026-09-17-structural-reading-layer.md`](brainstorms/2026-09-17-structural-reading-layer.md)。

| | 交付 | 驗收（哪個數字會變） |
|---|---|---|
| **Q4** ✅ | `python -m query.structure <node>`：五個角度一次查出，零 LLM、零判斷、不寫 authority | 手打十幾條查詢 → 一條命令。一跑就冒出具體缺口：`mat:inp_substrate` 有 15 條需求側邊、圖說它 sub=5 最卡，但**供給側 3 條全部沒填 sub** |
| **Q5** ✅ | append-only 讀圖 ledger（`alpha/structure_reading/`）＋ staleness 分級 ＋ 心跳第 2 段 ＋ pq1 段 `stale_structure_readings` ＋ `webapp materialize --structure-readings` | 心跳第 2 段從沒有這一行 → 有「結構讀圖 N 份／該重讀 M」；佇列段 12 → **13**（查證：`python -c "from engine_b import queue_segments as q;print(len(q.SEGMENTS))"`）；state kind 6 → **7**（查證：`python -m webapp status`）。**2026-09-17 實測：ledger 0 筆，心跳印「一份都還沒寫」**——那是誠實的起點，不是壞掉。⚠ API 端點已開（`/api/v1/structure-readings`），**前端頁面刻意未做**：心跳那一行已足夠，等 ledger 有幾十筆再看要不要給它一頁 |

⚠ **刻意還沒做的兩件**（都不是遺漏）：
① **A/B 判準表不機械化**——先產出幾十份、看它講得準不準再談（INV-5：先量測後放閘）；
② **不自動重新推理**——重讀是研究，只在互動 session（D12）。分級本身是零 LLM 的，所以這不花 token。

⚠ **它維護的是「讀圖跟圖還一不一致」，不是「讀圖對不對」。** 後者要靠 outcome 量測（Phase 3／5）。

## 每個 Phase 的 completion gate（八項，缺一不得宣稱完成）

出自 [`historical-failure-matrix.md`](refactor/historical-failure-matrix.md) §9。
**不得僅以「tests pass／CLI works／architecture looks cleaner」判定完成。**

1. Historical regression suite pass（golden fixtures）
2. Runtime invariant audit pass（`audit invariants`）
3. No unexplained semantic diff（old/new dual run）
4. **No new dual authority**
5. No silent-drop path（每個 filter 都能報 input／accepted／filtered／reasons）
6. Point-in-time tests pass
7. All migrated lifecycle objects reachable
8. **該 phase 負責的 critical historical failure 已有 executable protection**

> ~~現況：36 筆歷史事故中，**🔴 僅有文字保護的有 10 筆**。~~（2026-09-17 Step 1.3 重量：
> **36 筆中 ✅ 21／🟡 8／🔴 7**——`historical-failure-matrix.md` §4 的「17／9／10」與本句都是已腐壞的快照，
> §1 的逐列狀態才是現值。查證：`python -c "import re,collections;ls=open('docs/refactor/historical-failure-matrix.md',encoding='utf-8').read().split(chr(10));c=collections.Counter(next((m for m in ('✅','🟡','🔴') if m in l.rstrip('|').rsplit('|',1)[-1]),'?') for l in ls if re.match(r'^\| \*\*F-[0-9/]+\*\* \|',l));print(dict(c))"`）
> 各 Phase 的責任分配見該檔 §9。
> ⚠ **§9 的「各 Phase 🔴 責任分配」表用的是舊 refactor 的 Phase 編號**（contracts／Portfolio-Risk／
> vertical slice／Engine D 分解…），**與本檔 Alpha Edge 的 Phase 0–7 沒有對應關係**。
> 第 8 項要對照的是「本 Phase 實際動到哪些事故的形狀」，不是編號對編號。

### Phase 6 completion gate 逐項核對（2026-09-17；每項附查證命令）

| # | 項目 | 結論 | 依據 |
|---|---|---|---|
| 1 | Historical regression suite pass | **過** | golden 19 passed；全套 **2,620 passed／1 skipped** |
| 2 | Runtime invariant audit pass | **過** | FAIL 0／PASS 13／檢查 **4,160 筆**（`python -m audit invariants`） |
| 3 | No unexplained semantic diff | **過** | 新增一張 Engine C 表與一個 harvest 來源型別；既有表、既有 harvest 來源、既有 artifact 逐位未動（`python -m webapp status`） |
| 4 | No new dual authority | **過** | 月營收是**可重建的 ETL 表**（L10：MOPS 歷史頁按年月永久可查），不是 append-only judgment ledger、不需 pq2；重訊只 `register` lead，不自動 triage、不入圖、不寫 Engine C。台股清單的 SSOT 仍是 registry（`registry_taiwan_tickers()` 唯一來源） |
| 5 | No silent-drop path | **過** | `select_tickers` 逐列報 `input／accepted／filtered`，且把「沒有你的公司」與「代號解析不出來」**分成兩個計數**（版型變了才看得出來）；`harvest_mops` 對解析不出市場的 ticker 記 `parse_failed` 而不是跳過 |
| 6 | Point-in-time tests pass | **過** | `published_at` 永遠 NULL 且由 `published_at_basis` 自己宣告為什麼（MOPS 的「出表日期」是產表日不是公告日，`AGENTS.md` 明令不得冒充）；`disclosure_deadline` 是可機械推導的上界。`PointInTime` audit PASS；`test_report_date_never_becomes_published_at` |
| 7 | All migrated lifecycle objects reachable | **過** | **這正是本 Phase 第三件的內容**：黑洞 **3 → 0**，且「無到期的等待 N」成為心跳第 3 段的常駐計數器。`QueueLiveness`／`QueueSegments`／`Expiry` 全 PASS |
| 8 | critical historical failure 已有 executable protection | **過** | 本 Phase 動到兩個形狀：**F-31（Engine A 無 as-of）**——月營收的 `published_at`／`report_date`／`disclosure_deadline` 三欄分開，測試鎖住不得互相冒充；**L13-1（管子只接了一頭）**——`parked_without_expiry()` 的黑洞由計數器每天現形。**本 Phase 沒有新增任何 🔴** |

**結論：八項全部過，驗收行逐條達標，Phase 6 標 ✅。**

### Phase 3 completion gate 逐項核對（2026-09-17，五欄 amendment 核准後；每項附查證命令）

| # | 項目 | 結論 | 依據 |
|---|---|---|---|
| 1 | Historical regression suite pass | **過** | golden 19 passed（`python -m pytest -k golden`）；全套 2,617 passed／1 skipped |
| 2 | Runtime invariant audit pass | **過** | FAIL 0／PASS 13／檢查 4,154 筆（`python -m audit invariants`） |
| 3 | No unexplained semantic diff | **過** | 本 Phase 只新增 `account_scorecard` 這個 state kind 與 `Metric.revisit_after` 一欄；既有 artifact 逐位未動（`python -m webapp status` 其餘 kind 的 digest 不變） |
| 4 | No new dual authority | **過** | 計分表是**純消費端**：只讀 `pending_leads.json` 與 `config/signal_sources.json`，寫的是 ignored derived cache。tier 的寫入端**刻意不存在**（模組沒有寫入函式，測試斷言之），升降走一季一次的 pq2 manual |
| 5 | No silent-drop path | **過** | 每一欄都帶 `input／accepted／filtered／reasons`（`lead_filter`、`excess_return_filters`、`prior_30d_filter`）。查證：讀 `state/account_scorecard.json` 的 `lead_filter.reasons`（實測 `no_named_company 120`／`ticker_without_registry_company_id 40`） |
| 6 | Point-in-time tests pass | **過** | 每則點名以**貼文當天**的收盤價蓋章、超額報酬以 `called_on + horizon` 取價；`point_in_time` 欄位隨 artifact 落地。`PointInTime` audit PASS |
| 7 | All migrated lifecycle objects reachable | **過** | 兩種缺席都有明確終局：`insufficient_sample` 帶 `revisit_after`（實測 2026-09-27）、`capability_absent` 指出要建什麼。`QueueLiveness`／`QueueSegments` 全 PASS |
| 8 | critical historical failure 已有 executable protection | **過** | 本 Phase 動到的是 **F-20 的形狀**（截斷／缺席被當成 0）：守門測試 `test_time_bound_absence_carries_a_revisit_date_and_capability_absence_does_not` 鎖住「要建能力的缺席不得假裝只是等時間」，`tests/test_account_scorecard.py` 另有 22 條。**本 Phase 沒有新增任何 🔴** |

**結論：八項全部過，Phase 3 標 ✅。** ⚠ 與 Phase 1 相反——Phase 1 是八項全過但驗收行未達標所以不標；
Phase 3 是八項全過**且**驗收行（改寫後）逐條達標。

### Phase 1 completion gate 逐項核對（2026-09-17，Step 1.3；每項附查證命令）

| # | 項目 | 結論 | 依據（現跑，不抄） |
|---|---|---|---|
| 1 | Historical regression suite pass（golden fixtures） | **過** | `tests/fixtures/golden/` 14 份 fixture＋`tests/test_golden_fixtures.py` 在全套內綠。查證：`python -m pytest tests/test_golden_fixtures.py -q` |
| 2 | Runtime invariant audit pass | **過** | FAIL 0｜PASS 13｜檢查 4,087 筆。查證：`python -m audit invariants` |
| 3 | No unexplained semantic diff（old/new dual run） | **過** | Step 1.0 對 218 條「公司→向下」邊做過舊解析器 vs 新解析器 dual run：39 條改判全部有分類（升 31 條每條指得出一個解析到非主詞 registry 公司的 origin，違規 0；降 8 條全是主詞自家文件被括號註解污染成第三方的歸位）→ 0 筆 unexplained。Step 1.1／1.2 是**純新增**（新抓取器、新 SourceDoc、新邊），不改既有輸出路徑，依 §8「純新增不需 dual run」。⚠ 誠實補一句：當時的舊實作副本放在**未追蹤**的 `.pytest_tmp/old_bottleneck.py`，**那次 dual run 今天已不可重跑**，只剩 commit `69c8388` 的逐筆紀錄 |
| 4 | **No new dual authority** | **過** | 三樣新東西都沒有建立第二個 current-state authority：抓取器只寫 `library/raw/`，不落任何快取表；`product_line_revenue_share` 登記在 `config/engine_c_observation_fields.json` 由 Engine C 承擔，且**刻意不覆寫** `segment_revenue_share`（決策 D2——報導部門占比與產品線占比是兩個語意，不是同一真相的兩份）；`config/source_routes.json` 是 how-to 不是事實。查證：`python -c "import json;d=json.load(open('config/engine_c_observation_fields.json',encoding='utf-8'));print([f['field_name'] for f in d['fields'] if f['field_name']=='product_line_revenue_share'])"` |
| 5 | No silent-drop path | **過（本 Phase 的範圍內）** | 兩支新抓取器逐則報 input／accepted／filtered／reasons（`fetchers/mfn.py:165`、`fetchers/rns.py:165`，CLI 印到 stderr）；排序的 as-of 投影由 `AsOfProjection.reasons()` 報 `published_after_as_of`／`undated`（`query/bottleneck.py:630`）。⚠ **門檻 4 的 filter 在 CLI 上今天只印覆蓋率、不印逐檔 reasons——那是 Phase 4 的驗收項**；本次收尾以唯讀重量補出逐檔報表（見下） |
| 6 | Point-in-time tests pass | **過** | `audit invariants` 的 PointInTime：SourceDoc `published_at` 202/215（94.0%）、EdgeAssertion 可定日 657/674（97.5%）；13 份未定日**逐份列名**且擋住的邊有計數（L11-5 留 null 不猜）。三份新文件 `published_at` 皆 ≤ `retrieved_at`。查證：`python -m audit invariants --only PointInTime` |
| 7 | All migrated lifecycle objects reachable | **過** | audit 的 Lifecycle 598 筆／Expiry 85 筆（進行中的等待全部有到期日）／QueueLiveness 31 筆／QueueSegments 1,760 筆全 PASS；[579]–[585] 六個編號全部帶 `action/digest/commit/cohort` receipt 結案，池中無本 Phase 未決編號。查證：`python -m engine_b.todo list` |
| 8 | **critical historical failure 已有 executable protection** | **過（但編號對照不成立，見上）** | 本 Phase 實際動到的是**排序的證據分級**（F-20 截斷集合被當全集）與**新來源的定日**（F-31 Engine A 無 as-of）。兩筆在 §1 都已是 ✅：`alpha.contracts.RankedList` 強制帶 `full_ids`、`contains()` 只讀它，突變測試會紅；`PointInTimeUnsupported`＋`PointInTime` audit 今天 PASS。**本 Phase 沒有新增任何 🔴**。⚠ 未清掉也不在本 Phase 範圍的 🔴 仍有 7 筆（含 F-03） |

**結論：八項全部過，但 Phase 1 仍不標 ✅**——completion gate 過不等於驗收行達標。
驗收行明訂「可投資排序的 TW／TWO／ST 檔數仍為 0 就不得標完成」，2026-09-17 實測**仍是 0**。

#### 第 5 項的逐檔報表：門檻 4 的 filter（INV-3，2026-09-17 唯讀重量）

`input 222｜accepted 37｜filtered 185`——filtered 的理由只有三種：
`substitutability` 未填 **162**、`substitutability=3` **15**、`substitutability=2` **8**。
**沒有任何一檔是因為走不到需求錨被濾掉**（所有 TW／TWO／ST 公司的向下邊都接得到錨）。

| 公司 | 向下邊 | 現況 | **缺哪一份文件**（不得靠放寬門檻取代） |
|---|---|---|---|
| 上詮 `co:foci`（3363.TWO） | 1 → `tech:fiber_attach_unit` | sub 未填、自報·filing | Himax 或長約對手方的**客戶端具名確認**。一手只有自述目標（「長期目標係成為 CPO 光纖陣列元件之主要及重要供應商」）與產能投資；Hunterbrook 是媒體，依決策 C1 維持待判定 |
| 聯亞 `co:landmark_optoelectronics`（3081.TWO） | 1 → `tech:cw_dfb_laser` | **sub=3**（2026-09-17 新填） | 2026-08-26 四年長約的**條款**（最低採購量／獨家性／價格）。對照組是 IQE→Tower 判 4：雙向最低採購承諾逐字可讀。條款只可能由**對手方**（未具名的美國客戶）的法定文件揭露 |
| 華星光 `co:luxnet`（4979.TWO） | 1 → `tech:cw_dfb_laser` | sub=2 | **對照組，本 Phase 明訂不得改判**；2026-09-17 複查未動 |
| 全新 `co:vpec`（2455.TW） | 2 → `tech:cw_dfb_laser`、`tech:photodiode` | sub=2 ×2（本輪補邊） | 客戶端具名＋替代難度的正面證據。**現有一手證據方向相反**：全新在自家年報點名聯亞與 IQE 是同層競爭者 |
| IET-KY `co:intelliepi`（4971.TWO） | 2 → `tech:vcsel`、`tech:photodiode` | sub=2 ×2（本輪補邊） | 同上；英特磊自家年報點名全新與 IQE |
| Sivers `co:sivers_semiconductors`（SIVE.ST） | **15，其中 14 條 sub 未填** | `prod:els_8ch_module` 那條**已外部印證但 sub 未填**——這是最接近進榜的一格 | Ayar Labs 自家文件（`ayarlabs.com` 對本機 UA 回 403）；`tech:wdm_laser_16ch` 要它才能由供應商自報回到雙方聯合 |
| **穩懋 `co:win_semiconductor`（3105.TWO）** | 1 → `co:sivers_semiconductors` | **已是外部印證、sub 未填** | ⚠ **2026-09-17 收尾新發現，不在 Step 1.2 的七家工單裡**。證據等級已經夠，缺的只有 `substitutability` 的判讀依據 |
| Aehr `co:aehr_test_systems`（AEHR） | 1 → `tech:cpo_full_stack_test` | sub 未填；~~該 tech 節點**走不到任何需求錨**~~（2026-09-18：**已可達**，錨 `tech:ai_switch` 3 跳） | ~~先補「`tech:cpo_full_stack_test` → `tech:cpo`／`tech:ai_switch`」那條邊，否則補了 sub 仍然排序不可見~~ **2026-09-18 實測推翻：那條邊一直都在圖裡**（`tech:cpo --constrained_by--> tech:cpo_full_stack_test`），缺的是 traversal 不是邊——已隨 Phase 4 ①放閘修好。**現在缺的只有 `substitutability` 的判讀依據**，補了就進榜（what-if：第 27 名） |

查證：`python -m query.bottleneck --top-n 60`（表內不會出現任何 `.TW`／`.TWO`／`.ST`）。



## 硬約束（轉向不放寬任何一條）

1. **不重建 Neo4j。** 資產是 EdgeAssertion 的 provenance，不是節點數。
2. **Decision Store schema 不動。** append-only 紀錄 Git 救不回（L10）。
3. **四個人工 gate 不放寬**（graph admission／Engine C 判讀寫入／thesis mutation／live）；**L8 不放寬**。
4. **`rank_bottlenecks()` 仍是唯一排序權威**；篩選層只在它的順序上過濾，不重算、不加權。
5. **系統仍然不給 alpha 部位尺寸、不下單、不連 broker**（`AGENTS.md` Alpha 呈現契約）。
6. **beta 訊號不得以任何名義復刻**（2026-08-01 實測 0 勝 3 敗）；**beta 開發凍結（D6）**，只保留大盤比例觀測。
7. **不因籃子空就放寬篩選條件**——讓它非空的路是研究。
8. **last30days 不串進無人值守管線**（本機未接 X、輸出沒有 provenance 契約，只做一次性探勘）。
9. **既有測試檔全部保留**——可改 import 路徑，不可刪斷言。
10. **改任何 `python -m <module>` 命令字串前，先走 sandbox impact review 五步**——`.codex/rules` 的 exact prefix 會靜默打斷 daily。
11. **六條 hard invariant 全程適用**（`historical-failure-matrix.md` §2）。
12. **Local-first；Core 不得 import `mcp_server`。**
13. **alpha 原則上不用貸款資金是使用者自己的紀律，系統不建 gate（D14）；Sheet 是部位真相，Decision Store 只留可選 receipt。**
14. **`AGENTS.md` 不是憲法**——不可變的是五條 authority separation；**lesson 一條都不刪**。

## 凍結與明確不做

- **beta 開發凍結**（D6）：beta 個股呈現若擋路可拆，不為維持 beta 架構繞路。
- **明確不排程（理由已量測，勿重開）：** 舊表整份在 archive「明確不排程」節——peer valuation／`peer_groups.json`、
  等待機制三套併入 Event Watch、待辦池 evidence conflict 類型、ETF 完整 look-through 管線、Confidence 五軸重構、
  技術指標擴充、貸款 glide path、parked lead embedding 召回、`coverage_gaps` 走 `DEPENDS_ON`。
  **轉向不重開這些**：否決的理由是量測結果，不隨目標改變。
- pq2 **[577]**（COHR sole_source 核實）、**[578]**（COHR 賭注改寫）擱置（D9）。
- 決定紀錄 §9：不給部位尺寸、不下單、不連 broker、不動四個人工 gate、不放寬 L8、不在 Step 0 動任何程式。

## 研究主題範圍

- **以 AI capex 為起點找邊邊，同時開始找下一個大題材（D4）**；選題由使用者，機制是 `system-decompose`。
- 2026-09-09 定案照舊：新需求錨的 decompose 提案由系統自動鑄成 pq2（manual 型），核准後與既有主題同級；
  同時 open 的提案最多兩個；drop 過的系統除非新 lead 再點名否則不重生。
- **HBM 不變：SK Hynix／Samsung 不主動 onboarding**（使用者對擁擠度的判斷）；humanoid 的可投資機會在零組件供應商不在整機。
  完整歷史（2026-08-20 定案與 2026-09-09 修正）見 archive「研究主題範圍」。

## 舊 backlog 未結案項（自封存表移入；只留標題與去向，內容看 archive）

| 舊表列（archive 的開放 backlog） | 舊標記 | 去向 |
|---|---|---|
| 賭注 V4：variant 收斂納入 outcome 量測 | ○ | **Phase 5**（量測） |
| **圖中 409 條 claim 全部帶 `disproof_condition`，但沒有任何東西在定期比對它與圖裡的新證據——已觸發三個月沒人發現**（2026-09-18 實測，pq2 [603] 撞到） | 🔴 | **Z2 proposal**。事發：`gsr_inp_substrate_market_2026_05_16_cl2` 的 disproof 逐字寫著『Any of the three announces and executes a material capacity expansion』，而 JX 在 **2026-06-16** 宣布 1,200 億日圓／產能 7-10 倍——**觸發它的那份新聞稿就在同一張圖裡**（`jx_metals_inp_capacity_pr_2026_06_16`，tier 2，三條 confirmed claim），條件成立三個月沒有任何東西響過，而 2026-09-17 的結構讀圖還照著過期那句寫。這正是 L7 說的**永遠不會響的火警**。⚠ 對照組就在旁邊：Q5 的結構讀圖 staleness **做到了**（digest 變動零 LLM 自動偵測、落 pq1 段、心跳現形）——**同一族的機制，claim 這一側沒有**。規模：**409/409 條 claim 帶 disproof_condition，而被標成 TRIGGERED 的只有 1 條**（就是本輪手動標的那條）。⚠ 難點在於比對本身多半需要語意判斷（L15：語意交給語言處理、權限 deterministic），所以方案要先回答「哪一部分可以零 LLM」——例如「這條 claim 指名的實體上，有沒有比它的 as-of 更新的 claim」就是純結構問題，可以先做那一半。查證：`MATCH (c:Claim) WHERE c.disproof_condition IS NOT NULL RETURN count(c)` <br>**2026-09-18 下午：零 LLM 那一半已量完，量測改變了提案的形狀——先看這段再開工。**<br>①**接法原本就會錯：** `Claim.source_ids` 是**片段 id**（`..._s1`）不是文件 id，用它去 join `SourceDoc` 會得到「409 條全部沒有發表日」這種太整齊的假結果（我第一版就是這樣）。正確是 `(Claim)-[:CITES]->(SourceDoc)`，或 `c.source_doc_id`。<br>②**母體實測：** claim **409**／全部帶 `disproof_condition`；`subject_kind` 是 `node` **328**、`edge` **81**（**edge 那 81 條沒有 `:ABOUT` 邊，結構上接不到主體**——任何只走 `ABOUT` 的方案會靜默漏掉五分之一）；有發表日 **396／409**；可比對母體（有日期＋有主體）**323**。<br>③**鑑別力（L14-4 恆亮測試）：主體上有更新 claim 的 174／323＝53.9%**——沒有恆亮，但偏高。分佈：1 條 49 個／2 條 22／3 條 14／4 條 16／5 條 11／6 條 12／7 條 10／8 條以上 40。<br>④⚠⚠ **決定性測試：它抓不抓得到促成它的那個案例？** 答案是**抓得到，但理由是錯的、而且排不上前面**。`gsr_inp_substrate_market_2026_05_16_cl2` 的主體是 `mat:inp_substrate`，它上面確實有 **4 條**更新的 claim——但**真正觸發它的那三條 claim 掛在 `co:jx_advanced_metals` 上，不是 `mat:inp_substrate`**。所以這個訊號抓到它是因為「這個主體上有新東西」，**不是因為找到了那個觸發事件**；4 條在分佈裡落在中段，依「更新條數」排序大約第 100 名／174。**結論：它是「回頭再看一眼」的排序訊號，不是「這條響了」的偵測器——文件與心跳都必須這樣寫，否則使用者會以為有人在守著。**<br>⑤⚠ **最關鍵的設計缺口：沒有『清除』就是牆不是閘門（L14-4）。** 更新 claim 數只會隨證據累積單調上升，一條被標記之後永遠標記著。**對照組 Q5 結構讀圖之所以會動，是因為它有 append-only ledger ＋ `result_digest`——重讀一次就 current 了。** claim 這側要能滅，同樣需要一筆「我在 T 看過這條 disproof，當時主體上的證據指紋是 X」。**所以真正的提案不是偵測器，是那個 ledger**；偵測器只是它的 staleness 視圖。<br>⑥**要使用者決定的那個岔路：** 為此**新建一份 claim review ledger**（與 `alpha/structure_reading` 同族，多一個 append-only authority、多一個佇列段、多一行心跳），還是**先用既有的 `thesis/lifecycle.json`／`catalyst_watch` 承載**（便宜、但那兩個是 thesis 層不是 claim 層，語意要先對齊）。兩條路的成本與 blast radius 差很多，**這是 Z2 的停止點**。<br>現況查證：`MATCH (c:Claim) WHERE c.disproof_condition CONTAINS 'TRIGGERED' RETURN count(c)` → 今天 **1／409**。 |
| **心跳排在 07:00，而 daily（含 harvest 與 APP materialize）07:26 才結束——心跳第 1 段每天報的都是『昨天』** | 🔶 | 維持營運／小排程調整。2026-09-18 實測：心跳說「APP artifact 今天已 materialize **0** 份」與「harvest 最後一輪 09-17 21:16」，而 20 分鐘後 artifact 有 8 份、harvest 也跑了（07:03）。**數字本身沒錯，錯的是它在問「今天」的時候今天還沒開始**——L12 的形狀（同一個表示承載「真的沒跑」與「還沒輪到」）。⚠ 修法不是把心跳延後就好：延後會讓「daily 死掉時心跳照發」這個**它存在的全部理由**變弱。方案要同時保住兩件事（例如心跳印出 daily 的完成時戳與自己的相對位置）。查證：`python scripts/writer_guard.py check` 的 `daily_run_finished_at` 對照心跳檔的時戳 |
| **🔴🔴 `is_component_of` 承載了第二種語意——92 條裡 17 條是「A 是 B 的一種」，而封閉字彙沒有可換的 relation**（2026-09-18 全 relation 重判時量出來） | 🔴 | **Z2，三個選項要使用者選，已鑄 [613]。** 判準是抽取端與 schema 共用的那一句：`is_component_of` — **A is a part inside B**。<br>**逐條重判 92／92（100% 覆蓋，全部有逐字——V1 之前是 0 段）：** keep 49（53.3%）｜flag_evidence 18（19.6%）｜**flag_taxonomy 17（18.5%）**｜reverse 5｜delete 2｜retype_develops 1。**合計有問題 43／92＝46.7%**，與 `enables` 那次（37／82＝45%）幾乎一樣——但**形狀不同**：`enables` 錯在方向，本次錯在**它被拿來表達另一種關係**。<br>**為什麼不只是分類潔癖（實測）：** `query/structure.py` 的「下一層：它自己卡在誰身上」把 `A is_component_of B` 讀成「B 卡在 A 身上」。對真零件正確，對這 17 條完全錯誤。**全圖最核心的 `tech:cpo`（54 條邊）：那個角度 24 條裡有 5 條其實是「CPO 有哪些變體」**（`glass_cpo`／`intel_glass_cpo`／`coupe`／`broadcom_3d_silicon_cpo`／`scale_up_cpo`）——讀這個角度判瓶頸的人，會把「CPO 的五種實作路線」誤讀成「CPO 的五個瓶頸」。另外 `tech:deposition_etch_clean` 4 條（Lam 的產品線分類表）、`tech:ai_switch` 4 條（它們本身就是交換器）。<br>**三個選項（packet §3）：** A＝刪掉 17 條（乾淨但下次抽取會重生）；B＝新增一個 relation（`is_variant_of`／`is_a`；表達力對了，但動封閉字彙＝Z2，要同時決定它算不算 counter_path、進不進走訪清單、`structure.py` 哪個角度收它）；C＝保留現狀只在讀路徑過濾（最小改動，但兩種語意仍混在一起，L12）。**建議 B**——A 會讓同一批邊重生，C 正是 L12 說的「下游被迫二選一而兩邊都錯」。<br>**另外 8 條是真的錯，已鑄 [614] 可直接執行：** reverse 5（⚠ 其中三條**共用同一段 Coherent 法說逐字**，而那段話的結構是「CPO 需要 → fiber attach unit → 裡面有 microlens array 與 PM fiber」——三層關係全部被接反）；delete 2（`serdes`→`ai_switch` 逐字一個字沒提 SerDes、`scale_out_cpo`→`cpo` 逐字只提 scale-**up**，兩條都是 L6 Gap 4 型）；retype 1（`prod:reliant is_component_of co:lam_research`——產品不是公司的零件）。<br>判決逐條在 `library/private/alpha/is_component_of_rejudge/classification.json`（key 是 assertion id），packet 在同目錄 `packet.md`。<br>**✅ 2026-09-19 使用者選 B，已交付。** ①`schema/vocab.json` 的 `relation` 新增 `is_variant_of`（14 → 15）＋`_is_variant_of_comment`；②`prompts/extract_system.md` 加**可操作的判準**（Would removing A leave B incomplete? 是 → `is_component_of`；A 是成為 B 的其中一種方式 → `is_variant_of`）＋三正一反的例子，`schema/graph_schema.md` 同步；③17 條改判。<br>⚠⚠ **最關鍵的決定：新 relation 刻意不進任何走訪清單**——`counter_path_relation`（變體不是替代路線）、`sole_source_beneficiary_end`（不帶 sole_source 語意）、`query/structure.py` 的五個角度（封閉清單，加第六個要先答出「它在哪個實際案例裡改變過結論」L17-4）、`query/bottleneck.py` 的 `UPSTREAM_RELATIONS`（變體不是上游）**四個都不收**。**加這個 relation 的目的是讓那 17 條從不該在的地方離開，不是給它們一個新位置。**<br>**驗收 4/4：** `tech:cpo` 的「它自己卡在誰身上」**25 → 20**（正好 −5）；五條變體在該角度 grep 命中 **0**；`tech:ai_switch` 下一層剩 2 條；排序 `input 221／accepted 37／filtered 184` **逐位不變**。<br>**守門測試 `tests/test_is_variant_of.py` 四條**，重點是第二條——它守的不是「這個 relation 存在」，是**「它沒有偷偷進到任何走訪清單裡」**，因為那正是下一個人最容易順手做、而且做了不會有東西變紅的事。<br>**[614] 的 8 條同日交付**（4 個抽取檔、6 改寫、2 移除、孤兒邊 8、reprojected 83）。⚠ **我在 packet 裡的預測算錯了一條**：預測 `tech:cpo` 下一層 24 → 22，實際 24 → **25**。原因是 `A is_component_of B` 被讀成「B 卡在 A 身上」，所以翻轉 `cpo ⊂ fiber_attach_unit` 是讓它**進入** cpo 的下一層而不是離開（24 ＋1 ＋1 −1 ＝ 25，完全對得上）。**連寫 packet 的人都把方向算反了——那正是本條要解的語意問題。**<br>**新寫 `loader/migrate_relation_rejudge.py`（通用版）**：吃判決檔、key 用 `assertion_id`（同一條 canonical edge 的多份斷言可以有不同判決）、verdict 字彙 `reverse`／`delete`／`retype:<rel>`／`reverse_retype:<rel>`——**換型與對調是正交的兩件事，字彙明說不靠記憶**（舊腳本的 `retype_is_component_of` 只換型、`retype_depends_on` 換型又對調，看名字分不出來，那是 L12 在字彙層的小型版）。四步與 `_supersede`／`_cleanup` 直接沿用既有實作不複製。⚠ 它的 `not_found` 非空會 raise——**判決指到抽取檔裡不存在的 assertion 不是「沒事做」**（L13-2）。<br>⚠⚠ **交付後自驗，17 條裡有 2 條改判後仍不精確，記在這裡不隱藏：** `prod:digit is_variant_of tech:logistics_tote_transfer`（機器人不是「物流搬運任務的一種」，它**執行**那個任務）與 `prod:reliant is_variant_of tech:non_leading_edge_fab`（設備不是「非先進製程廠的一種」，它**服務於**那個市場）。**我在判讀時把三種東西都塞進了 `flag_taxonomy`：變體、實例、應用／服務**——前兩種 `is_variant_of` 表達得了，第三種表達不了。這是 L17-4 的形狀（general 過了頭到資料不支持的那一格）。⚠ **目的仍達成**（它們離開了「下一層」角度，不再被誤讀成瓶頸），但**標籤不精確**——正確的 relation 可能是 `enables`（Digit 使能物流搬運）或根本不該有這條邊，而 `deploys` 的定義要求主詞是 operator，也套不上。**需要另一次判讀，未自行再改。**<br>其餘 15 條讀起來乾淨：`jericho3`／`jericho4`／`spectrum_x`／`tomahawk6` 是 AI 交換器的一種、`glass_cpo`／`coupe`／`intel_glass_cpo`／`broadcom_3d_silicon_cpo`／`scale_up_cpo` 是 CPO 的一種、`altus_family`／`sabre_family`／`vector_family`／`reliant` 是沉積蝕刻清洗設備的一種、`hbm` 是 DRAM 的一種、`quantum_x_photonics` 是 scale-out CPO 的一種。查證：`MATCH (ea:EdgeAssertion) WHERE ea.relation='is_variant_of' RETURN ea.src_id, ea.dst_id`。<br>⚠ **新增 relation 時漏了兩個消費端，被既有守門測試抓到**（`tests/test_robotics_ontology.py` 兩條）：`schema/intermediate_format.schema.json` 的 `relation` enum（測試訊息逐字說「**validate 會擋下合法資料**」）與 `schema/neo4j_setup.cypher` 的 relationship-type token 預熱（`routine_writer` 是最小權限帳號，**不能建立全新的 type token**）。**我盤點時 grep 的是 `is_component_of`，而這兩處用的是大寫 token 與 JSON enum，grep 不到**——那正是封閉字彙該有的保護：它不靠盤點的人記得。⚠ token 實際上已經存在（migration 用 admin driver 建了 17 條真實關係，type token 一旦產生就留著），所以不需要重跑 setup；補 cypher 是為了讓 fresh clone 也對。 |
| **重複節點偵測器的兩條規則抓不到「名字完全不同」的重複——而那正是最該抓的一種**（2026-09-18 判讀 28 對候選時撞到） | 🔶 | **已知限制寫在 `THIS_IS_NOT` 裡，現在有了具體案例。** 圖裡有**三個** foundry/logic 節點，其中 `tech:foundry_logic_advanced` 與 `tech:leading_edge_logic_foundry` **指的是同一件事**（先進節點的 foundry/logic），但兩條規則都抓不到：token 集合互不為子集、name 也完全不同（「Foundry and Logic Leading-Edge Manufacturing」vs 「Leading-edge foundry/logic nodes (7nm and below)」）。**它是被提名的另外兩對（都是 taxonomy）順帶帶出來的**——也就是說，這次找到它靠的是人讀，不是偵測器。<br>**28 對判讀的分布**（`duplicate_candidates_review.md`）：A 明確該併 **4 對（14%）**｜B 傾向併但證據不夠硬 5 對｜C taxonomy／版本關係 13 對｜D 產品 vs 技術 7 對。**C＋D 共 20 對的結論是「不併」，而那個結論今天沒有地方可寫**——下一次跑偵測器它們會原封不動再出現一次（同上方「刻意不併沒有結構化登記處」那條）。<br>⚠ 提案方向未定：加第三條規則（語意相似度）會引入 LLM 或 embedding，而那與「零 LLM 的機械偵測」的定位衝突（L15-2）；**更可能的解法是讓偵測器吃 `name` 的 token 而不只是 id 的 token**——待量測後再提。<br>**✅ 2026-09-19 [615] 已核准並執行：A 組 4 對合併完成**（blazar／reliant／foundry_logic／optical_interposer）。**候選 32 → 28 對、「沒人提過」28 → 23、圖中節點 189 → 185。**<br>⚠ **第四組刻意偏離機械選法**：機械選法取最短 id 會選 `prod:optical_interposer`(24)，實際取 `tech:poet_optical_interposer`(28)——它的逐字自稱是 platform、邊 6 vs 2、逐字 3 vs 2，而**前綴本身承載語意（`prod:` vs `tech:`）而機械選法看不見它**。理由寫進 registry note 以便日後推翻。這是 [611] 那條判準的延伸：**id 的選法是機械的，name 的選法不是；現在多一句——前綴也不是。**<br>⚠ 本條所述的漏報案例**在合併後改變了形狀**：`foundry_logic_advanced` 已併入 `foundry_logic`，所以原本該併的那一對現在是 `tech:foundry_logic` ↔ `tech:leading_edge_logic_foundry` 的子集關係，等下一輪判。**規則的漏報本身沒有被修**——提案方向（讓偵測器吃 `name` 的 token 而不只是 id 的 token）仍待量測。 |
| **🔶 V1–V3 已交付，剩 V4｜逐字證據在載入那一刻被丟掉——於是每個下游只能相信 label，而 label 的 base rate 是 35% 錯**（2026-09-18 zoom-out，使用者問「是不是以後每一檔產 bet 都會遇到」） | 🔶 | **Z3，已寫成 [`2026-09-18-verbatim-never-reaches-the-decision.md`](brainstorms/2026-09-18-verbatim-never-reaches-the-decision.md)**（goal／current／trap／new architecture／V1–V4／proposed Step 全在該檔）。**四個實測：** ①`extractions/` 有 **1,105 段逐字、192,055 字元**，**進到圖裡 0 段**；②`EdgeAssertion.source_ids` 在圖裡命中 **0 個**（懸空參照——它們是片段 id，只存在於抽取檔）；③三個主要讀路徑 `query/structure.py`／`query/bottleneck.py`／`query/graph_context.py` 提到 `quote` 的次數 **0**；④`loader/load_to_neo4j.py` 有 `MERGE_NODE`／`MERGE_EDGE`／`MERGE_SOURCE_DOC`／`MERGE_EDGE_ASSERTION`／`MERGE_NODE_CLAIM`／`MERGE_EDGE_CLAIM`——**唯獨沒有 sources**，而 `quote` 在 schema 有定義、抽取端有產出、`validate.py` 會檢查。<br>**base rate：** 今天只重判了一個 relation（`enables` 82 條），**29 條要改（35%）、8 條逐字根本不支持任何關係（10%）**；`is_component_of` 上也已找到同型錯誤（`external_laser_source is_component_of isolator`，[610]）。<br>**為什麼沒有東西會紅：迴圈是封閉的**——抽取時 LLM 給 label → 程式照 label 做 → 測試驗「程式有沒有照 label 做」→ 回到 label。**逐字是唯一在迴圈外的東西，而它沒有路回得來。**六條 invariant 沒有一條管「這個 label 的逐字支不支持它」。<br>⚠ **prompt 層修不動**（兩個理由）：①靠自律——L11-6 這條判準早就在 `AGENTS.md`，今天照樣發生（L16 的形狀：判準有 SSOT 但沒跟著資料走到需要的地方）；②**就算想挖也沒東西可挖**——今天所有發現都是**繞過自己的工具**直接 `grep extractions/` 找到的，`python -m query.structure` 的輸出裡一段逐字都沒有。**判準一句話：深挖若需要繞過自己的工具，它就不會例行發生。**<br>**proposed current Step ＝ V1 只做一件事**：`loader` 新增 sources MERGE ＋ `(EdgeAssertion|Claim)-[:QUOTES]->(Source)`。**純新增，既有節點／邊／屬性／排序／artifact 一個都不動。**驗收四條：①圖裡逐字 **0 → 1,105 段**；②`source_ids` 命中率 **0% → 100%**；③既有輸出逐位不變（accepted 37 列位移 0、`audit invariants` FAIL 0、pytest 不減）；④loader 冪等。<br>⚠ **它動到 Engine A 的 loader 與圖 schema（多一個 label ＋ 一種關係），依 L17-2 是 Z2／Z3，停在這裡等人。**<br>**V2–V4 不得先做**（都要指得回逐字，否則又是一層 label）：V2 讀路徑加 `--quotes`；V3 重複節點偵測器（**現在跑就有 37 對候選、31 對同 `abstraction_level`、涉及 43／297 節點＝14.5%**，一個 30 行純字串比對，只提名不合併）；V4 bet skill 的輸入契約改成「引用逐字，不引用欄位」。<br>**建議同時沉澱一條 `AGENTS.md` Lesson（L18）**，逐字草稿在 brainstorm 檔第 8 節——**改判準句要你核准，我沒有自行寫入。**<br>**✅ V1 已交付（2026-09-18，commit `7e6a3bd`）：** `loader/load_to_neo4j.py` 新增 `MERGE_SOURCE`＋`LINK_QUOTES`（`(n)-[:QUOTES]->(:Source)`），`scripts/backfill_source_quotes.py` 回填存量。**圖裡逐字 0 → 1,057 段**（`Source` 1,058 個、`QUOTES` 邊 2,959）；`source_ids` 命中 **0% → ~100%**；既有輸出**八項逐位不變**。⚠ `Source` 是 append-only 證據不是可覆寫投影（`quote`／`locator` 用 `coalesce` 保值）。⚠ **首版只接了邊斷言與 claim、漏了節點**，被驗收測試抓到——「只多一行字、不動任何一列」這種宣稱必須可否證。<br>**✅ V2 已交付（同一輪）：** `python -m query.structure <node> --quotes` 每條邊印得出它自己的逐字；該工具先前**輸出裡一個字都不是文件實際寫的**。<br>**✅ V3 已交付（2026-09-18 晚）：** `query/duplicate_nodes.py`（新模組）——**兩條純字串比對規則**（`id_token_subset`、`name_identical`），scope 與 `identity/entities.py` 的 `ENTITY_PREFIXES` 對齊（公司走 registry，INV-1）。**三個落點，都是既有的地方**：CLI `python -m query.duplicate_nodes`、APP coverage 頁新區塊（`coverage` artifact 的 `duplicates` 鍵，schema bump `coverage/1`→`/2`）、`engine_b/queue_segments.py` 新段 `duplicate_node_candidates`（consumer ＝ research-drain 第 4 段，**skill 已同步改寫**，否則 consumer 是說謊）。<br>**實測（2026-09-18 晚，191 個非公司實體節點）：候選 34 對｜同層 28｜涉及節點 49／191｜registry 提過 4｜沒人提過 30。** ⚠ 與 zoom-out 記的「37 對／43 之 297」不同是**分母與存量都變了**：當時分母含公司節點，且 [610] 已把 `tech:inp_eml` 併掉——**合併會讓候選自己消失，那就是清除機制在運作**。<br>**為什麼放在 coverage 同一頁**：重複節點正是 🔴 的誤報來源——一個已經有供應商的東西被攤成兩個節點後，孤立的那個看起來像空白（`config/entity_aliases.json` 的 `_readme` 逐字記過這個後果）。所以 research-drain 第 4 段**先問「它是不是旁邊那個」，再問「誰供應它」**。<br>**「這會讓哪個數字變」有答案，而且是實測不是推論（L14-5）：4 個 🔴 真缺口裡有 2 個同時是重複節點候選的一端**——`tech:scale_up_cpo`（↔`tech:cpo`，同層）與 `tech:scale_across_components`（↔`tech:scale_across`，不同層）；🟡 建模待補 14 個裡另有 2 個（`tech:foundry_logic_advanced`、`tech:scale_out_cpo`）。⚠ **這不代表那 2 個 🔴 是假的**——它代表在派研究去挖它們之前，先有一個可機械生成的問句：「它是不是旁邊那個？」答案兩個方向都可能，而**逐字就在旁邊**。查證：`python -m webapp materialize --coverage` 後比對 `research_gaps` 與 `duplicates` 兩個鍵的節點交集。<br>⚠ **第三條規則被真實資料當場推翻**（第三次了，形狀同 D15／稀釋那盞）：想抓 `dram_manufacturing`／`dram_production` 那型而寫的「同 token 數、只差一個 token」，一跑就讓**所有單 token id 互相全配**（`prod:reliant` 配上 `mat:photoresist`）。那不是門檻沒調好，是規則零鑑別力。**改法是明說自己抓不到那一型**（寫進 `THIS_IS_NOT`），不是用會誤報的規則假裝抓得到（L16-4）。<br>⚠ **首版用裸的 `in` 判 registry note，被本模組自己的測試抓到**：`tech:nand` 是 `tech:nand_flash` 的子字串，於是它被每一筆 nand 家族的 note「提到」。改成 id 邊界正則＋「是 canonical／是 alias／note 逐字提過」三選一。<br>⚠⚠ **`mentioned` 刻意不叫「已決定不併」**：registry 的 note 是自由文字，「刻意不併」與「留待研究判斷」（兩者今天都真實存在）長得一模一樣——機械讀得出「有沒有提到這兩個 id」，讀不出它說了什麼。所以只端 note 逐字給人讀，**不替它下結論**（那會是宣稱 authority 沒有的東西）。守門測試：判決字眼只准出現在免責段。<br>**每一對都印兩端各自的逐字**——這是 V1 之後才做得到的事，也是 V3 存在的全部理由：`prod:reliant` 與 `prod:reliant_product_line` 兩端的逐字**是同一份 10-K 的同一句話**，一眼就看得出來；2026-09-18 之前這個判斷只能用 id 與 name，而兩者都是抽取時 LLM 取的（用 label 驗 label，L18）。守門測試 `tests/test_duplicate_nodes.py` **11 條**。<br>**⚠ V3 交付途中抓到 V1 留下的一個回歸，當下修（L17-2：十行內、不動 contract）：** `query/coverage_gaps.py` 的 `degree` 用裸的 `-[any_rel]-`，V1 讓逐字進圖之後它把**證據邊也算成結構邊**。實測 **188 個節點的 degree 全部虛增**，其中 `tech:lta_framework` 與 `tech:scale_out_network`（結構邊 0、各有 2 段逐字）**失去了 `isolated` 標記**。後果不是數字難看——`isolated` 決定「下一步」印哪一句，而「先確認它該掛在 stack 哪一層」與「誰供應它」是兩個不同的研究動作。修法是 Cypher 加一行 `WHERE type(any_rel) <> 'QUOTES'`；修後 `tech:scale_out_network` 的 `isolated` 回到 `true`、`tech:scale_up_cpo` 的 degree 5 → 4。⚠ **這正是 V1 那輪「既有輸出逐位不變」宣稱的一個反例**——當時驗的八項裡沒有 coverage artifact，而它是 derived cache、要 materialize 才看得到。守門測試 `tests/test_coverage_gaps.py::test_degree_never_counts_verbatim_as_a_structural_edge`。<br>**✅ 交付當天就走完第一趟全程（提名 → packet → 核准 → 入圖 → 清單變短）：** 使用者 2026-09-18 核准 [611]（三個美中出口管制節點併為 `tech:us_export_controls`）與 [612]（一條 `is_component_of` 方向相反）。**候選 34 → 32 對｜「沒人提過」30 → 28｜涉及節點 49／191 → 46／189。**<br>⚠ **這一步是 L14-4 三個免 outcome 測試裡「不會滅」那一條的答案**：清單**真的會變短**，而且變短的機制是既有的（合併 → 節點消失 → 候選自動消失），不是新加的。⚠ 同時也量到另一半：登記 [611] 之後、還沒 migration 之前，那兩對從 `unmentioned` 自動移到 `mentioned`（4 → 6）——**registry 一寫下決定，偵測器當場就不再拿它問第二次**。<br>**[611] 的執行細節**：`config/entity_aliases.json` 新增一組（`semantic_reviewed`＋`approval_receipt: todo:611`）後**直接重跑既有的 `loader/migrate_entity_alias_backfill.py`**——那支是泛化版（吃 registry 的每一組），所以沒有寫第三支一次性腳本。7 條邊搬移、4 份抽取檔重載、兩個 alias 節點刪除；重跑冪等（`groups_to_migrate` 空）。⚠ **圖裡 canonical 的 `name` 仍是三者中最不精確的那個**（`U.S. Export Controls on Semiconductor Equipment`，漏了 China 與 Technology）——`MERGE_NODE` 保留既有 name，而改 name 不在 [611] 的 exact action 內，**刻意未做**。⚠ 另外查證了一件差點寫錯的事：`config/entity_aliases.json` 的 `name` 欄**不被任何程式讀取**（`identity/entities.py` 的 `_validate` 不驗它、migration 也不寫它），所以不得在那裡宣稱圖會改名。<br>**[612] 的執行細節**：使用者給了兩個選項（翻轉／刪除），選**翻轉**——刪除會丟掉一段 tier-2 逐字，翻轉後它成為同一條 canonical edge 的第二份 provenance（`documents` 1 → 2），保留證據沒有下行風險。實作是**擴充既有 `migrate_enables_rejudge_20260918.py` 的方向修正清單**（單一常數 `ISOLATOR` 改成 `DIRECTION_FIXES` tuple），不寫第三支腳本（L17-2：十行內、不動判決檔格式）；檔頭已註記**第三次撞到同型就要抽成資料檔**。驗收：`tech:cw_dfb_laser` 的『下一層』4 → 3 條、腳本重跑冪等（`files` 0）。⚠ 那份抽取檔（`coherent_ofc_march25.json`）本來就在 `.gitignore` 的排除清單裡（一批法說會逐字稿抽取檔都是），**所以這次的改動不進 Git**——不會回歸（fresh clone 沒有那份檔就不會重建那條邊），但也沒有版本記錄。<br>**兩次入圖後排序都逐位不變**：`input 221｜accepted 37｜filtered 184`（兩個編號動的都不是參與排序的邊）。<br>**狀態：V1 ✅｜V2 ✅｜V3 ✅｜V4 ○**（bet skill 的輸入契約改成「引用逐字，不引用欄位」，仍未做）。 |
| **V2 只做了一半：`--quotes` 印得出「這條邊的逐字」，印不出「這一格的逐字」**（2026-09-18 重讀 `mat:inp_substrate` 時撞到） | 🔶 | **Z2 proposal**（動的是 `_Q_QUOTES` 的分組鍵＝讀路徑契約，依 L17-2 不是當下修）。現況：`query/structure.py::fetch_quotes()` 以 `(src, relation, dst)` 為 key 收集逐字，把**同一條 canonical edge 上所有 EdgeAssertion 的逐字合併**印出。於是要回答「這條邊的 `substitutability=3` 憑什麼」，仍必須**繞過工具**直接查 EdgeAssertion 的 `attributes` 與 `source_ids`。<br>**實例（本輪真的走了一遍）：** `mat:inp_substrate` 供給側三條的 `sub=3` 掛在 [596] 建的三筆 `gsr_inp_substrate_market_2026_05_16_sub596_*` 上，它們各自的逐字**全部是市佔數字**（《Three players control over 90% of the market》＋《~36%／~42%／~13% share》）——**沒有一段在講「客戶換不換得掉」**。而同一條 canonical edge 上還並存著方向相反的一段（AXT 10-K：《Other products, such as InP and low EPD GaAs wafers, have fewer competitors》）。合併印出來的結果是：**讀的人看得到逐字，卻對不上是哪一段支撐哪一格。**<br>⚠ **這不推翻 V2，是 V2 的下一階**：V1 之前一個字都沒有，V2 之後有字但指不到格。判準仍是 L18-4——深挖若需要繞過自己的工具，它就不會例行發生。<br>**提案方向（未定案）：** 讓 `--quotes` 依 EdgeAssertion 分組並印出該筆的 `attributes`，使「這一格是哪一筆斷言填的、那一筆的逐字是什麼」在同一個畫面上。⚠ 要選的問題：一條邊常有 5–7 筆 assertion，全印會把表撐爆——是只印「有填屬性的那幾筆」，還是加一個 `--why <屬性>` 旗標？ |
| **✅已交付 2026-09-18：結構讀圖的「證據」欄在全圖每一條邊上都印「供應商自報」——那是 dataclass 預設值，不是判定**（V3 交付後重讀 stale 讀圖時撞到） | ✅ | **當下修（L17-2：四行、不動 contract、唯一 owner 已存在）。** `CanonicalEdge.evidence` 的 `"self_reported"` 是**預設值**，賦值只寫在 `rank_bottlenecks()` 裡，而 `query/structure.py` 不經過它。**實測 526 條 canonical 邊裡 430 條（81.7%）被印錯**——真實分布是外部印證 **217（41.3%）**／待判定 108／自報·filing 105／供應商自報 96。<br>⚠ **後果不是欄位難看**：讀五個角度判 A（護城河）還是 B（量）的人，看到的是「所有證據都是供應商自報」，會系統性低估整張圖的證據品質；而**「還沒算」與「算出來就是自報」在畫面上完全同形**（L12：預設值偽裝成觀測）。<br>⚠ **它藏在一句自己寫對了的註解底下**：`_load_edges()` 的註解逐字寫著「共用 `collapse_assertions`，不自己收斂——否則結構讀圖與排序會對同一條邊給出不同的值，而那是 L16 說的每個消費端重造一份」。**那件事正在發生，只是發生在它沒想到的那一欄**：收斂共用了，賦值沒有。<br>**修法是去用既有的唯一 owner**（`classify_evidence`），不是在 `structure.py` 重造判定；守門測試 `tests/test_structure_reading.py::test_evidence_column_is_computed_not_a_dataclass_default` 同時擋「不算」與「重造一份」（斷言 `externally_corroborated`／`EVIDENCE_RANK` 不得出現在該模組）。<br>**驗收（現有資料幾筆真的變了，L14-1）：430 條**；兩份 stale 讀圖各自受影響 19 條（`mat:inp_substrate`）與 16 條（`tech:cw_dfb_laser`）。實例：`co:axt supplies_to mat:inp_substrate` 由「供應商自報」改為**自報·filing**（AXT 自己的 10-K，法律責任）；`co:sumitomo_electric`／`co:jx_advanced_metals` 兩條改為**待判定**（來源是 Reuters 與 Global Semi Research，第三方媒體，origin 解析不到公司）。⚠ `result_digest` 因此變了，但**沒有新增任何「該重讀」**：現有讀圖只有 2 份，且兩份本來就已 stale。查證：`python -m query.structure mat:inp_substrate` 看供給側三條的證據欄。 |
| **「刻意不併」沒有結構化登記處——重複節點候選清單只會因為真的合併而變短**（2026-09-18 V3 交付時撞到） | 🔶 | **Z2 proposal，且有使用者要選的問題，所以沒有當下做。** 現況：`config/entity_aliases.json` 只有 `canonical` 區塊（＝「這兩個是同一個」），**「這兩個不是同一個」沒有地方寫**——今天那兩筆決定（`tech:advanced_packaging_gf` 是 GF Fab 8 特指、`tech:3d_nand_manufacturing` 是證據不足的幻覺節點）藏在別筆登記的 `note` 自由文字裡。後果是機械的：偵測器只讀得出「有沒有提到這兩個 id」，所以那些對永遠留在清單上——**清除率趨近 0 就是牆不是閘門**（L14-4）。⚠ 今天不急：`unmentioned` 30 對裡沒有一對是「看過且決定不併」的，所以清單現在仍有鑑別力；**要等第一批被看過之後才會變成牆**。<br>**⚠ 更正：掃一眼那 30 對就知道不是「不急」，是下一步立刻會撞到**（2026-09-18 交付當天實測）：**10 對是 `tech:cpo` 這個 hub 的變體**（`glass_cpo`／`inp_cpo`／`scale_up_cpo`／`cpo_fiber_attach`…），多數是**特指或製程步驟**不是重複；另有 **2 對是「產品 vs 技術」**（`prod:aaoi_400mw_narrow_linewidth_pump_laser` 對上 `tech:pump_laser`／`tech:narrow_linewidth_laser`）——那是「產品實作了某個技術」，該補邊不該合併。**也就是說 30 對裡多數的正確結論是「不併」，而那個結論今天無處可寫。** 逐對判讀的產出留不住 ＝ 下一個 session 會再看到同樣 30 對，**這正是「不會滅」的具體樣子**（L14-4）。⚠ 所以**刻意沒有在交付當天逐對判讀**：先有地方寫，再判讀。<br>順帶一個**真候選**供選項評估時參考（不是判定）：`tech:export_controls_china`／`tech:us_export_controls`／`tech:export_controls_us_china` **三個節點的逐字都在講同一件事**（美國對中半導體出口管制），而它們是概念節點，合併不影響排序——**適合當第一個試走完整條路的案例**。<br>**要使用者選的問題（所以停在這裡）：** 「不併」是不是研究判斷？若是，它應與 `semantic_reviewed` 同門檻（必須帶 `approval_receipt` 指向 pq2 編號）——那代表**每否決一對都要鑄一個號**；若否，就是一個沒有 receipt 的宣告，而 registry 的 `_readme` 明寫「沒有 receipt 的語意判斷不是 canonical identity，只是某個 agent 的意見」（L15）。⚠ **兩個方向都能舉出具體災難，這正是 L12 的訊號**——不是門檻沒調好，是「這兩個是同一個」與「這兩個不是同一個」被壓在同一個表示（`canonical` 區塊）裡。<br>⚠ 另一半同樣要選：`tech:eml` 的 note 逐字記著原判斷「`tech:inp_eml` 是 InP 材質特指，不併」**在 2026-09-18 被逐字推翻**——所以任何「不併」登記都必須是**可被推翻的**，不能是終局。那要求它帶 `revisit_when` 或至少留得下 supersede 紀錄。 |
| **✅已交付 2026-09-18：`query/graph_context.py` 的 claim 截斷不說話——讀圖只看得到 154 條裡的 20 條，而截斷本身沒有任何訊號**（2026-09-17 實測發現） | ✅ | **Z2 proposal**（改的是排序與呈現契約，不是補資料）。⚠ **這是 [602] 提案時以為的病因的真正病因**：診斷寫「圖中缺 AXT↔JX 的 `competes_with` 邊，所以 JX 的擴產證據不在 AXT 的 context slice 裡」——實測**最短路徑只有 2 跳**（`co:axt → mat:inp_substrate → co:jx_advanced_metals`），早就在 `_Q_COMPANY_CLAIMS` 的 `[*1..2]` 母體內；真正被擋住的是 `LIMIT`：`claim_limit=20` 時 JX 出現 **0 次**，`claim_limit=200` 時出現 **7 次**。排序鍵是 `demand_proof_level` + `confidence DESC`（**不是跳數也不是相關性**），JX 那三條 confidence 0.8／0.8／0.75 排在 0.90 那批後面就被切掉。**補一條邊改不動這件事**——它會讓供應關係表多一列，但 claim 的排名一位都不會動。形狀是 F-20（截斷集合被當全集），而且**沒有任何東西會變紅**。查證：`python -c "from query.graph_context import build_context; ...build_context(d, company_id='co:axt', claim_limit=20 vs 200)"` 數 `JX Advanced Metals` 出現次數 <br>**✅ 2026-09-18 交付（讓它說話那一半；「選得更好」另計）。** 六個區段全部改走 `_fetch()`：**第一趟用原封不動的查詢取列、第二趟拿掉 `LIMIT` 只數總數**，每段標題下多一行 `N／M`，文件層級在有任何一段被截時多一段 caveat。⚠ **沒被截斷時也要印**（`（10 條全部列出，沒有截斷）`）——否則「全部列出」與「這一段壞了」在輸出上同形。<br>**before → after 實測**（六段裡**五段先前沉默**）：公司模式 claims **20／154**（AXT）、**20／243**（COHR）；全圖 supply **50／515**、claims **20／325**、demand **10／23**、瓶頸 **30／52**；公司模式 supply 10–38 條**從來沒被截過**，所以它印「全部列出」。<br>⚠⚠ **實作中被自己的驗收測試抓到一次，值得記：** 首版把 `LIMIT` 從 Cypher 拿掉、改在 Python 端截，看起來完全等價——**實測不是**。帶 `LIMIT` 時 Neo4j 走 Top-N 運算子，平手（本圖大量 `confidence=0.90`）的 tie-break 與全排序不同，於是 `co:axt` 的 20 條 claim **換了一批**。舊版連跑兩次完全相同，證明那是改出來的、不是它本來就不穩。**修法宣稱「只多一行字，不動任何一列資料」，那個宣稱必須可否證**——所以驗收就是逐行 diff：現在四份 context（全圖＋AXT＋COHR＋Sivers）的資料列 **177／95／123／109 行全部逐行相同**。<br>⚠ 另一個當場炸出來的：`_Q_BOTTLENECK` 含裸的 `coalesce(r.attributes, '{}')`，對它呼叫 `.format()` 會把 `{}` 當位置參數（CypherSyntaxError）。判斷條件必須是「模板裡有沒有 `{limit}`」。<br>**另外補的一件：token guard 的二次收縮先前只印到 stderr。** 讀 context 的是下游 LLM 與使用者，他們看不到 stderr——那件事對消費端而言等於沒發生（L13-2）。現在收縮訊息寫進 context 本身。<br>守門測試 `tests/test_graph_context_truncation.py`（6 條，含「取列那一趟不得拿掉 LIMIT」與「六個區段都要被 `_LIMIT_RE` 認得」）。<br>⚠ **剩下的一半沒做，而且它需要決定：** 「選得更好」——今天的排序鍵是 `demand_proof_level` ＋ `confidence DESC`，**不是相關性也不是跳數**，JX 那三條 0.8／0.8／0.75 就是這樣被切掉的。要改排序鍵得先答「改成什麼、會讓哪幾份 context 真的變」，那是另一個 Z2。**但現在至少不會再有人把截斷後的集合讀成全集。** |
| **歸零旗標的第四盞（going concern）結構上點不亮——沒有欄位承載「結構化的 going-concern 判讀」**（2026-09-18 交付歸零旗標時撞到） | 🔶 | **Z2 proposal**（動的是 Engine C 的欄位字彙＝動 contract，依 L17-2 不是當下修）。現況：`litigation_and_audit_flags` 是 `judgment` 自由文字欄，全庫只有 **1 筆**（6594.T，不在籃子裡）；而 D7 指定的第一個案例 **IQE.L 的 going-concern 逐字已經在庫裡**——它躺在 `debt_maturity_and_covenants`（KPMG 查核報告逐字：董事結論 no material uncertainty、查核人員『could have given rise to』、2026-05-28 募資後重評）。**分類有 SSOT 卻沒跟著資料走到需要它的地方**（L16）。方案要回答的是**字彙**：`none`／`material_uncertainty`／`qualified_opinion`／`adverse`／`disclaimer` 這類值的措辭精度本身就是一個 claim（L11-1：『公司自揭 material uncertainty』≠『審計出具保留意見』），所以字彙選錯的代價不是不方便而是一個假 claim。⚠ **刻意今天不做**：加一個欄位會讓**現有資料 0 筆改變**（L14-1），真正的 binding constraint 是「有沒有人寫下那個判讀」，而那是 pq2（Engine C 判讀寫入）。驗收：IQE.L 的第四盞燈由灰變色，且 64 盞裡的 `unlit` 下降。查證：`python -c "import json;print(json.load(open('library/private/app/state/basket.json',encoding='utf-8'))['wipeout_ledger']['lamps'])"` |
| **IQE.L 的現金跑道用 yfinance 的 `free_cash_flow_ttm` 算出 394 個月，與它自己年報揭露的流動性軌跡對不上**（2026-09-18 歸零旗標實測發現） | 🔶 | **研究（pq1）不是開發**：yfinance 給 IQE.L 的 FCF TTM 是 **−1.27M**（GBP），據此算出跑道 394 個月 → 燈判成黃「撐得過一個 going-concern 評估期」；而 KPMG 查核報告逐字記著 base case 流動性 headroom 2025-12 £11.7m → **2026-05 £6.1m**（募資到位前的低點）。**燈沒有錯，錯的是它吃到的那個數**。修法不是改規則（規則對其他 13 檔都合理），是補一筆 H1 2026 的 `runway_inputs`——那是 `mechanical` 欄位（**不需 pq2**），缺的是有人去讀 2026 年 9 月的 H1 RNS。⚠ 這正是歸零旗標把數字留在稽核層的價值：`runway_months=394.5` 一眼就看得出可疑，而燈只給顏色時看不出來。查證：`python -m briefing alpha-card IQE.L` 的 13f 段 inputs |
| **`counter_path_relation` 封閉字彙被兩個不同的問題共用——結構讀圖的「反向路徑」角度裡有一條結構上不該算的邊**（2026-09-18 重讀 `mat:inp_substrate` 時量出來） | 🔶 | **Z2 proposal**（動封閉字彙＝動 contract，依 L17-2 不是當下修）。實測：全圖 `constrained_by` **8 條**、散在 **14 個節點**，**100% 同時**出現在 `counter_path` 與 `demand_side`／`next_layer` 兩個角度——**零獨佔覆蓋**；移出 `counter_path` 會讓 14 個節點的該角度變動，其中 **10 個由非空變成空**。語意上 8 條沒有一條是「有東西在取代它」（`X constrained_by Y` ＝ X 被 Y 卡住：對 Y 是需求側證據、對 X 是下一層）。⚠ **但它不是漏掉**：`schema/vocab.json` 的 `counter_path_relation` 逐字是 `["competes_with", "constrained_by"]`，而 `engine_d_runtime.adapters.counter_path_relations()` 的 docstring 明寫「它猜不到 constrained_by」——**SSOT 是刻意包含它的**。真正的形狀是 **Engine D 的 coverage gate 問「什麼會讓這條因果鏈斷掉」、結構讀圖問「有沒有東西在取代它」，兩個不同的問題共用一個封閉字彙**（L12 出現在字彙層，不是欄位層）。所以方案要回答的是**要不要把字彙拆成兩個**，而不是改 `query/structure.py` 的私有 `_COUNTER`（⚠ 它目前是第三份硬編副本，與 SSOT 恰好一致所以沒人發現——L16-1 的 (b) 層）。⚠ 具體後果（今天不會誤觸發，但會）：`counter_path` 屬 `DISPROOF_KINDS`，**哪天有人替另一家公司補一條 `constrained_by` 指向某個瓶頸節點，該節點讀圖的 disproof ③「反向路徑新增」就會響——而那其實是「又多一個人繞不過它」，是利多不是反證**。驗收（哪個數字會變）：`mat:inp_substrate` 的 `counter_path` 由 1 → 0，且全圖 counter_path 與 demand_side／next_layer 的重複邊由 **8 條 → 0 條**。查證：`python -m query.structure mat:inp_substrate`（反向路徑段）｜`python -c "import json;print(json.load(open('schema/vocab.json',encoding='utf-8'))['counter_path_relation'])"` |
| `review_conditions` 對照不到期中實績——`metric_observations` 只從 `fiscal_year_results` 攤平，而半年／季別的期中觀測在 `OBSERVATION_PERIOD_KINDS`（只有 `fiscal_year`／`fiscal_quarter`）下**表達不出來**（2026-09-17 寫 AXTI variant 時撞到） | 🔶 | **Z2 proposal**（動封閉字彙＝動 contract，依 L17-2 不是當下修）。⚠ 後果是具體的：AXTI variant 的每季核查條件只能靠人看 Q3／Q4 財報，機器對照要等 FY2026 年報（約 2027-02）——**一個會響但太晚響的火警**（L7）。 |
| APP 的標的列表還不知道「等財報」這種終局——它會顯示成「還沒做」（2026-09-12） | ○ | 維持營運（當下修檔次，不占 Phase） |
| AEVA／TSEM 已查證未揭露（2026-09-04） | ○ | 留 archive（資訊，不是工作） |
| 節點屬性沒有衝突解決機制（偵測那一半已於 2026-09-10 交付） | 🔶 | 留 archive，等第二個實例再泛化（L17-4） |
| 誠實回 `unknown` 的軸讓該檔結構上永遠到不了段 5 終局（2026-09-10） | 🔶 | **Phase 5**（重看 readiness 語意時一併） |
| Engine C 快照的 `gross_margin` 是 TTM、`operating_margin` 是單季（2026-09-11） | 🔴 | 已由同表 2026-09-13 ✅ 列解決（重複列），結案 |
| 入圖後自動追蹤把非公司節點也建成 cohort（2026-09-11） | 🔴 | 已由同表 2026-09-12 ✅ 列解決（重複列），結案 |
| 殘餘缺口已鑄成 Engine C 觀測 pq2 之後，collector 仍為同一缺口鑄「去做研究」的 pq2（2026-09-11） | 🔴 | 維持營運；**Phase 2** 整理佇列段時一併 |
| 段 5 的工單把「需求側節點」與「供給側候選」當成同一種東西（2026-09-11） | 🔴 | **Phase 4**（正是篩選層要分的） |
| registry 缺 `market_currency` 會讓 headline 整格消失，錯誤訊息不指向 registry（2026-09-11） | 🔴 | 維持營運（當下修檔次） |
| `FiscalYearActuals.revenue` 是單一口徑，GAAP 與 non-GAAP 連營收都不同（2026-09-11） | 🔴 | **Phase 7**（橋契約） |
| 基期觀測有 8 檔同時有兩筆以上「生效」紀錄（寫入端已擋，既有 14 組還在；2026-09-13） | 🔶 | **Phase 7**（橋契約） |
| 橋的形狀對另類資產管理業不成立（加法鏈等使用者決定兩個契約問題；2026-09-13） | 🔶 | 留 archive——另類資產管理不是邊緣瓶頸供應商，轉向後優先序最低 |
| 共識口徑的核實只有一條路（`year_ago_actual`），基期被重述時結構性不可用（2026-09-13） | 🔴 | **Phase 7**（橋契約） |
| `*_fx_tolerated` 被套用在同幣別標的上（Z2，動的是 2026-09-11 定案的容差；2026-09-13） | 🔶 | 留 archive，待使用者決定，不排程 |
| 「卡世界」只能靠 `--skip` 人工宣告；機械代理實測恆亮（否定結果；2026-09-13） | 🔶 | 留 archive（記下來是為了不要有人再去建它） |
| **✅ 2026-09-19 已交付：催化劑那一格 15/16 亮著，而成因是 `Catalyst.resolves` 這個「可選」欄位從未被填過——新格式 62 個判斷檔、104 條催化劑，填寫數 0**（2026-09-19 實測，啟動 prompt ①） | ✅ | **Z2 proposal**（動到 `FILTER_REASONS` 封閉字彙）。①**gate 的意圖正確，但它問錯了問題：** 想問「這個賭注有沒有裁決點」（與 L7〔disproof 要附核查頻率＋48h 觸發動作〕同源），實際問的是「一個可選欄位填了沒」——L15-1（gate 攔的若是缺一個參數就是攔錯）說該修的是它問問題的方式。②**三個免 outcome 測試（L14-4）：恆亮 15/16＝93.75%；實測清除率 0**（V1 於 2026-09-15 交付，四天內新格式檔案清除 0 條）；**但因果機制講得出**（沒有裁決點的賭注不知道何時認錯）——所以是「問錯問題」，不是「該拆掉」。③**V1 交付當天的驗收數字就已經是警訊：** not_modeled 73 → 72，**唯一那 1 筆是交付者自己手填的 COHR**，而它住 legacy 路徑 `library/private/alpha/cohr_judgment.json`、不在 `judgments/`——**等於機制交付時下游真實資料變了 0 筆**（L14-1：驗收要寫成「現有資料有幾筆真的變了」）。④**15 檔的成因是四種形狀被壓成同一句話**（L12〔一個表示承載兩種語意〕；違反 AGENTS.md「缺席不得被壓成一句無資料」）：**A. 有日期、有假設可指名、只差 `resolves`＝4 檔**（LITE／SOI.PA／POET／000660.KS）**← 攔錯**；**B. 催化劑有散文但沒填日期＝6 檔**（AVGO／AXTI／IQE.L／MP／TSM／TSEM）；**C. 日期晚於目標價日＝1 檔**（GFS）**← 判準正確運作的唯一實例**；**D. 沒有催化劑／沒有判斷檔＝4 檔**（LYC.AX／5802.T／6324.T／MU）。⑤**驗收數字（先量後改）：補上 `resolves` 後真的會變的是 4 檔**，`no_catalyst_in_horizon` 15 → 11；**但籃子通過數仍然是 0**，因為那 4 檔都還沒寫賭注——**修這一格不會讓籃子非空，只會讓「為什麼空」變得誠實**。⑥⚠ **不得直接把 `resolves` 改必填**（L11-6：提案時就要答出「最先壞掉的是哪一筆現有資料」）：現有 104 條催化劑全部沒填，改必填會讓 62 個判斷檔全數無法通過契約驗證、alpha view 整片倒。⑦⚠ **更不得放寬 `_catalyst_in_horizon` 讓沒有 `resolves` 的催化劑也算**——那是「為了讓籃子非空而放寬篩選條件」，AGENTS.md 明文禁止，且會讓 gate 失去它唯一講得出的因果機制。查證：`python -c "import json,pathlib;ps=list(pathlib.Path('library/private/alpha/judgments').glob('*.json'));cs=[c for p in ps for c in (json.loads(p.read_text(encoding='utf-8')).get('catalysts') or [])];print(len(ps),'檔',len(cs),'條，有 resolves 的',sum(1 for c in cs if c.get('resolves')),'條')"`<br>**交付（2026-09-19）：** 照④的四種形狀把 `no_catalyst_in_horizon` **拆成四個理由**（`catalyst_missing_resolves`／`catalyst_undated`／`catalyst_after_value_date`／`no_catalyst_recorded`），舊 key 移除（L16-3：已淘汰的同義詞連寫入都要拒絕）。形狀本身由 **producer 宣告**：新增 `CatalystSection.shape` 這個**永遠 available** 的機械計數（`total`／`linked`／`unlinked_dated`／`unlinked_undated`／`earliest_unlinked_date`），一路投影到 overview 的 `catalyst_shape`，呈現層只做順序判定、**不 parse 理由句**（L16）。⚠ **為什麼需要新增一格而不是讀既有的 `ripeness`：** `quantitative_link` 在沒有 linked 催化劑時是 `not_modeled`，而型別層禁止無值狀態帶值（`VALUELESS_STATUSES`）——於是四種「沒有」在下游**結構上**長得一模一樣。<br>⚠ **⑥⑦ 兩條禁令都沒有被違反：** `resolves` **沒有**改必填（104 條仍全空，62 個判斷檔照常通過驗證）；`_catalyst_in_horizon` **一字未放寬**——**四個理由全部仍然是 filtered，被擋的檔數一個沒少**。<br>**before → after 實測**（查證：`python -m webapp materialize --basket` 後讀 `basket.json` 的 `filter.reasons`；守門測試 `tests/test_catalyst_shape_reasons.py` 8 條）：`no_catalyst_in_horizon 14`（一句話）→ **`catalyst_undated 6`（AVGO／AXTI／IQE.L／MP／TSM／TSEM）｜`catalyst_missing_resolves 3`（SOI.PA／POET／000660.KS）｜`no_catalyst_recorded 4`（MU／LYC.AX／5802.T／6324.T）｜`catalyst_after_value_date 1`（GFS）**——逐檔與本列④的分類**完全對上**（⚠ 唯一差異：④把 LITE 算在 A，但 LITE 本輪已有 linked 催化劑並通過 filter）。籃子 accepted 仍是 1、首選仍是 LITE。<br>**現在讀得出來的那句話：** 14 檔裡**只有 GFS 這 1 檔是判準真的在運作**，另外 13 檔是我們自己沒填——9 檔沒日期或沒記錄、3 檔只差 `resolves`。**修這一格不會讓籃子非空，只會讓「為什麼空」變得誠實**（本列⑤原話）。 |
| **✅ 2026-09-19 已交付（兩半都做完）：`review_required` 拿 +1y 的共識去判定校準在 0y 的 base 假設，rollover 時整批假警報；且覆蓋由 22 人崩到 1 人被讀成「共識上修 +19.2%」**（2026-09-19 實測，做 LITE 賭注時撞到） | ✅ | **Z2 proposal**（動 refresh 判定，下游是 readiness／closure gate／drain 選題，多個 owner）。①**錯位是結構性的：** `briefing/alpha_view/changes.py::market_and_consensus_changes` 用 `forward_eps_from(price, pe_forward)` 偵測共識變動，而 `builder.py:2677` 自己逐字寫著那是 **+1y**；實測 **15/15 有假設的檔，base 全部校準在 0y**——所以每次 forward year rollover（每年必然發生一次），每一檔都會收到一個**與自己 base 無關的年度**觸發的複查要求。②**當下驗收數字：16 檔有 2 檔正在收假警報（LITE、AXTI）**，判準是「+1y 變動 ≥ noise floor 1% 而 0y 沒動」。⚠ 其中 **AXTI 是已研究到底的兩檔之一**——假警報正落在最該信任的那兩份判讀上。③**LITE 的實例（本輪主線因此卡住）：** +1y（FY2028）由 33.3012 → 39.70（+19.2%），**同時 `analyst_count` 由 22 崩到 1、`estimate_low`＝`estimate_high`＝39.70（區間寬度 0）**；而 thesis 真正校準的 FY2027 從 21.6426 → 21.6706，**只動了 +0.13%**。**base 其實完全沒有被推翻，訊號卻說它需要重做。**④**「1 個人的看法不是共識」這個判準系統裡早就有，只是沒機械化**（L16：分類有 SSOT 卻沒跟著資料走到需要它的地方）：POET 的 Abstention `ab_42838d2eec25b735` 逐字寫著「**僅 1 位分析師，high＝low，區間寬度為零——那不是共識，是一個人的看法**」；packet 的 `valuation_market_implied.method` 也已對 rollover **fail closed**。**同一個問題，估值層 fail closed、refresh 層照報**（L12：一個表示承載兩種語意）。⑤**實測覆蓋崩塌不是 LITE 獨有：** 全籃子 forward EPS 實為 1–2 人看法的有 **2 檔（LITE n=1、POET n=1）**，佔 12.5%——**有鑑別力、不恆亮**（L14-4），所以值得做成 gate 而不是拿掉。⑥⚠ **L11-6「如果這個修法是錯的，最先壞掉的是哪一筆」：** 若直接改成只比 0y，**最先壞掉的是 SOI.PA 與 IQE.L**——它們實測是「兩年都動」（SOI.PA +1y +12.5%／0y −2.0%；IQE.L +1y −6.1%／0y +1.3%），改判準後會少收到 +1y 那一側的訊號。**提案要先去看那兩筆**，決定是「改成比 0y」還是「兩個年度各自成事件、標明是哪一年」。⑦另記一筆同源可疑值：**MP 的 0y EPS 變動實測 +3512.3%**（分母極小的除法），本輪未追。查證：`python -c "import sqlite3;print(list(sqlite3.connect('library/private/engine_c/stockbot-engine-c-private-v1-458db5270ee2.db').execute('SELECT snapshot_date,fiscal_label,estimate_avg,analyst_count FROM consensus_estimates WHERE ticker=? AND metric=? AND relative_label=? ORDER BY snapshot_date DESC LIMIT 4',('LITE','eps','+1y'))))"`——應看到 `FY2028 … 39.7, 1`（第四欄是 analyst_count，值為 1）。<br>**交付（2026-09-19）：** 做的是④那一半——**把「一個人的看法不是共識」機械化**（`briefing/alpha_view/changes.py::single_analyst_view`）：`analyst_count ≤ 1` 或 `low==high`（區間寬度 0）時，那個變動量到的是**誰還在看變了**，不是**他們怎麼看變了**——**不發事件、改發 note**（INV-3，比照該函式既有的「首次出現＝資料覆蓋變了」先例）。同時 `detect_changes` 改變順序：先取會計年度別共識，再把 +1y 的覆蓋傳給 `price/pe_forward` 那個代理量，讓它知道自己量到的是什麼。所有 CONSENSUS 事件的 detail 現在都標明**哪一年（0y／+1y）與覆蓋人數**。<br>**before → after 實測**（守門測試 `tests/test_consensus_coverage_collapse.py` 6 條）：覆蓋崩塌 **2／16 檔**（LITE 的 +1y n=1 寬度 0；POET 兩年都是）＝12.5%，**有鑑別力、不恆亮**（L14-4）。**LITE 的 thesis review 理由由假轉真**：before `consensus：forward EPS（price/pe_forward）33.3 → 39.7（+19.2%）`→ after `consensus：FY2028（+1y）revenue 共識 9.43981e+09 → 9.61372e+09（+1.8%；覆蓋 21 → 21 人）`。AXTI 前後都 `review_required`（覆蓋 5 人正常，+1.1% 是真變動）；POET 前後都 `current`。⚠ 所以**假警報檔數不是「2 → 0」**，是「那個假理由不再出現」——誠實的數字是 **1 檔的 1 個理由消失**。⚠ 另：本列原記 LITE 的 0y 覆蓋是 22 人，今天實測 **25 人**（又一個會腐壞的數字）。<br>**🔶 剩下的那一半（未做，因為它是一個真的取捨）：** 事件現在**標明**了年度，但 `THESIS_POLICY` 仍把**任何** `consensus` 事件變成 `review_required`，不看年度。要讓「只有 base 校準年度的變動才觸發」必須給 `ChangeEvent` 加年度維度（動 `alpha/refresh/contracts.py`＋`policy.py`，多個 owner）。⚠ **L11-6：最先壞掉的是哪一筆？** 是 **COHR**——AGENTS.md 明文寫著它「thesis 講 FY28，模型只有 FY27 一格」，`+1y` 一律不觸發會讓跨年度 thesis 結構上收不到訊號。所以兩個選項是**(a)** 只有 base 年度觸發 `review_required`、其他年度降級成 note；**(b)** 兩個年度各自成事件、都觸發但在 UI 分開呈現（現況＋標明，等於 (b) 的前半）。**這一題要使用者選**，不因常設授權自行決定。<br>**後半交付（2026-09-19，使用者「三題找你推薦」後）：** `ChangeEvent` 加 `fiscal_relative_label`（additive），`changes.py` 兩個來源都填（`fiscal_consensus_changes` 用 `item.relative_label`；`price/pe_forward` 代理量硬填 `+1y`——`pe_forward` 對應的就是 forward year）；`resolver.py` 的 accepted 迴圈加一道 `_is_off_year_consensus`：**`consensus` 類且年度明確不是 `0y` → 不進 hits**。<br>⚠ **三條刻意的邊界：** ①`None`（拿不到年度）**照舊觸發**——fail open，少報一個真變動比靜默吞掉更糟；②**只對 `consensus` 生效**，`financial_actual`／`company_guidance`／`graph_edge` 一律不受影響；③**不是靜默丟棄**——進 `excluded_changes['consensus_other_fiscal_year']` 計數，並逐條進 `notes`（INV-3）。<br>⚠ **選項 (a) 而不是 (b)，理由是 L14-4：** 原本的擔心是「COHR 的 thesis 講 FY28，+1y 不觸發就收不到訊號」——但那個缺口的解是**多年橋（Phase 7）讓模型真的有 FY28 那一格**，不是讓一個對 15/16 檔都會亮的訊號一直響。恆亮＝零鑑別力。被降級的事件仍然逐條印出來，COHR 的 FY28 變動看得到，只是不強制複查。<br>**before → after 實測**（守門測試 `tests/test_refresh_fiscal_year_scope.py` 6 條；查證：`python -m briefing refresh LITE`）：**LITE thesis `review_required` → `current`**（需要動作 2 → 1），那條 `FY2028（+1y）revenue +1.8%` 改印在 notes；**AXTI thesis `review_required` → `current`**（需要動作 2 → 1），`+1y 代理量 +1.1%` 同樣改印在 notes；**COHR 前後都 `current`**（不受影響）。**2/16 檔的假警報清掉，而訊號本身沒有消失。** |
| **✅ 2026-09-19 已交付：首屏短評結構上表達不了「判斷錯了值多少」——D2 的對稱只做了一半**（2026-09-19 實測，寫 LITE 首屏短評時撞到） | ✅ | **Z2 proposal**（動 `alpha/narrative/contracts.py::PLACEHOLDERS` 封閉字彙）。①**事實：** `PLACEHOLDERS` 有 `base_target`／`bet_target`／`payoff`／`base_return`，**沒有 `downside_target`／`downside_return`**；實測 `validate_slot_text('if_right_if_wrong', '…{downside_target}…')` **拒收**（「用了未登記的 placeholder」）。而同一格的 `do_not` 又寫著「**目標價與報酬一律 placeholder**」，字面值也不准打——**兩條合起來讓首屏結構上講不出下檔數字**。②**這不是缺資料：** overview 的「一把尺」早就同時有四個值（LITE 實測 `base_target=1065.76、bet_target=1078.72、downside_target=881.63、downside_return=-0.0134`）。**值有 SSOT，只是沒跟著資料走到首屏那一格**（L16）。③**與 AGENTS.md 的衝突是逐字的：** D2 寫「2026-09-16 起那把尺多一端：**判斷錯了值多少**」，且「賭注與**判斷錯了值多少**（反證觸發後的假設套同一條橋）**對稱**」——首屏是使用者唯一會讀的那一屏，對稱在這裡斷掉等於沒有對稱。④**驗收數字要誠實：現有短評只有 1 筆（COHR），加了 placeholder 它一筆都不會變**——binding constraint 在**寫入端**不在存量（L14-1 的例外情形：此處要量的是「未來每一份短評能不能寫出對稱」，所以提案必須同時指定一個可驗收的存量動作，例如把 COHR 那筆補寫下檔句並重印首屏）。⑤⚠ **L11-6「如果這個修法是錯的，最先壞掉的是哪一筆」：最先壞掉的是沒有 downside 假設的那些檔。** 實測 16 檔只有 **LITE 與 AXTI** 有 downside scenario；其餘 14 檔填 `{downside_target}` 時無值可填，**若靜默填 0 或空字串就違反「缺席不得被壓成一句無資料」**。提案要先決定缺席時印什麼（`absence_kind` 由產生缺席的那段程式宣告，呈現層不得猜——L16），**再**開字彙。查證：`python -c "from alpha.narrative.contracts import PLACEHOLDERS;print('downside_target' in PLACEHOLDERS, sorted(PLACEHOLDERS))"`——應印 `False`。<br>**交付（2026-09-19）：** `PLACEHOLDERS` 開兩個具名欄位 `downside_target`／`downside_return`（**不是把封閉字彙打開**，守門測試有一條就在驗未登記的仍被拒收）；`_brief_values()` 多吃 `downside` section 並填值——那兩個數早就在 `brief_scale` 裡，只是沒跟著資料走到那一格（L16）。`if_right_if_wrong` 的 `do_not` 加上「**對了與錯了要對稱**」。缺席沿用既有機制：`fill_brief` 印「（尚無）」＋該格標 `partial`＋理由列出哪幾個 placeholder 沒值，**不補 0**。<br>**before → after 實測**（守門測試 `tests/test_brief_downside_symmetry.py` 5 條）：**有短評的檔 3 筆**（⚠ 本列原記「只有 1 筆 COHR」是舊數字，今天實測 LITE／AXTI／COHR 各 1 筆）；首屏講得出「判斷錯了值多少」的 **0／3 → 2／3**。存量動作已做：LITE 與 AXTI 各 append 一筆 supersede 的短評（六格逐字照抄，只動那一格），首屏現在逐字印出 **LITE「每股 804.21 USD，對現價是 −13.6%」**、**AXTI「每股 35.15 USD，對現價是 −49.8%」**。COHR 維持沒有下檔句——**它沒有 downside scenario，那是「還沒做」不是「跌幅有限」**，硬寫才會違反缺席契約。<br>⚠ **本輪順手撞到並當下修掉的第二個缺陷（L17-1）：** 第一版 LITE 的補寫在 placeholder 旁邊寫了一句「上檔 {payoff} 對下檔 {downside_return}：**這不是一個不對稱的賭注，兩邊差不多大**」——寫的當下心裡用的是倍數校準前的 +10.1%／−10.0%，authority 實際填出來是 **+5.7%／−13.6%**，**那句話當場就是錯的**。已 append 一筆更正，並把判準寫進 `do_not`：**不得在 placeholder 旁邊寫一句依賴那個數字大小的結論**——數字由 authority 每天重填，那句話不會。這是「目標倍數是會腐壞的快照」（本表同日那一列）在短評層的同一個形狀。 |
| ~~**overlay 的 `scope` 與 base 不一致時，variant／downside 不覆蓋而是「疊加」**~~ **✅ 2026-09-19 交付（使用者核准方案 (a)）** | ✅ | **Z2 proposal**（`scope` 是自由字串卻決定 override 去留 ＝ L16-3 的「字彙一旦有行為後果就必須被強制」）。①**機制：** override 的匹配鍵是 `driver[scope]`。LITE 的 base `operating_margin_delta` 的 scope 是 `guided_q1_margin_held_flat`（不是 `total`）；overlay 寫成 `total` 時**兩條假設同時生效**，營益率變成 29.8＋10.2＋10.7 ＝ **50.7%**。②**實測代價：** 假的 variant EPS **29.71**、目標價 **1,343.12**、隱含報酬 **+50.3%**（真值分別是 23.86／1,078.72／+20.7%）。⚠ **這個錯誤的方向是「對自己有利」**——若未手算對照，它會直接成為籃子的 `top_pick`，而 `alpha-card` 的每一格都照常印、`audit invariants` 照常 PASS 13、43 條賭注測試照常綠。**這是 L18 的形狀：測試問的是「程式有沒有照標籤做」，不是「標籤對不對」。**③**為什麼撞得到：** 本輪唯一的偵測手段是**寫入前先手算一次三情境**，發現系統值與手算不符才回頭查 scope。這不是可例行化的紀律（L18-4：深挖若需要繞過自己的工具，它就不會例行發生）。④**提案方向（擇一，需使用者決定）：** (a) 寫入端在 overlay 的 scope 未命中任何 base 假設時**拒收並列出可選 scope**；(b) `scope` 收斂成封閉字彙；(c) payoff section 增印「本 overlay 命中了哪一條 base、base 值是多少」——實測 `overrides[0].dependencies` **已經有 `base_value`**，疊加時它會缺席或對不上，所以 (c) 可能是最小改動。⑤⚠ **L11-6「最先壞掉的是哪一筆」：** 若選 (a)，最先壞掉的是**合法的新 scope**——一條 overlay 本來就可以引入 base 沒有的切分。提案要先去看 AXTI／COHR 既有 overlay 的 scope 是否全部命中 base（實測 AXTI 用 `total`、base 也是 `total`；COHR 用 `mix_and_utilization`），**再**決定拒收與否。<br>**✅ 2026-09-19 交付。** ⑤那一步先做了，結果是決定性的：掃全部 62 檔 ledger，有 overlay 的只有 3 檔（AXTI／COHR／LITE）、**12 條 overlay 紀錄**，其中 **8 條命中 base、4 條未命中且全部是 LITE 那個已知 bug 的 `scope='total'`——合法引入新 scope 的 0 條**。所以 (a) 的唯一風險在現有資料上不存在，使用者據此核准 (a)。**落地**：寫入端 `alpha/providers/assumptions.py::append_assumption_record` 新增閘門（overlay 的 `(driver, scope)` 未命中生效 base key 即 `ContractViolation`，錯誤訊息列出該 driver 在 base 的可選 scope）＋純邏輯 helper `alpha.fundamental.assumptions.live_base_keys`＋CLI `--allow-new-scope` 讓「真的要新切分」變成 opt-in。**⚠ 撤回紀錄明文跳過這道檢查**——撤回沿用被撤回那筆的 scope，擋住它等於把「寫錯的 overlay 永遠撤不回」那個 bug 裝回去（同日才修好的）。**before → after 實測（L14「現有資料有幾筆真的變了」）：12 條 overlay 重放過閘門 → 放行 8、擋下 **2**、撤回紀錄跳過 2；擋下的那 2 條正是造出假 +50.3% 的 `LITE variant/downside operating_margin_delta[total]`，**零誤擋**。守門測試 `tests/test_overlay_scope_gate.py`（8 條，含撤回路徑與 `live_base_keys` 語意）。查證：`.venv\Scripts\python.exe -m pytest tests/test_overlay_scope_gate.py -q` |
| **✅ 2026-09-19 已交付：目標倍數是一個會腐壞的快照，而「目標倍數＝市場倍數」是一條不會腐壞的判準——沒有任何機制在盯兩者背離**（2026-09-19 [616] 紅隊審查實測） | ✅ | **Z2 proposal**（動估值層的 refresh／review_condition，不是動判準本身）。①**事發：** LITE 的 `va_8e435096b30b3d53` 逐字宣告「目標倍數＝校準倍數，**零折溢價**」，校準式是 `現價 978.535（bar_date 2026-09-08）÷ 共識 EPS 21.6426 ＝ 45.21x`。11 天後股價 893.61（**−8.7%**）、共識僅 +0.13%，於是 alpha-card 的兩欄拆解印出 **倍數差異貢獻 +9.6%**——**同一筆假設同時宣告「零溢價」又印出 +9.6% 溢價，兩者不可能都對**。②**代價（本輪差點直接進 `top_pick`）：** base +19.3%／賭對 +20.7%／**判斷錯 −1.3%**看起來是極不對稱的好賭注；重新校準到 41.24x 後是 +8.8%／+10.1%／**−10.0%**，**幾乎完全對稱**。**那個不對稱不是 LITE 的性質，是倍數陳舊的假象。**③**這正是 AGENTS.md「現況數字會過期，判準不會」的教科書實例**，而且它比文件更難發現：判準（倍數＝市場倍數）一旦落地成一個**固定數字**，判準就從此不再被執行，而每一天的股價變動都在擴大背離。④**系統其實已經偵測到了，只是沒有人被叫醒：** `implied_return` 早就印著「⚠ 倍數桿：目標倍數 45.2x 較市場對共識付的 41.2x 溢價 9.6%——溢價必須指得出 re-rating 證據」。**警告在，但它不進 refresh、不進 readiness、不進籃子 filter、不進心跳**——這是 L13-1（產出要出現在下游消費者手上）與 L16 的合體。⑤**母體實測（驗收數字，L14）：11 檔有生效 `target_pe`，其中宣告 `derivation=calibrated_to_market`（＝零折溢價）卻背離今天的市場倍數 >5% 的有 **4 檔**：AVGO +5.1%、**SOI.PA −96.5%**、5802.T +7.9%、000660.KS +6.9%。（COHR −20.5%、LYC.AX −9.1% 是 `derivation=independent`，背離是刻意的，**不該被叫醒**——這直接證實了下面⑥的擔心不是假想。）⚠ SOI.PA 的 −96.5% 量級異常，可能不只是陳舊（該檔另有 valuation abstention），**提案時要單獨看那一筆**。⑥**提案方向：** 給估值假設一條機械 `review_condition`——`|target_pe / (price/consensus_eps) − 1| > X%` 時轉 `review_required`；X 的取值要先量「現有幾檔會亮」（L14）。⚠ **不要改成每天自動重新校準**：那會讓 fair value 永遠跟著股價走、隱含報酬只剩 EPS 一桿，等於偷偷取消「倍數也是一個可以有主張的桿」——`derivation=calibrated_to_market` 與其他 derivation 的區別會消失。⑦⚠ **L11-6「最先壞掉的是哪一筆」：** 最先壞掉的是**刻意主張折溢價**的估值假設（`derivation` 不是 `calibrated_to_market` 的那些）——它們的背離是有意的，不該被叫醒。提案要先掃一次 `library/private/alpha/valuation/*.jsonl` 的 `derivation` 分佈，**再**決定條件只套哪一類。⑧另記：被取代的 `va_8e435096b30b3d53` 把同期共識列在 `evidence_refs`，而現行型別層已擋（provenance 循環，須改列 `calibration_refs`）——**那筆寫於這道 gate 之前**，代表既有 ledger 裡可能還有同類紀錄。查證：`python -c "import json,pathlib;[print(p.name,[r for r in (json.loads(l) for l in p.read_text(encoding='utf-8').splitlines() if l.strip()) if any('consensus_estimate' in e for e in (r.get('evidence_refs') or []))] and p.name) for p in pathlib.Path('library/private/alpha/valuation').glob('*.jsonl')]"`<br>**交付（2026-09-19）：** `alpha/valuation/drift.py`（純函式）＋`scripts/target_pe_drift_check.py`（驗收基準）＋**心跳段 2 一行**（L14：真正的防呆是會自己出現的常駐計數器，不是要人讀的段落）。門檻 5%，取值依據是實測分佈（47 筆 `calibrated_to_market`：>3% 有 22、**>5% 有 14**、>10% 有 5、>15% 有 1）——⚠ **不取 10%，那會漏掉事發那一筆**（LITE 當時 9.6%）。<br>⚠ **這個 gate 先被驗證過會不會攔錯東西（L14-2／L15-1），而實測兩個最大的「背離」都不是背離：**①**SOI.PA 的 −96.5% 是把兩個不同的量相比**——它的生效假設是 `target_ev_to_sales=7.25`，**根本沒有 target_pe**，拿 7.25 去跟 `price/eps`＝204x 比得出的數字沒有意義（本列原記那個 −96.5% 因此**撤回**）；②**HEXA-B.ST 的 −91.3% 是幣別不一致**——報價 SEK、共識 EPS 是 EUR，`95.56 ÷ 0.31346 = 304.9` 是把 SEK 除以 EUR，隱含匯率 11.4456 SEK/EUR 正是真實量級，**那筆 26.64x 是對的**。③反向更危險：**6680.HK 報 HKD、共識 CNY，相除後看起來是 +0.9%「幾乎完美校準」**——假的沒問題比假警報更危險，因為沒有人會去看它（L13-2：成功與失敗在同一個訊號上同形）。所以三道 fail closed 在比值之前：parameter／method 不對、`derivation` 不是 `calibrated_to_market`（刻意折溢價不該被叫醒，L11-6）、單位不可比 → 各自回 `not_applicable`／`cannot_compare` **並帶理由**（INV-3）。<br>**before → after 實測**（查證：`python scripts/target_pe_drift_check.py`；守門測試 `tests/test_target_multiple_drift.py` 8 條）：盯背離的機制 **0 個 → 1 個（每日自動出現）**；全 ledger 生效 target_pe **49 筆**，`drift_exceeds 13｜within_band 32｜not_applicable 2（COHR／LYC.AX）｜cannot_compare 2（HEXA-B.ST／6680.HK）`。⚠ 本列原記的 4 檔今天只剩 2 檔還在名單上（AVGO 由 +5.1% → **+2.1%**、000660.KS 由 +6.9% → **+0.5%**）——**兩天內就變了，那本身就是「倍數會腐壞」的再一次實測**。<br>⚠ **刻意沒做**：不自動重新校準（那會讓 fair value 永遠跟著股價走、隱含報酬只剩 EPS 一桿）；不接進籃子 filter（背離是「該去看」不是「不合格」）。 |
| **✅ 2026-09-19 已交付：Phase 4 的「市值上限」在這個籃子上結構性不可用：packet 的 `market_cap` 是未正規化的裸數字，警告只活在 alpha-card、沒跟著欄位走**（2026-09-19 [616] 審查時發現） | ✅ | **Phase 4 的前置條件**（不是獨立項；不做這一步，D11 的市值門檻會把真正的目標標的擋掉）。①⚠ **先講清楚：系統沒有錯，而且已經說出來了。** alpha-card 對 IQE.L 逐字印著「行情幣別／報價單位：GBP／**GBp**」「⚠ 報價單位 GBp ≠ 結算幣別 GBP：以報價單位計的欄位（price／forward_eps／**market_cap**）與結算幣別差 100 倍，**跨標的比較前必須正規化**」「市值（price × shares，**未正規化**）…單位：quote_unit（GBp） × shares」。②**問題在下游：** `packet.deterministic.market` 給的是 `market_cap: 54840950600.8`、`currency: null`——**裸數字、無單位、無 FX**。警告活在人類介面，沒跟著欄位走（L16）。③**為什麼這會讓 Phase 4 做錯，而且錯的方向最糟：** IQE.L 真實市值約 **£548M**（41.20 便士 × 1.33B 股），是本籃子裡最像「邊緣小公司」的標的之一；但裸數字是 **54.8B**，任何「市值上限 $5B」的門檻都會把它**當成超大型股擋掉**——**D11 的門檻會系統性地擋掉它最想找的那一類**。④**範圍不是一兩檔：** 16 檔涵蓋 USD／GBp／JPY／KRW／AUD／EUR／TWD，**跨幣別是常態不是例外**；實測裸市值 000660.KS 1,238,696,784,795,000（KRW）、5802.T 6,403,142,173,750（JPY）——與 LITE 的 80,156,817,000（USD）並排比較沒有任何意義。⑤**既有防護擋不到這一種（L17-3 的對稱面）：** `alpha/providers/fundamentals.py::_share_count_absence` 已經會在「快照股數與財報稀釋股數差太多」時**拒絕輸出市值**，理由逐字是「一個錯 5.25 倍的市值在比較時不會露出任何破綻」——**同一句話對 100 倍的 GBp 完全適用**，但那道防護守的是**股數**那一側，價格單位那一側沒有對稱物。⑥**提案方向：** `identity/currency.py` 已有 `resolve_quote_unit`／`is_minor_unit`／`to_settlement`（現成，不必新建）；缺的是 **(a)** 快照沒有存 `quote_unit`（只有 `financial_currency=GBP`），要先確定它從哪裡讀；**(b)** packet 的 market 區塊要嘛帶單位、要嘛在無法正規化時**比照股數那側直接缺席**（`market_cap_absence_reason` 這個欄位已經存在，是現成的落點）。⑦⚠ **L11-6「最先壞掉的是哪一筆」：** 若選「無法正規化就缺席」，最先壞掉的是**所有非 USD 標的的市值**——本籃子 16 檔裡會缺席 6 檔以上，而 D11 的市值門檻**對它們就變成無法判定**。所以提案要同時決定「市值缺席時 D11 怎麼判」（INV-3：不得靜默 filtered，也不得靜默放行）。查證：`python -c "import json;d=json.load(open('library/private/app/state/basket.json',encoding='utf-8'));print([r['ticker'] for r in d['rows']])"` 後逐檔看 alpha-card 第 3 行的「行情幣別／報價單位」。<br>**交付（2026-09-19）：** 修的是 **`MarketSnapshot.currency` 這個宣告了但從未被賦值的欄位**——`identity/registry.py` 早就把 `market_currency` 拆成 `market_quote_unit`（`GBp`）與 `market_currency`（`GBP`）兩欄（L12 先分開再各自定規則），缺的不是分類而是**把分類帶到 packet 上**（L16）。①`MarketSnapshot.currency` → 拆成 `quote_unit`＋`settlement_currency`（舊欄位無任何賦值也無 production 消費者，唯一讀它的 `briefing/alpha_view/sources.py::_current_price` 一直在走 fallback）；②provider 從 registry 填值，**解析不到就讓 `market_cap` 缺席＋理由**，與股數那側對稱（L17-3③）；③⚠ **刻意不在 provider 內換算 USD**——那需要 FX、等於每次 materialize 打外部；正規化留在需要比較的那一層。**這同時回答了啟動 prompt ② 的第一個問題：FX 不進 materialize。**<br>**before → after 實測**（查證：`python -m pytest tests/test_market_quote_unit.py -q`，4 條）：籃子 16 檔的 `quote_unit` 有值 **0／16 → 16／16**；其中 6 檔非 USD（SOI.PA／IQE.L／LYC.AX／5802.T／6324.T／000660.KS）**先前結構上無法與 USD 標的區分**。市值缺席 **0／16 → 0／16**（零誤擋——L11-6 先量過：27 家缺 `market_quote_unit` 的公司**全部沒有 research_ticker**，本來就沒有行情快照）。`scripts/alpha_screen_check.py` 重造的那份 `_market_currency` 已刪除改問 registry，**16 檔逐檔數字與判定一字不差**（純重構驗證）。 |
| **D11 門檻落地後量到的決定性事實：通過篩選的 5 檔裡有 4 檔「儀器算不出 payoff」（80%），被擋下的 11 檔裡只有 1 檔（9%）——儀器的盲區正好是目標區**（2026-09-19 實測） | 🔴 | **這是「多年反向橋」該不該提前的關鍵證據**（AGENTS.md 現把它排在 ROADMAP 最後一個 Phase）。①**量測**：`python scripts/alpha_screen_check.py`（門檻 市值 ≤ US$10B、覆蓋 ≤ 12，落在 `config/alpha_screen.json`）→ `input 16／accepted 5／filtered 11`，**缺值 0**。通過：**IQE.L 0.76B/3｜POET 1.35B/1｜6324.T 3.74B/9｜AXTI 4.59B/5｜SOI.PA 5.80B/6**（市值為 USD、已正規化）。②**交集**：通過的 5 檔中，**POET／SOI.PA／IQE.L／6324.T 四檔已宣告 `valuation/forward_earnings_multiple.target_pe` 的 Abstention**（虧損年沒有可校準的市場倍數），唯一算得出 payoff 的 AXTI **payoff 為負**。③**對比**：被 D11 擋下的 11 檔裡只有 MP 一檔有同樣的 abstention。**80% vs 9%。**⚠ 上一輪（同日稍早）用 `analyst_count` 中位數比（10 vs 22）只能說「相關但不是同一件事」；**換成 D11 的真實門檻之後，這個對比硬得多**。④**所以 Phase 4 做完，籃子會是空的——而空的原因不是「沒有好公司」，是「我們的估值儀器對這一類公司不適用」。**這正是 AGENTS.md 早就寫下的那句話的實測版：「現行主流程（FY+1 EPS × 目標倍數找 20% 錯價）**是給高覆蓋大型股的儀器**，對不上目標」。⑤**它不改變「籃子空就空」這條禁令**：不得為了非空放寬 D11，也不得為了非空去硬寫一個沒有倍數可乘的 target_pe。**唯一的出路是換儀器**（多年反向橋「五倍要什麼為真」），或誠實維持空籃並只在 AXTI 這類算得出來的標的上表達判斷。⑥⚠ **L11-6「如果這個結論是錯的，最先壞掉的是哪一筆」：** 最先壞掉的是那 4 筆 Abstention 本身——若其中任一筆其實**不該** abstain（例如 6324.T 的共識 EPS 其實為正、只是當時沒查），80% 這個數字就會掉下來。**提案前先逐筆重讀那 4 筆的 `reason` 與 `revisit_when`**，不要拿這個數字直接去改 Phase 排序。<br>**L11-6 的前置已做完（2026-09-19，使用者指定）：逐筆重讀那 4 筆 Abstention 的 `reason` 與 `revisit_when`。****結論：四筆全部該 abstain，一筆都不該撤回 → 80% 這個數字站得住。**逐檔：**IQE.L** FY2026 內部與共識 EPS 都是 −£0.011，**FY2027 仍是 −£0.002**（「不是等一年就好」）；**POET** FY2026 共識 −$0.23 且**僅 1 位分析師、high＝low**；**6324.T** **盈餘是正的**（內部 FY2027 EPS 47.32），但現價對它 126.2x，任何合理區間（30–80x）都產出 −50%～−80% 的 gap，「**而那個數字的資訊全部來自隨手選的倍數**」；**SOI.PA** FY2027 是**谷底年**（對現價 200 倍以上），「**市場的定價錨在 FY2028 之後，而本系統的目標期間只到 FY2027**」。<br>⚠⚠ **比那個 80% 更有力的證據在 `revisit_when` 那一欄——四筆有三筆逐字指名同一個解：**6324.T「或估值層 v2 新增 **mid-cycle／multi-year method** 後重評」；SOI.PA「目標期間推進到 FY2028」「內部 EPS 橋能支撐**兩年期預測**」；IQE.L／POET「或估值層新增適用於**虧損期**的 method」。**再往外看，`abstentions/` 一共 11 檔宣告不主張目標倍數，理由高度同型**（虧損年／口徑不可比／谷底年／近零分母）。其中 AAOI 那筆逐字最直接：「這一格缺席的原因是 **method／口徑**，不是缺料，也不是『AAOI 不值得看』——**它是「v1 的本益比法看不到它」**」，且 revisit_when ③逐字「**那是開發項，載體是 ROADMAP 不是本 ledger**」。<br>**所以：研究層早就把結論寫下來了，而且自己指名那是 ROADMAP 的開發項。**支持提前的證據不是那個統計，是這 11 筆各自獨立寫下、卻指向同一件事的解除條件。⚠ Phase 排序的實際變更仍需**五欄 amendment**，本輪已提出、等使用者決定。查證：`ls library/private/alpha/abstentions/` ＋逐檔讀 `reason`／`revisit_when`。 |
| **daily 排程結束時沒收乾淨：writer_lock 留著過期、`daily_done_today` 沒翻轉、產出沒提交**（2026-09-19 實測一次） | 🔶 | **維持營運（先觀察會不會重複，再決定修不修）。** 實測：lock 由 `scheduled` 於 05:33 取得、`expires_at` 07:03，到 08:45 仍在檔案裡，而持有它的 **PID 29272 已不存在**；同時 state 檔在 **05:47–05:52 確實被寫出**（basket／ranking／beta／coverage／positions／watches），`library/leads/` 三個檔有 863 行未提交改動（含新鑄的 [618]–[625]）。**所以它跑完了、也有產出，只是沒收尾。** 互動 session 以 `python scripts/writer_guard.py release` 清除後恢復。⚠ **為什麼不當下修**：不知道是排程被中斷、還是收尾路徑本來就沒有 release／commit 這一步——**兩者的修法完全不同**，而目前只有一次觀測（L14-5：維持營運不必對齊終點，但它也不算進展）。⚠ **下次再發生時要先看的**：`daily_run_finished_at` 有沒有更新（有→收尾路徑缺 release；沒有→排程被中斷）。查證：`python scripts/writer_guard.py check`——`writer_lock` 非 null 且 `expires_at` 已過 ＝ 又發生了。 |
| **✅ 2026-09-19 已交付：base 已經含自己的觀點的檔，卻仍被籃子標成「還沒寫賭注」**（2026-09-19 實測） | ✅ | **留 ROADMAP 待排**（與 Phase 4 的籃子條件同一處）。①**實測：** `opinion_stance` 分佈 `consensus_inverted` 9／`independent` 3／`company_guidance` 2／`no_opinion_bearing_assumptions` 1／`undeclared` 1；其中 **LITE、SOI.PA、LYC.AX、6324.T 這 4 檔**是 `independent`／`company_guidance` 但 `bet_state='unanswered'`（＝籃子印「還沒寫賭注」）。②**為什麼這不是單純的待辦：** base 的 derivation 是 `company_guidance` 時，「我們的差異看法」內容就是「相信公司的指引」，**那個看法已整個住在 base 裡**，於是 variant overlay 只剩指引區間本身的寬度——本輪 LITE 實測賭注空間只有 **+0.5pp（+19.3% → +20.7%）**。硬要把格子填滿，寫出來的會是「隨便樂觀一點」，正是 `ASSUMPTION_SCENARIOS` 註解說要擋的 bull case。③⚠ **但不得因此自動把它算成有賭注**：COHR 是 `independent` **且**有 variant（錨在指引上緣），證明「base 有觀點」與「寫得出 overlay」可以並存——**所以這不是一條可以自動推導的規則**，提案要決定的是籃子那一格該印什麼字（現在的 `no_bet` 至少是誤導）。<br>**交付（2026-09-19）：** `BET_STATES` 加第四格 `opinion_in_base`（L12：先分開再各自定規則）。判準可機械驗證且**只消費既有 SSOT**（`opinion_stance` 是 `alpha/fundamental/contracts.py` 的純函式，本處不重算，L16）：沒有 payoff、沒有 abstention、且 stance ∈ {`independent`, `company_guidance`} → `opinion_in_base`。⚠ **它仍不是終局**——兩格都還欠一個答案，分的是**欠的是什麼**；`abstained` 優先於它（宣告不主張是已研究的終局）；`bet` 優先於一切（COHR 是 `independent` **且**有 overlay，證明兩者可以並存，所以這不是一條能自動推導成「有賭注」的規則）。**filter 理由與去留一字未動**——改的只是那一格印什麼字。<br>**before → after 實測**（查證：`python -m webapp materialize --basket` 後讀 `basket.json` 的 `bet_ledger`；守門測試 `tests/test_bet_state_opinion_in_base.py` 4 條）：籃子 `bet 3｜abstained 0｜unanswered 13` → `bet 3｜abstained 0｜**opinion_in_base 3**｜unanswered 10`（SOI.PA／LYC.AX／6324.T；⚠ 本列原記 4 檔含 LITE，但 LITE 本輪已寫出賭注 → `bet`）。**量的候選那一邊更大：`unanswered 12` → `unanswered 4｜opinion_in_base 8`**（2455.TW／3081.TWO／3105.TWO／4971.TWO／4979.TWO／5016.T／6680.HK／SIVE.ST）——先前 12 家全被說成「沒人看過」，實際上三分之二的 base 已經有觀點。心跳段 4 的兩行都加了這一格（護城河與量的候選對稱，L17-3③）。籃子通過數仍是 1、首選仍是 LITE。 |

---

## 開工前必讀

### 已撤回的診斷

> **這一節不是自責，是一份檢查清單。** 每一筆都是「已經寫進 commit／ROADMAP／程式註解，
> 事後被推翻」的技術診斷——不是待辦、不是 bug，是**曾經看起來完全正確的錯誤結論**。
>
> **共同形狀：錯誤有方向性——全都朝「產生一個有洞察力的結論」偏**，而且每一個都能用專案
> 自己的 lesson 語言包裝（L12 一表兩義、L15 gate 攔錯東西）。
> **模式匹配是提出假說，不是確認假說。** 一個現象能被套進某條 L，只代表它值得查。
>
> **用法：** 宣稱「找到根因了」之前，先跑一條**試圖讓自己的結論變成假的**命令
> （不是驗證它為真——那是確認偏誤）。專案對每個 thesis 都強制 `disproof_condition`，
> 這一節是把同一個要求套到自己的技術診斷上。

| 日期 | 被推翻的診斷 | 一條就能否證它的命令 |
|---|---|---|
| 2026-08-19 | COHR「Engine C 的 `bar_date` 是憑空生成的、`price` 對不上任何收盤」 | `date(2026,8,17).strftime('%A')` → `Monday`。**一本日曆就能否證** |
| 2026-08-19 | 待辦池 `decision_review` 不退場是因為「空 `blockers` 被判成非純系統」 | `python -m decision_lab card <decision_id>` → `card.blockers` 有 **7 個碼**，不是空的 |
| 2026-08-19 | 「`execution_fx_stale_since_decision` 未登記，掉進泛用 prefix」 | 讀 `config/decision_blockers.json` 的 `_matching`（**最長**匹配，不是第一個）。真相是它早就以 exact prefix 登記 |
| 2026-08-19 | 「`live_choices` 仍為 0 筆，live 路徑從未被走過」——**直接引用自家文件** | `select count(*) from live_choices` → **1** |
| 2026-08-19 | 「`commercial_maturity` 積壓缺的是有人去讀年報附註」 | 逐一看 7 個積壓的 `missing_data` → 6 個是 `research_assessment_missing`。**靠讀年報能下降的是 0 個** |
| 2026-08-28 | 「COHR live reassess 失敗的根因是 `--as-of` 沒給」 | 修好 marker 後**不給 as-of 再跑一次** → marker 沒出現。真因在 `adapters.py::current_holdings` 另一處吞例外 |
| 2026-08-28 | 「`co:lumentum` 有兩個 cohort，重複偵測有漏」 | `sed -n '869,876p' decision_lab/store.py` → 註解逐字記著已檢查過這個確切案例。回空集合是正確行為 |
| 2026-08-28 | 「U2 把 `weakest_axis` 改成 level 排序是**零行為變化**的純重構」 | 改完直接 `pytest tests/test_probe_sizing.py` → `[missing_ref]` 立刻紅 |

**有可執行檢查的診斷活不過幾分鐘；沒有的全靠當下願不願意多查一步。**
（實測：U2「零行為變化」被測試抓到用了 3 分鐘；靠運氣發現的兩筆活到下一輪。）
所以落地前不是把診斷寫得更清楚，是**把診斷寫成一條會紅的檢查再落地**。

⚠ **這一節自己的 disproof：** 若之後仍發生「診斷已落地才被推翻」，代表它沒生效。
屆時該做的是把否證步驟綁進會自己執行的東西（測試、hook、commit 前檢查），
**不是把這張表寫得更長**。


### 看起來像缺口但不是——請勿「修正」

- **人工 runway 觀測寫入後 `financial_runway_manual_required` 仍亮，多半是 100 天鮮度窗，
  不要去改窗。** 那個窗刻意對齊財報節奏，**正解是用最新一季財報刷新觀測**。
  ⚠ runway 觀測的 `as_of` 應填**資產負債表日**，不是申報日。
- **5 個 cohort 的最新 `expiry` 仍是 `+72h` 預設值，不要去清。** 它們的 lifecycle 全部已
  `expired` 且 `catalyst_watch` 根本不顯示它們；依 L14，修它們會讓 **0 筆**下游資料變化。
  根因已由 `300b8e0` 修復並有測試防迴歸。
- **Beta 例行成交不進 Engine D 的 `record-fill`，這是設計正確。** `record_live_fill` 要求
  一整條責任鏈（decision → choice → fill），目的是回答「Engine D 的建議準不準」。
  beta 例行投入沒有 decision、沒有接受動作——**它是時間表不是決策**。硬塞會讓
  outcome attribution 變成把 QQQ 漲跌歸因給「今天是 15 號」。
  正確分工：beta → `library/trades/trade_log.jsonl`；alpha thesis 驅動 → 同時進 trade_log 與 Engine D fill。
- **`_bar_identity()` 的 ETL 不得加「info 與 history 不一致就 quarantine」的交叉驗證。**
  它會把完全正確的資料 quarantine 掉，正是 L15 說的「gate 攔下的不是它想攔的東西」。


---

## 想法怎麼變成程式

```
ROADMAP「開放 backlog」  →  docs/brainstorms/  →  docs/plans/  →  實作
     （還沒決定要做）        （需求與盲點審查）     （規格與驗收）
```

四階不是每次都要走完。判準是**改錯的成本**：小工作直接做；需要先想清楚需求與反面的走
brainstorm；範圍大到需要驗收條件才開 plan。`docs/plans/` 已轉純歷史
（見 [`plans/README.md`](plans/README.md)）。


## 什麼值得開發 / 什麼交給 Claude

**值得開發：** 知識累積（更多公司 onboarding、更多高品質文件——這是**研究方向**非開發項，
已由 `research-drain` 的閉包語意涵蓋）｜Skill 介面｜高槓桿 fetcher｜資料品質檢查。

**不值得自己開發：** 長文解讀｜Text2Cypher｜自動選文件頁面｜節點重要性評分｜公司識別
（Claude 做得更好）｜**自動代替使用者做最終投資決定或送單**（Engine D 可提出有邊界的建議，
但 live 接受、覆寫與 broker 下單永遠需要人工）。

