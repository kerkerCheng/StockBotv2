# StockBotv2 — Roadmap

> **本檔只放 active future work。** 判準與契約在 [`AGENTS.md`](../AGENTS.md)；
> 指令與程序在 [`OPERATIONS.md`](OPERATIONS.md)。
>
> **交付歷史、已結案項目與需求推導**已於 2026-09-03 移到
> [`docs/archive/roadmap-pre-alpha-refactor.md`](archive/roadmap-pre-alpha-refactor.md)（逐字保留）。
> 每一項的去向見 [`docs/refactor/roadmap-migration.md`](refactor/roadmap-migration.md)。

---

## ⚠ 開發項只住這裡，不進 pq2（2026-08-31 使用者定案）

**本檔是系統開發項的唯一載體。** 改的是程式、config、schema 或呈現邏輯，而不是圖／
Engine C／thesis／資本裡的任何一筆事實 → 它是開發項，寫進本檔，**不鑄 pq2 編號**。

判準：**`go` 之後改變的是「我知道什麼」還是「系統怎麼運作」？** 前者是研究（pq2 編號），
後者是開發（本檔）。例如「補某條邊的 substitutability」改變圖裡的事實＝研究；
「改 `rank_bottlenecks` 的排序鍵」改變系統行為＝開發。

理由是**兩種東西的決策資訊完全不同**：研究項要的是「證據夠不夠、授權到哪」，一行決策行
就夠；開發項要的是「這會讓哪個數字變、驗收條件、與其他開發項的相對優先序」（L14 第 5 點），
而那些只有在本檔的表格裡排得出來。

系統主動提出的開發構想寫進本檔待排程，**不主動要求 `go`**。開發項落地後若要動圖或
authority，那是另一個 pq2 編號。判準全文見 [`AGENTS.md`](../AGENTS.md)「授權載體唯一」。

**每項強制四欄：做什麼／為什麼／驗收條件（哪個數字會變）／前置。**
沒有驗收條件的不准進佇列（L14 第 1 條）。

---

# 現行路線圖：Alpha Research Refactor

**North Star：** 系統要能回答
**「我們知道了什麼市場可能還沒有充分 pricing，以及這個 expectation gap 是否值得轉化成資本曝險？」**

```
Evidence → Knowledge → Causal Understanding → Fundamental Impact
        → Market Expectations → Variant Perception → Alpha
        → Portfolio → Capital Decision → Outcome Learning
```

三層責任不再混在一起：
**Knowledge Graph** ＝研究記憶與結構 edge｜**Alpha Research Core** ＝investment reasoning
engine｜**Engine D** ＝capital permission 與 accountability。

設計文件（動工前必讀）：
[`current-architecture.md`](refactor/current-architecture.md)（實測盤點）｜
[`target-architecture.md`](refactor/target-architecture.md)（契約與邊界）｜
[`engine-d-decomposition.md`](refactor/engine-d-decomposition.md)（逐檔搬遷）｜
[`phase-1-plan.md`](refactor/phase-1-plan.md)（施工圖）｜
**[`historical-failure-matrix.md`](refactor/historical-failure-matrix.md)（36 筆歷史事故 → 六條 hard invariant → completion gate）**

## Phase 表

| Phase | Goal | Deliverables | Exit criteria（哪個數字會變） | Dependencies |
|---|---|---|---|---|
| **0** ✅ | Architecture inventory ＋ AGENTS 分類 ＋ MCP 降級 ＋ 歷史事故矩陣 | 六份 `docs/refactor/*.md` ＋ 舊 roadmap 封存 | 六份文件邏輯一致；舊 roadmap 22 個標題都有去向判定；36 筆事故已分類 | — |
| **1** ✅ | Alpha contracts ＋ 三個防事故型別 ＋ audit 骨架 ＋ golden fixtures ＋ 舊五軸轉換 | `alpha/{contracts,causal,provider,errors,identity,testing}.py`＋`alpha/audit/`；5 個測試檔；`tests/fixtures/golden/` 14 類 | ✅ 全數達成：`alpha/` 零外部相依；**23/23 突變證明斷言會紅**；**既有 package 變更 0 行**；1,175 → **1,283 passed / 0 skipped**；golden fixtures 14/14；舊五軸 dual run 41 cohort、UNEXPECTED 0；F-20／F-25／F-31 三個 🔴 歸零 | Phase 0 |
| **2** ✅ | First research vertical slice（標的＝COHR） | B4：`alpha/providers/{graph_neo4j,fundamentals}.py`、`alpha/context.py`（Q1 deterministic）、`alpha/models/session_assessor.py`（**不接 API**）、`python -m alpha research COHR` | ✅ 全數達成：①每個 score 都 explain 得到 `EvidenceRef`（無引用即 reject）；②圖零新增節點（provider 唯讀）；③Q1 直接消費 `rank_bottlenecks`，不重算；④突變 23 → **30 個、空跑 0**；⑤全套 1,283 → **1,394 passed / 0 skipped** | Phase 1 |
| **3** ✅ | Engine D decomposition ＋ `mcp_server` domain 抽出 | B2／B3／B5／B6：shared infra 升格、alpha 模組搬入、`sizing` 切三段、`brief` 拆四 pane；core → `mcp_server` import 5 → 0 | ✅ **依賴方向指標全數歸零**（這才是本 Phase 要買的東西）：`decision_lab` 相依環 **3 → 0**；搬遷 shim **15 → 0**；`PENDING_B6_COUPLINGS` **1 → 0**；core → `mcp_server` **5 → 0**；`engine_c`／`portfolio`／`risk`／`shared`／`query` → Engine D **各 0**。<br>量體：`mcp_server/` 4,016 → **673**（domain 2,261 行抽到 `intake/`）；`decision_lab/` 10,653 → **8,807**；`brief.py` 1,462 → **763**。<br>套件品質：1,394 → **1,441 passed / 1 skipped**；突變 30 → **46 個、空跑 0**；`audit invariants` **FAIL 0｜PASS 10｜SKIPPED 2**；golden fixture 漂移 **0**（每一批都是）。<br>daily 端到端**內容不變已實測**（B6 對 HEAD worktree 同 `--as-of` 逐欄 diff：items 45／45、sheet-only 覆蓋分類逐筆相同、markdown 正規化後 diff 0 行）。<br><br>⚠ **原 exit criterion「`decision_lab/` ≤8,000 行」已於 2026-09-04 撤回**，理由是它**算錯了**，不是做不到就改分。逐檔查證後：設計文件 §6 把已搬到 `shared/` 的 `capital_authority.py` **419 行重複計入**，另有四處估計失準——`brief` 估 ~400（實際地板 **763**：`_current_authority_context` 102＋`_reassessable_axes` 60＋`ranking_annotations` 48＋`identity_registration_pending` 43 是純 Engine D，只能拆成另一個同套件檔案，對總行數是 0）、`sizing` 估 ~300（實際 **450**，字彙移出後剩下全是 §3 自己判為 D 的 gate）、`references` 估可 D∩A 切半（實際 **整支是 D**：它同時要 frozen bundle 與 `AXIS_REFERENCE_AUTHORITIES`，「讀者是研究者」不決定層級）、`workflow` 估 ~600（實際 **0 可移**，研究部分 3c 已移走）。重算後真正的地板是 **~8,630**（僅剩 `cli.py` 三個研究輔助子命令 ~170＋`adapters/holdings.py` 8），而唯一夠大的來源是 `store.py` 3,125 或 `context.py` 713——設計文件都明列「不搬」，且為了一個**代理指標**去動 append-only authority 的模組與 point-in-time 凍結機制，正是 L14 禁止的事（未經量測的機制不得決定行動）。**行數是代理，方向才是目的**；方向指標已全綠。 | Phase 2 |
| **3.5** ✅ | Portfolio / Risk 搬家（B1） | `portfolio/{policy,exposure,allocation,brief}.py`、`risk/{policy,snapshot}.py` | ✅ 全數達成（隨 Phase 3 各批一起落地，2026-09-03～04）：`portfolio/`＋`risk/` 共 **2,693 行**已在 `decision_lab/` 之外；`engine_c → decision_lab` import **0**（`portfolio`／`risk`／`shared`／`query` 亦為 0）；三個硬擋逐筆一致（`single_position_nav_cap` 0.05、nominal 12.5%／20%、effective 30%／40%）。查證：`python -c "import ast,pathlib;print(sum('decision_lab' in (n.module or '') for p in pathlib.Path('engine_c').rglob('*.py') for n in ast.walk(ast.parse(p.read_text(encoding='utf-8'))) if isinstance(n,ast.ImportFrom)))"` | Phase 2 |
| **3.9** ✅ | **`AGENTS.md` 結構瘦身 ＋ 新增 `docs/ARCHITECTURE.md`**（一次做完） | PROCEDURE 搬 OPERATIONS／skills；L1–L16 加「Learned invariant／事發／可改？」欄；CURRENT_ARCHITECTURE 段落 → 新的 `docs/ARCHITECTURE.md`；四引擎表 → 五條 authority separation ＋ 六條 hard invariant | ✅ **2026-09-04 交付。**<br>**實測 before → after：** `AGENTS.md` **802 行／52,229 字元 → 638 行／26,147 字元（字元 −50%）**；新增 `docs/ARCHITECTURE.md` 290 行；`OPERATIONS.md` 554 → 693；`CONCEPTS.md` 190 → 215；pq2 呈現規格落到 `skills/daily-brief`（Luna 契約當時落到 `skills/luna-reviewer`，**該 skill 已於同日稍晚退役**，見下方 backlog）。<br>⚠ **原 exit criterion「≤450 行」已於 2026-09-04 經使用者裁決改記為字元數。** 理由**不是做不到就改分**：內容確實砍掉一半，差距全部來自**折行慣例**——舊檔把整段塞成一行（87 字元/非空行），新檔照 ~80 欄折行（49 字元/非空行）；同樣內容照舊密度重排約 **300 行**。而 450 行這個目標的**目的**是省 session context，**context 成本是字元／token 不是行數**；為了湊行數把 bullet 重排成長行會讓 `git diff` 顆粒度變粗、之後改一句話整段變紅。**判準改成：字元數必須降，且下列內容驗收全過。**<br>**內容驗收（全過）：** ①**16 條 lesson 一條未刪**，各自標明 implementation 可不可改；②40 條 invariant 逐條仍在（含 `工作語言`／五條 authority／六條 INV／四個 gate／資本三硬擋／Alpha 交付要求／Beta 兩條相關性警告／技術訊號 0 勝 3 敗的實測記錄）；③`grep` 不到與 `.codex/rules`／`skills/*` 重複的清單。<br>⚠ **搬移過程被自家剎車抓到兩次，兩次都是修對而不是放寬：** ① `tests/test_codex_daily_permissions.py` 斷言 sandbox review 的七個 token 在 `AGENTS.md`，搬走四個後當場變紅——改成「**判準驗 AGENTS、程序驗 OPERATIONS**」兩份各驗自己該有的，每個 token 仍被斷言存在；② 往 `OPERATIONS.md` 追加時**差點造出第二份 16 條 fixed entry 清單**（該檔早就有一份），已刪除並在原處留下「不要在這裡再抄一份」的警語——「清單會腐壞，判準不會」正是這次搬移要消除的違規；③ `tests/test_routine_prompts.py` 斷言 Beta 心跳欄位在 `AGENTS.md`，欄位細節搬 ARCHITECTURE 後變紅——同樣改成兩組各驗自己該有的，並把「舊燈號語意已明文廢止」「逐檔表不得省略、每列須明示最新完整交易日」兩條**判準**寫回 AGENTS。<br>**驗收：** 1,538 passed / 1 skipped；`audit invariants` FAIL 0｜PASS 11｜SKIPPED 1；golden 漂移 **0**；`sync_agent_skills.py --check` 無漂移；突變 76 個空跑 0。 | Phase 3.5（boundary 定下來才動） |
| **4**<br>🔶進行中 | Expectation Gap | Engine C 欄位擴充（forwardEps／revenue estimate／segment revenue）、peer registry、implied fundamentals | 🔶 **4a 已完成（2026-09-04）：估計修正與股價變動分離。** `engine_c/estimates.py`＋`alpha/providers/fundamentals.py::estimate_revision`＋`alpha/context.py` 的 method 字串。<br>⚠ **第一項交付的前提被實測推翻，而那是好消息。** 原計畫「Engine C 缺 forwardEps，要擴充欄位」——實測 yfinance 的 `forwardPE` **恆等於** `price / forwardEps`（COHR／NVDA／2330.TW／6324.T／SIVE.ST 相對差 <1e-7），而 `price` 與 `pe_forward` 我們**每天都存**：`financial_snapshots` 1,931 筆有 1,836 筆（95%）兩者皆有值，最早回到 2026-07-08。**新增欄位今天才開始有資料，導出立刻有兩個月歷史。**<br>真正修掉的是 L12：`estimate_revision_30d` 原本取 `pe_forward` 的 30 日變化，而倍數同時被「分析師改估計」與「股價漲跌」推動，下游無從分辨。分開後實測（31 個觀測，2026-08-03→09-03）：**COHR forward EPS +68.3% 而股價 +0.6%**、LITE +81.4%／+18.7%、IREN −73.6%／−2.2%；而 AAPL／GOOGL／TSM／MU／INTC 全部落在 ±5% 內。**「便宜」與「估計與價格脫鉤」被分開了**——後者才是 gap。<br>驗收：可算出修正的標的 **70/70 檔**（exit 要求 ≥5）；`低 P/E 但共識與 thesis 一致 → gap ≈ 0` 已由上述成熟大型股實證；1,441 → **1,457 passed**；突變 46 → **49 個空跑 0**；`audit invariants` FAIL 0；golden 漂移 0；`python -m alpha research COHR` 端到端顯示 method 字串。<br>⚠ **單位陷阱已編碼成測試**：導出值以**報價單位**計。實測 IQE.L（`GBp` 報價）導出 −0.196 便士 vs yfinance 回報 −0.00196 英鎊，**差 100 倍**。因此本模組只保證同一標的的**時間序列比值**（單位消掉），**不得跨標的比絕對值**。<br><br>**剩餘（4b／4c，尚未動工）：**<br>🔶 **4b 機制已完成、資料待核准（2026-09-04）：** `segment_revenue_share` 已登記進 `config/engine_c_observation_fields.json`（`gate_member=false`，五項 gate 契約未動）並接上 `alpha/providers/fundamentals.py` — 有觀測就流到 Q3，沒有就誠實 `None`。⚠ **讀不到一律 `None` 不回 `{}`**：空 dict 會通過「有沒有分部資料」的檢查，然後讓 Q3 讀成「每一塊業務都是 0%」，比誠實說不知道危險得多。<br>✅ **gate 判準已改（2026-09-04 使用者定案）：** Engine C 人工 ledger 的 pq2 不再按「存哪張表」畫，改按 **`verifiability`＝可否確定性重導**（沿用 L10）。`segment_revenue_share` 與 `runway_inputs` 判 `mechanical` → **不需 pq2**；其餘 12 個判 `judgment` → 維持。補償控制：`mechanical` 欄位的 value 強制必須是可機械比對的 JSON 數值（散文一律拒絕）——放行與收緊同時發生。⚠ `gate_member` 五項與 `mechanical` 的交集必須為空，已寫成斷言。<br>**真實資料仍是 0 檔**，但現在卡的是工作量不是核准： yfinance 沒有分部資料，只能從 10-K／年報分部附註（IFRS 8／ASC 280）人工讀入，而寫 Engine C manual ledger 是四個 gate 之一——**每一檔都要一個 pq2 編號**。查證：`python -c "import sqlite3,json,pathlib;p=pathlib.Path('library/private');db=p/json.loads((p/'runtime_pointer.json').read_text())['engine_c'];print(sqlite3.connect(f'file:{db}?mode=ro',uri=True).execute(\"SELECT COUNT(*) FROM manual_fields WHERE field_name='segment_revenue_share'\").fetchone()[0])"`<br>✅ **4c 的第一項已交付（2026-09-04）：`revenue_estimate_next_fy`。**<br>⚠ **它拖到現在，是因為上面這行原本寫著一句假話。** 原文：「yfinance 只有 `revenueGrowth`（成長率）與 `epsCurrentYear`，**沒有絕對營收估計**」。照 AGENTS「引用自家文件的現況陳述前先跑查證命令」實測——`yf.Ticker().revenue_estimate` 直接給 `+1y` 的 avg／low／high／growth／numberOfAnalysts，**73/73 檔全覆蓋**（含 `000660.KS`／`002472.SZ`／`2301.TW`）。**這是同型錯誤第三次**（前兩次見「現況數字會過期」那節）——一句沒人回頭驗證的陳述，把一個 30 秒可得的欄位擋了一整個 Phase。<br>落地：`engine_c/migrations/20260904_add_revenue_estimate_next_fy.sql`（三欄）＋`db.py` 兩種方言＋`etl_yfinance._next_fy_revenue_estimate`＋`ConsensusSnapshot` 兩個新欄位。實跑 COHR：`revenue_estimate_next_fy=14.67B`／共識成長 `+38.2%`／22 位分析師。<br>⚠ **兩個單位陷阱寫成測試：**①絕對值以**報表幣別**計（SK Hynix 是 534 兆 KRW），只能比同一標的的時間序列，`..._growth` 才可跨標的比；②**營收成長 ≠ EPS 隱含成長**——實測 COHR 市場隱含 +244.6% vs 共識營收 +38.2%，那 206 個百分點的差**不是 gap**，它同時包含「市場預期利潤率大幅擴張」。突變測試守著「不得合併成一個 `growth_gap` 欄位」。<br>**剩餘：**`market_implied_margin` 仍需 segment 資料（非美股 11 檔見下方 backlog）；③ peer registry ——⚠ **建議先不建 `config/peer_groups.json`**：`config/sector_anchors.json` 已經是一份分組 SSOT，再造一份就是 L16（分類已有 SSOT 時不要重造）記過的事。要建必須先答出「需求鏈分組對估值比較為何不堪用」，且要有消費端——否則是一個「寫得進去卻不影響任何決策」的死 config。<br>🔴 **4c 已補（2026-09-04）：4a／4b「已完成」的定義本身是錯的。** 使用者實測質疑「我原本想看到的是消費端有所改變，像是未來定價空間，那些在哪裡？我感覺我看到的東西還是跟以前一樣」，查證屬實——`market_implied_growth`／`revenue_estimate_next_fy_growth`／`estimate_revision_30d` 在**六個面向使用者的 surface 引用次數全是 0**（`query/bottleneck.py`／`alpha_purity_snapshot.py`／`decision_lab today`／`briefing/*`／alpha-status 與 daily-brief 兩份 SKILL）。欄位只流進 `alpha research --emit-packet` 的研究包——**那是給 session 讀的，不是給人讀的**。當時驗的是「欄位算得出來、73/73 有覆蓋」，那是 L13 明文禁止的驗收方式，而且踩在使用者唯一在乎的那件事上。已補 `scripts/alpha_expectation_gap.py` 並列入 alpha-status Pane 1 必附項（三條限制各有測試＋第 83 個突變：不排序／不相減／不可算不寫 0）。<br>⚠ **交付前的固定檢查**：`grep <新欄位> <使用者會讀的每個 surface>`，答案 0 就不得寫進「已完成」欄。 | Phase 2 |
| **5** ✅ | Causal propagation | `StructuralEvent` → `CausalPath` → `CompanyImpact` | ✅ **5a＋5b 均已完成。5a 二階傳播（2026-09-04）：** `Neo4jGraphResearchProvider.propagate()`＋`get_second_order_beneficiaries`／`get_second_order_victims`（原本明確拋 `NotImplementedError`）。<br>✅ **exit criterion 達成**，實跑 36 條真實排序列：`mat:inp_substrate` supply_disruption／tightening → **co:nvidia 是二階受害者**，路徑 `mat:inp_substrate →constrains→ co:coherent →supplies_to→ co:nvidia`，帶 2 條 `EvidenceRef`；`co:coherent` tightening → **co:lumentum／co:broadcom 二階受益**（替代鏈）。<br>傳播規則寫成明文（規則不是量測）：邊化約成「A 依賴 B」，只有兩條路徑——**依賴鏈**（遞移依賴 subject 者，tightening 時 VICTIM）與**替代鏈**（供同一 chokepoint 的其他公司，tightening 時 BENEFICIARY）；`loosening` 整組反向。<br>三個刻意不做：**不加權總分**（magnitude 由跳數＋路徑最低 substitutability，且**二階最高只到 MEDIUM**——推論不得自稱與觀測同級）、**不編時間**（`lead_time_weeks` 缺席就 `UNKNOWN`，不套「供應鏈大概一季」）、**不取平均信心**（`CausalPath.confidence` 取最弱一段，契約強制）。<br>首跑抓到自己的兩個 bug 並寫成回歸測試：① 逆走的邊沿用原標籤，路徑印成 `mat:inp_substrate -depends_on-> co:coherent`——**因果方向剛好講反**；② magnitude 用 `_finite`(夾 0..1，confidence 專用) 讀 `substitutability`(1–5)，**每筆都變 None**，看起來像資料不足其實資料在。<br>驗收：1,470 → **1,483 passed**；突變 54 → **59 個空跑 0**；`audit invariants` FAIL 0；golden 漂移 0。<br><br>✅ **5b 已於 Phase 6 一併完成（2026-09-04）：`get_structural_changes_since` 不再拋 `NotImplementedError`**——它比對「`since` 的 as-of 投影」與「當前圖」的差集。實跑：`since=2026-06-30` → 1 個事件（`co:axt supplies_to co:lumentum`，`substitution`／`loosening`，observed 2026-07-29）；`since=2026-03-01` → 3 個。<br>首跑同樣抓到自己的兩個 bug：① `observed_at` 取整條邊最早的引用日，於是 `since=2026-06-30` 產出 **`observed_at=2026-03-06` 的「變化」**——一個發生在觀察窗之前的事件。成因是差集混了兩件事：真的新邊，與**舊邊剛跨過 `sub>=4` 門檻**（早期 assertion 沒填 `substitutability`）。改成「事件日必須來自窗內新到的那份文件，窗內沒有新文件就不是事件」。② 第一個已知供應商被報成 `substitution`／`loosening`——那是**我們開始研究一個新領域**，不是世界鬆了（同 `documents` 不參與排序的理由）。兩個修法讓 `since=2026-03-01` 從 17 個「事件」收斂成 3 個。<br>⚠ **`capacity_constraint`／`qualification` 兩個分支在真實圖上一次都沒觸發過**（實測：`since` 兩側都存在的邊 19／32 條，屬性有變的 **0** 條）——它們目前只由 `tests/test_alpha_as_of_projection.py` 守著。 | Phase 2 |
| **6** ✅ | Backtest / validation | as-of 圖投影、anti-lookahead 測試、epoch 錨點 | ✅ **2026-09-04 交付。** `query/bottleneck.py::project_assertions_as_of`＋`Neo4jGraphResearchProvider._rank(as_of)`＋`loader/source_dating.py`（回填走廊）＋`alpha/backtest.py`＋`audit` 的 `PointInTime` check。<br>**實測 before → after：** SourceDoc `published_at` **166/200（83.0%）→ 187/200（93.5%）**；EdgeAssertion 可定日 **382/662（57.7%）→ 645/662（97.4%）** ✅ 達標（≥95%）。查證：`python -m audit invariants --only PointInTime`<br>⚠ **`published_at` 100% 沒有達成，而那是刻意的。** 剩 13 份文件、擋住 17 條 assertion：9 份是**常設產品／公司頁**（Nabtesco／Nidec／Novanta／Proterial×2／Harmonic Drive／Aehr／HyperLight 官網），那類頁面**本來就沒有出版日**；2 份圖上連 URL 都沒有（`damnang`／`silicon_matter` 分析文）；2 份有 URL 但頁面不帶日期（TradingView 轉載的 Zacks、Coherent datasheet PDF——後者抓取工具自陳「這看起來是 logo 檔不是 datasheet」，**兩義訊號不採用**）。**留 null 並列進報告**（L11-5：「我找不到」≠「它不存在」），不用 ingest 日期冒充——那會讓所有東西看起來都是最近才發表的。<br>**as-of 投影實跑：** `2025-06-30` → 2 列、`2026-03-01` → 19 列、`2026-06-30` → 32 列、當前 36 列。⚠ **過濾在排序之前**：`rank_bottlenecks` 是在 assertion 上 collapse 屬性的，先排序再砍列會留下「列是對的、值是偷看來的」，那是 lookahead 最難察覺的形式。<br>**保險絲換條件、沒有拿掉：** `_reject_as_of` 已由兩條 `PointInTimeUnsupported` 取代——圖上完全沒有 `published_at`、或 `as_of` 早於最早證據時仍然拋（回空 list 會與「那天沒有瓶頸」同形，L13）。三條測試守著它。<br>**回填走廊四道補償控制**（放行與收緊同時發生）：屬性白名單／`--basis` 必填並落地／寫入值另存供比對／`url_path` 由 audit 實際重導。⚠ 順手修掉一個靜默炸彈：`MERGE_SOURCE_DOC` 原本無條件 `SET sd.published_at = $published_at`，抽取 JSON 沒帶日期時會把回填**洗成 null**，而不會有任何東西報錯；已改 `coalesce`。<br>**排序前段 vs 後段等權報酬：3 期全部為正**（`+97.8%`／`+3.6%`／`+9.2%`，`scripts/rank_forward_returns.py`）。⚠ **不得讀成排序有效**：第一期幾乎全由 `SOI.PA` 一檔決定（+334.1%，偏離該期均值 +264%），所以輸出強制列出逐檔報酬與「這期主要由誰決定」。期數個位數、標的十來檔且高度集中在 AI 光互連，前後段都不是獨立賭注。<br>**baseline diff（動手前凍結，回填後比對）：** 268 筆既有 decision 的五軸 effective level **0 筆改變**（frozen decision 不回寫，point-in-time contract 成立）；變的是 `python -m alpha research COHR` 的 `context_digest`（`12406e75` → `e682eb67`），因為 evidence_index **9 → 19**——排序列現在帶得出實際引用的 SourceDoc 與其發表日。Q1 分數與 evidence_quality 等級**不變**（`origin_entity` 仍刻意留 None，L8 獨立性計數不受影響）。<br>**驗收：** 1,483 → **1,538 passed / 1 skipped**；突變 59 → **76 個空跑 0**；`audit invariants` **FAIL 0｜PASS 11｜SKIPPED 1**（12 項）（`PointInTime` 由 SKIPPED → PASS，只剩 Phase 4 的 `GateDiscrimination`）；golden 漂移 **1，已解釋為 EXPECTED_CHANGE**（`point_in_time_boundary`：assertion dated 382 → 645、claim dated 300 → 359，正是本 Phase 要推的那個數字）。 | Phase 4、5 |
| **7** ✅ | Portfolio / Risk 完整化 | view → target exposure → hard limits | ✅ **2026-09-04 交付。** `portfolio/alpha_exposure.py`——Phase 3.5 已把 `portfolio/`／`risk/` 搬出 `decision_lab/`，但**兩者對 `AlphaSignal` 的引用實測是 0**：研究端排出了候選、投組端知道持有什麼，中間一條線都沒有，使用者得自己在兩份輸出之間對照 ticker。這支把它接起來，回答「**這些候選我現在持有多少**」。<br>**驗收：**①**不新增任何 alpha 尺寸**——`test_output_contains_no_position_sizing_field` 掃描輸出的每個 key，命中 `target`／`suggested`／`size`／`shares`／`supported_range`／`ceiling` 等字樣即紅；參考線欄位刻意命名為 `single_position_nav_cap_reference`，**名字要自己說出它不是 gate**（超過 5% 不產生 blocker、不告警）。②**target exposure 可由 `AlphaSignal[]` 導出**——join 到持股後每檔給出 `nav_pct`／`held`／`sleeve`，並附 `held_count`／`unheld_count`／`candidate_nav_pct_total`。<br>⚠ **最重要的一條不是功能是分辨力：持股讀不到時整份降級並帶出 `blockers`，不逐檔輸出 0.0%。** 那會讓使用者看到「你一檔都沒買」，而事實是「我沒讀到你買了什麼」——兩者導向相反的行動（L12）。呈現層的措辭也被測試綁住（必須出現「讀不到」「無法」，不得出現「未持有」）。<br>⚠ 三個刻意不做：**不重排候選**（唯一排序權威是 `rank_bottlenecks`，突變已守）、**不 import `alpha/`**（duck typing，保持 `portfolio/ → alpha/` 零相依）、**沒有 ticker 的 signal 列進 `unresolved_signal_indexes` 而不是靜默丟棄**（INV-3）。<br>**驗收：** 1,538 → **1,556 passed / 1 skipped**；突變 76 → **79 個空跑 0**；`audit invariants` FAIL 0；golden 漂移 0。 | Phase 3.5 |
| **8** ✅ | Automation / productization | daily／weekly／skills／MCP 適配 | ✅ **2026-09-04 交付（使用者確認排程未在跑後實跑）。**<br>①**16 條 sandbox rule impact review**：本 session 新增的三支入口（`scripts/backfill_source_dating.py`／`scripts/rank_forward_returns.py`／`portfolio/alpha_exposure.py`）**一律不進 unattended rule**——前兩者互動專用（一個會寫圖、一個吃對外網路），第三個不是 CLI；十六條 fixed entry **數量未變**（`tests/test_codex_daily_permissions.py` 斷言 `prefix_rule(` 恰好 16 次）。結論表見 OPERATIONS。<br>②`sync_agent_skills.py --check` **無漂移**（luna-reviewer 退役後 11 個 skill，兩端轉接層已重生）。<br>③**daily 端到端綠**，11 步逐一實跑：harvest（新增 3 筆 lead／總計 989）→ Engine C ETL（**73/73** snapshot）→ Beta 快照（逐檔心跳完整、2 檔 TWSE 較新而正確隔離）→ Alpha purity → catalyst watch → outcome 快照（**已量測 20/20** 個有 Shadow 錨點的 cohort）→ pending priority → pq1 drain（budget 12，本輪 3 件）→ `decision_lab today` → `todo sync`（新增 0／29 項待辦／watch 35 筆）→ state publisher。<br>⚠ **publisher 第一次刻意 fail closed：`guard_unpushed_commits`。** 它偵測到有未 push 的本機 commit，拒絕把兩個 writer 的變更混進同一份 diff——**那是它該做的事，不是故障**；push 之後重跑即 `status: pushed`。這也是這條窄 pathset 設計唯一一次被真正觸發驗證。<br>⚠ **端到端綠不等於資料沒缺口**（L13：驗的是管線，不是內容）。本輪暴露的既有缺口原樣留著、不順手補：MP 的 `disproof`／`catalyst` 皆未填（L7：沒有證偽條件的警報永遠不會響）；**28 檔只有 14 檔有結構化催化劑日期**，其餘只有散文 catalyst，`expiry 早於催化劑` 這類錯誤在它們身上測不到。兩者都需要 pq2／使用者決定，不由本 Phase 代勞。 | Phase 3 |

### ⚠ `AGENTS.md` 的改動分兩類

**A 類——防止文件說謊，不可延後。** code 改動讓某句話變成假的，就在**同一個 commit** 改掉。
依據是 2026-08-29 實測：程式已於 `6aa31de` 拔掉 beta 訊號，三份文件卻仍在描述**已不存在的
行為**——**管子換了但說明書沒換**，下一個 session 會照著說明書把已被量測為有害的機制講回來。
逐 Phase 的小改，清單見 [`roadmap-migration.md`](refactor/roadmap-migration.md) §10。

**B 類——結構瘦身，一次做完＝Phase 3.9。** 約 310 行 PROCEDURE 搬走、L1–L16 五欄重寫、
四引擎表換成五條 authority separation。放在 3.5 之後是因為 architecture boundary 到那時
才真的定下來；更早寫的瘦身版本會在後面每個 Phase 再被改一次。

⚠ **`docs/ROADMAP.md` 本身的重構已於 Phase 0 完成**（673 → 227 行、逐字封存、22 個標題
全有去向判定）。之後只剩每個 Phase 完成時回填實測 before → after，那是維護不是重構。

## 每個 Phase 的 completion gate（八項，缺一不得宣稱完成）

出自 [`historical-failure-matrix.md`](refactor/historical-failure-matrix.md) §9。
**不得僅以「tests pass／CLI works／architecture looks cleaner」判定完成。**

1. Historical regression suite pass（golden fixtures）
2. Runtime invariant audit pass（`audit invariants`）
3. No unexplained semantic diff（old/new dual run）
4. **No new dual authority**
5. No silent-drop path（每個 filter 都能報 input／accepted／filtered／reasons）
6. Point-in-time tests pass
7. All migrated lifecycle objects reachable
8. **該 phase 負責的 critical historical failure 已有 executable protection**

> 現況：36 筆歷史事故中，**🔴 僅有文字保護的有 10 筆**。各 Phase 的責任分配見該檔 §9。

## 重構期間的硬約束

1. **不重建 Neo4j。** 資產是 662 條 EdgeAssertion 的 provenance，不是節點數。
2. **Decision Store schema 不動。** 268 筆 append-only 紀錄，Git 救不回（L10）。
3. **四個人工 gate 不放寬：** graph admission、Engine C 觀測寫入、thesis mutation、live。
4. **`rank_bottlenecks()` 仍是唯一排序權威。**
5. **系統仍然不給 alpha 部位尺寸**（`AGENTS.md` Alpha 呈現契約）。
6. **beta 訊號不得以任何名義復刻**（2026-08-01 實測 0 勝 3 敗）。
7. **既有 126 個測試檔全部保留**——可改 import 路徑，不可刪斷言。
8. **改任何 `python -m <module>` 命令字串前，先走 sandbox impact review 五步**——
   `.codex/rules/stockbot-automations.rules` 的 16 條 exact prefix 會靜默打斷 daily。
9. **六條 hard invariant 全程適用**（`historical-failure-matrix.md` §2）：
   IDENTITY（ticker 不是 entity identity）／LIFECYCLE（每個 active object 答得出五問）／
   **NO SILENT DROP**（「查不到了」不是合法 lifecycle）／QUEUE LIVENESS（producer 指得出
   consumer）／MEASURED GATE（未量測的機制不得享有默認信任）／POINT-IN-TIME & PROVENANCE。
10. **Core 不得 import `mcp_server`。** MCP／remote 是 optional adapter，
    依賴方向只准 peripheral → core。⚠ 今天**不是** 0：`engine_b/todo.py`、
    `query/health_audit.py`、`scripts/*` 共 **4** 個消費端（`crons/weekly_scan_digest.py` 已於
    2026-09-08 刪除：它自 U7b 之後就沒有掛在任何 hook 上，是死碼）。
11. **Local-first：新核心必須能在完全沒有 MCP 的情況下運作。**
    若 MCP 相容性與新核心架構衝突，**優先選擇新核心架構**。
12. **`AGENTS.md` 不是憲法。** 「四引擎架構」是 CURRENT_ARCHITECTURE；
    不可變的是五條 authority separation（`target-architecture.md` §12）。
    ⚠ **lesson learned 一條都不刪**——綁定實作的改寫成
    Context → Failure → Learned invariant → Current implementation → 可改？

---

## 開放 backlog（與重構平行，可獨立進行）

> **⚠ 2026-09-04 實測：這張表自己腐壞了——8 個「開放」項目有 6 個早就做完。**
> 逐項查證後才發現：footer 的 `live_choices` 已顯示 1 筆／fill 1 筆（與 DB 一致）、
> publisher pathset 早在 09-02 由二擴四、`current_holdings` 已有 `failure_kind` 三態
> 診斷、`checkpoint_decision_review` 已把格式驗證移到副作用之前、
> `Core → mcp_server` import 已是 0、`store.py` 早有建立時的同公司 cohort 偵測（現況 0 條重複）。
>
> **這正是本檔自己記過的形狀**（「清單會腐壞，判準不會」）：完成的人改了 code、
> 沒回頭改這張表，於是下一個 session 會照著它去做已經做完的事。
> **規則：這張表的每一列都必須附「哪個數字會變」的查證命令**，開工前先跑一次；
> 跑出來已達標就是直接標 ✅，不是重做一遍。



| 做什麼 | 為什麼 | 驗收（哪個數字會變） | 前置 |
|---|---|---|---|
| **研究閉環（2026-09-09 使用者定案；Z3 zoom-out，PLAN_PROPOSAL 全數核准）——P0 判準定案** ✅ 2026-09-09 記入 | Goal：讓單檔判讀 ready 的檔數與需求錨數大到能回答「全部是負的，是方法偏空還是市場太貴」；不是檔數本身，是**每一檔到終局**（ready／剩餘 blocker 全 settled／全掛在 pq2 編號）。使用者五個判準已定：①**倍數原則**（無 re-rating 證據＝校準倍數；隱含報酬兩欄拆解）→ `AGENTS.md`「隱含報酬的兩個桿」；②**常規授權類別**（注意力 gate 預先授權，SSOT `config/standing_authorization.json`）→ `AGENTS.md`「授權介面唯一」；③**分散度優先於主線**（decompose 提案自動鑄 pq2、核准後同級）→ 本檔「研究主題範圍」；④**批次起草 forward view 可以**，條件：深度優先、evidence_refs 必須解析、卡上永遠帶〔session 判斷〕標籤；⑤**「球在誰手上」唯讀清單住 APP** → `AGENTS.md`「APP 呈現契約」。 | 無數字；P2／P3／P5／P6 以它為前置。查證：`grep -n "2026-09-09 使用者定案" AGENTS.md docs/ROADMAP.md` | 無 |
| **研究閉環 P1：機械積壓接上 consumer** ✅ **2026-09-09 交付（GO）** | 2026-09-08 實測：39 個 fired watch 沒人接（`todo sync` 與 triage 各呼叫一次 `check_watches`、各只處理自己那一種喚醒對象）、5–7 個 decision_review 只因凍結 context 過期而 REVIEW 卻在等 `go`、佇列的段序只存在於 skill 散文——新工作類型長出來時沒有任何東西會叫（L13＋L16）。 | ✅ `python -m engine_b.event_watch counters` 的 `fired_unconsumed` **39 → 3**（35 筆 lead 型由 `engine_b.cli consume-fired` 重排回 pq1、1 筆 pq2 型由修正後的 `todo sync` 收掉；剩 3 筆是假設對照，屬研究段 0b，`drain` 逐筆列出）。⚠ 上線首跑揪出 `check_watches` 兩個潛伏缺陷並同 change 修掉：①**追源重排不是事件**（requeue 把 triage receipt 重寫成今天，35 筆重排後下一次 sync 又叫醒 21 個 watch、其中 7 個觸發者就是重排回來的同一批）；②**同一則 lead 只能叫醒同一個 watch 一次、且叫醒時把當下全部命中的歷史 lead 一併標記**（`consumed_leads`；先前 fired watch 是死的所以沒人發現，回 active 後 NVDA／TSM 的 entity_filing watch 各有 8–28 則歷史 Form 4 命中，逐則消化要數十輪 sync 才收斂；批次標記後 2 輪收斂、之後 sync 冪等）。`stalled` 由 11 升到 32 是同一件事的另一面：回 active 的 watch 標的已全部消化，靠到期或主動輪詢收斂——這是 consumed-marker 的既有設計，不是退步；✅ `python -m engine_b.todo reassess-stale`：候選 **3 → 0**（[412][454][455] 皆轉 MONITOR 結案；判準比 hint 更嚴——只收純 `system_internal` blocker 的，帶 `awaiting_external` 的 [276][279][472][479] 不算，因為 reassess 對它們只會多 append 一筆同樣 REVIEW 的 decision）；✅ `engine_b.cli counts` 的 `triaged_go` **1 → 38**（37 筆重排；它們依 `priority.py` 排隊吃研究預算，`drain` 只列前 12）；✅ 新稽核 `python -m audit invariants --only QueueSegments`（INV-4；段序封閉字彙 `engine_b/queue_segments.py`，分不到段的狀態 FAIL）**PASS，forward_view_backlog=27 首次成為可見計數**；✅ `QueueLiveness` 起算點改為 `triage.decided_at`（重排當天不再報 27 天假警報）；✅ 測試 +14（`tests/test_queue_segments.py` 等）。**不碰四個 gate：apply 0、Engine C 判讀寫入 0、thesis 0。** ⚠ [473]/[474] **不 drop**——它們是圖已寫入的 `ra_admission`，正確結案是 `complete-ra` 認領路徑（本表另一列），`drop` 會偽稱「略過該 RA」。 | 無 |
| **研究閉環 P2：隱含報酬兩欄拆解＋倍數預設原則** ✅ **2026-09-09 交付（GO）** | 兩檔 ready 都是負的，但負值大半來自倍數折價習慣（見 P0 ①）；不拆開，P3 批次寫 27 檔只會得到 27 個讀不出東西的負數。 | ✅ `alpha/implied_return/attribution.py`：恆等式 `(1+R) = (E_int/E_cons) × (PE_t / (P/E_cons))`，輸出 `eps_contribution`／`multiple_contribution`／`interaction`（三者相加＝price_return，契約層驗到 1e-9）；拆不出來就 `missing`＋`absence_kind`（共識缺＝provider_missing、口徑不合＝inputs_incompatible、共識非正＝method_not_applicable），**報酬本身不受影響**。倍數差距 ≥5% 時模型另發 warning 引原則。✅ read model 13a、analyst 頭條、APP 兩個頭條區各多兩格；`python -m briefing analyst-view COHR --format json` 實測（現價 301.88，bar 09-08）：總報酬 −25.9% ＝ EPS 差異 **−5.0%**（8.944 vs 共識 9.416）＋倍數差異 **−22.0%**（25x vs 市場對共識付的 32.1x）＋交互項；LYC.AX −36.1% ＝ **−20.0%** ＋ **−20.2%** ＋交互項。**判讀：COHR 的負值八成五來自倍數桿；LYC.AX 兩桿各半。** 若依原則把 COHR 目標倍數 supersede 為校準倍數，隱含報酬會落到約 −5%——那是**判斷變更**（另寫一筆 ValuationAssumption，屬研究、鑄 pq2），本 Step 不代寫。✅ 測試 +5（`tests/test_implied_return.py`），既有 85 條不動。⚠ 同 change 修了 `tests/test_full_chain_acceptance.py` 的釘法：原本 `today=2026-09-07` 卻取**當前**價與 ledger，COHR 的 D&C 假設 09-08 re-append 後整條鏈在該日缺席，17 條紅了兩天沒人發現；改成 `as_of=TODAY`（PIT 投影）後期望值**一個都沒改**就全綠——「釘的是一個 authority 都沒有合法更新時的樣子」本來就該用 as-of 表達。查證：`python -m briefing implied-return COHR --format json`；`python -m pytest tests/test_full_chain_acceptance.py -q` | P0 ① |
| **研究閉環 P3：每檔閉環進 research-drain（forward view backlog 段）** ✅ **2026-09-09 機制交付；2026-09-11 實證達標** | 27 檔（P4 後 70 檔）blocked 的機器工單早就存在（analyst view `readiness.blocker_details` 逐格帶 `absence_kind`），只是沒有 consumer。閉環＝拿它當工單、一格一格消、每消一格重新 materialize，到終局才開下一檔（深度優先）。 | ✅ `alpha/closure.py`：終局判定（ready／全 settled）、下一檔五條確定性規則（`NEXT_PICK_RULE`：有共識 → EPS 為正 → 產業能加一 → 瓶頸名次 → ticker）、摘要；`python -m webapp status` 多一段「每檔閉環」，`engine_b.cli drain` 首行多一行段 5。✅ `config/standing_authorization.json`＋`engine_b/standing_authorization.py`（封閉性：ITEM_TYPES 每種必須分到 authorized 或 never，四個 gate 類型在 never）＋`engine_b.todo standing-go [--run]`（段 2b：對 decision_review／source_trace_review 執行使用者本來會下的 go；pending／等世界／付費的不碰）。⚠ standing-go 是互動限定，daily 採用歸 P5 過 sandbox review。**首跑（2026-09-09）：候選 6 全部執行**（[459][460][466][467] dispatch 回 pq1、[279][479] reassess），跳過 10（6 筆在等世界、4 筆使用者明示 pending），失敗 0——這 6 個編號從此不再出現在「等你 go」的段落。✅ `webapp status` 首次印出「每檔閉環」：到終局 3（ready 2／settled 1）／未到終局 70／有 ready 檔的產業 2／4，**下一檔 SOI.PA**（有共識、EPS 為正、成熟／特殊製程尚無 ready 檔、瓶頸排序第 5；四格全缺）。✅ 測試 +14。**到終局檔數 3 → N 的實證**由首輪 research-drain 完成（depth-first：下一檔由 `webapp status` 印出，不由人挑）。查證：`python -m webapp status`；`python -m engine_b.todo standing-go` | P0 ②④、P2 |｜✅ **2026-09-11 實證**：`python -m webapp status` → **到終局 14（ready 11／settled 3）／未到終局 59**（分母同為 73）——P3 記的「3 → N」已由 **3 → 14** 兌現。⚠ 本列左欄記的「五條確定性規則」是**交付當時**的樣子；規則此後長到七條（09-11 加「使用者沒有明示 defer」與「已有基期觀測」），SSOT 一律看 `alpha/closure.py::NEXT_PICK_RULE`，不看這一格。
| **研究閉環 P4：materialize 的第二種宇宙 `--registry-listed`** ✅ **2026-09-09 交付（GO）** | registry 73 家上市公司有 43 家不在 tracked，它們已有 Engine C 財務與共識，只差被 materialize 與寫 forward view。**實作與計畫不同，理由要記下：** 原計畫是給 `discover_tracked_tickers` 加第四來源，但那個導出同時餵 pq1 priority（tracked 加分）與 EDGAR harvest 的 watch 推導——加進 73 家等於讓 daily 多抓 43 家 Form 4、並把 priority 加分稀釋成人人有獎，兩個都是行為變更。P4 要的只是「APP 看得到更多檔」，所以另開 materialize 自己的宇宙。 | ✅ `python -m webapp materialize --registry-listed`；`python -m webapp status` 的 artifact 數 **30 → 73**（新增者全部 blocked，那是誠實的起點）；`audit invariants --only QueueSegments` 的 `forward_view_backlog` 隨之由 27 升到 **70**（73 檔中 71 blocked、2 ready、1 settled）——那是「終於看得見的工作」，不是退步。pq1 的 tracked 來源仍只有三個（`tests/test_webapp_universe.py` 守著）。⚠ daily 收尾的 materialize 命令**未改**（仍 `--tracked`）：加旗標是 unattended surface 變更，歸 P5 一起過 sandbox review。 | 無 |
| **研究閉環 P5：daily 收尾吃機械段＋常駐計數器** ✅ **2026-09-09 交付（GO；R2 由使用者 opt-in 後執行）** | 機械段（consume-fired、reassess-stale、standing-go、todo sync 的 pq2 翻醒）先前只有互動 session 會跑；daily 是無人值守 Codex sandbox，每支新命令都要過 sandbox impact review 五步。判斷類（forward view、decision gap 研究）刻意**不**進 daily——LLM 無人值守寫判斷檔的 allowlist 縮不到安全範圍。 | ✅ rules **17 → 19**（`engine_b.todo reassess-stale`、`engine_b.todo standing-go`；`consume-fired` 留 sandbox、不進 rule）；✅ prompt／skill 步驟 3 加三支、步驟 8 加 `--registry-listed`、brief 首屏加「今日自動清了 N…每檔閉環…下一檔 X」；✅ permission contract test 斷言 split（consume-fired 不得進 pattern；dispatch／resolve 仍不放行）；✅ 五步結論見 OPERATIONS。✅ **R2（使用者 opt-in，獨立 reviewer）：CONDITIONAL_GO → 三處同 change 修掉**：①`_load_brief_items` 對 brief 服務降級（status unavailable／error，不拋例外）回 None 而非 []；②`advance_decision_review` 在 brief 讀不到時 fail closed（outcome `reassessed_blockers_unknown`，不下「已解決」結論、不排隊）——沒修的話無人值守可能把真實 user_decision blocker 靜默關掉；③prompt「十七條」筆誤。另依 R2 的 queue-liveness 發現補：`engine_b.cli drain` 列出 standing-go 排入的 assessment-gap 工單（先前只有 research-drain 看得到）；reassess-stale 加 7 天冷卻。R2 留下的非阻斷邊界：`catalyst_missing`／`disproof_missing` 這類「要使用者提供文字」的 user_decision blocker 若單獨出現，standing-go 會把它排入 pq1 但研究補不了——今天真實資料未觸發，記為已知邊界。⚠ **端到端驗收在下一次 06:30 排程**：brief 首屏出現那一行才算管子兩頭接上（互動 session 觸發不了 Codex sandbox）。查證：`python -m pytest tests/test_codex_daily_permissions.py -q` | P1 |
| **研究閉環 P6：第二種估值方法（虧損公司）** ✅ **2026-09-09 機制交付（GO；使用者定案 EV／Sales）** | v1 只有本益比法；tracked 30 檔有 4 檔 forward EPS 共識為負（AEVA／IQE.L／POET／SIVE.ST）、1 檔無共識（CCXI），ready 上限約 25 檔，而被排除的正是最早期的那批。 | ✅ `alpha/valuation` 新增 `ev_to_sales`（`METHOD_PARAMETERS`／`METHOD_FUNDAMENTAL_INPUT`／`FAIR_VALUE_FORMULA`／`METHOD_ACCOUNTING_BASES` 同步；`BalanceSheetInput` 帶 Engine C 淨負債快照；稀釋股數取 fundamental model 的 `diluted_shares[total]` 假設；任一缺 missing 不補 0；`method_applicability` 對營收非正拒算）。✅ read model 自動選方法（本益比法 `method_not_applicable` **或 `deliberate_abstention`**——後者 2026-09-09 SOI.PA 實例補上：谷底年 EPS 為正但無可錨定倍數，由 append-only Abstention 宣告後才切；只是「還沒寫 target_pe」**不**切，方法選擇是判斷不能靠不寫——且 ledger 有 EV/S 假設才改跑）；本益比法 baseline 一格未動（測試守著）。✅ 測試 +5。**方法適用上限 25 → 30 要等四檔各寫一筆 `target_ev_to_sales` 假設**——那是研究判斷（research-drain 每檔閉環的一格），不是本列能自己完成的。已知近似：淨負債是現況快照不是目標期末；兩桿拆解對 EV/S 無定義。 | P0 ① |
| **研究閉環 P7：每檔閉環的產線化**（使用者 2026-09-10 核准五欄 amendment，選 B：機械段進排程） ✅ **2026-09-10 交付（GO）** | 2026-09-10 實測：73 檔 materialize 過的標的裡 **68 檔 blocker 完全同形、零例外**（`headline=upstream_unavailable、fundamental/why/research=not_yet_recorded`）——不是 68 個研究題目，是同一題乘 68。同時 P4 把分母從 27 推到 68，而分子仍靠一檔一檔手做。使用者的問題是「一檔一檔來，最後我需要說七十幾次繼續？」——**根因是 skill 把段 5 的閉包條件寫成「本輪至少 +1」**，一句話同時當進度下限與停止條件（L12）。實測 09-09：`23:18→23:28` 做完 LITE、`23:33` 就收工，段 5 還剩 67 檔。 | ✅ **P7-c 停止條件**：`alpha.closure.closure_gate` ＋ `python -m webapp closure-gate`（exit 0 閉包／1 還有工作／2 讀不到 fail closed）；`--skip` 讓「卡 pq2／卡世界」成為呼叫端的**顯式**宣告（gate 不 parse `todo_pool.json` 散文猜 pq2 歸屬，L16）。「本輪 +1」降級成進度斷言。skill 明文寫入「深度優先是**同時開幾檔**的上限（1），不是**一輪做幾檔**的上限」。✅ **P7-a 基期實績機械化**：`fetchers/edgar_xbrl.py`（SEC XBRL companyfacts → revenue ＋ gaap.operating_income 兩個數，那正是 causal bridge 需要基期提供的全部）＋ `scripts/backfill_fiscal_year_results.py`。**基期實績覆蓋 6 檔 → 42 檔**（寫入 36 筆）；**37 檔的 headline 理由由「Engine C 無 fiscal_year_results 觀測」變成「缺 nci_attribution 假設」**——機械格清掉，剩下的是判斷格。實測撞出並修掉三個靜默錯誤：只認 10-K（六檔 20-F 發行人被誤判「沒有年度事實」，實際各有 2,500–8,700 筆 fact，L11-5）、namespace 先到先贏（HIMX 取到 us-gaap 的 FY2017 而非 ifrs-full 的 FY2025）、多幣別 guard 永遠不觸發（單位檢查排在收斂之後）。另加 550 天陳舊門檻（TSM／UMC 在 companyfacts 最新只到 FY2024，照寫會得到看起來正常、實際落後一整年的 forward view）。✅ **選項 B sandbox impact review 五步**：rule +1（**只放行這一支**；`record_mechanical_observation.py` 接受任意 `--field`，刻意排除）、permission contract test +2、端到端 smoke **冪等**（第二次 written 0／skipped_current 36）。同 change 補兩道缺口：`append_manual_observation` 不擋 judgment 欄位 → 閘門改由腳本啟動時查 registry（非 mechanical exit 3、無 CLI 參數可換欄位）；`_existing` 只問「有沒有」→ 改成比年度，否則明年的 10-K 永遠不會被撿起來（L17）。✅ **P7-b 品質計數器**掛在 closure-gate（每輪必跑的那一支，計數器才會自己出現）：隱含報酬正負分布｜倍數＝校準倍數的檔數｜有折溢價主張的檔與貢獻。**首跑就抓到實質問題：**4 檔 ready 有 3 檔帶倍數折價主張（COHR −22.4%、LYC.AX −19.7%），只有 LITE 接近中性（−1.1%）——正是 `AGENTS.md`「隱含報酬的兩個桿」警告的那個習慣。⚠ **到終局檔數仍是 5，本 Step 未變**——P7-a 降的是每檔成本，不產生判斷；檔數要由 research-drain 的判斷段長。測試 +25、全套 2225 passed；audit invariants 13/13 PASS。查證：`python -m webapp closure-gate`；`python -m pytest tests/test_edgar_xbrl.py tests/test_closure.py -q` | P0 ①④、P2、P3、P4 |
| **研究閉環 P∥：decompose 新需求錨的自動提案** ✅ **2026-09-09 機制交付（GO）** | 圖的候選來源是圖自己；36 條可行動排序 25 條是 AI 光互連，需求錨全是 AI capex。唯一能長出新層的是 decompose，而選題今天全靠使用者想起來。 | ✅ `engine_b/decompose_proposals.py`＋`engine_b.cli decompose-propose`：三道機械判準（需求錨不在 `sector_anchors.json` 各組且不在圖的 coverage 快照裡；drop 過沒新 lead 不重生；同時 open ≤2）通過才鑄 `manual` 型 pq2，hint 固定寫系統名／新錨理由／點名 lead／預估層數／`go` 只授權跑 system-decompose（不含入圖、onboard、tier）。提名本身是語意工作，由 research-drain 收尾、weekly Stage 1、使用者貼入的 lead 供給（skill／prompt 已改）。✅ 測試 +6。**產業組 5 → 6 以上**要等第一題被核准並拆完才會動——那是研究，不是本列能自己完成的。 | P0 ③ |
| **段5 有一段「會計年度已結束、但財報還沒公布」的空窗** ✅ **2026-09-12 交付（GO；使用者「直接修吧」）** | `HorizonAssumption` 契約要求 `horizon_end > created_at`（INV-2：寫下時就已經過去的等待不是判斷），而目標期間來自共識的 `0y`，provider 在財報公布前會**一直把已結束的那一年標成 0y**。兩邊各自都對——是一個標籤同時承載「進行中的當年」與「已結束、未公布的當年」兩種語意（L12）。先前的處置是每輪手打 `closure-gate --skip`，也就是把一個**機械可判的事實**交給人記得。 | ✅ `alpha/closure.py` 新增**第三種終局** `awaiting_report`（`BacklogRow.awaiting_report_since`；判定所需的「今天」住 provider，核心層保持無時鐘）＋ `_target_period_ends()`（讀 `_consensus_flags` **同一張表**的 `0y` `fiscal_period_end`，不新造第二份期間推導，L16）；`summarize`／`render_summary` 把三種終局分開印（`ready X／settled Y／等財報 Z`）；新 `render_awaiting_report()` 逐檔印期末與**已過天數**。✅ **驗收（實測）：`--skip` 清單少 3 檔，而且抓到我自己漏掉的一檔**——先前手動只認出 MU 與 6594.T，偵測器找出 **JBL**（FY2026 於 2026-08-31 結束、才過 12 天，我把它誤 skip 成『型別不符』）。三檔：6594.T（已過 165 天，申報延期案）／JBL（12 天）／MU（15 天）。段 5 到終局 28 → **31（ready 22／settled 6／等財報 3）**。✅ 品質計數器改成只看 ready／settled——等財報那幾檔本來就沒有估值，混進來只會在「讀不到」欄長出假計數。✅ 測試 +8（`tests/test_closure.py`：ready/settled 優先、讀不到不算終局、離開佇列且**不需要 `--skip``）、三種終局分開數、**出口存在**（期末往前滾就回到佇列）、天數而非閾值、`_target_period_ends` 只取最新 snapshot）。查證：`python -m webapp closure-gate`（看「等財報公布」那一行）；`python -m pytest tests/test_closure.py -q` | 無 |
| **APP 的標的列表還不知道「等財報」這種終局——它會顯示成「還沒做」**（新增 2026-09-12；上一列的 symmetric face） | `webapp/api.py::_group_of` 只認 `("ready","settled")`，其餘一律歸 `not_started`（面向使用者寫「還沒做」）。而 `closure_terminal` 由 `webapp/materialize.py` 用 `row_from_artifact()` 算——那條路**只讀 artifact**，拿不到共識期間，所以結構上永遠算不出 `awaiting_report`。⚠ **這不是上一列造成的回歸**（實測：`row_from_artifact('MU', artifact).terminal` 改動前後都是 `None`），是 L17-3③ 那個形狀——**偵測做了，對稱面沒做**。但它是一句對使用者不誠實的話：AGENTS.md「APP 呈現契約」要求使用者分得出「還沒做」與「沒人能做」。 | 驗收：`#/` 標的列表把 MU／JBL／6594.T 由「還沒做」改成一個能分辨「在等財報」的分組，**且 request path 不得新增任何外部讀取**（要帶的是 materialize 端寫進 artifact 的欄位）。⚠ 驗收刻意不是「那三檔變綠」——時間一到它們本來就會離開這個狀態。 | 上一列。⚠ **修法層級＝顯形不是根除，理由要寫出來**：根除是讓 `materialize.py` 把目標期間寫進 artifact，但那會讓 per-view 的 materialize 多一次 Engine C 讀取，動到「materialize 是唯一會跑模型的入口」那條邊界的成本結構，是另一個 Step 的 scope；而今天只有 3 檔，資料還不足以逼出通解（L17-4：general 到資料支持的那一格為止）。 |
| ✅ **research-drain 首輪閉環（SOI.PA）留下的三筆小債**（新增 2026-09-09） | ①`engine_b.event_watch` CLI 只有 `consume`，假說型（fact_verification）watch 被**無關的** tier-1 lead 誤觸（entity 交集）時沒有「對照後回 active」的命令——今天 ew_0005／0007／0057 三個都是這種（COHR 8-K 是 RSU、AAOI 8-K 是租賃），用 Python 直接 `reactivate`＋補 `consumed_leads` 處理，稽核留在 watch `note`；②`alpha/closure.py::NEXT_PICK_RULE` 不看 pq2 的使用者 defer——SOI.PA 的 [461] 是使用者明示 defer，規則仍選它為下一檔（結果仍合理，但「使用者剛說先不要」與「系統排第一」同時成立是 L12 形狀）；③SOI.PA 的 EPS 共識口徑未核實（yfinance `year_ago_actual` −3.28 vs 法定稀釋 EPS −6.17），EPS 比較 `not_applicable`、兩桿拆解做不出——需要確認共識供應商是否剔除減損，屬 ETL／資料層，不是研究。 | ①加 `event_watch reactivate <id> [--note]`（並在 research-drain skill 段 0b 寫明「對照結果為無關 → reactivate，不 consume」）；②NEXT_PICK_RULE 第 0 條：pq2 有 `deferred_at` 的檔往後排（仍列出，不藏）；③consensus ETL 記錄 EPS 口徑欄位或在 compare 的理由句寫出兩個數字（已寫）。全部 R0／R1。 | 無 |｜✅ **2026-09-11 交付（三筆全清）**：①`engine_b.event_watch reactivate <id> [--note]` 已有 CLI，`woken_by` 清空＋`reactivations` append（**不覆蓋**建 watch 時的 `note`），「為什麼判定無關」從 transcript 變成 watch 上的稽核欄位；research-drain 段 0b 的判準句同步。修法層級＝**顯形**（誤觸仍會發生——entity 交集本來就會誤觸；改的是它有沒有一條留痕的出路）。②見下一列的 `NEXT_PICK_RULE` 第 0 條。③`compare.describe_basis_mismatch()` 把**兩個數字**寫進 `unverified` 的理由句。驗收（L14-1）：**2 筆真的變了**——TSM『provider 10.65 vs 一手 gaap 10.43（差 +2.1%）』、SOI.PA『−3.28 vs −6.17（差 −46.8%）』，從此「差 2%」與「差 40%」在畫面上分得出來。⚠ 它**只描述不分類**，不宣稱成因是匯率還是口徑——那是下面「`incompatible_basis` 兩義」那一條的事。
| ✅ **full-chain 驗收 rollover 兩條紅：假設在 fiscal_rollover 情境被判 `invalidated` 而非 `superseded`**（新增 2026-09-09） | `tests/test_full_chain_acceptance.py` 的 `test_a_fiscal_rollover_does_not_let_old_period_numbers_pose_as_current`／`test_a_scoped_no_attention_claim_states_its_scope_and_what_lies_outside` 於 09-07 驗收報告時為綠，09-09 實測紅：rollover 視角下 `oa_b43e83a1413ab91c`（D&C +60%）、`va_07dcbc388c814a91`、`ha_586fe0ff9658a2d4` 全部 `invalidated`，理由是 `supporting evidence 解析不到`（`engine_c://consensus_estimate/COHR/revenue/2027-06-30`、`engine_c://manual_observation/mo_db5586…`）。兩個疑點：①consensus ref 在 ledger 裡是 calibration 角色，refresh 卻當 supporting 判 invalidated；②rollover 的 `rollover_actuals` 覆寫基期後，manual observation refs 從 evidence index 消失。09-07 之後動到這條鏈的只有 `1a5d1ad`（Coverage Pilot 的 6 個 generic 修正，含共識對帳）。**與 P1／P2 無關**（stash 驗證過），已以 `xfail(strict=True)` 標記讓它在報表現形且修好會自動提醒。 | 兩條測試拿掉 xfail 後綠；`python -m briefing refresh COHR --scenario fiscal_rollover` 的假設狀態全部 `superseded`、無 `invalidated`。R1 development profile（fail closed／unaffected behavior） | 無 |｜✅ **2026-09-11 交付（GO）**：**不是產品缺陷，是測試釘點自己腐壞**。實測隔離出來的是 `view()` helper 的 `today=TODAY(09-07)` 配 `as_of=None`——兩個互相矛盾的視角：ledger 讀「現在」，而 `today` 停在 09-07，於是 09-10／09-11 才 append 的假設被判成「此時點尚未成立」，連帶把 D&C 成長、target_pe、horizon 三筆判成 `invalidated`，`rollover_actuals` 也因 revenue 未知而整個不發生（`period_end` 停在 2027-06-30 而非 2028-06-30）。**與 rollover 無關**：`scenario=None` 同樣重現。查證：`today=None` 或 `today=2026-09-11` 時全部 `superseded`、`invalidated` 0 筆。修法＝`_coherent_today()`——PIT 釘點才釘 `today`，當前視角一律不釘；**不是把日期往後挪**（那下一次 append 又紅），是讓那個混合視角在本檔結構上不可能被表達。兩條 `xfail(strict)` 已移除，`tests/test_full_chain_acceptance.py` **35 passed / 0 xfailed**。修法層級＝**根除**（就這一檔的釘點而言：混合視角不再寫得出來）。⚠ 刻意不在 `fetch_alpha_investment_view` 加防呆——production 全部走 `today=date.today()`（CLI 一處都沒傳 `today`），只有測試造得出這個組合，替造不出來的情境加 gate 就是 L17-4 的「在沒有事實支撐的地方泛化」。
| **呈現責任重切：state → APP、Daily → 心跳（2026-09-08 使用者定案；Z3 zoom-out）** 🔶 **七段全部 ✅ 2026-09-08 交付（B1／B2-beta／B2b／A／C／B2-positions／白話化）；只剩 ≤7K 實測** | Daily Brief 誕生時（2026-07-22）沒有 APP，所有「使用者需要看得到的東西」只能塞進 daily。實測 08-31 那份 brief 具名 section 14,436 字裡**持久狀態佔 64%**（四 pane 3,958＋Beta 3,434＋外部事件 1,117＋賣出側 562＋無事 126），決策＋進度只佔 36%——心跳被淹沒。判準一句話：**昨天和今天一樣的住 APP（想看時看），今天變了的＋要你決定的住 Daily（你每天讀）。** 使用者定案三條：①**pq2 完整四段留在 Daily**（核准載體是對話，APP 無寫入端點，放 APP 只多一次摩擦）；②**Beta 搬 APP**，呈現走常見的資產分布圖＋折線（水位只呈現不排序；AGENTS Beta 契約在 Phase C 同一 commit 改：逐檔表每日心跳 → 只印門檻跨越／狀態翻轉）；③第一個 Step＝B1。研究方向（drain 排什麼、research-drain 閉包）與本項正交，不動。 | **B1 ✅（`ranking` kind）：** `webapp/contracts.py::STATE_SCHEMA_VERSIONS` 封閉 kind 字彙＋`StateArtifactStore`（`library/private/app/state/<kind>.json`；`resolve_state_dir` 是唯一解析規則）；`python -m webapp materialize --ranking [--as-of]` 與 `python -m query.bottleneck` 同一條路；`GET /api/v1/ranking`＋`#/ranking` 畫面（首選／已知限制不摺疊／可行動＋純結構兩表／產業分組／需求鏈／新鮮度／不是什麼）。**驗收：APP 讀出的 36 列（兩份排序）與現算 `rank_bottlenecks()` 逐列逐格 diff 0**；`python -m query.bottleneck` 與 `--by-sector` 的 markdown **改前改後逐位元組相同**（13,067／2,533 bytes）；固定文字與落差判準抽成 `query.bottleneck` 常數／函式，markdown 與 artifact 同源（L16）；四種 request-path 證明已涵蓋 `/ranking`；`webapp status` state 1 份、`verify` 5/5；1,989 → **2,034 passed／1 skipped**（新增 26＋3）。查證：`python -m webapp status`；`python -m pytest tests/test_webapp_ranking.py -q`。報告 [`2026-09-08-webapp-ranking-b1.md`](reports/2026-09-08-webapp-ranking-b1.md)。<br>**B2-beta ✅（2026-09-08）：** `python -m webapp materialize --beta` 以 `--no-refresh --no-record-risk` 純讀呼叫 `daily_beta_snapshot.run()`（不重抓行情、不 append 風險快照）；`GET /api/v1/beta`＋`#/beta`：可動用資金 hero → 現在的配置（水平堆疊條，色跟 sleeve 走）→ 距目標多遠（分歧條，灰帶＝容忍區間）→ 風控儀表（總曝險／槓桿 ETF 資金占比／換算槓桿曝險／貸款占 NAV／TSMC 已知至少）→ 逐檔卡（心跳、52 週位置儀表、自身收盤折線＋十字線 tooltip＋表格版）。標籤全部從 `portfolio.allocation` 對照函式取；序列只搬 `session_date`＋`close_adjusted`，**動能欄位整組擋在 artifact 外**（測試掃鍵名）。**驗收：** 每格與 report 逐鍵 `is`／`==` 相等；真實 6 sleeve／14 檔、序列 7–28 個交易日（DRAM 0，誠實）；四種 request-path 證明涵蓋 `/beta`；焦點 148 passed、完整套件 **2,056 passed／1 skipped**（1,989 → 2,056；B1 後再 +22：beta 17＋既有調整；full-chain 黑箱測試改用與子行程同一時鐘）；daemon `/health.state_kinds == [ranking, beta]`。⚠ 調色盤用 dataviz 參考實例已驗證值、**未跑 validator**（無 node）；**未做 render-and-look**（請使用者開頁面看）。報告 [`2026-09-08-webapp-beta-b2.md`](reports/2026-09-08-webapp-beta-b2.md)。<br>**B2b ✅ `coverage`＋`watches`（2026-09-08）：** `materialize --coverage`（照抄 `coverage_gaps.scan()` 的分桶，並把 🔴 桶依**前綴**切成真子瓶頸 `tech:`／`mat:` 與抽取副產品 `prod:`——機械比對非語意判斷，判準住 `query/coverage_gaps.py`）與 `materialize --watches`（照抄 Event Watch registry 的 counters／sweep_due／is_stalled ＋ `leads.trace_backlog`，**停滯**與**追源需處置**獨立成區）。`GET /api/v1/{coverage,watches}`＋`#/coverage`／`#/watches`（KPI 計數＋清單，依 dataviz「是不是圖表」判準：>7 類且各有意義→表格／清單，不是更多顏色）。**驗收：** `python -m query.coverage_gaps` 與 `python -m engine_b.event_watch list` 的輸出**改前改後逐位元組相同**（1,833／14,758 bytes）；真實 🔴 真缺口 5／🟡 15／✅ 170、在等 38／停滯 11／fired 未消化 39；四種 request-path 證明涵蓋兩條新路由；焦點 151 passed、完整套件 **2,091 passed／1 skipped**（2,056 → 2,091；新增 26 條＋既有調整）；daemon `/health.state_kinds == [ranking, beta, coverage, watches]`。報告 [`2026-09-08-webapp-coverage-watches-b2b.md`](reports/2026-09-08-webapp-coverage-watches-b2b.md)。<br>**B2-positions ✅（2026-09-08）：** 抽出 `collect()` 與三個純函式（`equal_weight_aggregate`／`live_lane_rows`／`anchor_health`），**markdown 與 artifact 讀同一份**（L16）；計數器查證後不需重構——`DecisionStore.capital_expression_counters()` 本來就回傳 dict。`materialize --positions` **只呼叫 collect() 不呼叫 render**，所以不 append 排序快照、不寫聚合檔：唯讀。`#/positions` 的排版順序本身是判準——**真實成交 → 樣本效度 → 計數器 → 聚合 → 逐檔**，反過來排的話有效 n≈1 的觀測會讀起來像 20 個獨立驗證（前端測試斷言這個順序）。⚠ **驗收改用「去數字骨架」而非逐位元組**：今天是交易日，盤中價讓連續兩次執行本來就不同（實測 AXTI +44.2%→+49.7%）；行數 74→74、排序後骨架差異 **0**、`main()` 只剩 collect→render 六行。真實 20 列／真實成交 1 檔（COHR）／只有 paper 18 個；2,088 → **2,110 passed／1 skipped**。報告 [`2026-09-08-webapp-positions-b2p.md`](reports/2026-09-08-webapp-positions-b2p.md)。<br>**Phase 收尾：** APP 六個畫面（單檔判讀／瓶頸排序／資產配置／研究缺口／在等什麼／我的部位）；Daily 模板具名 section **6,030 → 2,671 字元**。**還剩一件**：C 的「≤7K 實測待下一份 brief」（下一份 brief 自己會給答案，不需再開工）。持股 NAV bucket 比例（來自 `decision_lab today`）、52 週長序列（需 ETL 端存序列）、**每檔報價單位登記**（Engine C／policy 都沒有 quote unit 欄位，卡片價格暫標「未登記」不猜）是 beta 的三筆 debt。<br>**A ✅ daily 自動更新 APP（2026-09-08）：** daily 收尾多一步 `-m webapp materialize --tracked --ranking --beta --coverage --watches`，放在 `publish_daily_state.py` 之前。**sandbox impact review 五步全做完**：放行的是 **`materialize` 子命令不是 `webapp` 模組**——`serve` 會綁定本機 port（listener surface，由開機自啟的 vbs 長駐），刻意留在禁止側；`--tracked` 的 ticker 清單由 `engine_b.routine_config.discover_tracked_tickers()` 導出，**與 pq1 drain 同一權威**，不手寫（手寫清單腐壞的方式是「某一檔安靜地不再被 materialize」，不會有東西報錯）。**materialize 失敗是 fail-soft**（只記健康段、不中止 Daily）——它不碰共用檔，代價只是畫面舊一天而且 APP 自己會標 stale；harvest 失敗仍必須中止整輪（那個持有 writer lock）。**驗收：** fixed entry **16 → 17**，`grep webapp` 只有 materialize 一行；artifact **4 → 34 份**（analyst view 4 → 30 檔＋4 種 state），exact command 端到端 **108 秒**、`verify` 34/34；兩個突變證明（整條移除／放寬成整個 `-m webapp`）皆變紅；完整套件 **2,094 passed／1 skipped**（2,091 → 2,094：權限守衛 1＋流程守衛 2）。⚠ **Codex sandbox 的 escalation 只能由下一次 06:30 實跑驗證**——本機跑的是同一個 exact command，但重現不了 Codex 的 workspace-write sandbox。報告 [`2026-09-08-daily-materialize-a.md`](reports/2026-09-08-daily-materialize-a.md)。<br>**C ✅ Daily 收斂成心跳（2026-09-08）：** Daily 不再重印瓶頸排序（含產業分組）／資產配置（目標配置表＋逐檔行情表）／研究缺口／事件監看清單，改成一張四列小表只給計數與「較昨變動」；pq2 四段、pq1 進度、**部位與問責**（尚無 APP 畫面）、賣出側、健康段、建議摘要一格未動。**`AGENTS.md` 兩條 Beta 契約同 commit 改寫並明文寫出廢止**：「逐檔表不得省略」→ 住 `#/beta`、Daily 只印門檻跨越；「兩條相關性警告每天講一次」→ APP 頁尾常駐（**不是放寬**：每次看都看得到，不依賴當天有沒有讀 brief）。新增反向保險：**APP 當天沒被 materialize 時 Daily 必須印出來**，否則「看不到」與「沒發生」同形（L12）。**hooks 重審結論與計畫相反**：原寫「thesis 到期三個嘴 → 一個」，查證後 hook 對已進池項目會靜默、`lifecycle.json` 有 3 條 active 且都有 `next_check`（最近 09-28）——**它會觸發，不是死機制**，且 09-05→09-08 排程停擺那三天只有它會說話，因此**維持 2 個 hook** 並把分工寫進 OPERATIONS。真正刪掉的是 `crons/weekly_scan_digest.py`（自 U7b 起沒掛在任何 hook 上的死碼）與其測試。**驗收：** 模板具名 section **6,030 → 2,569 字元（−57%）**；實際 brief 估算 **~6.9K**（≤7K 目標，**實測待下一份 brief**）；五條合約守衛**改家不刪除**——契約句跟著呈現搬進 beta artifact，燈號改驗產生端 `portfolio/allocation.py`，APP 端防回填改成機械斷言（producer 一個字都不准吐已廢止字彙）；完整套件 **2,088 passed／1 skipped**（2,094 → 2,088：刪掉死碼 `weekly_scan_digest` 的 6 條測試）。報告 [`2026-09-08-daily-heartbeat-c.md`](reports/2026-09-08-daily-heartbeat-c.md)。<br>**C 未完成的一項：** ≤7K 是估算不是實測；「較昨變動」目前只有排序有機器基準（`ranking_order_snapshots.jsonl`），缺口／watch／sleeve 的變動靠 agent 當日比對，若日後常寫不出來，該加 counters 快照而不是把那行拿掉。<br>**白話化＋單檔走勢圖 ✅（2026-09-08）：** 使用者回饋「太多展開、太多字、太多『這不是』，全部是內部術語基本上看不懂」＋「點進去要有股價走勢」。**字彙一個字沒改**——別名住 `briefing/analyst_view/contracts.py`（`PLAIN_PANEL_TITLES`／`PLAIN_LINE_LABELS`／`PLAIN_ABSENCE_SHORT`／`PLAIN_READINESS`），經 `.meta.json` → `/api/v1/meta` 送到前端（`absence_kind`／panel key 是 read model 與 ledger 的身分，L10；先例 `ACCOUNTING_BASIS_DISPLAY`）。**前端那份硬編碼 `ABSENCE_SHORT` 已移除**——它正是 L16 的重造品，字彙一改它就安靜偏離。單檔頁重排成 **結論 → 股價走勢 → 卡在哪 → 最脆弱 → 什麼會推翻它 → 我們 vs 市場 → 一個「完整細節」**；**刪的是版面不是內容**，六個面板 renderer 全在 details 裡（測試斷言），API 一個欄位沒少。走勢圖取 `alpha/providers/close_series` 的 180 個**已收盤**交易日（不含盤中 bar，L12），存進 artifact 的 `price_series` 但**不進 `freshness_identity`**——價格動了不算認知變了；`priceCard` 內禁 `sma`／`rsi`／`macd`／均線／突破（測試掃程式碼），它是脈絡不是訊號。「錨點前／入圖以來／超額」→「起算前 30 天／起算後到現在／同期比 QQQ 多／少」，「結案歸因」→「已結案的判斷」，術語降到說明列。**別名不得宣稱 authority 沒有的東西**：測試禁 `建議`／`推薦`／`安全`／`值得買`／`該買`，「可以買」只准以否定形式出現（`ready` 逐字寫「不是「可以買」的意思」）。**驗收：** 30 檔全有序列（145–180 個交易日）；`plain_absence_short` 與 `absence_kinds` 的 key 集合相等（少一個就會有一格顯示裸 key）；2,110 → **2,114 passed／1 skipped**（新增 4 條守衛）；daemon 重啟後 `/api/v1/stocks/COHR` 帶 180 筆。⚠ **未做 render-and-look**（我看不到瀏覽器，版面好不好讀要使用者開一次才算數）。**第二輪（同日回饋「你只是把東西收乾淨而寫『見展開』」）：** 命中的是真缺陷——`renderRow` 遇到結構化的值就印「見下方展開」，而**底下並沒有那個展開**；「我們和市場差在哪」那張卡的三列全是這種值，**整張卡其實是空的**。修法：`structuredText()` 給 13 種形狀各一句人話，認不得的退回逐格 `鍵：值`（醜但看得到）；該卡改成四欄真表（項目／我們估／市場共識／我們比市場），缺共識的列印缺席徽章＋理由不留白也不寫 0，表下補賣方目標價與預估本益比並標明那是別人的數字。**完整細節從兩層展開變一層**：面板內 `drill(` 歸零、改用 `group()`（有標題直接看得到內容），`renderDetail` 內只剩一個展開。順帶修掉兩個孤兒——`renderHeadline`／`renderReadiness` 在第一版改版後沒人呼叫，「算術 vs 判斷的分解」與「readiness 判準原文」等於安靜消失。另補 `tests/test_webapp_static_js.py`：**前端沒有建置步驟、本機沒有 node**，少一個括號會整頁空白而後端每一項檢查都是綠的（L13 同形訊號）——這支做括號平衡與死函式兩項機械檢查，含誤報測試與突變驗證（尾端多一個 `}` 立刻紅）。2,114 → **2,122 passed／1 skipped**。報告 [`2026-09-08-app-plain-language.md`](reports/2026-09-08-app-plain-language.md)。<br>**B1 non-blocking debt：** registry 只有 4 家有 `display_name`，36 列裡 25 列顯示 `co:*`（mechanical，補 registry 即可，不需 pq2）；每列 disproof 不在本表（在單檔判讀）；Pane 1 的 alpha-card 摘要與純度（市值／analyst_count）欄位未搬。 | 順序是 invariant：**APP 先讀得到，Daily 才能不印**（否則 L13 重演）。B → A → C；B2 之前先讀 `dataviz` skill。 |
| **Agent Development Flow v1（token-aware bootstrap）** ✅ **2026-09-07 交付；2026-09-11 使用者驗收通過** | 開發請求目前沒有統一入口：使用者從「我想做什麼」開始，而 agent 得自己猜要站多遠看問題、要不要開第二個 context。實際成本落在兩處——**跨 App 複製貼上**，以及**沒有判準時預設多開一個 agent**（每次 spawn 都是冷啟動，要重新推導主 agent 已有的 context）。同時 `blind-spot-audit` 的 15 個 lens 有 3 個（B8／B9／C10）整段建立在**隨 U7 移除的資本表達層**與 **Engine D 舊責任**之上——`supported_range`、五軸 ceiling、「D 的 supported range 怎麼回到使用者」——reviewer 會對著不存在的機制開火。⚠ RSI／MACD 這組 beta 訊號字彙**原本就不在該 skill 裡**，測試對它們的禁令是預防不是修補 | ✅ 新增 `docs/AGENT_WORKFLOW.md`（Zoom Z0–Z3／Review R0–R2／八欄 STEP_RESULT／四值 verdict／七種 message／R2 六條 trigger）＋`skills/development-flow/SKILL.md`（intake、routing、development 與 operational 兩個 review profile）；`skills/blind-spot-audit/SKILL.md` 收斂成 research profile（B8／C10 RETIRE、B9 REWRITE、D13–D17 與 C12 MOVE_TO_DEVELOPMENT／OPERATIONAL、A2 REWRITE 成「Convergence without differentiated evidence is healthy」、新增 A6 假設出處／A7 thesis→model／A8 相關性與退出）。<br>**canonical Markdown 3 份（hard max 4）**，機械擋住碎檔重生。<br>驗收：1,989 → **2,002 passed / 1 skipped**；突變 180 → **187 個**（新增七個全部證明會紅，空跑 0）；`audit invariants` FAIL 0｜PASS 12｜SKIPPED 0；`sync_agent_skills.py --check` 無漂移；`.codex/rules` 的 `prefix_rule(` 仍是 16。<br>⚠ **這個 Phase 不得用自己剛建立的流程宣告自己有效**——bootstrap 由既有人工雙邊流程驗收，第一次 dogfood 的結果才是第一份證據。<br>⚠ **2026-09-07 review findings 已修兩處：** ① **R1——不得把整條研究路徑路由到 pq2。** 實際契約是 intake／source-trace／extraction 在 **pq1** 由 routine 自動研究，只有 **exact authority mutation proposal**（prepared RA 入圖、Engine C 判讀寫入、thesis mutation、手動 authority）才鑄 pq2 編號；`substitutability` 的例子同步改寫成「追源與抽取在 pq1，只有最後那筆 graph-write 提案才鑄號」。② **R2——`STEP_RESULT` 實際是八欄不是七欄。** active docs／skill／tests／測試函式名已統一為「八欄」，並補一條**會去數**的斷言（`test_the_declared_step_result_count_matches_the_actual_block`）——原本那個數字是手寫標籤，沒有任何東西量它。歷史 commit 與報告不回頭改寫。<br>**Non-blocking debt（本輪不處理，使用者已裁定）：** blind-spot-audit 的 A8 lens 把「AI 光互連」寫死在文字裡——那是**今天這張圖的集中方向，不是恆真判準**，圖的重心一旦移動，該句會安靜地開始說謊。<br>查證：`python -m pytest tests/test_agent_workflow.py -q`／`python scripts/verify_test_nonvacuity.py --only AgentFlow`／`python scripts/sync_agent_skills.py --check` | — |｜✅ **2026-09-11 驗收（使用者：「可以先收掉」）**：第一次 dogfood 就是 2026-09-11 這一輪（開放 backlog 收斂），三條查證當場重跑：`python -m pytest tests/test_agent_workflow.py -q` **15 passed**；`python scripts/verify_test_nonvacuity.py --only AgentFlow` **7/7 通過、空跑 0**；`python scripts/sync_agent_skills.py --check` 無漂移。⚠ 本列自己寫的「不得用自己剛建立的流程宣告自己有效」仍然成立——收掉它的是**使用者**，不是這個流程。⚠ 本列記的 non-blocking debt（blind-spot-audit 的 A8 lens 把「AI 光互連」寫死在文字裡）**未解**，隨本列結案轉為獨立追蹤。
| ~~**StockBot Web App／API MVP（Phase 2 Step 5）**~~ ✅ **2026-09-07 交付並結案（原判 CONDITIONAL GO → 條件同日解除 → 判定 GO；標的＝COHR／LYC.AX／6324.T／IQE.L）** | Step 3.5 已經把「看懂一檔」排好了，但**唯一的入口是終端機**，而且每看一次就要跑一次完整的鏈（實測 4.2 秒／檔，會連 Neo4j、Engine C 與三本 private ledger）。日常真正會發生的事是「在手機上想起某檔，想看一眼」——那條路徑今天不存在。⚠ 更關鍵的不是速度：**只要 request path 有能力重跑研究，它就有能力改變（或看起來改變）系統對一家公司的認知**，而「使用者剛才看到的是哪一版判斷」就再也答不出來 | ✅ 完整報告 [`docs/reports/2026-09-07-webapp-mvp.md`](reports/2026-09-07-webapp-mvp.md)。**產品 invariant 落地：`LLM changes cognition; APP reads cognition`**——materialize 與 serve 是兩條不同的責任鏈。<br>**新增 `webapp/`（contracts／store／materialize／api／__main__／static）**＋`python -m webapp materialize｜serve｜status｜verify`。**沒有新增任何套件**：`starlette`＋`uvicorn` 早已隨 `mcp>=1.28` 安裝，前端是 vanilla JS、零建置工具鏈、零外部資源。<br>**「點一下不重跑研究」是四種互相獨立的機械證明**（`tests/test_webapp_request_path.py` 25 條）：①import 靜態掃描（serve 端三個模組的 import allowlist）②runtime 模組哨兵（打完一輪 request，`sys.modules` 對 20 個禁止前綴的新增量 **0**——擋函式內延遲 import）③檔案系統快照（request 前後對 artifact 目錄與 `library/private/` 取樹狀雜湊，**完全相等**——同時擋寫入與 cache-miss 自動重建）④socket 封殺（`connect`／`create_connection` 直接 raise，回應照樣完整正確）。⚠ 第 4 條攔的是 `connect` 而不是 socket 建構子——攔建構子會攔到 TestClient 的 event loop 而不是被測物（L15-1），且**先自我量測**證明封殺真的生效。<br>**兩個 digest 刻意分開**（L12）：`content_digest`（整份內容，偵測半份寫入與事後竄改）與 `freshness_identity`（**只含認知狀態**——價格動了不算認知變了）。malformed／版本不符／缺欄位一律 fail closed 回 **503**；⚠ **「artifact 讀不到」（503）與「這檔沒有研究結論」（200＋blocked）不得同形**。stale 照樣回、**絕不重建**。<br>**修掉 Coverage Pilot 留下的兩筆語意債：**①**「刻意不主張」與「還沒寫」不再同形**——新增 `alpha/absence.py`（`absence_kind` 封閉字彙 11 種，與 `status` 正交，**由產生缺席的那段程式自己宣告**，消費端不得 parse 散文）＋ `alpha/abstention/`（append-only；`_assert_no_value_fields` 讓它**結構上不可能攜帶數字**；`reason` 與 `revisit_when` 必填，L7）。6324.T 實測由 `not_yet_recorded` → **`deliberate_abstention`**，而 **fair value 仍 `None`、readiness 仍 `blocked`**——settled 只回答「該不該去補」，不讓判讀變好。②**`accounting_basis` 呈現別名**：字彙一個字不改（它是三本 private ledger 每筆紀錄身分的一部分，L10），`gaap` → 「As reported（法定財報口徑）」，label **一律不含準則名稱**（Lynas／HDS／IQE 在 ledger 裡都寫 `gaap`，authority 沒有「是哪一套準則」那一格）。順帶修掉 `company_label` 一直是 `co:coherent（COHR）`——registry 新增 `display_name`。<br>**四檔實跑：** COHR **281.86 USD → 223.6 @ 2027-06-30／−20.7%（年化 −24.6%）ready**；LYC.AX **15.62 AUD → 9.949／−36.3% ready**；6324.T **5,970 JPY／blocked at valuation（`deliberate_abstention`）**，畫面同時顯示內部 EPS 47.32 與 implied multiple 126.2x、**沒有補任何倍數**；IQE.L **47.518 GBp**（未與 GBP 混為同尺度）／blocked。⚠ **IQE.L 的 blocker 是 `upstream_unavailable` 不是 `method_not_applicable`**：它刻意沒有 operating assumption（pq2 [486] 未做），內部 EPS 本身就缺席，鏈**在方法適用性被檢查之前就停住**；標成「方法不適用」會是一個系統今天推導不出來的結論（L11-2）。<br>**量測（L14）：** materialize **4.20–4.31 秒／檔** vs API 讀取 **1.4–17 毫秒**（單檔明細 4.1–8.4 ms、清單 16.5 ms），**差三個數量級**；artifact 90–189 KB、API 回應 11.8 KB（清單）／65–139 KB（明細）。**不做進一步優化**。<br>**Baseline：COHR 核心 10 格一格未動**（8.9441／9.4163／Q4 weak 0.25 current／223.6034／281.86／2027-06-30／−20.67%／−24.64%／entry missing 不進 blockers／ready；`judged_context_matches` True）。<br>**驗收：** 1,884 → **1,989 passed／1 skipped**；突變 163 → **180、空跑 0**（⚠ 其中一條原本是空跑——「錯誤回應吐出內部訊息」只走 `HTTPException` 處理器，catch-all 那段沒有任何測試看得到；補一條專門觸發未預期例外的測試後才通過。L14 對我自己寫的守衛同樣適用）；`audit invariants` **FAIL 0｜PASS 12｜SKIPPED 0**；golden **漂移 1（EXPECTED_CHANGE：registry 多了 `display_name`，diff 只有一行）**；既有測試 **2 處**調整（兩條 import allowlist 放行 `alpha.absence`，**同時補上「那一支自己也必須零相依」的斷言**，L15）。<br>**⚠ 原判 CONDITIONAL，條件已於同日解除 → GO**：使用者已完成 Cloudflare 三步，`https://stockbot.minatoyukina.uk` **未登入回 302 導向 Access 登入頁、登入後四檔清單可開**；既有 `mcp.`（無 token 回 404）與 `neo4j.` 未受影響；`config.yml` 只多一條 ingress、catch-all 仍在最後、備份存在。**Google OAuth 實際上也已生效**——Access event analytics 顯示 `Identity provider = google`、`Users = c3035281@gmail.com`、`Granted 1`。⚠ **不是照文件的步驟 0 手動設的**：新版 Cloudflare One 在建立 Access 應用程式的流程中已自動把 Google 加成 identity provider，步驟 0 因此變成「沒被自動加上時才要做」。**開機自啟也已完成**（`stockbotv2-graph-services.vbs` 檔尾追加 `serve`，抽成獨立 vbs 實跑驗證：APP 起得來、無可見視窗、`Get-NetTCPConnection` 顯示只綁 `127.0.0.1`；備份 ＋ diff 確認除檔尾 6 行外未動）。⚠ **Cloudflare 已把 Zero Trust 主控台改名 Cloudflare One 並重排側欄**，交付當下寫的是舊版路徑；實地確認後的新路徑已回填文件（Access 應用程式＝`Access controls → Applications`；身分識別提供者＝**`Integrations → Identity providers`**，不再在 Settings 底下）。**步驟 1 必須排在 2 之前**（DNS 一建立就開始解析，那時若無 Access policy，網址是公開可讀的）——這也是刻意不代為建立 DNS 的理由，實際執行時使用者也照這個順序走。程序、驗收指令與安全邊界見 [`deploy/cloudflare/README.md`](../deploy/cloudflare/README.md)。既有 tunnel／`neo4j`／`mcp` 兩條 ingress **一個字都沒動**。<br>**⚠ 目前只 materialize 了四檔**：要看別的公司必須先跑一次 materialize，APP **不會**替你補。全 cohort 的 materialize 排程需先走 sandbox impact review，本 Step 刻意不做。<br>**刻意不做：** runtime chatbot／LLM、broker、買賣、部位尺寸、Portfolio 排序、FY2028 model、Valuation v2、Post-MVP Alpha Edge A–H 任一項、[486] IQE going-concern、全 cohort 覆蓋擴充、原生 iOS／Android app。 | ✅<br>**結案（§16）誠實邊界三條：** ①**Access policy 的內容由使用者目視確認，未經負面案例測試**——analytics 的「1 user granted」只證明**你進得來**，不證明別人進不來（L14 的形狀）；②**Session Duration 是否兩層都設 1 個月未確認**，症狀要過幾天才出現而且容易怪錯地方；③**只有四檔有 artifact**，其他公司要看必須先手動 materialize——APP 不會替你補。 | ✅ |
| ~~**Coverage Pilot（2–3 檔 heterogeneous）**~~ ✅ **2026-09-07 交付（判定 GO；標的＝LYC.AX／6324.T／IQE.L）** | 到 Step 4 為止整條鏈只在 COHR 一檔上跑過，所有「fail closed」都是在**同一組資料形狀**上驗的。要買的是「**第二檔資料不齊時，第一檔不會被污染**」在真資料上成立 | ✅ 完整報告 [`docs/reports/2026-09-07-coverage-pilot.md`](reports/2026-09-07-coverage-pilot.md)。**結果：1 檔 ready、1 檔 blocked at valuation（刻意）、1 檔 blocked at model applicability（基期已建立、共識已對帳，停在方法層）**——成功的定義本來就不是三檔全綠。<br>**揪出 6 個 generic 缺陷，全部在 COHR 上不可能出現，其中 4 個會產生看起來確定、實際錯誤的數字：**①**報價單位判準折疊大小寫**——`units_comparable` 寫 `a.upper()!=b.upper()`，於是 `GBp`（便士）與 `GBP`（英鎊）判為同尺度，而**那正是這道 gate 唯一要擋的 case**（`GBX`／`ILA`／`ZAc` 只是字母剛好不同才被偶然攔下）；實測 fair value 0.05 GBP 對現價 47.518 GBp 被判 comparable、relative_gap −99.9%。修法改走 `identity/currency.py` 這個唯一 registry（L16）。⚠ golden fixture `minor_unit_quotes` 守的是 registry 本身**不是消費端**，所以一直是綠的。②**本益比法套在虧損公司上**——負 EPS × 倍數 ＝ 負 fair value、gap 判 comparable、relative_gap **−100.8%**（做多不可能跌超過 100%）；新增 `method_applicability()`，把「缺料」與「方法不適用」分成兩種語意（L12）。③**共識營收從未與基期對帳**——6324.T 實測同一批 yfinance 共識裡 EPS 的 `year_ago_actual` 是**連結**、營收的是**單體**，差 **−43.9%**；新增 `reconcile_consensus_base()` 與比較狀態 `unreconciled_base`。④**`implied_multiple_at_price` 被綁在 fair value 存在才算**——它是純算術，而「沒有目標倍數」正是最需要看它的時候（6324.T：**126.2x**）。⑤**相關性警語無條件斷言每一檔都屬於 AI 光互連**——對 LYC.AX（稀土）與 6324.T（減速機）是假話；修法是把主詞放回「這份圖的組成」，**不是按產業條件化**（那需要一份系統沒有的分類 SSOT，L16）。⑥**口徑判定的絕對容忍是美元級校準**——`abs_tol=0.011` 在**便士級** EPS 上讓 IQE.L 的 GAAP −0.0377 與 adjusted −0.0282（只差 0.0095）一起命中而判成 `unverified`，而 provider 的 `year_ago_actual` 其實**精確等於** adjusted；改為純相對容忍（L15-1：gate 攔下的不是它想攔的東西）。<br>**量測（L14）：** 新 gate 逐一實跑——共識對帳 4 檔中攔下 **1** 檔；implied multiple 3 檔中新增 **1** 檔；口徑容忍 4 檔中改變 **1** 檔（且改成可證明正確的答案）。不是恆亮的牆，也不是不會響的閘門。<br>**Baseline：COHR 核心 10 格 ＋ context 一致性一格未動**（8.9441／9.4163／Q4 weak 0.25／223.6034／281.86／2027-06-30／−20.67%／−24.64%／entry missing 不進 blockers／ready）。<br>**驗收：** 1,860 → **1,884 passed／1 skipped**；突變 156 → **163、空跑 0**；`audit invariants` **FAIL 0｜PASS 12｜SKIPPED 0**；golden **漂移 1（EXPECTED_CHANGE：manual observation 116 → 120）**，已重新擷取。<br>⚠ **本次自己犯過一次 L11-4 並修正**：第一版判定「IQE.L 沒有年報」（官方 IR 頁 JS 動態載入、`href="*.pdf"` 0 命中），但**年報直接 URL 早就在自家庫裡**——修正後基期建立完成，並因此揪出第 6 個缺陷。<br>⚠ **執行環境：** golden 必須用 `.venv/Scripts/python.exe` 跑——全域 python 缺 `google-auth`，`multi_account_holding` 會誤報漂移（把環境問題讀成回歸，L13-2）。<br>**刻意不做：** APP／API、Portfolio、Post-MVP A–H、任何新的 valuation method、graph admission、thesis／variant view 變更。 | ✅ |
| ~~**Full-chain Adversarial Acceptance（Phase 2 Step 4）**~~ ✅ **2026-09-07 交付（判定 GO；標的＝COHR）** | Step 0–3.5 每一層各自有測試，但**沒有任何東西驗過「故意把整條鏈弄壞時它會 fail closed，而不是產出看起來合理但語意錯誤的結果」**。而 Q4 重判當天已經實際踩到一次：兩個代理（`estimate_revision_30d +68.3%`、`market_implied_eps_growth +238.7%`）看起來完全正常，實際是會計年度換檔假象——**當時是靠人在 rationale 裡用散文擋下來的** | ✅ **新增 `tests/test_full_chain_acceptance.py`（35 條整合測試）**＋完整矩陣報告 [`docs/reports/2026-09-07-full-chain-adversarial-acceptance.md`](reports/2026-09-07-full-chain-adversarial-acceptance.md)（22 個 scenario：refresh 依賴 10、PIT 5、missing／identity 8、語意陷阱 6、黑箱 4）。**不建第二份 dependency graph**——全部用既有 `assumption_ids`／`evidence_refs`／refresh policy 表斷言；每一列都同時斷言 `expected_affected` **與** `expected_unaffected`（少了後者，一個把所有東西都標 review_required 的實作會全綠）。<br>**修掉 3 個真 bug：** ①**forward-year rollover 污染**（correctness）——`revision_over` 比較窗口兩端的 forward EPS 卻沒確認兩端是同一個會計年度；COHR 2026-08-13 換尺讓導出 EPS 一天跳 **+62.3%**（8.21→13.59），30 觀測窗口跨過它就報成「分析師上修 +68.3%」。修法是 fail closed：身分由當日 `consensus_estimates` 的估計值**反查**（值相等，容差 1e-3），兩端不同或不明一律 `comparable=False`、`eps_change=None`（不是 0）＋逐字理由；**刻意不用幅度門檻猜**（那本身是未量測的 gate，L14）。實測 **+68.3% → not_comparable**。②**頭條「需要重看：無」是全域措辭配局部範圍**——`--scenario fiscal_rollover` 下畫面上方寫 `overall=review_required`、頭條寫「無」，兩句互相否定（L12）；`AnalystPanel` 補 `attention_scope`／`attention_total`，現在寫「本節之外另有 N 項」。③**備份計數器只報年齡不報覆蓋**——「3 天前備份」看起來健康，而 `fair value 223.60` 的唯一來源（三本 private ledger，09-05／09-06 建立）**不在那份備份裡**；`load_backup_status` 補 `unbacked_files`，首屏紅字寫出（實測 **17 個**）。<br>**`GateDiscrimination` 落地（SKIPPED 1 → 0）：** 不是 invent gate——資料源是 Decision Store 的 `coverage_assessments`（**269 份帶時戳評估、41 cohort、63 個具名 blocker**）。觸發率分母＝全部 assessment；清除率分母＝**曾響過且之後還有過評估**的 cohort（第一版用「曾響過」當分母，把 `identity_unresolved` 誤判成 0/9 不會滅——實測那 9 個有 6 個只被評估過一次，根本沒有清除機會，L13-2）。實跑 **PASS**：63 個量過觸發率、24 個樣本夠也量得出清除率，無恆亮（最高 65.8%）無不會滅（最低 33.3%），39 個 `insufficient_data` **不當作通過也不當作失敗**；`judged==0` 時回 **SKIPPED 不是 PASS**。<br>**Evidence coverage 邊界結論：** 是 deliberate boundary（context 先建、模型層後擴充，反向會循環），但**以前沒說出來，等於 coverage hole**。**不放寬 resolver**；改為在 packet 宣告 `evidence_scope`（涵蓋什麼／不涵蓋什麼／不涵蓋的住哪），並把拒絕訊息拆成「ref 不存在」與「ref 存在但不屬於這一層」兩種語意。`AlphaInvestmentView` §4.6 早就列得出共識與人工觀測的 ref——**同一批 EvidenceRef 物件的 superset 擴充，不是第二份 evidence authority**。<br>**Baseline：** 核心 10 格**一格未動**（8.9441／9.4163／Q4 0.25 current／223.6034／281.86／2027-06-30／−20.67%／−24.64%／entry missing 不進 blockers／ready）。兩處漂移已 trace：`judged_context_matches` True→False（Q4 原料描述進 digest，判斷需 re-anchor——A3 人工 gate，本輪不動）；refresh 成果 30→29（`estimate_revision_vs_price` 現在不存在，改在 consensus section 以 `not_applicable`＋理由現形）。<br>**驗收：** 1,809 → **1,860 passed／1 skipped**；突變 148 → **156、空跑 0**（並修掉一條因本 Step 而變空跑的舊突變）；`audit invariants` **FAIL 0｜PASS 12｜SKIPPED 0**；golden 漂移 **0**。<br>⚠ **原判 CONDITIONAL GO，條件已於同日解除 → GO**：使用者指示後已跑 `backup_private.py run --no-drive`（`20260907T070519Z`，524 檔；`alpha/` 由 4 檔變 **7 檔**，三本 ledger 全部收錄；`unbacked_files` **17 → 0**），並以 pq2 **[483] go** 完成判斷檔 re-anchor（`_packet_digest` 換錨點＋`_restated` 新增第 3 條；逐鍵驗證只動這兩格，Q4 仍 weak 0.25 current，`judged_context_matches` 回到 True）。**剩下的缺口是異地副本**：`drive: skipped`，需要一次瀏覽器 OAuth（`backup_private.py auth`）；另 `restore_verification` 指向已被輪出的備份，跑 `verify-restore` 可清掉。<br>**刻意不做：** 新金融模型、onboarding、改 thesis／variant view、FY2028 model、APP／API、Portfolio、Post-MVP A–H。 | ✅ |
| ~~**Analyst Consumer v1（Phase 2 Step 3.5）**~~ ✅ **2026-09-07 交付（標的＝COHR）** | Step 1–3 把數字都做出來了，但它們**依資料結構排列**：估值第 13 節、報酬 13a、共識第 5 節、假設第 8 節、refresh 第 15 節。使用者打開一檔股票要在很短時間內看懂的五件事（我們相信什麼／跟市場差在哪／怎麼算到這裡／最弱假設是什麼／什麼會讓結論需要重看）**橫跨上述所有 section**，沒有任何一個入口把它們依閱讀順序排好；而且 `alpha-card` 完整卡是 229KB、16 節，不是拿來「看懂一檔」的東西 | ✅ **產品決策先記下（見上方「主流程的終點是 Implied Return」）：** 主流程 `Evidence → Internal Forecast → Valuation／Future Target Value → Horizon → Implied Return`，**終點是 implied return**；`EntryCriterion` 是 optional analytical capability，**不是必填、不是 research completeness gate**；不要求使用者宣告固定 10%／15%／20%；opportunity-cost／risk-adjusted hurdle／portfolio comparison 留給 Portfolio／Investor Policy 階段。已寫進 ROADMAP＋`ARCHITECTURE.md` §6.7＋`CONCEPTS.md`（Analyst View／Core Readiness／Optional Analytical Capability 三條）＋`OPERATIONS.md`＋`skills/alpha-status`。<br>**新增：** `briefing/analyst_view/`（contracts／compose／render）＋`python -m briefing analyst-view <T> [--as-of] [--scenario] [--sandbox-hurdle] [--format markdown\|json] [-o]`。四個核心 panel（headline／fundamental／why／research）＋一個 optional panel（entry），各自宣告自己回答哪幾個消費者問句（`QUESTIONS` 六問）。<br>**「不重算」是型別層事實不是自律：** `AnalystLine` 刻意**沒有 `value` 欄位**，每一行的 `datum` 都是 `AlphaInvestmentView` 裡**同一個物件的參照**；測試用 `id()` 集合逐行比對——compose 只要自己 `Datum(...)` 生一格（值一模一樣也算）就會紅。三支檔案另有 import allowlist、**float 字面值 0 個**、「不得 import 任何 `alpha.*` 模型模組」三道掃描。唯一被允許的數值動作是**排序**（既有敏感度依 `abs(fair_value_relative)`），它不產生新值。<br>**「最脆弱的輸入」是宣告好的列入規則，不是新判斷：** `WEAK_INPUT_RULES` 封閉字彙六條（`largest_modeled_sensitivity`／`refresh_flagged`／`heuristic_proxy_input`／`session_judgment_input`／`weakest_known_axis`／`unknown_axis`），每條 `WeakInput` 必須說出自己被哪一條列進來。**頭條那句話也不是 consumer 造的**——直接取 `implied_return.epistemics.one_sentence` 並註明出處。<br>**實跑 COHR（2026-09-07）：** 頭條 **281.86（bar 2026-09-04）→ 223.60 @ 2027-06-30 → −20.7% simple／−24.6% 年化**；readiness **`ready_with_flags`**，flag 只有一條（`fundamental：review_required`，來源是 Q4 需重判），`blockers=[]`。**entry ledger 0 筆 → `Not set (optional)`**，只出現在 `optional_unavailable=['entry：missing']`，**core readiness 一格不動**（測試逐欄比對「有判準 vs 沒判準」兩份投影）。以 `--sandbox-hurdle 0.15` 驗算：門檻價 **199.43**、gap **+41.3%**、`above_analytical_entry`、`assessment=clean`，**readiness 仍然完全相同**。<br>**PIT：** `--as-of 2026-09-05` → 估值假設尚未寫下 → headline `missing`、readiness **`blocked`**、`fair_value`／`price_return` 皆 **null**（現價仍在）；`--as-of 2026-08-15` → 無基期 → `blocked`，現價顯示 **325.83**（真實歷史價，不是今天的 281.86）。<br>**驗收：** 1,785 → **1,809 passed**（＋1 skipped，既有；新增 24 條全在 `tests/test_analyst_view.py`）；突變 **140 → 148、空跑 0**（⚠ 舊列寫「130 → 141」，實測 HEAD 的 `MUTATIONS` 是 **140**，此處以實測為準）；`audit invariants` FAIL 0｜PASS 11｜SKIPPED 1；golden 漂移 **0**；**既有測試 0 處需要調整**（純新增；只在 `briefing/alpha_view/render.py` 加了四個公開別名讓 consumer 重用「缺席怎麼印」，不改行為）。查證：`python -m briefing analyst-view COHR --format json \| python -c "import json,sys;v=json.load(sys.stdin);h={l['key']:l['datum']['value'] for l in v['headline']['lines']};print(v['readiness']['state'], v['readiness']['blockers'], v['entry']['status'], h['price_return'], h['annualized_price_return'])"`<br>**刻意不做：** Step 4 全鏈對抗式驗收、APP／API、Portfolio、Post-MVP A–H 任一項、任何新的 forecast／valuation／return 公式、為 consumer 新建 attribution model、runtime LLM、跨標的比較與排序。 | ✅ |
| ~~**Entry Logic v1（Phase 2 Step 3）**~~ ✅ **2026-09-06 交付（標的＝COHR）** | Step 2 給出「年化隱含 −24.6%」，但答不出使用者真正會問的下一句：**「那要跌到多少我才有興趣？」** read model 的 `entry_logic` 整段是 `not_modeled`（`required_return`／`entry_price`／`wait_for_price_threshold`／`actionable_now` 四個空格），而且 required return 這個東西當時**沒有任何 authority 擁有它** | ✅ **Audit 先行（authority 盤點）：** `required return／hurdle` **不是公司事實**（A1／A2 不擁有）、**不是研究對公司的信念**（A3 的內容不含它）、**也不是資本決策**（A5 是「當時憑什麼決定、使用者選了什麼」，append-only）——它回答「**我的資本**要求多少報酬」，主詞是投資人。結論：新增第三種知識種類 **`investor_policy`**，與 A2 現價同一種地位（**注入的輸入**：只讀、不猜、不補預設、不自動改），**不把 capital permission 偷塞進 Alpha**。<br>**新增：** `alpha/entry/`（contracts／criteria／model，純邏輯零外部相依）：`EntryCriterion`（`ec_*`，private append-only ledger `library/private/alpha/entry_criteria/<T>.jsonl`；`basis` 封閉字彙**只有** `investor_policy`；**刻意沒有 `period`**——要求報酬不隨估值換年度而失效；**刻意不共用 `select_assumptions`**：那支以會計期間為身分並要求證據解析到公司 evidence index，而投資人的機會成本本來就不在那裡）＋`EntryAssessmentResult`。公式（唯一定義處 `alpha/entry/contracts.py`）：`entry_price = fair_value / (1 + annualized_hurdle) ** (holding_period_days / 365.25)`；`price_to_entry_gap = current_price / entry_price − 1`；`comparison = current_price <= entry_price ? meets_analytical_hurdle : above_analytical_entry`。＋`alpha/providers/entry_criteria.py`＋`python -m alpha entry-criterion <T> --list／--add／--retract`＋`python -m briefing entry <T> [--sandbox-hurdle] [--scenario …] [--as-of]`＋read model 第 13c 節 `entry_logic`（原 `NotModeledSection` 插座換成 `EntryLogicSection`；capability `analytical_entry_threshold_v1`）／精簡卡 `entry_logic` 欄。<br>**型別層強制的授權邊界：** `EntryCriterion`／`EntryAssessmentResult` 在 **import 當下**被 `_assert_no_capital_fields` 掃描（`FORBIDDEN_POSITION_TOKENS` ＋ buy／sell／order／action／trade／permission）——長出那種欄位是 import 失敗不是 lint 警告；`meets_analytical_hurdle` 刻意帶 `analytical` 字樣，只表示「現價 ≤ 門檻價」。<br>**alignment 不對齊不得冒充 clean：** `aligned`／`spot`（明示）→ `assessment=clean`；`horizon_before/after_value_date` → 算術照列但 `review_required`＋理由，型別層擋住「不對齊卻標 clean」。<br>**Refresh 整合：** change class `entry_criterion`（Q1–Q5、假設層、fair value、implied return 一格不動）；artifact `entry_criterion`（判斷型，`refs` 為空——判準沒有 supporting evidence，結構事件／共識／指引都不動它）／`entry_assessment`（確定性；policy `{market_price, financial_actual}`）。<br>**實跑 COHR（2026-09-06）：** ledger **0 筆判準** → `entry_logic` 誠實 `missing`，理由逐字寫「**缺投資門檻判斷**（entry criterion）……**這不是資料 ETL 缺口**，是使用者尚未寫下『要求多少年化報酬』；不補 10%／15%／20%」；**上游照抄值仍看得見**（fair value 223.60、value_date 2027-06-30、horizon 2026-09-04→2027-06-30／299 天、年化隱含 −24.6%）。以 `--sandbox-hurdle 0.15` **非持久**驗算（`author=sandbox`，ledger 拒收、`persisted=false`）：門檻價 **199.43**、現價 281.86、gap **+41.3%**（+82.43）、`above_analytical_entry`、`assessment=clean`。情境：price_only→entry_assessment `recalculate`、判準 `current`；graph_edge／consensus_revision／new_actual／disproof→上游 review 傳播成 `review_required`，**判準始終 `current`**（投資人政策不因公司證據動搖）。PIT：`--as-of 2026-09-05`／`2026-08-15` 上游缺→missing；`--as-of 2026-09-06` available。<br>**驗收：** 1,769 → **1,785 passed**（＋1 skipped，既有）；突變 130 → **141、空跑 0**；`audit invariants` FAIL 0｜PASS 11｜SKIPPED 1；golden 漂移 0；既有測試 4 處依 intended 語意調整（`entry_logic` not_modeled→missing、卡表 not_modeled 清單、renderer 節名）。⚠ **突變實測抓到兩件真事**：①「criterion 放進 `assumption_ids` 的傳播路徑」砍掉後測試仍綠——判準沒有 supporting evidence，第二輪傳播對它**今天走不到**，所以**不留那條測不到的守衛**，改寫進 `artifacts_from_entry` 註解（L14 同樣適用於我自己寫的守衛）；②有一條測試呼叫 ledger 寫入時沒指定 `directory=`，突變把守衛關掉後**真的把一筆 sandbox 判準寫進 private ledger**——已刪除該行並要求測試一律指定 tmp 目錄（測試不得有能力污染 authority）。查證：`python -m briefing entry COHR --format json \| python -c "import json,sys;r=json.load(sys.stdin);print(r['meta']['status'], r['entry_price']['value'], r['criterion']['status'])"`<br>**刻意不做：** buy／sell／sizing／portfolio／capital permission；Step 3.5 Analyst Consumer；APP／API；Valuation v2；下方 Post-MVP A–H 一項都沒實作。 | ✅ |
| ~~**Base-case Implied Return v1（Phase 2 Step 2）**~~ ✅ **2026-09-06 交付（標的＝COHR）** | Step 1 只給「fair value 與現價差 −20.7%」，沒有時間：v1 估值契約只有 `target_period`（EPS 屬於哪一年）與 `as_of`（知識視角），答不出「223.60 是今天的 fair value（A）還是某個未來日期的 target value（B）」——兩種讀法算術相同、報酬語意不同；read model 的 `expected_return` 全是 `not_modeled` | ✅ **Audit 先行：** v1 契約**答不出** A／B → 最小擴充：`ValuationAssumption.value_date_convention`（封閉字彙 `spot`／`target_period_end`，**沒有預設**；舊紀錄讀成 unspecified，content-addressed id 不變）＋`ValuationResult.value_date`／`value_date_semantics`（`VALUE_DATE_FORMULA` 唯一定義處）。fair value 算術不變；**時點未宣告時報酬層拒算**。<br>**新增：** `alpha/implied_return/`（contracts／assumptions／model，純邏輯零外部相依）：`HorizonAssumption`（`ha_*`，private append-only ledger `library/private/alpha/horizon/<T>.jsonl`，與營運／估值假設**同一套** epistemic system 與**同一支選取器**；`horizon_end` 是明示日期，寫下時已過去的 horizon 契約拒收）＋`ImpliedReturnResult`（現價／`price_as_of`／fair value／`value_date`／horizon 起迄／持有天數／`price_return`／`annualized_price_return`／`total_return_status`＝**not_modeled**／`return_convention`／trace／`input_dependency`；型別裡**沒有** probability-weighted 欄位）＋`alpha/providers/horizon_assumptions.py`＋`python -m alpha horizon <T> --list／--add／--retract`＋`python -m briefing implied-return <T> [--scenario …] [--as-of]`＋read model 第 13a 節 **`implied_return`**（原 `expected_return` 插座改名；capability `base_case_implied_return_v1`）／估值 section 多 `value_date` 格／精簡卡 `implied_return` 欄。公式（唯一定義處 `alpha/implied_return/contracts.py`）：`price_return = fair_value / current_price − 1`；`annualized = (1 + price_return) ** (365.25 / days) − 1`；`days = horizon_end − bar_date`。四個輸入缺一就 missing，**不補 12 個月**。<br>**Refresh 整合（沿用 alpha/refresh）：** change class `horizon_assumption`（Q1–Q5 與假設層一格不動）；artifact `horizon_assumption`（判斷型，帶 `expires_at=horizon_end`——到期即 stale，INV-2）／`implied_return`（確定性；policy `{market_price, financial_actual}`）；傳播上游泛化到 `oa_*`／`va_*`／`ha_*`。<br>**實跑 COHR（2026-09-06）：** `va_07dcbc388c814a91` supersede `va_30800fc2323efd6b`（25x 不變，宣告 **`target_period_end`**：223.60 是 **2027-06-30** 的值——25x 是「FY27 幾乎入帳時市場對那一年 EPS 願付的倍數」，今天的 29.9x 只是 calibration；讀成 spot 會變成「今天就該跌到 223.60」的即時錯價主張）；`ha_586fe0ff9658a2d4`＝FY27 fair value 於 **2027-06-30** 前實現（session_judgment；與 value_date 對齊，早於它是「提前定價」、晚於它報酬被稀釋，兩者都沒證據）。結果：**從 2026-09-04（現價 281.86，bar）到 2027-06-30，299 天，simple −20.7%、年化 −24.6%**；total return `not_modeled`；`input_dependency=session_judgment`。情境：price_only→只有 implied_return recalculate；graph_edge→估值假設 review→fair value／implied_return review（horizon current）；consensus_revision→估值＋horizon（calibration 命中）review；new_actual→heuristic 營運假設 review→傳播；disproof→review。PIT：`--as-of 2026-09-05` 估值假設與 horizon 皆 created_after_as_of→missing，JSON 無 `ha_*`；`--as-of 2026-09-06` available；`--as-of 2026-08-15` 無基期→missing。<br>**驗收：** 1,753 → **1,769 passed**（＋1 skipped，既有）；突變 122 → **130、空跑 0**；`audit invariants` FAIL 0；golden 漂移 0；既有測試 7 處依 intended 語意調整（`expected_return` 插座→`implied_return`、沒資料 not_modeled→missing、估值 `value_date` 格）。查證：`python -m briefing implied-return COHR --format json \| python -c "import json,sys;r=json.load(sys.stdin);print(r['meta']['status'], r['price_return']['value'], r['annualized_price_return']['value'], r['horizon_window']['value'])"`<br>**刻意不做：** Entry Logic／buy-sell／portfolio／consumer UI／機率加權情境／Valuation v2（historical normalized multiple、peer multiple、growth durability、margin／ROIC quality、cycle position——**backlog 不遺失**，25x 仍是 session_judgment）；total return（無股利預測）；跨標的比較；只有 COHR 一檔有 horizon 判斷。 | ✅ |
| ~~**Valuation Model v1（Phase 2 Step 1）**~~ ✅ **2026-09-06 交付（標的＝COHR）** | read model 的 `expected_return`／`scenarios.target_valuation`／`internal_vs_price_implied` 全是 `not_modeled`；內部 EPS 算得出來卻沒有任何「值多少」的確定性表達；估值判斷若存在也只在 session 口頭，沒有 provenance／as-of／supersede | ✅ **Audit 先行（用資料證明）：** 內部可靠的 forward metric 只有 FY2027 non-GAAP 稀釋 EPS（橋 v1 無 FCF／D&A／EBITDA，snapshot 的 debt／cash 無會計年度身分）→ EV/EBITDA、DCF、reverse DCF **沒有資料可餵，不硬做**；v1 唯一 method＝`forward_earnings_multiple`。<br>**新增：** `alpha/valuation/`（contracts／assumptions／model，純邏輯零外部相依）＋`alpha/providers/valuation_assumptions.py`（private append-only JSONL，`library/private/alpha/valuation/<T>.jsonl`）＋`python -m alpha valuation <T> --list／--add／--retract`＋`python -m briefing valuation <T> [--scenario …] [--as-of]`＋read model 第 13 節 `valuation`／精簡卡 `valuation` 欄／daily brief 卡表「Fair value vs 現價」欄。`ValuationAssumption` 與 `OperatingAssumption` 同一套 epistemic system（同 basis 字彙、同 ref 角色、同 append-only／as-of／supersede，**同一支選取器**）；id 前綴 `va_`；`accounting_basis` 必填 gaap／non_gaap；同期共識與市場倍數只能是 calibration（契約拒收 supporting）。**沒有 hidden default**：ledger 沒生效倍數就 missing。<br>**Refresh 整合（沿用 alpha/refresh）：** change class `valuation_assumption`（Q1–Q5 一格不動）；artifact `valuation_assumption`（判斷型）／`fair_value`／`fair_value_gap`（確定性、**分開**：fair value 的依賴不含現價 ref、policy 無 market_price）；傳播上游可為 `oa_*` 或 `va_*`。<br>**實跑 COHR（2026-09-06）：** 估值假設 `va_30800fc2323efd6b`＝FY2027 non-GAAP 目標本益比 **25x**（session_judgment；supporting＝FY26 8-K 實際值、Q1 FY27 指引、NVIDIA supplies_to 邊、InP 6 吋 fab depends_on 邊；calibration＝snapshot forward/trailing PE、FY27／FY28 EPS 共識；review_condition Q2 FY27 D&C <1.85B → review）。內部 FY27 non-GAAP EPS **8.94** × 25 ＝ fair value **223.60 USD**；現價 **281.86**（bar 09-04）；gap **−58.26／−20.7%**；價格隱含（對內部 EPS）倍數 **31.5x**（市場對 FY27 共識 9.42 付 29.9x、對 FY28 共識 13.96 付 20.2x）。認識論：算術＝橋＋乘法＋基期實際值；判斷＝7 條營運假設（3 session＋4 heuristic）＋1 條估值假設；**給定內部 EPS，整個 gap ＝ 25/31.5 − 1**。情境：price_only→fair value current／gap recalculate；consensus_revision→估值假設（calibration 命中）review_required→fair value／gap review_required；graph_edge→營運＋估值假設 review→傳播現形；new_actual→heuristic 營運假設 review→fair value review；disproof→D&C 條件觸發→review。PIT：`--as-of 2026-09-05` 估值假設 created_after_as_of→missing（EPS 在）；`--as-of 2026-08-15` 無基期→missing，JSON 無 `va_*`。<br>**驗收：** 1,739 → **1,753 passed**；突變 116 → **122、空跑 0**；`audit invariants` FAIL 0｜PASS 11｜SKIPPED 1；golden 漂移 0；既有測試 2 條依 intended 語意調整（`scenarios.target_valuation` not_modeled→missing；卡表 11→12 欄）。查證：`python -m briefing valuation COHR --format json \| python -c "import json,sys;v=json.load(sys.stdin);print(v['meta']['status'], v['fair_value']['value'], v['fair_value_gap']['value'])"`<br>**刻意不做（Step 2 以後）：** expected return／horizon／entry logic／buy-sell／portfolio／機率加權情境／逐情境目標估值／多 method（EV/EBITDA、DCF 要先有內部現金流）／跨標的比較；**沒有內部 FY28 view** 是 COHR 這條 25x 判斷最大的弱點（rationale 已明說）；只有 COHR 一檔有估值假設。 | ✅ |
| ~~**Alpha Investment Read Model（canonical Alpha Card backend contract）**~~ ✅ **2026-09-05 交付** | 散在 Graph／AlphaSignal／ResearchContext／Engine C／catalyst／disproof／briefing 裡的資訊沒有一份 presentation-independent 的結構化表示；不同種類的知識（deterministic／proxy／session 判斷／尚未建模）在純文字裡看起來同樣可信 | ✅ `briefing/alpha_view/`（contracts／builder／render／sources）＋`python -m briefing alpha-card <TICKER>`＋`decision_lab today` 的「Alpha Card 摘要」區。**實測 COHR：** 16 個 section 的 `capability_map` 為 available 4／partial 3／stale 4（判斷是 09-04 對舊 context 做的，現形而非藏起）／**not_modeled 5**（internal_fundamentals／earnings_bridge／expected_return／downside／entry_logic）。`Datum` 型別層強制 Missing != Zero；renderer 只依賴 contracts＋`shared.markdown`（import 掃描守著）。新增 3 個測試檔 45 條、突變 +10。順手修一條正確性：`alpha.context._implied_valuation` 對非正 PE 不再算出 −240% 這種數（與 `alpha_expectation_gap.py` 同判準）。<br>**同日審計後修正（CONDITIONAL GO → GO 的三個擋門項）：** ① as-of 視角下 Decision Store／thesis／watch_state 沒有時點投影，builder 一律標 `not_applicable`（INV-6；先前會拿 08-31 的 coverage 與 12-01 的檢核點冒充 06-30 的知識）；② 精簡卡 `condition_count`／`structured_count` 在無判斷時為 `null` 不是 0，renderer 的 `has_signal` 分支移除；③ 人填的 thesis 檢核點與 status 由 `deterministic` 改 `observation`。不擋門但一併做：Decision Store 改 `mode=ro` 連線＋Engine D 自己的 `company_decision_facts`（cohort 選擇規則寫進回傳值）、五檔精簡卡共用一個 provider、移除 view 自算的 `target_vs_price`（已在 `alpha_expectation_gap.py`）、`sole_source=False` 標註上游 `bool()` 壓平、拿掉永遠是 None 的 `attention` 欄位。<br>**Phase 1.1 語意硬化（同日，使用者指定四項）：** ① as-of 改成「authority 答得出就真的歷史過濾」——Decision Store 依 cohort／decision／coverage／lifecycle 事件／variant perception 的時間戳過濾（`company_decision_facts(as_of=…)`），builder 只收帶相符 `point_in_time_as_of` 標記的事實；判斷日期晚於 as_of 的 session 判斷視為 lookahead 拒用；thesis 檔無歷史維持 `not_applicable`。實測 COHR as-of 2026-08-15：decision 08-15、coverage expiry 11-30、lifecycle 由事件回放為 `expired`（epoch 2 要到 08-19 才開始），整份 JSON 無任何 08-31／12-01 的事實。② 精簡卡 `condition_count`／`structured_count` 無判斷時為 `null`，renderer 全面改為只看 status。③ 人填的檢核點／thesis status 標 `observation`。④ **上游** `rank_bottlenecks` 保留 `sole_source` 三態（`None`≠`False`），`alpha.ranking` 跟著傳三態，`_structural_diff` 只把明確轉換當事件；排序鍵不變（None 與 False 同為 0）。新增 `tests/test_sole_source_tristate.py`、COHR 歷史整合測試 `tests/test_alpha_view_as_of_cohr.py`；突變 96 → 99。查證：`python -m briefing alpha-card COHR --format json \| python -c "import json,sys;print(json.load(sys.stdin)['capability_map'])"` | ✅ |
| ~~**Research Refresh／Dependency Invalidation v1（Phase 2 Step 0.5）**~~ ✅ **2026-09-06 交付** | Step 0 語意驗收（CONDITIONAL GO）暴露：完整 `ResearchContext.digest` 一變（09-04 判斷後 price 264.4→281.9）就讓 variant view／Q4／falsification／scenarios 整份 stale；假設只要 evidence_ref 還解析得到就永遠 accepted；FY2027 實際值寫入後模型只剩 generic `missing`；`company_guidance` 有 producer 沒 consumer；D&C +60% 把同期營收共識當 evidence（provenance 循環）；heuristic proxy（interest 118.1M）永遠 current；rationale 內嵌的 disproof 無人監看 | ✅ **`alpha/refresh/`**（contracts／policy／artifacts／resolver，純邏輯零外部相依、`tests/test_refresh_engine.py` import 掃描守 no-LLM）＋`briefing/alpha_view/changes.py`（authority 時序 → `ChangeEvent`，不是 digest diff）＋`scenarios.py`＋`python -m briefing refresh <T> [--scenario …] [--as-of]`＋read model 第 15 節 `refresh_status`／精簡卡 `refresh` 欄／`SectionStatus` 多 `review_required`／`invalidated`。假設契約 additive：`dependency_roles`／`review_conditions`／`provenance_semantics`（舊行標 legacy，不改寫）；同期共識不得是 supporting（契約拒收）。<br>**實跑 COHR（2026-09-06）：** 判斷 09-04、假設 09-05；偵測到 market_price ×2、financial_actual／company_guidance（一手日 08-12 早於判斷 → 不是新變化）、operating_assumption ×8。結果 **Q1／Q2／thesis current、Q4 review_required**（判斷後新增內部假設）、7 條假設 current、舊 D&C superseded（re-append 成 v2：supporting 6 條＋calibration＝FY27 營收共識＋review_condition「Q2 FY27 D&C <1.85B → review」，並建 Event Watch `ew_0076` 指向 `oa_b43e83a1413ab91c`）。七個情境：price_only → 只有 2 個市場 proxy recalculate、其餘 current；consensus_revision → 數值 gap／proxy recalculate、Q4／thesis review_required、Q1／Q2／假設 current；graph_edge → Q1 recalculate、引用該邊的 Q2／thesis／D&C／margin 假設 review_required（EPS 等傳播現形、標 `propagated_from`），tax／shares／NCI／interest／Industrial current；new_guidance → revenue_growth×2／margin／tax 假設 review_required，shares／NCI／interest current；new_actual → 橋 recalculate、4 條 heuristic review_required、D&C（判斷型）current；fiscal_rollover → FY2027 假設全部 superseded（不沿用）、模型 review_required「new fiscal base available；target advanced → FY2028；new operating assumptions required」；disproof → 條件被觀測滿足 → D&C review_required、值不變。`--as-of 2026-09-04` 排除 9 件變化／8 個成果，當時全部 current。<br>**驗收：** 新增 2 個測試檔 29 條；突變 107 → **116、空跑 0**；`audit invariants` FAIL 0；golden 漂移 1（`watch_states`：新建 ew_0076，EXPECTED_CHANGE）。查證：`python -m briefing refresh COHR --format json \| python -c "import json,sys;r=json.load(sys.stdin);print(r['overall'], r['counts'])"`<br>**剩餘（誠實）：** 語意 materiality 只有 class 級（價格不觸發判斷複查是立場不是量測）；`evidence`／`graph_claim` class 無 runtime 來源（圖不記撤回）；自然語言 rationale／disproof 不解析——COHR 另外三條含條件的假設（Industrial「連兩季」、margin、shares）尚未 re-append 成 machine-readable（「連兩季」語意 v1 條件契約不支援）；ResearchContext 未持久化，digest 殘餘只能整批標 `context_digest`；consensus_estimates 覆蓋 1/73 → 實跑單檔 ETL 後 2/73，屬 ETL 排程時序（daily 會補齊），不是 refresh 的事。 | ✅ |
| ~~**Causal Fundamental Model vertical slice**~~ ✅ **Phase 2 v1 交付（2026-09-05，標的＝COHR）** | read model 已把缺口顯性化：`internal_fundamentals`／`earnings_bridge`／`expectation_gap.internal_vs_consensus` 原本全是 `not_modeled`；`consensus` 只到相對標籤 `+1y` 的營收；`causal_paths` 是 structural 不是 financial | ✅ **驗收數字（實跑 COHR，`python -m briefing alpha-card COHR --format json`）：** `capability_map` 的 `internal_fundamentals`／`earnings_bridge` 由 `not_modeled` → **`available`**（capability `financial_causal_model`）、`expectation_gap` 帶 capability `numeric_internal_vs_consensus`。FY2027（至 2027-06-30，non-GAAP 口徑）：內部營收 **10.228B** vs 共識 10.618B（19 位）→ **−3.7%**；內部稀釋 EPS **8.94** vs 共識 9.42（22 位，口徑以 yearAgoEps 5.61＝8-K non-GAAP 稀釋 EPS 核實）→ **−5.0%**；營益率共識缺 → `consensus_missing`。七條假設全部解析到證據（selection 7/7），每個輸出 `calculation=deterministic`、`input_dependency=session_judgment`。<br>**新增 authority／契約：** `alpha/fundamental/`（contracts／assumptions／bridge／compare／model，純邏輯零外部相依）、`alpha/providers/assumptions.py`（private append-only JSONL ledger）、`python -m alpha assumptions <T> --list／--add／--retract`、Engine C `consensus_estimates` 表（ETL 在抓取當下把 `0y`／`+1y` 解析成 `fiscal_period_end`，`shared/fiscal.py`）＋兩個 mechanical 欄位 `fiscal_year_results`／`company_guidance`、provider 三個唯讀入口（`fiscal_consensus`／`fiscal_year_results`／`company_guidance`，皆 as-of）。<br>⚠ **實測抓到的三件事：** ① 既有 `revenue_estimate_next_fy` 存的是 `+1y`——COHR 在 FY2026 財報後那是 **FY2028**，欄位名叫「下一會計年度」（L12）；同期比較一律改用 FY-identified 的 `fiscal_items`。② yfinance 的 EPS 共識是 **non-GAAP**（yearAgoEps 5.61 ＝ 8-K Table 8 的 non-GAAP 稀釋 EPS，GAAP 是 4.12）；內部 EPS 若從 GAAP 營益率出發就永遠不可比，所以基期觀測必須帶 non-GAAP 對帳。③ `financial_snapshots` 的 TTM 欄位沒有會計年度身分，也沒有利息／稅／NCI／稀釋股數——基期只能從一手財報的 mechanical 觀測來，不能從 yfinance `info` 湊。<br>**PIT 實測：** `--as-of 2026-08-15` 的歷史卡 `internal_fundamentals=missing`（截至 08-15 無 fiscal_year_results 觀測；假設 7 筆全計為 `no_target_period`），整份 JSON 沒有任何 `oa_*` id 滲入。<br>**語意變更（intended）：** 沒有假設／沒有基期觀測的公司，`internal_fundamentals`／`earnings_bridge` 由 `not_modeled` 改為 **`missing`**（系統有能力了）；`expected_return`／`downside`／`entry_logic` 仍 `not_modeled`。既有測試 6 條依此調整。<br>**驗收：** 新增 3 個測試檔 58 條；突變 +9（分部缺假設補零／跨期硬減／未核實口徑相減／as_of 後假設生效／未解析證據生效／ETL 寫 0／晚於 as_of 的觀測／builder 收錯時點 model／缺料填 0）；`audit invariants` FAIL 0｜PASS 11｜SKIPPED 1；golden 漂移 1（`stale_observations`：COHR 新增兩筆 mechanical 觀測，EXPECTED_CHANGE）。查證：`python -m briefing alpha-card COHR --format json \| python -c "import json,sys;v=json.load(sys.stdin);print(v['capability_map']['internal_fundamentals'], v['expectation_gap']['internal_vs_consensus']['value'])"`<br>**剩餘缺口（刻意）：** 毛利率／營業費用拆分、FCF、季度期間、估值／預期報酬／下檔／進場、opportunity ranking；COHR 的 `interest_and_other_net` 沿用 FY26 全年值偏保守（Q4 run-rate 只有 17.6M/季）、`company_guidance` 是季度而 bridge 是年度；只有 COHR 一檔有基期觀測與假設，其他公司 `missing`。 | ✅ |
| ~~`decision_lab today` footer 的 `live_choices=0` 與 outcome 的 1 筆 live fill 不一致~~ ✅ **2026-09-04 驗證已解** | L12 一表兩義：讀 footer 的人會以為 live 路徑從未走過，而那是 2026-08-19 已踩過的坑 | 兩個 surface 對同一 DB 回答一致 | 建議併入 Phase 3 拆 brief 時一起修 |
| ~~`event_watches.json`／`hypotheses.json` 不在 state publisher 窄 pathset~~ ✅ **已於 2026-09-02 由二擴四** | 排程更新了 watch 狀態卻只能留本機未提交，兩個 writer 的變更混在同一份 diff | 擴 pathset 或明文定為互動側責任並寫進 OPERATIONS | 擴 pathset 需 sandbox impact review |
| ~~`current_holdings` 用裸 `except Exception` 壓平三種失敗~~ ✅ **2026-09-04 驗證已解** | 「Sheet 真的沒持股」「網路讀不到」「憑證失效」收斂成同一個 `holdings_unavailable`（L12） | 三種情形產生可區分的 blocker，且至少一個測試能分辨「空持股」與「讀取失敗」 | 建議併入 Phase 3 拆 `engine_d_runtime/adapters.py` |
| ~~`checkpoint_decision_review` completed 路徑非原子~~ ✅ **2026-09-04 驗證已解** | 裸 `pd_*` 能通過前半驗證並先寫 DB，最後 `resolve()` 才拋錯 → work order completed 但 todo item 仍 awaiting_approval，CLI 修不回來（2026-08-19 實測 [166]） | 用裸 `pd_*` 呼叫 `todo work` 時 work order 狀態不變；可寫成測試 | 無 |
| ~~**移除最後兩個 LLM API 相依**~~ ✅ **2026-09-04 交付** | 使用者定案不再使用 API。全 repo 只剩 `extract.py` 與 `thesis/generate_lane_memo.py` 呼叫 `anthropic`，兩者都是 legacy——實際流程早已是 session-in-the-loop（`assessment-scaffold` → session 寫判斷 → `reassess --assessment`，268 筆紀錄佐證） | ✅ **達標**：production 碼的 LLM SDK import **0**、API key 讀取 **0**（`tests/test_no_llm_api_dependency.py` 是常駐剎車，含 openai／google／cohere／mistral 一併擋）。<br>兩支都改成與 `assessment-scaffold` 同一個形狀：`--scaffold` 產提示 → session 寫 → `--response`／`--envelope-file` 吃回來。<br>⚠ **拔 API 時刻意保留 deterministic 後處理**，那才是這兩支不可替代的部分：`extract.py` 的 source id 前綴是 L6 的教訓（局部 ID 跨文件 MERGE 會命名空間衝突）、`generate_lane_memo.py` 的 envelope 驗證與 evidence gate 是 L8 的執行點——兩者與「哪個模型產出 JSON」完全無關，所以一行沒動，並各有測試釘住。<br>⚠ **兩段必須擇一、不得預設走某一段**：沉默地挑一段會讓使用者以為抽取完成了，而它其實只產了一份提示（L13）。<br>⚠ 註解與 docstring 裡的「不再需要 ANTHROPIC_API_KEY」**刻意留著**——測試只看 `os.environ` 實際取值，不看敘述；把移除理由也當違規會逼人刪掉它，而那正是下次被原樣加回來的原因（同 beta 訊號、luna-reviewer 的處理）。 | ✅ |
| ~~**非美股 11 檔的 `segment_revenue_share`**~~ ✅ **2026-09-04 結案** | 美股 15 檔已補（Q3 從 0 檔可算變成 15 檔），非美股卡在**沒有統一 API**：台股走 MOPS、日股 TDnet、HK HKEX、UK RNS、AU ASX、DE／SE 公司 IR、FR AMF URD。~~實測台股 3081：管道通但抓到的是 2023 年報且 `部門別`／`分部` 命中 0 次——小型單一部門公司~~ **⚠ 此結論已於 2026-09-04 撤回**（見右欄）：它是從一份**不含財務報表附註**的股東會年報得出的，而分部附註本來就不在那份文件裡。原文保留供對照——撤回的理由比結論本身更值得記。 | 🔶 **11 → 8（2026-09-04 補 3 檔）**：IQE.L（AR2025 Note 4.3）／SIVE.ST（AR2025 Note 6，分部定義見 Note 5）／SOI.PA（URD 2025-2026 §5.1.1.3）。有 segment 資料的標的 15 → **18**。<br>**取得路徑已打通並可重用：** WebFetch 對 >10MB 的年報 PDF 會直接撞 `maxContentLength` 硬上限（**這是明確的「讀不到」，不是 L11-5 那種兩義訊號**）；改走本地 `urllib` 下載 ＋ `pypdf` 逐頁 regex 定位分部附註即可，13MB／388 頁都解得開。⚠ 猜 URL 會拿到 HTML 錯誤頁（Soitec 第一次就是），要先用搜尋找到真正的文件位址。<br>⚠ **SIVE.ST 的分部占比會誤導，逐字補充已寫進 provenance**：Wireless 佔 69.5% 但其中 178,706／213,137（84%）是 **NRE 合約收入**，硬體收入只有 33,760；Photonics 的 93,420 全是硬體。以硬體計 Photonics 是 Wireless 的 2.77 倍——**分部占比與「哪一塊是產品業務」不是同一個問題**。<br>⚠ 過程中自己犯並更正一筆：SOI.PA 首錄把 `Automotive & Industrial` 由 0.1166 寫成 0.1165，只為讓三項加總剛好 1.0000——那違反 `mechanical` 的定義（任何人重讀都得到同一個數）。已以 append-only correction record supersede，兩筆都留在 ledger（L10）。<br>**剩 8 檔**：3081.TWO／3363.TWO／6268.T／6324.T／6594.T／6680.HK／LYC.AX／SHA0.DE。<br>⚠ **2026-09-04 第二輪：3 檔落地、4 檔查出「為什麼還不能記」，並推翻一條既有結論。**<br>🔴 **3081.TWO 的舊結論「小型單一部門公司」不成立，必須撤回。** 本輪抓到**當期（FY2025，115/05/08 上傳）**英文年報 112 頁／281,784 字，`operating segment`／`IFRS 8`／`reportable` 命中仍是 0——但**那份文件根本不含財務報表附註**（`Independent Auditor` 0 次、`Notes to` 0 次）。**從一份結構上就不會有分部附註的文件得出「未揭露」，是 L11-5 的教科書案例**，而舊結論正是這樣來的。3081 目前狀態應為**尚未查證**，不是已查證未揭露。<br>✅ **工具缺口一已修（2026-09-04）：`fetchers/mops.py` 現在抓得到財報區。** 成因是 `mtype` 寫死 `F`（股東會區），而分部附註在 `A`（財報區）；`fetch_document` 的錯誤訊息還說「檔名可能有誤」——**檔名其實是對的**（L12 一表兩義）。新增 `consolidated_financial_statement`／`separate_financial_statement` 兩種 kind ＋ `KIND_MTYPE` 對照，`--list` 也跟著走同一個 mtype。impact review：未新增 CLI 名稱／網路主機／credential，prefix rule 涵蓋新參數，**十六條 entry 數量未變**。<br>→ **3081.TWO 與 3363.TWO 已補**，兩檔都是**從分部附註本身**確認的單一部門：3081「歸屬為單一報導部門」、3363「本集團彙總為單一營運部門報導」。有 segment 資料 18 → **20**。<br>🔴 **6594.T（Nidec）刻意不記錄，而且這是今天最重要的發現：它的合併財報遭出具 disclaimer of opinion（無法表示意見）。** 逐字：Nidec 2026-04-27 公告載「the Company received an audit report containing a disclaimer of opinion on the consolidated financial statements in the securities report for the fiscal year ended March 31, 2025」，另有 internal control 的 material weakness 與第三方委員會調查。⚠ **disclaimer of opinion 強於 qualified opinion**——審計師明確拒絕表示意見。因此那份財報的分部數字**不符合 `mechanical` 的前提**（「任何人重讀都得到同一個數」的前提是那個數經得起核對）。已鑄 pq2 **[476]** 寫 `litigation_and_audit_flags`（judgment，需核准）。系統原有 3 條 Nidec 線索全是諧波減速器技術面，**沒有任何一條知道這件事**。<br>🔧 ~~**工具缺口一**~~ `--kind financial_report` 回的仍是股東會文件（開會通知／議事手冊／股東會年報）；台股的分部附註在「財務報表」那一區，fetcher 沒有涵蓋。3081／3363 都卡在這裡。**這是 backlog 的下一步**：擴 fetcher 到財報區，不是繼續讀年報。<br>🔧 **工具缺口二：JS 渲染的 IR 頁列不出連結。** LYC.AX 的 annual-reports 頁 WebFetch 只拿得到框架、拿不到 PDF 連結；猜檔名已證實會拿到 HTML 錯誤頁（Soitec）或 404（Nabtesco 四種 pattern）。**猜 URL 失敗不得推論成「當期文件不存在」。**<br>⚠ **6268.T（Nabtesco）已試過，刻意不記錄**：分部數字抓得到且交叉核對過（H1 FY2025/12 四分部 CMP 59.2／TRS 46.4／ACB 52.6／MFR 7.4，合計 165.6 十億日圓＝簡報首頁的合併營收），但那是**期末 2025-06-30**，距今約 14 個月。IR 站的當期簡報 URL 猜不到（四種 pattern 全 404），而**再猜下去就會從「我找不到」滑成「它不存在」**（L11-5）。用舊期分部配當期財務正是 3081 記過的坑，所以維持 `None`。下次要補這檔請從 IR library 頁面找到當期文件位址，不要猜檔名。確實單一部門的登記為單段 1.0 並在 provenance 註明；查不到**當期**揭露的維持 `None`（不得用舊年度湊）<br>✅ **2026-09-04 第三輪：剩下 5 檔全數處置完畢，本列結案。** 有 segment 資料 20 → **25**。查證：`python -c "from engine_c.db import get_conn;` `c=get_conn();` `print(len(c.execute(\"select distinct ticker from manual_observations where field_name='segment_revenue_share'\").fetchall()))"`<br>→ **6268.T（Nabtesco）**：第二輪「猜檔名 404 ⇒ 當期文件位址不明」的**卡點已解，方法是從 IR library 頁列舉**（`nabtesco.com/en/about/ir/library/settlement/` 可爬）。四種 pattern 全 404 的真正原因是**舊檔名帶著公司自己的拼字錯誤 `Birefing`**——猜不到不是因為文件不存在，正是 L11-5 那句話的實例。當期（2026 H1）四分部 CMP 27.18%／TRS 32.50%／ACB 35.24%／MFR 5.08%，三重交叉核對通過（分項合計＝Total 167.4；p12 YoY 瀑布圖 143.3→167.4 對得上；回推 2025 H1 與去年簡報逐欄相符）。公司自陳 caveat 逐字入 provenance：「The numbers above are referential. It may change after auditing.」⚠ 真正的瓶頸業務是 CMP 裡的 Precision reduction gears **26.9%**，不是整個 CMP。<br>→ **6324.T（Harmonic Drive）**：報告分部是**地區別**（日本／中国／北米／欧州），對純度問題無用；附註自陳「同種・同系列の精密減速機事業を専ら営んでおります」＝全公司單一事業。改記**產品群別**（決算短信「経営成績等の概況」）：減速装置 77.8%／メカトロニクス製品 22.2%，重導 46,333＋13,224＝59,557 百万円＝連結売上高。⚠ **WebSearch 摘要把地區別誤標成產品別**（「日本 26,626」被寫成「Component 26.626 billion」），只讀摘要就會整筆記錯（L11-3：多個二手同說不等於一手已證實）。<br>→ **SHA0.DE（Schaeffler）**：`urllib`／`curl` 帶完整瀏覽器 header 仍被 403 擋，但 **WebFetch 會把抓到的 PDF 落成本機檔**，複製出來用 `pypdf` 解即可——這條繞過 bot 防護的路徑可重用。H1 2026 中期報告 p9「Revenue by division」五項合計 11,667＝集團營收。⚠ 先前從新聞稿加總只有 11,607、差 60m，差額正是表上的 **Others 60**——**只讀新聞稿會漏掉一整個 division**。<br>→ **LYC.AX（Lynas）**：JS 渲染的 IR 頁確實列不出 PDF（工具缺口二屬實），但頁內 iframe 指向 `wcsecure.weblink.com.au`，其 `HeadlineJsonP.aspx` 回傳**完整公告 JSON（含 pdfLink）**；ASX 公司普遍使用 weblink，這條路徑可重用。年報 p92 Note A.1 逐字「The Group has only one reportable segment under AASB 8 being its rare earth operations.」<br>→ **6680.HK（JL Mag）**：HKEXnews 的 `titleSearchServlet` 對合法 `stockId`（prefix.do 查得 1000129956）一律回 0 筆，原因未明——**未當成「沒有文件」**；改走巨潮 CNINFO 的 `hisAnnouncement/query`（深圳主掛牌、同一發行人合併報表）。分部附註自陳單一經營分部，故改記產品別：钕铁硼磁钢 85.63%／其他业务 14.37%，兩項合計＝营业总收入 4,649,284,465.03。<br>→ **6594.T（Nidec）維持不記錄**，理由同第二輪（disclaimer of opinion）——它是**刻意排除**，不是未查。<br>➕ **收尾時用「排序內覆蓋率」反查，又抓出兩檔沒人列過的缺口並補完：GFS（20-F Item 4 終端市場別，Comms/DC 僅 11.0%）與 5802.T 住友電工（決算短信 p16，情報通信僅 6.2%）。** 兩檔都不在原本那份 11／8 檔清單上——清單是按**已知待辦**列的，覆蓋率是按**下游實際要用到的標的**算的，後者才抓得到沒人想到的洞（L13：驗收要驗下游手上的東西）。排序內 14 檔標的現為 **13/14 有分部資料**，唯一缺口 TSEM 是已查證未揭露。查證：`python -c "from engine_c.db import get_conn;` `c=get_conn();` `print(sorted({r[0] for r in c.execute(\"select distinct ticker from manual_observations where field_name='segment_revenue_share'\")}))"` | ⚠ `segment_revenue_share` 是 `verifiability=mechanical`，**不需 pq2**；卡的是工作量不是核准 |
| **AEVA／TSEM 已查證未揭露**（新增 2026-09-04） | AEVA 只揭露「認列時點」（point in time／over time）不是產品別；TSEM 單一 foundry 分部，20-F 內無帶金額的產品別表 | 若日後年報開始揭露就補上。⚠ 這兩檔是**已查證未揭露**，與 11 檔非美股的**尚未查**性質不同（L11-5：「我找不到」≠「它不存在」），不得混為一談 | 無 |
| ~~**Luna reviewer 專用委派 skill**~~ ✅ **2026-09-04 退役** | — | **量測後退役**：2026-08-01 上線、實際使用 **2 次**（`luna_reviewer_2026-08-01_pq1_5`／`_2026-08-02_pq1_10`，共 10 筆 lead），之後 **34 天／744 個 commit 零使用**，`.toml` 自上線日起未再修改。兩個原因：①**成本模型變了**——subagent 每次 spawn 都是冷啟動、要重新推導主代理已有的 context，便宜模型省下的往往被 context 重灌吃掉；②**原生功能已覆蓋**——各 harness 的 subagent 本來就能指定便宜模型與唯讀型別。⚠ 真正重造的輪子是疊在 Codex 原生 custom-agent 上的 95 行 skill 協定層。**授權邊界沒有跟著消失**（回傳只是 review packet／主代理唯一 writer／不得委派寫入與四個 gate），已改寫成 provider-neutral 版留在 `AGENTS.md`。移除紀錄與不得回填的理由登記在 closed-vocabulary-registry 的「已移除」區。 | ✅ |
| ✅ **[473]/[474] 圖已寫入但 pq2 無法結案——RA publish 沒有「認領既有 commit」的路徑**（新增 2026-09-04） | `intake/publish.py` 要求發布 commit 必須 ①帶 action_id＋digest trailer ②**只碰 eligible paths**。2026-09-04 我在同一輪用 `git add -A` 提交去重工作時，把兩份 RA 的 `extractions/`／`library/raw/`／`library/intake/` 產物一起掃進 `db7fd75`，於是 `commit_pending_intake` 回 `no_action_changes`——沒東西可 commit，也就記不到 receipt。**圖的 mutation 已 durable 且 `_check_graph_write_readiness` 通過（`go` 授權的事已完成）**，卡住的只有 pq2 結案。⚠ **不得手寫 receipt 認領 `db7fd75`**：那個 commit 混了無關變更、永遠不滿足條件 ②，硬記等於偽造 authority receipt——那正是這套設計要防的事 | 兩條都要：①**流程規則**——工作樹有 pending RA 時**不得 `git add -A`**，先跑 `scripts/commit_pending_intake.py` 讓 action 拿走自己的路徑，再提交其餘；②**工具**——給 publish 一條可稽核的認領路徑（要求 commit 確實含全部 eligible paths ＋ 已 push ＋ 記錄「trailer 缺失」的原因），否則這個狀態只能靠人記得。驗收：[473]/[474] 能以真實 receipt `complete-ra` | 無 |｜✅ **2026-09-11 查證已解（非本輪工作，本輪只驗）**：②工具那一半已經在 `intake/publish.py`——`_out_of_band_commits()` ＋ `_record_out_of_band()`，三個條件缺一就回 None 走原路（每個 eligible path 都 tracked、都沒有未提交變更、最後改動它們的 commit 全部已在 remote 歷史裡），並記 `commit_provenance: out_of_band`，**不假裝它是有 trailer 的正規 commit**。查證：`python -c "import json;d=json.load(open('library/private/research_actions/ra_51171727ae171518974b4196f814c7b2.json',encoding='utf-8'));print(d['state'], d['git']['commit_provenance'], d['git']['out_of_band_commits'])"` → `pushed out_of_band ['3434d68…', 'db7fd75…']`；[473]/[474] 均已 `resolution: go`、receipt 帶真實 digest ＋ cohort（`verified_at` 2026-09-10）。①的「工作樹有 pending RA 時不得 `git add -A`」**刻意不再立成流程規則**：那是靠自律，而 ② 已經把後果從「永遠結不了案」降成「記成 out_of_band、稽核一眼看得出」——L17-4，不在資料支持不到的地方加規則。
| ✅ **非公司實體缺 identity registry——圖裡同一個東西有 2～6 個 id**（新增 2026-09-04；**機制已於 2026-09-10 交付**，語意重複另記為 pq2 [505]） | 公司走 `config/company_identity.json`（INV-1），**TechNode／MatNode 什麼都沒有，所以每份抽取各猜各的 id**（L6 的坑長在非公司實體上）。已實測到的重複群組：**NAND 家族 6 個**（`tech:nand`／`nand_flash`／`nand_technology`／`nand_manufacturing`／`nand_production`／`3d_nand_manufacturing`）、`tech:advanced_packaging`＋`_integration`、`tech:dram_manufacturing`＋`dram_production`、`tech:eml_laser`＋`tech:eml`。⚠ **它安靜造成兩個下游錯誤**：①孤立的重複節點被 `coverage_gaps` 判成「🔴 研究缺口」，會派研究去挖一個已經有供應商的東西；②屬性全空的 legacy 邊讓 `_check_graph_write_readiness` **卡死整個入圖閘門**（2026-09-04 實際擋住 [473]/[474]）。 | **不要一組一組合併**（那是 whack-a-mole，2026-09-04 已手動合掉設備類 3+2 個，隔天就會有新的抽取造出下一個）。要做的是**讓分類跟著資料走**（L16）。**2026-09-10 已交付機制**：`config/entity_aliases.json`（canonical-id SSOT，唯一 loader `identity/entities.py`，載入時驗封閉性）＋ loader 入圖前解析 node／edge 端點／node claim subject 三處＋ `unregistered_entity_ids()` 讓未登記的新 id 有地方會說話。⚠ **真正的根因不在合併，在抽取端**：`extract._known_entities_block()` 讀的是手寫範例檔的 **9 個** node，而圖中光是 chokepoint 前綴就有 196 個——LLM 造新 id 是因為它不知道那些 id 存在。改讀圖之後 9 → 200。<br>⚠ **原驗收條件驗不到它宣稱的目標，已更換**：舊條件用同名分組（`toLower(n.name)`），但六個 NAND 節點的 name 各不相同（『NAND Flash Memory fabrication』vs『NAND Flash Memory Fab』），**一組都抓不到**（2026-09-10 實測現值 9 組，全是別的）。一個驗不到目標的驗收條件比沒有更糟——它會在某天顯示通過而問題還在（L14-1）。<br>**新驗收（兩條，都可機械驗證）**：(a) `python -c "import extract; print(len(extract._known_entities_block().splitlines()))"` 應 ≥ 圖中實體數（2026-09-10：205 行／200 個實體）；(b) 名稱**逐字相同**的實體重複組為 0——查詢必須排除 `:Claim`（claim 節點名稱重複是正常的，不排除會把 6 個 claim 群組算進來）：`MATCH (n:Entity) WHERE n.name IS NOT NULL AND NOT n:Company AND NOT n:Claim AND any(p IN ['tech:','mat:','prod:','std:'] WHERE n.id STARTS WITH p) WITH n.name AS nm, collect(n.id) AS g WHERE size(g)>1 RETURN count(*)`。**[505][506][508] 執行後現值 0 ✅**（2026-09-10）｜原記載：（原三組已於 2026-09-10 合併完畢；[505] 另把 NAND 五節點併入 `tech:nand`，🔴 研究缺口由 5 降到 4、`tech:nand` 由碎片變 `covered`）。⚠ 剩下那 1 組是 `tech:ocs`＋`tech:optical_circuit_switch`，**它的『name 相同』是 [506] 重載自己造成的**：`MERGE_NODE` 直接 `SET n.name`，後載入的文件蓋掉前一份，`tech:ocs` 的 name 由『Optical Circuit Switching (OCS)』被改寫成『Optical Circuit Switch』（比對備份快照 `20260910T053041Z` 可驗）。因此它**不得**走 name_identical 登記——用被自己遷移改出來的 name 當機械依據是循環論證；已鑄 pq2 [508] 走 semantic_reviewed。<br>⚠ **連帶結論：本條驗收指標不是穩定的。** 重載任何文件都可能改變「名稱逐字相同」的分組。要嘛讓 `name`／`attributes` 也走聯集或保留最長（`MERGE_NODE` 目前只有 `source_ids` 會聯集，這個隱患 09-04 就記過），要嘛把驗收改成不受重載影響的判準。**在那之前，讀這個數字要先問「上一次重載是什麼時候」。**<br>**語意重複不列入本項驗收**：「這兩個名字不同的節點其實是同一個東西」是研究判斷，載體是 pq2 的 ra_admission（L15）。因此 NAND 6 個是 **[505]**；`advanced_packaging_gf`（GF Fab 8 特指）與 `inp_eml`（InP 材質特指）刻意不併 | ⚠ 這是**開發項不是研究項**：改的是系統怎麼解析 id，不是我知道什麼 |｜✅ **2026-09-11 兩條驗收皆達標（本輪只驗）**：(a) `python -c "import extract; print(len(extract._known_entities_block().splitlines()))"` → **199 行**，圖中同前綴實體 **192 個**（≥，達標）；(b) 名稱逐字相同的重複組查詢 → **0 組**。⚠ 原本記的「本條驗收指標不是穩定的」也已消失：`MERGE_NODE` 無條件覆蓋 `name`／`attributes` 已於 2026-09-10 修掉（`loader/load_to_neo4j.py::load()` 保留既有、只補缺 key、差異印 stderr），所以重載不再會改變分組。
| ✅ **`MERGE_NODE` 無條件覆蓋 `name`／`attributes`**（2026-09-10 修復） | 只有 `source_ids` 會聯集，`name` 與 `attributes` 是直接 `SET`——**後載入的文件整包蓋掉前一份**。09-04 的 `migrate_entity_dedup` 註解就記過這個隱患，2026-09-10 的 [506] 讓它第一次產生可觀察後果：一次 scoped 重載改掉 **7 個節點的 name**，其中 `tech:vcsel` 由『VCSEL laser』變成『200G VCSEL-based solution』（技術層被改寫成產品規格）、`tech:silicon_photonics` 由『Silicon Photonics』變成『Silicon Photonics transceiver』（語意變窄）。**沒有任何東西壞掉，也沒有任何警告。** 連帶後果：`tech:ocs` 的 name 被改成與 `tech:optical_circuit_switch` 相同，於是本表第 177 行的驗收 (b)『名稱逐字相同的重複組』**不是穩定指標**——重載任何文件都可能改變它。 | 兩條路擇一：①讓 `name`／`attributes` 也走聯集或「保留較早／較長」；②改用不受重載影響的驗收判準。**驗收：**造一份會改寫既有節點 name 的抽取檔，重載後 name 未被覆蓋（或有明確警告），且 `docs/ROADMAP.md` 第 177 行的驗收 (b) 在連續兩次重載之間數值不變。查證：`python loader/migrate_entity_alias_backfill.py --dry-run`（冪等，應永遠 skip 全部）＋ 比對備份快照的 name。 | ⚠ 存量的 7 筆 name 要不要還原是**研究判斷**，走 pq2 [509]；本項只管「以後還會不會再發生」 |
| ✅ **`doc_id` 不等於 `extractions/<doc_id>.json`**（2026-09-10 修復） | 全庫 206 個 doc_id 中 **11 個**的 `extractions/{doc_id}.json` 不存在（`Electronic_Chip_Package_and_CPO_Technology_for_Modern_AI_Era` 住在 `cpo_chip_package_paper.json`、`fn_10_k_20260818` 住在 `fn_10_k_fy2026.json`…）。`engine_b/todo.py:1227` 對它們會拋「找不到 extractions/…，graph receipt 無可稽核依據」——**但依據存在，只是檔名不同**。這是 L15 的形狀：gate 攔下的不是它想攔的東西（它想攔「沒有稽核依據」，實際攔的是「檔名慣例」）。⚠ 另有 **21 組 doc_id 對應多份檔案**（addendum 慣例，涉及 44 份），所以正解不是改檔名，是**掃內容建 `doc_id → list[path]` 索引**（`loader/migrate_entity_alias_backfill.py::_extraction_index` 已是正確寫法，可抽共用）。 | 抽一個共用的 extraction 索引模組，5 處改吃它。**驗收：** 對那 11 個 doc_id 逐一呼叫受影響的入口，全部解析得到（現況會拋錯）；且 21 組多檔的 doc_id 回傳全部檔案而非一份。 | ⚠ Windows 檔案系統不分大小寫，`Broadcom_q2fy26_cpo` 目前僥倖命中 `broadcom_q2fy26_cpo.json`——換 Linux 就會壞 |
| ✅ **新增的偵測機制沒有 consumer**（2026-09-10 修復） | `todo gated`（分辨「真的在等你」與「gate 已消失」）與 `audit invariants` 的 QueueLiveness 新規則，**都不在 daily 排程的 19 個 fixed entry 裡**——只有人手動跑才看得到。而 `engine_b.todo work` **是** fixed entry，也就是排程可以產生 `awaiting_approval` 工單，卻沒有任何東西會回頭檢查它們（INV-4：producer 指得出 consumer）。同一形狀還有 `identity/entities.py::unregistered_entity_ids`：它只在 load 當下 print 到 stderr，**沒有累積成任何可查詢的數字**——L14 明文「真正的防呆是會自己出現的常駐計數器，不是要人讀的段落」，而我做的正是後者。 | 把三者接進 daily：`gated` 的三態計數、未登記實體 id 的累計數、audit 的 FAIL 數。**驗收：** daily brief 首屏出現這三個數字，且刻意製造一筆 no-pointer 工單後隔天會被印出來。 | ⚠ 接進 daily 需要在同一個 change 完成 sandbox impact review（`.codex/rules` 要不要加 entry） |
| ✅ **`--awaiting-gate` 選填且完全靜默**（2026-09-10 修復） | `todo work --to awaiting_approval` 不給 `--awaiting-gate` 時不擋也不警告，於是預設產出的就是 `no_pointer` 工單。設計上刻意不擋（既有工單沒有這個欄位），但「不擋」不該等於「不說」。 | 進 `awaiting_approval` 且未給 gate 時印一行警告，指出它會被 `gated`／QueueLiveness 報成 no_pointer。**驗收：** 不給參數時 stderr 出現警告，給了就沒有。 | ⚠ 與上一項綁著做才有意義——沒有 consumer 的話警告也沒人看 |
| ✅ **harvest 的 EDGAR lead 標題無法區分同日多份同型 filing**（2026-09-10 修復） | ⚠ **原記載是誤診，已更正。** 原本記成「去重缺口」——實測那三筆 TSM Form 4 的 accession 各不相同（000656／000644／000648），是三份真實的不同 filing，去重（鍵是 url）完全正確。**問題在標題**：`{ticker} {form} filed {date}` 讓三筆長得一模一樣，看起來像重複，triage 與 brief 都難分辨。 | `crons/harvest_leads.py::filings_to_leads` 的 title 補上 accession 尾 6 碼。**驗收：** 同日多份同型 filing 的 title 互不相同（實測 `TSM 4 filed 2026-09-09 [000656]` vs `[000644]`）。 | ⚠ 記這一筆是因為**誤診本身值得記**：看到三筆一樣的標題就斷定去重壞了，而沒有先看 url——L11-6 的形狀（對自己的技術診斷要套跟引用的事實同一套追源紀律） |
| ✅ **`decision_lab.db` 的空 `manual_observations` 表**（2026-09-10 已刪） | 2026-09-10 執行 [507] 時一度用錯連線（`open_default_store()` 指向 decision_lab.db 而非 engine_c.db），`ensure_manual_observation_schema()` 在那裡建了表才失敗。**0 筆、無害**，但它讓 Engine C 的觀測 ledger 看起來有第二個 authority——而「只有一個 authority」正是五條分離規則要保的東西。 | `DROP TABLE` 或加一條 audit 檢查「decision_lab.db 不得有 manual_observations」。**驗收：** `sqlite_master` 查無此表。 | ⚠ 低優先，但別讓它留著變成某天有人「發現」的第二份真相 |
| 🔶 **節點屬性沒有衝突解決機制**（新增 2026-09-04；**偵測那一半已於 2026-09-10 交付**） | 邊有 `loader/edge_resolution.py`，**節點沒有**：`MERGE_NODE` 對 `n.attributes` 是直接 `SET`，**後載入的文件整包蓋掉前一份**（只有 `source_ids` 聯集）。實例：Lam 的 10-K 給 `tech:deposition_etch_clean` 的 `ramp_difficulty_intrinsic=4`、10-Q 給 `3`，目前是「誰最後載入誰贏」，而且**不會有任何東西報錯** | 2026-09-04 的 dedup 用「把各檔 attributes 改成一致的聯集」繞過，**那是繞過不是修好**。**2026-09-10 已交付它自己提的下限**：`loader/load_to_neo4j.py::load()` 現在保留既有 `name`／`attributes`、只補上既有沒有的 key，並把每一筆差異印到 stderr——「後載入的整包蓋掉前一份」不再成立，衝突不再靜默。**仍缺的是上限**：節點屬性的 assertion／resolution 層（等價於 `loader/edge_resolution.py` 之於邊），那是重構等級，依 L17-2 排程不當下做。⚠ 在它做出來之前，節點屬性的值衝突是「先寫的贏」＋一行 stderr，**沒有人會回頭裁決**。 | 與上一列同源 |
| ~~Engine D cohort 重複（claim-keyed vs company-keyed）~~ ✅ **2026-09-04 驗證已解** | 同公司可能同時存在兩個 cohort（2026-07-30 [74]／[75]） | 新建 cohort 時偵測同公司既有 cohort 並警告。**不回溯清理**（append-only） | 無 |
| ~~**把 `mcp_server/` 的 domain 抽出到 application layer**~~ ✅ **已隨 Phase 3 交付** | 實測：`mcp_server/` 4,016 行有 **79%（3,165 行）不是 MCP**——Research Action 的 domain、filesystem provenance 原語、local-only Git 發布，全被關在 transport package 裡。因此 5 個 core 消費端被迫 import 它，其中包含 pq2 待辦池本身 | `Core → mcp_server` 的 import **5 → 0**；`scripts/prepare_research_action.py` 不再呼叫私有 `_impl` 函式 | 併入 Phase 3（分類見 `target-architecture.md` §14.2） |
| ~~**`audit invariants` runtime checker**~~ ✅ **2026-09-04 交付（10/12）** | — | **驗收達標**：上線首跑就抓到真實問題——3 筆 `trace_attempts_ref` 有 2 筆指向已不存在的檔案（daily 把追源原文寫進 `library/raw/`、路徑寫進 leads state，但 publisher pathset 不含它）。搬到 top-level `audit/`（讀遍所有層的東西不能住 core）。**2026-09-04 Phase 6 補上 `PointInTime`（11/12）**——它會**實跑一次 as-of 投影**驗它沒漏出未來，不是只讀覆蓋率。剩 `GateDiscrimination`（Phase 4）。查證：`python -m audit invariants` | ✅ |
| ~~**Golden fixtures / 歷史回歸套件**~~ ✅ **2026-09-03 交付** | — | 14/14 類已凍結（`scripts/capture_golden_fixtures.py --verify` 偵測漂移）。B1／B5／B6 的 dual run 仍待各批執行 | ✅ |
| ✅ **Form 4 佔用 pq1 預算但歷史 graph delta 為 0**（新增 2026-09-10） | `crons/harvest_config.json` 的 `edgar_watch.forms` 含 `"4"`，實測 **267 筆 Form 4 lead：applied 0、action_prepared 0**，其中 **160 筆走完 triaged_go → pq1 → parked**（146 筆 `original_obtained`＝有人真的把 XML 抓下來逐欄核對過），另 107 筆 triaged_no_go。2026-09-10 這一輪又吃掉 3 個 drain 名額（TSM 三位高管，皆為 footnote 明載 ESPP predetermined 的 40／149／71 股自動申購——連「內部人信心」都不成立）。AGENTS.md 已明文「技術訊號整組移除」，內部人交易訊號從未在 scope；但 P4 也已指出多抓 43 家等於再放大這個噪音。**這不是 bug——不會壞、測試不會紅、每天安靜偏掉一點研究預算**（L17）。⚠ 不建議無條件砍：大額自主市場買賣仍可能是 disproof 證據，harvest 端只有 metadata（form／date／accession），分不出 ESPP 與自主交易；要嘛移除 `"4"`（1 行，接受失去該可見度），要嘛在 triage 前加一支確定性的 transactionCode／footnote 判讀（抓 XML 才分得出，成本較高）。**兩個方向都要人決定，所以不當下修**。 | `python -c "import json;d=json.load(open('library/leads/pending_leads.json',encoding='utf-8'));L=d['leads'] if 'leads' in d else d;L=list(L.values()) if isinstance(L,dict) else L;f=[x for x in L if ' 4 filed ' in (x.get('title') or '')];print(len(f), sum(1 for x in f if x.get('status') in ('applied','action_prepared')))"` → 現況 `267 0`；修完後驗收是**新增 Form 4 lead 數歸零**（或 parked-without-delta 佔比下降），不是「改動成功」 | 無 |｜✅ **2026-09-11 交付（GO；使用者定案：不移除、改註冊後自動 no_go）**：`crons/harvest_config.json` 新增 `edgar_watch.auto_no_go_forms: ["4"]`（`load_config` 驗它是 `forms` 的子集——列一個不在 forms 裡的 form 會讓規則看起來生效而實際是死設定，L16-3），`filings_to_leads` 把 `form_type` 帶進 payload（**不要下游 parse 標題去猜**，L16-1），`_register_all` 在註冊後立刻 `triage(go=False)`。**兩個被否決的替代方案**：移除 `"4"` 會連「大額裁量性內部人賣出」的脈絡一起丟（2026-09-11 實測 AAOI officer 賣 32,172 股、未勾 10b5-1）；triage 前加 XML transactionCode 判讀要先抓檔，成本高。驗收（L14-1）：真實 EDGAR dry-run，本日唯一一筆新 lead『META 4 filed 2026-09-10』**直接落在 `triaged_no_go`**（Form 4 lead 總數 267 → 275，持續成長）（原本會是 `pending` → 吃一個 triage／drain 名額）；記錄仍在、可 grep、可 `requeue-campaign` 叫回。測試 `tests/test_engine_b_leads.py`（2 條）。修法層級＝**根除**（這一型不再可能進 drain 佇列，不靠任何人記得）。⚠ **一個超出原驗收的連帶行為，記下來不藏**：`check_watches` 只讓 `triage.decision == "go"` 的 lead 喚醒 watch，所以 Form 4 從此**也不再叫醒 event watch**。實測歷史比重不小——46 個由 lead 喚醒的 watch 有 **11 個（24%）是 Form 4 觸發**，`consumed_leads` 252 筆裡 **185 筆是 Form 4**。方向與本條診斷一致（那是同一種噪音往上一層），但它不在原本寫的驗收裡，所以明寫。
| ✅ **`closure.NEXT_PICK_RULE` 的「EPS 為正」讀的是 +1y，而橋建模的是 0y**（新增 2026-09-10） | `alpha/closure.py::_consensus_flags` 是 `pick = vals.get("+1y") if ... else vals.get("0y")`——**刻意優先 +1y**。但 `alpha/fundamental/bridge.py` 的目標期間是「基期後一個會計年度」，對 12 月結算的公司就是 **0y**。實測 MP：+1y（FY2027）共識 EPS **+0.89562** → gate 判「EPS 為正」並把它選為下一檔；實際建模的 FY2026 同期共識 EPS 是 **-0.00374**，內部 GAAP EPS 推出來 **-0.2685**，forward_earnings_multiple **型別上不適用**，整檔只能走 Abstention（已鑄 [521]）。換句話說**這條規則會系統性地把虧損年的檔選成「可以做」**，而它存在的理由正是要避開那些檔。⚠ 不當下修：`NEXT_PICK_RULE` 被 `skills/research-drain/SKILL.md` 逐條引為「下一檔選誰不是自由心證」的權威，改它會改變 68 檔的順序——依 L17-2 屬「動到 contract」，要 Z2 proposal。候選修法：改讀 0y、或要求 0y 與 +1y 同時為正、或直接讀 analyst view 已算好的目標期間共識（後者最不會再漂，因為那正是橋自己用的那一格）。 | `python -m webapp status` 的「每檔閉環／下一檔」；修完後驗收是**被選中的檔其目標期間共識 EPS 為正的比例**（現況：MP 這一檔為否），不是「改動成功」 | 無 |｜✅ **2026-09-11 交付**：改為**要求 0y 與 +1y 同時為正**。不選「只讀 0y」是因為 0y 缺值的檔不少（新上市、換會計年度），只讀 0y 會把它們全判成 None 排到後面；「兩者皆正」在資料完整時等於讀 0y，缺值時退回 +1y，**方向一致地偏保守**。驗收（L14-1）：現有資料 **2 檔旗標翻轉**——MP（0y −0.00374 vs +1y +0.89562，正是本條記錄的案例）與 XPEV（0y −3.45667 vs +1y +1.235）。測試 tests/test_closure.py::test_consensus_flag_requires_both_periods_positive。
| ✅ **`NEXT_PICK_RULE` 沒有成本維度，會先把 session 推進最貴的那 29 檔**（新增 2026-09-10） | 實測盤點：段 5 未到終局的 62 檔裡，**33 檔已有 `fiscal_year_results` 基期觀測**（美股，P7-a 的 XBRL 補值做掉了），**29 檔沒有**（KS／SZ／TW／TWO／T／HK／SS／PA／ST／L／V／DE 等非美股）。沒基期的那 29 檔每檔要 session 自己去找一手年報／決算短信／업績발표，本輪實測成本差距約 **3 倍**（有基期：假設＋判斷檔約 8–10 次工具呼叫；沒基期：另加 8–15 次一手搜尋與 PDF 解析）。而 `NEXT_PICK_RULE` 只看「有共識 → EPS 為正 → 產業能加一 → 瓶頸名次 → ticker」，**沒有任何一格反映取得成本**，所以它會把 000660.KS、002472.SZ、005930.KS、012330.KS、2301.TW…一路排在 33 檔便宜的前面。配合深度優先（第 N 檔未到終局不開 N+1），一個 session 可能把全部預算花在 2–3 檔上。⚠ 這不是要改成「先做簡單的」——那正是規則要防的自由心證。可考慮的是**加一格確定性旗標**（`has_base_observation`），讓順序仍然機械但不再對成本盲目；要不要加、加在第幾順位是使用者的決定。⚠ 另記一個**與成本無關的方法論限制**（本輪由 000660.KS 撞出）：橋的 `interest_and_other_net` 與 `tax_rate` 都是「沿用基期」，但 SK Hynix 2Q26 淨利 ₩93.9 兆 **大於** 營業利益 ₩60.5 兆——非營業損益已是主導項且劇烈波動，沿用基期在結構上就不成立。這類公司需要的是 method 層的處理，不是更多資料。 | `python -c "import json,sqlite3,glob,os;c=sqlite3.connect('file:library/private/engine_c/stockbot-engine-c-private-v1-458db5270ee2.db?mode=ro',uri=True);have={r[0] for r in c.execute(\"select ticker from manual_fields where field_name='fiscal_year_results'\")};print(sum(1 for f in glob.glob('library/private/app/analyst_view/*.json') if not f.endswith('.meta.json') and os.path.basename(f)[:-5] in have))"` → 現況 33／62；驗收是**單一 session 內到終局的檔數**上升，不是「改動成功」 | 無 |｜✅ **2026-09-11 交付（GO；使用者定案：加 `has_base_observation`，放**只破平手**的位置）**：`BacklogRow.has_base_observation` 由 `alpha/closure.py::_base_observation_tickers()` 從 Engine C `manual_fields` 讀（讀不到留 `None`＝未讀到，**不是 False**——False 會把整批推到最後，L12），排序鍵放在四條判準之後、**ticker 之前**。⚠ 不放在 ticker 之後（＝字面上的「最後一格」）：ticker 唯一，那一格永遠碰不到，會變成看起來生效的死設定。**前四條的相對順序一格都沒動。** 驗收（L14-1）：59 檔未到終局中 **57 檔落在 3 個前四條同分的群組裡**——原本真正在決定順序的就是 ticker 字典序；套用後 **49/59 個位次改變**，#2–#10 的「已有基期觀測」由 **0/9 變 9/9**（原本是 005930.KS／012330.KS／2301.TW…一整排要自己去找一手年報的檔）。#1 仍是 000660.KS，因為它在第三條「產業能加一」就贏了——這正是「不改變前四條」的證據。測試 `tests/test_closure.py`（3 條）。⚠ 本列記的**方法論限制，2026-09-11 查證後更正**（原記載如下）：橋的 `interest_and_other_net`／`tax_rate` 沿用基期，對 SK Hynix 這類非營業損益主導的公司結構上不成立——那是 method 層，另行排程。⚠ 同時把兩個新維度接進 `OPEN_PROFILE_FIELDS`，讓它們**每輪 `closure-gate` 自己出現**（L14：真正的防呆是常駐計數器，不是要人讀的段落）：現況「沒有基期觀測 27／59」「使用者已 defer 5／59」。`webapp` 印的條目數改讀 `len(OPEN_PROFILE_FIELDS)`——原本寫死「三項可重疊」，加第四項那天就會變成假的。｜⚠ **2026-09-11 追查：上面那段診斷不是現況。** `000660.KS` 的 fundamental 今天是 `missing`（「尚未寫入任何 OperatingAssumption」），橋在每一步都已 fail closed，**沒有任何看起來合理的錯數字被產出**；而且它的基期觀測根本沒有 `interest_and_other_net`／`pretax_income` 可以沿用——作者刻意留空，並把理由寫進 `coverage_note`（含「一條方程式兩個未知數，**不得**合併塞進 `tax_rate`，那會一格兩義」）。**真正的缺陷在別處**：那段 `coverage_note` 被 parser 丟掉了，於是會去寫假設的人看不到它。已修，見下一列。<br>⚠ 原記載的「非營業損益主導型公司需要 method 層處理」**仍是未經量測的推論**：現有 11 個可算的樣本非營業佔比是 **0.1%–23.8% 連續分布，沒有空隙可以據以定門檻**（對照 FX 容差那次有 2.11%↔4.70% 的空隙）。所以本輪**不定任何閾值**——在沒有事實支撐的地方泛化會得到會誤報的分類（L17-4）。查證：`python -c "import json,sqlite3;c=sqlite3.connect('file:library/private/engine_c/stockbot-engine-c-private-v1-458db5270ee2.db?mode=ro',uri=True);print(sorted(round(abs((json.loads(v).get('gaap') or {}).get('pretax_income',0)-(json.loads(v).get('gaap') or {}).get('operating_income',0))/abs((json.loads(v).get('gaap') or {}).get('operating_income') or 1),3) for _,v in c.execute(\"select ticker,value from manual_fields where field_name='fiscal_year_results'\") if (json.loads(v).get('gaap') or {}).get('pretax_income')))"`
| ✅ **基期觀測作者寫下的註記被 parser 丟掉**（新增 2026-09-11） | `alpha/providers/fundamentals.py` 的 `fiscal_year_results` 解析是**列舉式**：只讀 `fiscal_year_end`／`currency`／`revenue`／`segment_revenue`／`gaap`／`non_gaap`／`exit_quarter`／`source_filed_at` 八個鍵，其餘**直接掉地上**，`FiscalYearActuals` 連欄位都沒有。實測 **46 筆基期觀測有 40 筆寫了 `coverage_note`、另 6 筆有 `unit_note`／`critical_note` 等字串註記，下游 0 筆看得到**。而那些話正是寫假設的人最需要的：002472.SZ「共識口徑是**扣非**，已一手核實」、5802.T「**股票分割口徑**：短信印的 EPS 與期中平均股數都是分割前」、000660.KS「非營業損益與稅率刻意未填，**不得**合併塞進 `tax_rate`」。**寫下它的人已經做對了事，是管子只接了一頭**（L13）。⚠ 這不是 bug——不會壞、測試不會紅、作者不會知道自己寫的東西沒人收到（L17）。 | `FiscalYearActuals` 加 `coverage_note`（一等公民）＋`author_notes`（**其餘鍵原樣保留**，不再列舉式丟棄）；builder 把它們逐字掛進 `internal_meta.warnings`，經 compose 進到 **fundamental panel 的 notes**——也就是寫假設前會讀到的那一格。逐字轉述不改寫：那是一手紀錄。驗收（L14-1）：**46／73 檔**的 fundamental panel 多出作者註記、共 **54 條**（先前 0）。⚠ 查證命令的路徑是 `view.fundamental.notes` **不是** `fundamental.notes`——頂層那個 key 不存在，查錯會得到 0 而看起來像沒生效（我自己就先查錯了一次）：`python -c "import json,glob;print(sum(1 for f in glob.glob('library/private/app/analyst_view/*.json') if not f.endswith('.meta.json') and any('基期觀測' in str(x) for x in ((json.load(open(f,encoding='utf-8')).get('view') or {}).get('fundamental') or {}).get('notes') or [])))"`。測試 `tests/test_fundamental_model.py`（2 條，其中一條拿全庫 19 個實際出現過的鍵去證明**每一個鍵都必須要嘛有欄位、要嘛進 `author_notes`**——新增欄位卻忘了接會變紅）。修法層級：**根除**（不是替某一檔標旗標——那會是 L17「只認得我當初那個案例」的機制；改的是解析層不得靜默丟棄任何鍵）。 | 無 |
| ✅ **`incompatible_basis` 同時承載「會計口徑不符」與「FX 換算率不符」**（新增 2026-09-10） | 共識口徑核實法是「拿 provider 的 `year_ago_actual` 對 `fiscal_year_results` 裡的 `diluted_eps`」，對不上就標 `unverified`，理由句寫「provider 未宣告且無法用去年實際值核實，不得與內部 gaap 相減」。實測 TSM：provider 的 FY2025 `year_ago_actual` = **10.65**，而 20-F 印出的 Diluted EPS per equivalent ADS = **US$10.43**——差的**不是會計口徑**（兩邊都是 as-reported IFRS 稀釋 EPS），是**換算匯率**：10.43 用 20-F 明載的年末 NT$31.37，10.65 隱含 NT$30.74（約當 2025 年均價）。同一個 `incompatible_basis` 訊號因此蓋掉兩種成因，而它們的處置完全相反——口徑不符要補 `non_gaap` 區塊，匯率不符要的是**用同一條 FX 路徑重算**（或明示接受 2% 的換算差）。⚠ 影響面不只 TSM：段 5 剩下的 66 檔裡有 000660.KS、005930.KS、012330.KS、5802.T、6481.T、2301.TW、3081.TWO、6680.HK、688836.SS…等一整批非美股，只要 provider 用均價、一手用期末價，全部會落進同一格。後果是**兩欄拆解（EPS 桿／倍數桿）對這批標的永遠缺席**，而那兩欄正是 P2 用來回答「全負是方法偏空還是市場太貴」的唯一證據。⚠ 這是 L12（一表兩義）的形狀，修法是先分開再各自定規則，不是放寬容差。 | `python -c "import json;d=json.load(open('library/private/app/analyst_view/TSM.json',encoding='utf-8'));print([l['datum']['reason'] for l in d['view']['headline']['lines'] if l['datum']['key']=='multiple_contribution'])"` → 現況為 incompatible_basis；修完後驗收是 **`closure-gate` 的「倍數＝校準倍數」計數 > 0**（TSM 的目標倍數就是校準倍數，卻因為這格而數不進去） | 無 |｜⚠ **2026-09-11 只走到「看得出差多少」，兩義未拆**：`compare.describe_basis_mismatch()` 已把兩個數字寫進理由句（TSM『provider 10.65 vs 一手 gaap 10.43（差 +2.1%）』、SOI.PA『−3.28 vs −6.17（差 −46.8%）』）。**這是顯形不是根除**——訊號仍叫 `incompatible_basis`，兩欄拆解對這批標的仍然缺席。不當下拆的理由寫在提案裡：要分開就得先決定「願意接受多少換算差」以及「是否用同一條 FX 路徑重算」，那是會改變投資判斷的方法論選擇，不是實作細節。｜✅ **2026-09-11 交付（GO；使用者定案「你給個推薦數字」）**：**兩義已拆**。新增第二個封閉字彙 `CONSENSUS_BASES`（`gaap_fx_tolerated`／`non_gaap_fx_tolerated`）——刻意**不動** `ACCOUNTING_BASES`，那是三本 private ledger 每筆紀錄身分的一部分（L10）。**容差 3% 是量出來的不是挑的**：全庫 13 檔有基期觀測＋同期 `year_ago_actual` 的樣本中，11 檔逐字相等（0.00%）、TSM **2.11%**、SOI.PA **46.84%**；而「最近候選 vs 次近候選」最窄的是 002472.SZ 的 **4.70%**——2.11% 與 4.70% 之間**沒有任何樣本**，3% 落在中間。**放寬的只有識別，判準反而更嚴**（L15-4）：exact 永遠贏過容差段、容差段內恰好一個候選才算數（0 個或 2 個都退回 `unverified`）、異號自動排除。**代價寫在字彙裡**：容忍 3% 的指紋差 ＝ 接受 forward 比較裡同量級的換算誤差，所以殘差以 `ExpectationComparison.fx_translation_delta` **結構化**帶到消費端（不是只寫在理由句，L16），兩個桿各自的那一行印出雜訊下限。<br>⚠ **原驗收條件已換掉**：舊條件是「`closure-gate` 的『倍數＝校準倍數』計數 > 0」，實測**沒有達成、而且不該達成**——TSM 的倍數桿 +1.7% 比它自己的 2.11% 殘差還小，把它算成「已確認校準」跟算成「主張了溢價」一樣錯。改成計數器的**第三格**（見下）。**新驗收**：兩欄拆解 `available` 的檔數 **8 → 9**（TSM 由 missing 變 available）：`python -c "import json,glob;print(sum(1 for f in glob.glob('library/private/app/analyst_view/*.json') if not f.endswith('.meta.json') and any(l['datum']['key']=='multiple_contribution' and l['datum'].get('status')=='available' for l in json.load(open(f,encoding='utf-8')).get('view',{}).get('headline',{}).get('lines',[]))))"`<br>⚠ **順手修掉一個先前就存在、被 TSM 撞出來的兩義**：「倍數桿算不算主張折溢價」有**兩個門檻**——卡片散文用 0.02、品質計數器用 0.005。TSM +1.7% 是第一檔落在兩者之間的，於是卡片寫「與市場一致、倍數桿沒有貢獻」，計數器同時把它列進「有折溢價主張」，使用者兩邊都讀得到而它們互相否定（L12／L16）。合併成唯一 SSOT `implied_return/contracts.MULTIPLE_NEUTRAL_TOLERANCE`，取**較嚴的 0.005**——放寬稽核計數器要人決定，收緊不用。計數器同時多一格「倍數桿落在換算殘差以下（讀不出來，不是校準也不是主張）」，現況 1 檔（TSM +1.7%，殘差 +2.1%）。
| 🔶 **誠實回 `unknown` 的軸讓該檔結構上永遠到不了段5 終局**（新增 2026-09-10） | packet 的 `axis_prompts.catalyst.do_not` 明文要求「找不到具體事件時回 unknown，**不要編一個**」，而 `unknown` 會讓 `research` panel 的 `catalyst_score` 變成 `missing` → readiness `blocked`。另一邊，`alpha/abstention/contracts.py` 的 `ABSTENTION_LAYERS` **只有 `valuation` 一層**、`ABSTENTION_SUBJECTS` 只有 `forward_earnings_multiple.target_pe` 一個 subject——**沒有任何機制可以宣告「刻意不主張 catalyst」**。兩條規則合起來的後果是：一個 session 越誠實，那一檔就越永久卡住，而 `closure-gate` 會一輪一輪把它重新提出來。⚠ 實測 GFS（2026-09-10）：headline／fundamental／why 三格都做完了，只剩 catalyst；本輪實際做了 bounded source-trace，找到的兩則 2026 年具名事件都**正確地**不具入圖資格（①2026-09-03 GF 向美國商務部發行 9,907,399 股 @US$37.85——資本事件、政府機關不符 Company node schema，排程已於 09-08 park；②2026-09-09 GF×Monolithic Power Systems 長期製造協議——無金額／無產能／無最低採購量，四維初判在「客戶端資本承諾」不過，且 MPS 不在 registry）。**研究做對了，系統卻沒有地方放這個結論。** 這是 L12 的形狀：`unknown` 同時承載「去研究」與「已研究、結論是沒有」兩種語意，下游被迫二選一。⚠ 影響面是段 5 全體：任何一軸誠實回 unknown 的檔都會落進同一格。候選修法：`ABSTENTION_LAYERS` 加 `research` 層＋對應 subject（如 `axis.catalyst`），並讓 `research` panel 認得 settled 的軸——**動到封閉字彙與消費端語意，屬 Z2，要 proposal**。 | `python -m webapp status | Select-String GFS` 現況 `blocked｜blockers: research=not_yet_recorded`；~~修完後驗收是**段 5「到終局」檔數中 settled 那一類 > 1**~~ ⚠ **2026-09-11 換掉：這個條件驗不到它的目標。** 實測今天 settled 已經是 **3 檔**（6324.T／IQE.L／MP）——條件顯示通過，而問題原封不動：三檔的 settled panel 全是 `headline`／`why`，`ABSTENTION_LAYERS` 仍然只有 `('valuation',)`，`research` 層一個都不可能存在。這正是本檔記過的形狀（『一個驗不到目標的驗收條件比沒有更糟』，同 identity registry 那一列）。**新驗收（兩條，都可機械驗證）**：(a) `python -c "from alpha.abstention.contracts import ABSTENTION_LAYERS as L; print(L)"` 含 `research`；(b) 段 5 到終局中**由 research 層 settled** 的檔數 ≥ 1（今天 0——不是 3）：`python -c "import json,glob,os;print(sum(1 for f in glob.glob('library/private/app/analyst_view/*.json') if not f.endswith('.meta.json') and any(d.get('settled') and d.get('panel')=='research' for d in (json.load(open(f,encoding='utf-8')).get('readiness') or {}).get('blocker_details') or []))) "` | 無 |｜⚠ **2026-09-11 實測後收窄影響面**：卡死條件**不是「軸 unknown」**，是**「`catalysts` 段整段空的」**。GFS 當天在沒有本條修法的情況下做到 ready——catalyst 軸仍是 `missing`（誠實的 unknown 沒被掩蓋），但只要 `catalysts[]` 放進一個有日期的真實監看事件（GF／MPS 2026-09-09 協議，expected_at 2027-03-31），該段就由 `missing` 變 `partial`，而 readiness 規則是「available／partial 都算有內容」。所以本條**不是段 5 全體的前置**，只在「完全找不到任何可監看事件」時才真的必要——那時 ABSTENTION_LAYERS 加 `research` 層仍然是唯一出路。優先序應隨之下調。｜🔶 **2026-09-11 機制交付（GO；使用者定案「照你說的做」）；現有資料 0 筆改變**：`ABSTENTION_LAYERS` 加 `research` 層、`ABSTENTION_SUBJECTS["research"] = ("axis.catalyst",)`——**只開一個 subject**，不是把五個軸一次打開（L17-4：general 到資料支持的那一格為止）。消費端在 `builder` 的 `catalyst_section`：`select_abstention` 有紀錄時 `absence_kind` 由 `not_yet_recorded` 變 `deliberate_abstention`，**status 仍是 `missing`、readiness 仍是 `blocked`**（AGENTS「settled 不讓 readiness 變好」），reason 逐字帶出 `reason`＋`revisit_when`＋`abstention_id`。選取規則與估值層**同一套**，沒有為新層另寫一份。<br>⚠ **誠實說清楚：現有 73 檔一筆都沒變。** 段 5 未到終局的 59 檔全部是 `research=not_yet_recorded`，而那是因為它們連 thesis／variant view 都還沒做——panel 的 `absence_kind` 取「第一個 status 等於 panel status 的來源 section」，所以 catalysts 一格 settled 還不足以讓 panel settled。**這個機制只在「其餘都做完、只剩 catalyst 找不到任何具名事件」時才會生效**，而今天沒有任何一檔在那個狀態（GFS 09-11 已自行做到 ready）。依 L14-1 這代表**還沒改到 binding constraint**，所以維持 🔶 不標 ✅。<br>驗收（換掉 09-11 那版仍驗不到目標的）：`python -c "from alpha.abstention.contracts import ABSTENTION_LAYERS as L; print(L)"` 含 `research` ✅；段 5 到終局中**由 research 層 settled** 的檔數 ≥ 1 → **現況 0**。機制本身已可證偽：`tests/test_absence_semantics.py` 四條（選取規則、settled 不改 readiness、呈現層不得自己打標籤），另以 000660.KS 實跑證明 `not_yet_recorded → deliberate_abstention`（記憶體注入，未寫 ledger）。
| ✅ **intake 沒有「更正既有 extraction」的路徑，而唯一的繞道會製造假的第二來源**（新增 2026-09-10） | `intake/provenance.py::inspect_provenance` 是 **no-clobber**：既有 `doc_id` 的 extraction 只要內容 hash 不同就回 `conflict`，RA 直接 rejected。設計意圖正確（發布後不可竄改），但**沒有配對的更正走廊**——`intake/` 全無 force／overwrite／supersede 入口，OPERATIONS 也沒有 re-extract 程序。⚠ 實測撞上（2026-09-10，段4 第一個 substitutability cluster）：`nvidia_sipho_blog_partner_roles` 的既有抽取只抓了各分組的**角色句**，夥伴公司名只存在於 `locator` 的描述裡、**沒有任何 quote 逐字包含公司名**。依 L6-③（具體公司名必須在 quote 裡逐字出現），圖因此**無法支撐任何「NVIDIA 具名了誰」的主張**——而那正是 substitutability 唯一合格的證據型態（L8：客戶端具名）。本輪已回一手取得逐字：『**Foxconn and Fabrinet:** These partners provide expertise in system-level CPO assembly and testing…』與『**Browave, Corning, Senko, TFC Communication, and Coherent:** …』。**研究做完了，卻沒有合法載體把它放進去。**⚠ 唯一的繞道是換一個新 `doc_id` 指向**同一個 URL**，那會在圖裡產生兩份 SourceDoc、同一個 `origin_entity`——正是 L8 要防的假交叉驗證形狀（雖然 origin_entity 去重與 `bottleneck.py` 已排除 documents 計數兩道控制把風險包住，但它會安靜地累積）。**因此不自行決定**：要開更正走廊（例如 `supersede_doc_id` ＋ 兩份都留）還是承認「同 URL 多 doc_id」是既有慣例（現行 `..._partner_roles` 的命名本來就把 doc_id 綁到頁面的一個 section），是使用者的決定。草稿已備妥：`library/leads/action_drafts/nvidia_cpo_partner_roster_substitutability_2026_09_10.json`（Fabrinet→CPO 封裝組裝 substitutability 3、Coherent→fiber attach 2，兩者 sole_source=false）。 | `python scripts/prepare_research_action.py --action-file library/leads/action_drafts/nvidia_cpo_partner_roster_substitutability_2026_09_10.json` → 現況 `rejected: conflicts=[...]`；驗收是**這份草稿能被 prepare 成 pq2 編號**，以及全圖 `substitutability` 覆蓋率由 78/523 開始上升 | 無 |｜✅ **2026-09-11 交付**：`intake/provenance.py` 新增具名更正走廊。合法更正三條件缺一即照舊 conflict：①新 extraction 明寫 `supersedes_extraction_sha256`；②該值**等於現存檔案的 hash**（必須指名取代哪一版，不能說「取代現在那份不管它是什麼」）；③舊版歸檔到 `extractions/superseded/<doc_id>.<hash8>.json`，raw 同步歸檔——**兩份都留**。分界是「更正讓兩版並存且指得出來，竄改讓舊版消失」。no-clobber 沒有被放寬：未具名的改寫仍然 rejected。測試 tests/test_intake_supersede.py（4 條）。
| 🔴 **Engine C 快照的 `gross_margin` 是 TTM、`operating_margin` 是單季，兩個相鄰欄位沒有任何期間宣告**（新增 2026-09-11） | `financial_snapshots` 同一列的兩個毛利欄位取自不同期間，實測逐位吻合：000660.KS 的 `operating_margin` **0.76328003** 等於 **2Q2026 單季**營益率 60,542,608÷79,318,746 = **0.7632825**（DART 반기보고서 rcpNo=20260814003509 逐字），而同列 `gross_margin` **0.76268** 等於 **TTM** 毛利率 144,276,245÷189,170,615 = **0.7626779**；該檔真正的 TTM 營益率是 **68.04%**，差 **8.3 個百分點**。⚠ 這不是單一檔的髒資料：`operating_margin > gross_margin`（同期間結構上不可能）在全庫 **73 檔中命中 4 檔**（000660.KS、MU、SNDK、5016.T，共 66 列快照）——而那個偵測器只抓得到「單季營益率大於 TTM 毛利率」這種極端，**只要毛利率在趨勢中，兩欄就一定不同期，只是看不出來**。受害最深的正是利潤率劇烈變化的公司，也就是最需要看清楚的那一批。⚠ 後果是安靜的：任何讀快照寫 `value_capture`／`gross_margin_trend` 的判斷都可能把單季當年度；本輪 000660.KS 的四軸判斷因此**刻意只引用 `gross_margin`、營益率一律取法定文件**，但那是靠人記得，不是機制（L16-2：修法是把分類附到 payload 上，不是再寫一份文件叫人記得去查）。⚠ 屬 L12（一表兩義）：修法不是挑一個期間，是**讓每個數字自己帶期間**（例如 `gross_margin_period`／`operating_margin_period`，或拆成 `*_ttm`／`*_mrq` 兩組欄位），然後 consumer 端拒絕跨期相比。動到 `FundamentalsSnapshot` 契約與全部消費端，屬 **Z2，要 proposal**。 | `python -c "import sqlite3;c=sqlite3.connect('file:library/private/engine_c/stockbot-engine-c-private-v1-458db5270ee2.db?mode=ro',uri=True);print(c.execute('select count(distinct ticker) from financial_snapshots where gross_margin is not null and operating_margin is not null and operating_margin > gross_margin').fetchone()[0])"` → 現況 **4**（可比 73 檔）。⚠ **這個數字是下限，不是問題的大小**（2026-09-11 當天補）：偵測器只抓得到「單季營益率 > TTM 毛利率」，所以**營益率為負的公司一律漏掉**。實測 AAOI：`operating_margin` −0.12884 逐位等於 Q2-2026 單季（−24,727/191,922），`gross_margin` 0.28916 是 TTM（172,330/595,970）——同一個缺陷，但因為是負數而不在那 4 檔裡。也就是說 4 是「連結構上不可能都被違反」的極端案例數，真正受影響的是**所有利潤率在變動的公司**。⚠ **驗收不是把它壓成 0**——那只要改取值就做得到，卻沒解決「兩欄不同期」本身。真正的驗收是：**每個 margin 欄位都帶得出自己的期間，且 consumer 在期間不同時拒絕相比**（現況 0 個欄位帶期間）。 | 無 |
| 🔴 **入圖後自動追蹤把非公司節點也建成 cohort，於是長出結構上不可研究的 pq2**（新增 2026-09-11） | `engine_b/todo.py::complete_ra_admission` 無條件把 applied lead 的 `focus_company_id` 交給 `decision_lab.workflow.ensure_shadow_for_company`，而後者要求回傳有效 `dc_*` receipt，否則 raise。實測：pq2 **[533]**（HBM 稀缺傳導到中國 AI 加速卡售價，2026-09-11 核准入圖）的 lead focus 是 **`tech:hbm`**，於是替一個**技術節點**建了 research-intent cohort `dc_bb19966e`，identity 永遠 `unresolved`——**不是還沒登記，是沒有東西可登記**（`config/company_identity.json` 只收公司）。它接著每次 `todo sync` 都被鑄成一個 `decision_review` pq2（本輪 [540] park 後立刻再生 [547]），而 `standing-go` 因為它屬常規授權類別會自動把它派進 pq1——一個**永遠完成不了的工單每輪都吃一個 drain 名額**。⚠ 三個相鄰但不同的缺陷，修法不同不要混：①**建立端**：`ensure_shadow_for_company` 應只接 `co:` 前綴，但拒絕之後 `complete_ra_admission` 要拿什麼當 receipt 是**動到入圖 gate 的收尾契約**——不自行決定。②**收集端**：pq2 collector 對 `identity.status == 'unresolved'` 的 cohort 仍鑄號；既有的 `system_internal_only` 分支已經是同一個形狀（『新出現的純系統狀態不建立 pq2』），但沿用它會把 `resolution` 寫成 `system_internal`，而那是另一件事——**要新增一個 resolution 值＝動到封閉字彙，屬 Z2**。③**blocker registry**：`identity_unresolved` 的 `next_step` 寫「在 `config/company_identity.json` 登記」，那句話對「公司還沒登記」是對的，對「主詞不是公司」是錯的（L16：一個代碼承載兩種情況）。⚠ 順帶釐清一個會被誤讀的計數器：`counters.orphan_cohorts` 現況 **9**，但**不是同一種東西**——8 個是未上市公司（co:agility_robotics ×4、co:proterial、co:hyperlight、co:niron_magnetics、co:ewellix，`bind_cohort_identity` 要 company_id 與 ticker 成對才綁，未上市因此 company_id 恆 null，**這是正確行為**），只有 1 個是非公司節點。這個計數器自己也是一表兩義。 | `python -c "import sqlite3,json;c=sqlite3.connect('file:library/private/decision_lab/decision_lab.db?mode=ro',uri=True);rows=c.execute(\"select d.cohort_id,e.payload_json from decision_cohorts d join decision_events e on e.cohort_id=d.cohort_id and e.event_type='qualified_signal' where d.company_id is null\").fetchall();print(sum(1 for _,p in rows if '入圖後自動追蹤 co:' not in json.loads(p).get('raw_text','')))"` → 現況 **1**（非公司節點的 cohort 數）。驗收是**這個數字歸零且不再增加**，以及**同一個 orphan cohort 不再每輪鑄一個新 pq2 編號**（現況每次 sync 生一個：[540]→[547]→…）。 | 無 |
| 🔴 **殘餘缺口已鑄成 Engine C 觀測 pq2 之後，collector 仍為同一個缺口鑄一個「去做研究」的 pq2**（新增 2026-09-11） | 一個 cohort 的研究做完後，殘餘 blocker 常常只剩 `financial_backlog_manual_required`／`financial_customer_concentration_manual_required`——也就是**值已經找到了，等的是使用者核准寫入 append-only ledger**。但 `_collect_decision_rows` 只看 blocker，不看『這個 (ticker, field) 是不是已經有一筆 pending 提案』，於是同一題同時出現兩個編號。實測（2026-09-11 本輪）：[543] GFS/backlog、[546] GFS/customer_concentration 已在池裡，collector 仍鑄 **[548]**『co:globalfoundries：補客戶端商業承諾』要求做研究；SK hynix 同形（[544]／[545] vs **[549]**）。⚠ 這正是 `AGENTS.md` 開宗明義要防的『同一題會在研究前與入圖前問兩次』，只是發生在另一個位置。本輪的繞道是人工把 [548]／[549] checkpoint 成 `awaiting_approval --awaiting-gate`（指向 [543]／[545]），**機制上該自動發生**：collector 看得到 `engine_c.pending_observations` 的 (ticker, field)，只是沒去看（L16-1(b)：分類有 SSOT，沒送到消費端手上）。⚠ 修法要小心別做成「有提案就整項消失」——那是靜默 drop（INV-3）；正確形狀是**帶著 gate pointer 入池**，使用者看得到它在等哪個編號。動到 pq2 鑄號規則與 `waiting_on`／`awaiting_gate` 的語意邊界，屬 **Z2，要 proposal**。 | `python -c "import json;from identity.registry import get_registry as g;r=g();p=json.load(open('library/leads/todo_pool.json',encoding='utf-8'));act=[i for i in p['items'] if not i.get('resolved_at')];obs={(i.get('title') or '').split('：')[-1].split('/')[0].strip() for i in act if i['type']=='engine_c_observation'};dr={str(r.research_ticker(i['company_id']) or '') for i in act if i['type']=='decision_review' and i.get('company_id')};print(sorted(obs & dr))"` → 現況 `['000660.KS', 'GFS']`（兩檔同時掛著「等核准寫入」與「去做研究」兩種編號）。⚠ **判準是交集不是計數**：先前寫成「標題含『補客戶端商業承諾』的項數」會把 [200]／[522]／[526] 這些真的還沒做研究的項目一起數進去（實測 5 而不是 2）——一個會誤報的驗收條件比沒有更糟。驗收是**這個交集恆為空**（同一檔不得同時有『去研究』與『等核准』兩個編號），不是「改動成功」。 | 無 |｜✅ **2026-09-12 交付（使用者指示）**：`intake/provenance.py` 加 `_supersede_completion_receipt()`。放行條件只有一個、而且是**既有補償控制的引用而非新條件**：現存收據指著的那一版 extraction 必須已歸檔在 `superseded/`——那個檔只會由 `publish_provenance` 的 supersede 分支產生，所以它等於一張「更正走廊確實走過」的收條。舊收據同樣歸檔到 `library/private/intake_state/superseded/<doc_id>.<hash8>.json`，**兩份都留**。驗收（L14-1，現有資料真的變了幾筆）：**1 筆**——同一個 action_id＋digest 重跑，`partial 0ba4f844 ddb48d7b` → **`applied 0ba4f844 0ba4f844`**（上一欄那條命令逐字可重跑）；`commit_pending_intake` 記成 `out_of_band`（commit 9937b84），`todo complete-ra 542` 通過，**[542] 以 `go` 結案**、receipt 帶真實 digest＋cohort `dc_b05cc136`。四條測試（`tests/test_intake_completion_corridor.py`）含**收緊面**：沒有歸檔證據時改寫收據照樣被拒。非空洞性已驗——把走廊函式換成 no-op 就重現原本的 `no-clobber conflict`。修法層級＝**根除**（對稱面補上，不是繞道）。⚠ 一併記下那條驗收的教訓：原列寫的是「這份草稿能被 prepare 成 pq2 編號」，而 `prepare` 不碰 publish，所以 `apply` 從來沒被走過——驗收要寫到**下游消費者**那一端（L13-1）。
| 🔴 **段 5 的工單把「需求側節點」與「供給側候選」當成同一種東西**（新增 2026-09-11） | `alpha/closure.py` 的 backlog 是「每一份 materialize 過的 analyst view」，而 analyst view 是照 registry 生的——於是**只在圖裡當客戶的實體也被排進閉環工單**。實測 2026-09-11（未 ready 的 61 檔，依 `SUPPLIES_TO` 出／入邊分類）：**① 只有入邊、沒有出邊的純需求側 6 檔**（2455.TW、AAPL、GOOGL、HEXA-B.ST、JBL、META）；**② 完全沒有 supplies_to 邊 21 檔**（AMD、ANET、APO、BX、CCXI、CDNS、CRWV、GXO、IREN、MSFT、NBIS、ORCL、SNDK、TSLA、TXN、XFAB.PA、XPEV、2301.TW、4971.TWO、6481.T、688836.SS）；**③ 有出邊但一條都沒填 `substitutability` 26 檔**；**④ 真的算得出 Q1 的只有 8 檔**。⚠ 對 ①②，四軸的問句本身型別不符：`value_capture` 問「能不能把結構重要性轉成 economic rent」，而圖根本沒有把它們模型化為供給側；硬寫只會產出一份看起來完整、實際上在回答一個沒被問的問題的判斷（L14-1 的反面）。⚠ **但 ① 裡有兩檔是分類錯誤而不是型別不符，這一點必須分開**：`co:vpec`（2455.TW）只有三條 COMPETES_WITH（對 co:iqe／co:landmark_optoelectronics／co:intelliepi）與一條來自 co:axt 的入邊——**它是磊晶片供應商，而它的直接競爭者 co:iqe 有帶 substitutability 的 supplies_to 出邊**；`co:jabil`（JBL）只有 DEVELOPS prod:jabil_sivers_1_6t_lro_module 與 co:sivers_semiconductors 的入邊。兩者都是**缺一條 supplies_to 邊**，不是「不是候選」。這正是段 4 覆蓋缺口判準②「同一次沒做完的拆解殘骸」的形狀。⚠ 修法方向（**不是自己決定**，因為它改變 pq2／閉環工單的組成）：候選判準要跟著資料走（L16）——例如 backlog 多一格 `graph_role`（supplier／demand_anchor／unmodelled），demand_anchor 的檔不進閉環工單而是進另一條「需求錨追蹤」清單（它們仍然有用：AI capex 的需求證據就住那裡），unmodelled 的檔則導向段 4 而不是段 5。⚠ 不得做成「悄悄過濾掉」——那是靜默 drop（INV-3），它們必須在某個計數器上看得見。屬 **Z2，要 proposal**。 | `python -c "import os,sys,json,glob,warnings;sys.path.insert(0,'.');warnings.filterwarnings('ignore');from dotenv import load_dotenv;from neo4j import GraphDatabase;load_dotenv();d=GraphDatabase.driver(os.environ.get('NEO4J_URI','bolt://localhost:7687'),auth=(os.environ.get('NEO4J_USER','neo4j'),os.environ['NEO4J_PASSWORD']));s=d.session();R={r['id']:(r['o'],r['i']) for r in s.run('MATCH (c:Company) OPTIONAL MATCH (c)-[o:SUPPLIES_TO]->() OPTIONAL MATCH ()-[i:SUPPLIES_TO]->(c) RETURN c.id AS id, count(DISTINCT o) AS o, count(DISTINCT i) AS i')};d.close();V=[json.load(open(f,encoding='utf-8')) for f in glob.glob('library/private/app/analyst_view/*.json') if not f.endswith('.meta.json')];V=[v for v in V if v['readiness']['state'] not in ('ready','ready_with_flags')];print(sum(1 for v in V if R.get(v.get('company_id'),(0,0))[0]==0), '/', len(V))"` → 現況 **27／61**（沒有任何 supplies_to 出邊的檔佔未 ready 的 44%）。驗收是**這個比例在閉環工單裡歸零**（它們被導到對的清單去，不是被刪掉），不是「改動成功」。 | 無 |
| 🔴 **registry 缺 `market_currency` 會讓 headline 整格消失，而錯誤訊息不指向 registry**（新增 2026-09-11） | `briefing` 算隱含報酬前要確認「fair value 的幣別」與「現價的報價單位」是同一把尺；身分來源是 `config/company_identity.json` 的 `market_currency`。**缺它時 headline 與 why 兩格直接 missing**，理由句是『unverified_unit：fair value 幣別或現價報價單位未知——不得假設同尺度』。那句話**是對的但不可行動**：它說了症狀，沒說去哪補（L16-2：修法是把分類附到 payload 上，不是叫人記得去查）。實測 2026-09-11：GLW 的四軸、六條假設、估值與 horizon 全部寫完後仍 `blocked`，追了兩層才發現 `co:corning` 在 registry 裡只有 `{company_id, research_ticker}`。同批缺的共 **11 家**（AAPL／ANET／APO／BX／CDNS／ENA.V／GLW／GOOGL／INTC／JBL／UMC），其中 GLW／INTC／UMC 都在段 5 的閉環佇列上——也就是這個坑本來會在同一輪再絆倒兩次。⚠ 本輪已把 11 家補齊，**每一筆都由價格來源自己確認**（yfinance `fast_info.currency`／`exchange` 實測：NYQ／NMS → USD 共 10 家，ENA.V 為 VAN → CAD），不是用「沒有外國後綴所以是美股」這種離線近似推的——AGENTS.md：報價單位 ≠ 結算幣別，不得為了通過驗證把報價單位改寫成 ISO code。⚠ **但補資料不等於修機制**：下一家 onboard 的公司若忘了填，同樣會安靜地卡住，而且症狀離成因兩層遠。候選修法：`audit invariants` 加一條 —— 凡 `research_ticker` 非空而 `market_currency` 缺的一律 FAIL，訊息直接寫出要改哪個檔的哪一格。動到 audit 的檢查清單（那是常駐計數器的一部分），屬 **Z2，要 proposal**。 | `python -c "import json;d=json.load(open('config/company_identity.json',encoding='utf-8'));c=d['companies'] if isinstance(d,dict) and 'companies' in d else d;r=list(c.values()) if isinstance(c,dict) else c;print(sum(1 for x in r if x.get('research_ticker') and not x.get('market_currency')))"` → 現況 **0**（本輪由 11 補到 0）。驗收**不是這個數字**（它已經是 0 了，而機制沒修）：驗收是**故意拿掉一家的 `market_currency` 之後 `python -m audit invariants` 會紅、且訊息指得出是哪一家哪一格**。 | 無 |
| 🔴 **`FiscalYearActuals.revenue` 是單一口徑，但有些公司的 GAAP 與 non-GAAP 連營收都不同**（新增 2026-09-11） | `alpha/fundamental/bridge.py` 算基期營益率時一律用頂層 `actuals.revenue` 當分母，而 `basis` 是由 `non_gaap.operating_income` 在不在決定的——**兩者可能不同口徑，契約沒有地方宣告**。實測撞上：Corning 的 core 口徑含 constant-currency 調整，**core sales US$16,408M vs GAAP net sales US$15,629M**（差 779M，5.0%）。若頂層 revenue 放 GAAP 的 15,629，基期 core 營益率會算成 20.22%，而公司印出的是 **19.3%**（3,160/16,408）。更糟的是共識**也在 core 口徑上**（provider 的 FY2025 year_ago_actual revenue 逐字就是 16,408,000,000），所以拿共識除以 GAAP 營收會把成長率算成 **+23.1%** 而不是 **+17.2%**——**高估 5.9 個百分點，而那 5.9pp 完全是匯率換算慣例造成的假成長**。⚠ 本輪的處置是**在那一筆觀測裡選了 core sales 當頂層 revenue、並把 GAAP 營收另存 `gaap.net_sales`，然後在 coverage_note 寫明「讀 GAAP 口徑請用那一格」**。這是可行的繞道，不是修法：它依賴下一個讀的人真的去讀那段 note（L16-2 正是禁這個）。⚠ 影響面目前只有 Corning 一家（本庫 46 筆基期觀測中唯一有 constant-currency 調整的），所以**不在沒有事實支撐的地方泛化**（L17-4）——修法方向是給 `revenue` 一個 `accounting_basis`，或把它下放到 `gaap`／`non_gaap` 兩個區塊各自持有，然後 bridge 取與 basis 一致的那一個。動到 `FiscalYearActuals` 契約與全部 46 筆既有觀測的解析，屬 **Z2，要 proposal**。 | `python -c "import json,sqlite3;c=sqlite3.connect('file:library/private/engine_c/stockbot-engine-c-private-v1-458db5270ee2.db?mode=ro',uri=True);n=0;\nfor t,v in c.execute(\"select ticker,value from manual_fields where field_name='fiscal_year_results'\"):\n    d=json.loads(v);g=d.get('gaap') or {};\n    if g.get('net_sales') and abs(g['net_sales']-d.get('revenue',0))/max(d.get('revenue',1),1)>0.01: n+=1\nprint(n)"` → 現況 **1**（只有 GLW）。驗收是**`revenue` 帶得出自己的 accounting_basis，且 bridge 取與 basis 一致的那一個**，不是把這個數字壓成 0。 | 無 |
| ✅ **2026-09-11 的 extraction 更正走廊少做了對稱面：`intake_state` 完成標記不能跟著更正，於是走廊對「已入圖的文件」完全不可用**（新增 2026-09-12） | 那一列（本表上方，✅ 2026-09-11 交付）給 `extractions/<doc_id>.json` 開了具名更正走廊：新 extraction 明寫 `supersedes_extraction_sha256`、值等於現存檔 hash、舊版歸檔到 `superseded/`。**那一半是對的、也實測生效了。**但同一條 publish 路徑還會寫第二個檔——`library/private/intake_state/<doc_id>.json`，它記 `extraction_sha256` 並由 `verify_graph_complete` 當成「圖是從哪一版 extraction 載入的」憑證。那個檔走的是 `_atomic_publish`（永不覆寫），而它**沒有配對的更正走廊**。⚠ 後果是走廊對它自己的目標案例不可用：**凡是先前已經入圖過的 doc_id，更正一定卡住**——而那一列自己舉的例子（`nvidia_sipho_blog_partner_roles`）正是已入圖的文件。實測（2026-09-12，使用者已 go 的 pq2 [542]）：`ra_ebcd479b6c6bf84b37c2f8f1b812d712` 要改 `co:micron_technology --supplies_to--> tech:hbm` 的兩個屬性，doc 是既有的 `mu_10_q_q3fy2026_20260625`。apply 的結果是 extraction 與 raw 都成功更正並歸檔、**Neo4j 的邊也成功寫入**，然後停在 `ValueError: no-clobber conflict at library/private/intake_state/mu_10_q_q3fy2026_20260625.json`。RA 因此停在 `state=partial`，`todo complete-ra 542` 回「Research Action 尚未完成 apply 與 publish」，**使用者已經核准的編號因此關不掉**。⚠ 這也暴露那一列的驗收條件停得太早：它寫的是「這份草稿能被 prepare 成 pq2 編號」——`prepare` 不碰 publish，所以 `apply` 這一段從來沒被走過（L13-1：驗收要寫成「產出出現在下游消費者手上」）。⚠ 修法方向：`record_graph_complete` 在既有 receipt 的 `extraction_sha256` 與新版不同、且 `extractions/superseded/<doc_id>.<oldhash8>.json` 存在（＝更正走廊確實走過）時，允許改寫並保留舊 receipt。補償控制沿用同一個（舊版可證已保存），不新開放行條件。⚠ **本輪不自行修**：它落在 `intake/provenance.py` 的入圖走廊上，而 `AGENTS.md` 明訂「動到四個人工 gate 之一 → 停下等人」。 | `python -c "import sys,json,io;sys.path.insert(0,'.');from intake.provenance import canonical_extraction_hash as h;e=json.load(io.open('extractions/mu_10_q_q3fy2026_20260625.json',encoding='utf-8'));r=json.load(io.open('library/private/intake_state/mu_10_q_q3fy2026_20260625.json',encoding='utf-8'));a=json.load(io.open('library/private/research_actions/ra_ebcd479b6c6bf84b37c2f8f1b812d712.json',encoding='utf-8'));print(a['state'], h(e)[:8], r['extraction_sha256'][:8])"` → 現況 `partial 0ba4f844 ddb48d7b`（RA 未完成；extraction 已是新版，receipt 還指著舊版）。驗收是**同一個 action_id＋digest 重跑 apply 回 `status: completed`、`todo complete-ra 542` 能結案**，不是「改動成功」。 | 無 |
| ✅ **apply 的 `status: partial` / `completed_document_count: 0` 不分辨「圖沒寫」與「圖寫了但收據失敗」**（新增 2026-09-12） | `_apply_research_action_impl` 的回傳在上述失敗時是 `{"status":"partial","completed_document_count":0,"document_count":1,"failed_doc_id":…}`。**但 Neo4j 的邊已經被寫進去了**——實測那條 `co:micron_technology --supplies_to--> tech:hbm` 的 `updated_at` 就是那次 apply 的時間、屬性正是核准的那組（`sole_source=false`、`substitutability=4`、670 字元的 rationale）。也就是「完成 0 份文件」這句話在圖已變更的情況下照樣回傳。⚠ 這是 L13-2 的形狀（成功與失敗在同一個訊號上同形），而且它**真的誤導了執行者**：2026-09-12 我讀到 `completed_document_count: 0` 就把 extraction 與 raw 復原成 apply 前的版本，**反而把「圖=新、檔案=舊」的不一致做出來**；是後來去查那條邊的 `updated_at` 才發現圖早就變了，再重跑 apply 讓兩邊回到同一版。⚠ 修法方向：回傳要能分辨三種狀態——圖未變更／圖已變更但收據未寫／全部完成；至少把「graph merge 已完成」這件事放進 `execution.documents[].result`，讓復原程序知道**不能**動檔案。現在的 `next_action: "Retry the same action ID and digest after recovery."` 沒有說 recovery 指的是什麼，而在圖已變更的情況下「復原檔案」正是錯的動作。 | `python -c "import sys,json;sys.path.insert(0,'.');import os,warnings;warnings.filterwarnings('ignore');from dotenv import load_dotenv;from neo4j import GraphDatabase;load_dotenv();d=GraphDatabase.driver(os.environ.get('NEO4J_URI','bolt://localhost:7687'),auth=(os.environ.get('NEO4J_USER','neo4j'),os.environ['NEO4J_PASSWORD']));s=d.session();r=s.run(\"MATCH (a)-[x:SUPPLIES_TO]->(b) WHERE a.id='co:micron_technology' AND b.id='tech:hbm' RETURN x.updated_at AS u, size(x.attributes) AS n\").single();d.close();print(r['u'], r['n'])"` → 現況 `2026-09-11T23:52:02… 670`（邊已是新版）。同時 RA 仍是 `partial`／`completed_document_count: 0`。驗收是**這兩個訊號不再互相矛盾**（回傳要說出圖已變更），不是把邊改回去。 | 無 |｜✅ **2026-09-12 交付（使用者指示）**：`intake/application.py` 加封閉字彙 `GRAPH_STAGES`（`not_started`／`merged`／`verified`／`projected`／`receipt_written`，**順序即進度**），`_load_extraction_impl` 逐步推進它，失敗時回傳帶 `graph_stage` 與 `graph_mutated`。分界就在 `merged`：在那之後圖已經變了，**復原檔案是錯的動作**。`next_action` 因此分成兩句——圖已變更時明寫「do NOT restore the extraction or raw files」，圖未變更時才說檔案還是 apply 前的版本。兩個欄位同時加進 `_safe_load_result` 的白名單（否則它們會在存進 action record 的那一刻被丟掉）並上浮到 `_apply_research_action_impl` 的頂層回傳——L16：分類要跟著資料**走到需要它的地方**，停在 `execution.documents[].result` 裡等於沒送到。⚠ 這一列的真實代價已寫在左欄：我自己就是被那個訊號誤導，照著把檔案復原，反而做出「圖=新、檔案=舊」的不一致。修法層級＝**顯形**（失敗仍會發生；改的是失敗之後看不看得出圖動了沒）。
| ✅ **品質計數器的「有折溢價主張」量到的是價格漂移，不是主張——13/13 本校準 ledger 的倍數桿恰好等於價格漂移的相反數**（新增 2026-09-12） | `closure.QualityScore` 用 `multiple_contribution = target_multiple / market_multiple_on_consensus − 1` 超過 `MULTIPLE_NEUTRAL_TOLERANCE`（0.005）就把該檔列進「有折溢價主張」，並要求「每一筆的 rationale 都必須指得出證據」。**但 `market_multiple_on_consensus = current_price / consensus_eps` 用的是最新一根 bar，而 ledger 裡的 `target_pe` 是當初校準那一天的價格算出來的定值。** 兩者同分母（同一個 `consensus_eps`），所以代數上 `multiple_contribution = calibration_price / current_price − 1` ——**它就是價格漂移，符號相反，與折溢價主張完全無關**。實測（2026-09-12，18 本 ledger）：13 本有卡片的校準 ledger，倍數桿與價格漂移逐檔對齊到小數第二位——AEHR −0.93%／漂移 +0.94%、AMAT −0.56%／+0.56%、AVGO +0.83%／−0.83%、FN −2.56%／+2.63%、GFS −2.12%／+2.17%、GLW −1.98%／+2.02%、HIMX −6.45%／+6.89%、INTC −2.54%／+2.60%、TSEM +2.38%／−2.32%、TSM +0.49%／−0.49%、000660.KS +2.21%／−2.17%、5802.T +6.02%／−5.67%、AXTI +6.39%／−6.01%。⚠ 而**真的主張折溢價的只有 2 本**（COHR 25.0x、LYC.AX 22.0x），它們的漂移是 +29.7%／+15.6%，解釋不了 −22.9%／−13.5% 的桿——那兩個是刻意選的整數目標倍數。⚠ **分類的 SSOT 早就存在、也已經跟著 payload 走了**：ledger 每筆都帶 `derivation`（`calibrated_to_market` ＝零折溢價／`independent` ＝有主張），16 本校準、2 本主張。計數器卻不讀它，改用數值門檻重算一份——**這正是 L16 的形狀**（分類有 SSOT、也送到手上了，消費端仍自己猜一份），而猜錯的方向固定偏向「看起來需要更多研究」：它會送人去替 12 檔找一份**依定義不存在**的折溢價證據。⚠ 這也是 L12：同一個 `multiple_contribution` 同時承載「主張了折溢價」與「校準之後價格動了」兩種語意，所以兩個修法方向都會壞——放寬容差會把 COHR 那種真主張一起放掉，收緊則讓每一檔在隔天開盤後都變成假主張。修法形狀照 L12：**先分開再各自定規則**——「有主張」讀 `derivation`；「校準後漂移」是**新鮮度訊號**，該說的是「這本 ledger 的校準價是 X 日的，重跑一次 valuation」，不是「去找證據」。⚠ **本輪不自行修**：`implied_return/contracts.py` 對這個數字寫著「放寬一個稽核用的計數器要人決定，收緊不用」，而把 12 檔移出「有主張」欄正是放寬；且它同時是卡片散文與計數器兩個 owner 的共用 SSOT（L17-2 的 Z2 檔次）。 | `python -c "import json,io,glob,os;print([(os.path.basename(f)[:-6], [json.loads(l) for l in io.open(f,encoding='utf-8') if l.strip()][-1]['derivation']) for f in sorted(glob.glob('library/private/alpha/valuation/*.jsonl'))])"` → 現況 16 本 `calibrated_to_market`、2 本 `independent`；而 `python -m webapp closure-gate` 同時印「倍數＝校準倍數 0 檔｜有折溢價主張 14 檔」。**這兩個數字互相否定。** 驗收條件是**對價格漂移不變**：ledger 一個字不改、只把行情快照換成新的一根，兩欄的分檔結果不得改變；且「有折溢價主張」欄應只剩 `derivation=independent` 那幾檔（現況 COHR、LYC.AX）。⚠ 驗收**不是**「計數器的數字變小」——那個用調容差也做得到，而調容差解不掉隔天又漂走。 | 無 |｜✅ **2026-09-12 交付（使用者指示）**：照 L12 的處方**先分開再各自定規則**，**沒有動 `MULTIPLE_NEUTRAL_TOLERANCE`**——那個數字的註解寫著「放寬一個稽核用的計數器要人決定」，而這次不需要放寬它。① 先讓分類跟著資料走（L16）：`multiple_derivation` 本來就在 artifact 裡，但住在**隔壁那條 line**，不在 `_find_attribution` 讀的那個 mapping 裡，所以計數器只好用門檻自己重算一份。現在 `ReturnAttribution` 帶 `multiple_derivation`（從 valuation ledger **原封帶過來，不在該層重新推導**），`attribution_payload` 一併輸出。② `QualityScore` 多一欄 `multiple_drifted`：`derivation=calibrated_to_market` 而桿非零 → **校準價過期**，不是主張。呈現層明寫「要的動作是重跑一次 valuation，不是補一份依定義不存在的折溢價證據」——兩欄要的動作不同，說不出來就等於沒分開（L13）。驗收（L14-1）：**12 筆真的變了**——`materialize` 後計數器從「校準 0／主張 14」變成**「主張 2 檔（COHR −22.9%、LYC.AX −13.5%）／校準價已過期 12 檔」**，而那 2 檔**恰好就是 ledger 裡兩本 `derivation=independent`**，與 SSOT 對上了。五條測試含**真正的那條驗收**：`test_classification_is_invariant_to_price_drift`——ledger 不動、只把漂移量換成 0.6%／2.1%／6.5%（代表三天行情），分檔結果必須完全相同。另含 fail-safe 方向的釘點：`derivation` 未宣告時**當成主張**（要人舉證），不當成漂移放過去——把未宣告當漂移會讓真的折溢價主張靜默免除舉證。修法層級＝**根除**（分類不再被重算）。⚠ 仍然開著的一格：`multiple_drifted` 只是**顯形**，它沒有讓過期的校準價自己更新。要它歸零得有「valuation 到期就重算」的機制，而那牽涉 append-only ledger 的 supersede 節奏，是另一題。
| ✅ **「入圖後自動追蹤」接受非 `co:` 的節點，而 cohort 的身分模型只有兩個出口（登記 ticker／等掛牌）——技術節點兩個都走不了，於是每次 reassess 就鑄一個新的 pq2 編號：`tech:hbm` 已燒掉 [540]／[547]／[551]／[554] 四個**（新增 2026-09-12） | 9 個 `company_id IS NULL` 的 cohort（44 個裡的 9 個）全部是「入圖後自動追蹤 <id>」自動建的。**其中 8 個追的是未上市公司**（`co:agility_robotics` ×4、`co:proterial`、`co:hyperlight`、`co:niron_magnetics`、`co:ewellix`）——它們有合法歸宿：`research_ticker_unavailable` 的 `resolution_mode` 是 `awaiting_external`，所以各自停在等事件並帶掛牌觸發條件（[200]／[421]／[407]／[492]／[493]），**一個編號用一次就停住了**。**第 9 個追的是技術節點 `tech:hbm`**，它兩個出口都走不了：永遠不會有 ticker（所以不能「登記」），也永遠不會掛牌（所以不能「等掛牌」）。它拿到的是 `identity_unresolved`（severity `fatal`、`resolution_mode` **`user_decision`**、`next_step`「提供 exact company_id 或在 config/company_identity.json 登記」）——一個結構上無法被滿足的要求。⚠ 於是形成閉環：進 pq2 decision_review → `go` 派回 pq1 bounded research → 研究無法替技術節點登記公司 → park → reassess → **鑄下一個編號**。實測四輪：[540]（2026-09-11 13:29 go → 14:24 park）、[547]（14:27 go → 14:29 park）、[551]（21:42 go → 21:48 park）、[554]（2026-09-12 00:02 至今開著）。**三次 `go` 全部由 `standing_authorization` 自動放行**（`decision_review` 的 go 被歸為注意力 gate），所以它不需要使用者按鍵也能持續消耗編號空間——而 pq2 是**使用者唯一的授權介面**。⚠ `AGENTS.md` 對這個形狀已有明文與先例：「collector 仍會重新推導的項目 drop 只會換號重生（`sheet_only` [18]-[33]→[46]-[60] 先例）——正確做法是修 collector 端分類」。**所以 `drop` 不是解，它是第五個編號。**⚠ 也是 L16 的形狀：blocker registry 的 `next_step` 早就說了該做什麼（登記公司），但 pq2 的 hint 渲染不讀它，一律印「go＝bounded research（派回 pq1）」——**系統要的動作與它對使用者提的動作不是同一件事**，這正是三次 `go` 零產出的直接原因。⚠ 修法有兩個方向，**刻意不在這裡選**（動到 blocker 封閉字彙與 collector 分類兩個 owner，L17-2 的 Z2 檔次）：①追蹤非公司節點是有價值的（HBM 本來就是瓶頸），那就給它自己的 lane——不帶 identity／financial／live blocker；②或者 auto-tracker 只接受 `co:` 前綴，技術節點改由 `event_watch` 追。⚠ **本輪不自行修**，但四個編號的帳要記在這裡，不要再記進 pq2。 | `python -c "import sqlite3,json;c=sqlite3.connect('library/private/decision_lab/decision_lab.db');ids=[r[0] for r in c.execute('SELECT cohort_id FROM decision_cohorts WHERE company_id IS NULL AND research_ticker IS NULL')];print([json.loads(c.execute('SELECT payload_json FROM decision_events WHERE cohort_id=? AND event_type=? LIMIT 1',(i,'qualified_signal')).fetchone()[0])['raw_text'] for i in ids])"` → 現況 9 筆，其中 8 筆是 `co:*`、1 筆是 `tech:hbm`。燒掉幾個編號另查：`python -c "import json,io;p=json.load(io.open('library/leads/todo_pool.json',encoding='utf-8'));print([(i['n'],i.get('resolution')) for i in p['items'] if i.get('ref_id')=='dc_bb19966e2aafab5e24f4fd74d7ed9443'])"` → 現況 `[(540,'go'),(547,'go'),(551,'go'),(554,None)]`。驗收是**兩面都要**：①`dc_bb19966e` 不再鑄出第 5 個編號；②那 8 筆未上市公司的 cohort **行為不變**（仍停在等事件、仍帶掛牌觸發條件）——只做到①很可能是把整個 auto-tracker 關掉，那會一起關掉會動的那 8 筆。 | 無 |｜✅ **2026-09-12 交付（使用者指示）**：兩端都堵，而且判準是**實測分出來的**、不是我原本推的那一個。⚠ 先記下修法過程中的一次自我更正：我第一版寫「主詞不是 `co:` 開頭就抑制」，實測發現**brief 根本沒帶主詞**——`tech:hbm` 在 `capture_signal` 就被 registry 依 INV-1 擋成 None，所以那個判準永遠不會觸發。真正乾淨的分野是量出來的：9 個 `company_id IS NULL` 的 cohort 中，**8 筆未上市公司的 `company_id_hint` 是 `co:*`**（主詞在，只是還沒掛牌，走 `research_ticker_unavailable`＝`awaiting_external`），**只有技術節點三個身分欄位全空**。所以判準是「完全沒有可識別主詞」——這時 `identity_unresolved` 的 next_step（登記 company_id）**連使用者也執行不了**，他得先去翻 `decision_events` 才知道要登記誰。① 源頭：`ensure_shadow_for_company` 對非 `co:` 主詞直接回 `skipped: subject_is_not_a_company` ＋ reason，**明講拒絕了什麼**（INV-3：每個 filter 都要報得出 filtered／reasons），不靜默丟掉。② collector：沿用既有的 `system_internal_only` 抑制通道（不鑄號、既有項目留 deterministic retirement audit、計數器照加），但把硬寫的 receipt 改成**跟著 row 走**——先前那句 `blocker-registry:system_internal` 套在別的抑制成因上就是假的（L11-1／L16）。③ 一併補上源頭修法的副作用：focus 是技術節點的 RA 會拿不到 cohort，於是 `complete_ra_admission` 會擋下它——那是把「不該建公司 cohort」換成「入圖核准永遠結不了案」，同一個病換位置。照 `commit:not_required` 的先例，receipt 允許 `cohort:not_applicable`，**把缺席寫出來、不假造一個 `dc_`**。驗收（兩面都測到，L14-1）：①`dc_bb19966e` 的編號序列停在 `[(540,go),(547,go),(551,go),(554,system_internal)]`——**沒有第 5 個**，retirement 留在 log 的 `cohort-without-subject:dc_bb19966e…` receipt 上；②[200]／[407]／[421]／[492]／[493] 五筆未上市公司實測 `resolution=None`、`waiting_on` 仍在，**行為不變**。五條測試（`tests/test_cohort_without_subject.py`）含既有 system_internal 路徑的回歸釘點與 receipt 的收緊面。修法層級＝**根除**（源頭不再產生這種 cohort）＋**顯形**（既有那一個留下可稽核的退場）。⚠ 仍未做、也刻意不做：**給非公司節點一個自己的 lane**。追蹤 HBM 本身有價值，但那是新功能而不是這個 bug 的修法；要做時的判準已寫在左欄（不帶 identity／financial／live blocker）。
| **橋的口徑選取要跟著「已核實的共識口徑」走，不是「有沒有 non_gaap 區塊」**（2026-09-12 research-drain 記；**Z2——等你點頭**） | `alpha/fundamental/bridge.py` 第 82 行：`basis = non_gaap if 基期有 non_gaap.operating_income else gaap`。這條規則**只認得 COHR 那個案例**（共識是 non-GAAP）。2026-09-12 撞到相反的一例：**CDNS 的 0y 共識是 GAAP**（year_ago_actual 4.06 ＝ 10-K 的 GAAP 稀釋 EPS，13 位分析師），若基期照實填 non_gaap 區塊，橋會算出 non-GAAP 的內部 EPS 再去對一串 GAAP 共識，得到約 **+70% 的假差距**——而 `verify_consensus_basis` 其實**已經知道**共識是 GAAP，只是那個結論沒有送到橋手上（L16：分類要跟著資料走）。本輪的繞法是把 non-GAAP 數字改記在 `non_gaap_reference` 鍵（原樣進 author_notes），資料保住、口徑不被切換——**那是繞過不是修好**。 | 修好後：`build_bridge` 多一個 `consensus_basis` 參數（或改由 read model 傳入已核實口徑），**CDNS 的基期可以照實填 `non_gaap` 區塊而內部 EPS 仍留在 GAAP**；驗收數字＝現有到終局各檔的 `internal_eps` 一個都不變（口徑判定與今天一致者結果必須相同），而 CDNS 由「靠 `non_gaap_reference` 繞過」變成「照實填 `non_gaap` 也正確」。查證：`python -m briefing valuation CDNS | grep 口徑`；`python -m pytest tests/test_fundamental_model.py -q` | 無（但**動到 build_bridge 的簽章，既有 35 本 ledger 全是消費端**，所以是 Z2 不是當下修） |
| **共識序列自我矛盾要有常駐計數器（0y 的估計 ≠ +1y 的 year_ago_actual）**（2026-09-12 research-drain 記；**Z2——等你點頭**） | 同一批 yfinance 共識裡，`0y.estimate_avg` 與 `+1y.year_ago_actual` 講的是**同一個會計年度**，對不上就代表兩列不是同一串數字。實測 CDNS：0y EPS 4.80568（13 位、GAAP）vs +1y 的 year_ago_actual 8.14245（25 位、non-GAAP），差 **69.4%**；兩列相除會得到「一年成長 +98.6%」，**完全是口徑切換的假象，而沒有任何欄位會報錯**。全庫掃描 144 組 0y 列共 3 組中招：CDNS 的 eps、6680.HK 的 eps（20.5%）與 revenue（5.0%）。⚠ 本輪已把偵測放進 `scripts/closure_probe.py`（每檔開工前會印），但那是**互動 session 才會跑的路徑**——偵測有了、常駐消費端還沒有（L17-3③：偵測做了誰消費）。 | 修好後：`python -m webapp closure-gate` 的品質計數器多一行「共識序列自我矛盾：N 檔（逐檔列 metric 與差距）」，**每輪自己出現**（L14：真正的防呆是會自己出現的常駐計數器）。驗收數字＝該行首次出現時 N **＝ 3**（CDNS eps、6680.HK eps／revenue），且 ANET／AAPL／AMD 不在列上。查證：`python -m webapp closure-gate | grep 自我矛盾`；`python scripts/closure_probe.py CDNS | grep ⚠⚠` | 無（`closure-gate` 目前只讀 artifact，要多讀 Engine C consensus_estimates，屬**輸出契約變更**） |
| **Engine C 沒有欄位承載「目標年度已報導的 YTD 實績」**（2026-09-12 research-drain 記；**Z2——等你點頭**） | `fiscal_year_results` 的設計前提是「目標期間完全在未來」，可選的 `exit_quarter` 講的是**基期最後一季**。但段 5 現在有 38 檔未到終局，而 0y 就是**當年度**——9 月時多數美股已報導 2~3 季。本輪四檔全部撞到：AAPL 已報 9 個月（佔共識全年 EPS 的 78%）、AMD／ANET／CDNS 已報 H1（48–72%）。這些 YTD 實績是**整個模型最硬的輸入**，目前只能寫進 OperatingAssumption 的 `rationale` 散文裡，`evidence_refs` 只指得到基期觀測——也就是**最重要的證據沒有 authority 載體**。 | 修好後：新增一個 `verifiability=mechanical` 的欄位（暫名 `interim_period_results`，value 帶 period_start／period_end／periods_reported／gaap／non_gaap 區塊），假設的 `evidence_refs` 指得到它。驗收數字＝本輪四檔（AAPL／AMD／ANET／CDNS）各補一筆後，`revenue_growth` 與 `operating_margin_delta` 的 `evidence_refs` 由 1~3 條變成含 YTD 觀測，且 `internal_eps` 一個都不變（補的是 provenance 不是值）。查證：`python -c "from engine_c.observation_fields import get_observation_field_registry as g; print(g().mechanical_field_names)"` | 無。⚠ **為什麼不是當下修**：新增 `mechanical` 欄位＝新增一條**不需 pq2 的寫入走廊**，而 `GATE_FIELDS` 長度被 `tests` 鎖住、字彙有行為後果（L16-3）——放行與收緊必須同時發生，這題要你點頭 |

---

## 產品決策：主流程的終點是 Implied Return，Entry Threshold 是 optional（2026-09-07 使用者定案）

> **stock-level 主流程：**
> `Evidence → Internal Forecast → Valuation／Future Target Value → Horizon → Implied Return`
>
> **終點是 Implied Return。** `EntryCriterion`／hurdle 是 **optional analytical capability**——
> **不是必填資料，也不是 research completeness gate。**

四條直接後果（違反即是 bug，不是風格問題）：

1. **沒有 hurdle 不得把股票標成「研究不完整」。** 只表示 *optional entry threshold unavailable*。
   Consumer 的 core readiness **只看核心四段**（headline／fundamental／why／research），
   optional panel 一律不參與。查證：
   `python -m briefing analyst-view COHR --format json | python -c "import json,sys;v=json.load(sys.stdin);print(v['readiness']['state'], v['readiness']['blockers'], v['readiness']['optional_unavailable'])"`
   ——`optional_unavailable` 會列 `entry：missing`，而 `blockers` 不因它增加。
2. **不要求使用者為了讓系統有答案而宣告固定的 10%／15%／20%。** 系統寧可誠實說「這一格沒有」，
   也不補一個看起來完整的預設值——補上去之後，門檻價會變成一個沒有人宣告過卻看起來像判斷的數字。
   （`alpha/entry/` 的原始碼裡不存在可當預設值的 float 常數，`tests/test_entry_logic.py` 用 AST 掃描守著。）
3. **主流程與 optional 能力的缺席理由不得同形。** 主流程缺一格 → core readiness `blocked`＋blocker 逐條列出；
   optional 缺席 → 只在 optional 欄現形。這是 L12（一個表示兩種語意）在消費端的應用。
4. **機會成本、風險調整後 hurdle、跨標的比較留給 Portfolio／Investor Policy 階段。**
   今天的 hurdle 是**單檔、單一 convention（年化要求價格報酬）、投資人明示**的東西；
   把它變成「跨標的比誰划算」需要先有 opportunity-cost 的定義與資料，而那不在 Alpha 這一層。

⚠ 這條決策**不放寬任何 gate**：live 資本仍 100% 人工，系統仍不給部位尺寸。

---

## Post-MVP Alpha Edge Roadmap（2026-09-06 記下；**本輪只記 roadmap，一項都沒實作**）

> **核心原則（先寫在最前面，因為它決定其他七項的成敗判準）：**
>
> **StockBot 不應為了製造 alpha 而強迫 Internal Forecast 與 analyst consensus 不同。沒有
> differentiated evidence 時，收斂到 consensus 是合法且健康的結果。Graph 的 mission 是找出
> 市場可能尚未充分反映的 causal dependencies，讓差異透過 explicit operating assumptions 進入
> deterministic earnings model，最後能 attribution 與驗證。**

這條原則的直接後果：**「內部 EPS 跟共識一樣」不是失敗，是一次誠實的觀測**；真正的失敗是
「差異存在但說不出它從哪條 edge 來」，以及「差異存在但事後沒人檢查它對不對」。
**2026-09-09 補：同一條原則適用於目標倍數**——沒有 re-rating 證據時預設等於校準倍數，隱含報酬固定拆成
EPS 貢獻與倍數貢獻兩欄（判準全文在 `AGENTS.md`「隱含報酬的兩個桿」；落地是下表「研究閉環」P2）。
以下 A–F 是把這條原則變成可執行系統的順序；G 是模型 backlog；H 是與消費端的相對順序。

### A. Graph → Fundamental Attribution

把 graph-derived belief 明確連到 operating assumption，再量化每條 variant belief 對
Revenue／Margin／EPS 的 contribution。未來應能回答類似：

```
Structural bottleneck   +0.55 EPS
Margin view             +0.30
Industrial weakness     −0.18
Interest assumption     −0.20
─────────────────────────────
Net variant vs consensus +0.47
```

**重點不是只顯示 Internal EPS vs Consensus，而是回答「為什麼不同」。**
（今天 `alpha/fundamental` 已有 `sensitivities`，但那是「動一格變多少」，不是「這個差異來自哪條 belief」。）

### B. Consensus Implied Assumptions／Reverse Bridge ✅ **2026-09-11 交付**

> **交付的比原構想窄，而那是刻意的。** 原文想「反推市場大致隱含的 growth／margin／other
> assumptions」——那是欠定的（共識只給一個總量）。實作改成**一次只解一個 driver、其餘固定
> 成我們自己的假設**，所以每個值都是條件解，畫面與 `method` 都逐字說出「兩列不能同時成立」。
> 落地：`alpha/reverse/`（二分求解，區間照抄 `ASSUMPTION_DRIVERS` 的合法上下限，
> **走真正的 `build_bridge`**，不另立第二套算術）→ `ExpectationGapSection.reverse_bridge`
> → APP「現價隱含什麼」。
>
> 實跑：COHR 在 25x 下市場隱含 EPS 12.14（我們 8.94、共識 9.42），要獨力撐起需 D&C 成長
> +126%（我們 +60%）；LITE 我們比市場樂觀 7.2%；TSM／AVGO 全部 `already_equal`——那是
> `consensus_inverted` 的**機械證明**，不是推論。
> 查證：`python -m pytest tests/test_reverse_bridge.py`
>
> ⚠ **EV/Sales 的反解沒做**（SOI.PA 走那條）：那是另一條算術（營收 → EV → 每股），
> 誠實 `missing` 並說明，不共用本益比法這一條。**只有一個實例時不抽象**（L17-4）——
> 等第二檔走 EV/Sales 再做。

**兩根桿的 derivation 都已落地（2026-09-10 EPS 桿／2026-09-11 倍數桿）。** 實測 11 本
valuation ledger：8 本的目標倍數是「＝校準倍數」（抄市場），只有 COHR 25x 與 LYC.AX 22x
是判斷——而畫面上 COHR 的 25x 與 TSM 的 25.71x 原本長得一模一樣。
兩桿對照的現況（2026-09-11，會變，查證用 `python -m webapp materialize` 後讀 artifact）：
**只有 COHR 與 LYC.AX 兩檔是兩根桿都有自己的判斷**。

**留下的 debt（都不排程，理由各自寫明）：**

| 項 | 為什麼現在不做 |
|---|---|
| COHR 的 D&C 假設仍 `unclassified` | 被 `ew_0076` 以 `hypothesis_ref` 指著，supersede 會讓那個 watch 指向已取代的紀錄；watch registry 是另一個 owner 的 authority，不是「十行內、不動 contract」的檔次（L17-2）。**沒有行為後果**：COHR 的 stance 靠另外兩條假設已經是 `independent` |
| outcome 時序的歷史 | 2026-09-11 之前是覆寫制，**歷史結構上拿不回來**（不是讀不到）。畫面上明說了 |
| §A Graph → Fundamental Attribution | **分母太小**：9 檔 ready 只有 3 檔 independent，而 COHR 的 −26.3% 裡 −22.4% 來自倍數、只有 −5.0% 來自 EPS。等 independent 到兩位數再開——現在的瓶頸是研究量不是能力 |


在資料足夠時，從 consensus Revenue／EPS 等**反推**市場大致隱含的 growth／margin／other assumptions。
目的是把比較從最末端往上游移：

```
Our assumption            vs   Market-implied assumption
（+60% D&C 成長）              （反推出來的 ~+X%）
```

**不得假造市場沒有提供的精確 driver**——共識只給總量時，反推出的分項是欠定的，該說「解不唯一」。

### C. Lead Indicator／Event Propagation

利用 Graph 的 upstream／downstream dependency 捕捉：customer capex、capacity expansion／delay、
qualification、supplier bottleneck、competitor capacity、substitution、product ramp。
把新 evidence **定位到受影響的 assumption**，透過既有 Refresh／Event Watch 觸發 review。

目標是**可能在 consensus 更新前形成 justified variant view**；**不是 auto-LLM 改 EPS**
（假設的變更永遠是人的動作，refresh 只標 state）。

### D. Bottleneck／Substitutability／Timing Quality

讓 graph edge 不只是「A related to B」，而逐步增加真正影響 earnings 的：
direction、constraint、substitutability、timing、capacity、qualification delay、value-capture path。

**這是 Q1 → Q2 → Q3 真正產生 edge 的核心**——沒有這一層，A 的 attribution 只會把
「我們讀了幾份文件」當成 belief 強度。

### E. Cross-company／industry coverage

把 COHR vertical slice 擴成多公司、多供應鏈，測試 Graph 是否真的能發現
**company-centric analyst model 容易漏掉的二階／三階 exposure**。

### F. Outcome Learning／Alpha Validation

最終必須驗證：**Graph-derived variant assumptions 是否真的比 consensus 更早／更準？**
保存當時的 PIT view，事後對 actual 計算：forecast error、consensus error、variant contribution、
which graph edges helped／hurt。

**沒有這層就只能證明系統「很會解釋」，不能證明有 alpha。**

### G. Model expansion backlog

Valuation v2｜bull／base／bear scenarios｜probability-weighted expected return（**只有真的有
probability contract 才做**）｜total return｜historical normalized multiples｜
~~peer valuation~~（**2026-09-11 移到「明確不排程」，理由已量測**）｜
growth durability｜margin／ROIC quality｜cycle position。

### H. Consumer／production sequence（near-term 固定順序）

```
Step 3 Entry Logic ✅（2026-09-06）
  → Step 3.5 Analyst Consumer ✅（2026-09-07）
  → Step 4 Full-chain adversarial acceptance ✅（2026-09-07，GO）
  → Coverage Pilot（LYC.AX／6324.T／IQE.L）  ✅（2026-09-07，GO）
  → Step 5 APP／API                          ✅（2026-09-07，CONDITIONAL GO）
  → 呈現責任重切 B1 ranking／B2 beta／B2b coverage＋watches  ✅（2026-09-08，GO）
  → 呈現責任重切 A（daily 自動更新 APP）✅（2026-09-08）
  → 呈現責任重切 C（Daily 收斂成心跳）✅（2026-09-08）
  → B2-positions（部位與問責搬 APP）✅（2026-09-08）
  → APP 白話化＋單檔走勢圖 ✅（2026-09-08，GO）
  → derivation ＋ opinion stance（分開「還沒形成觀點」與「算過但接近共識」）✅（2026-09-10）
  → 清單分段 ＋ 首屏常駐計數器「有我們自己的看法 N／已有判讀 M」 ✅（2026-09-10）
  → Reverse Bridge（§B）✅（2026-09-11）
  → Phase 只剩：C 的「≤7K 實測待下一份 brief」（外部驗證，不需再開工）
     ⚠ 順序修正（2026-09-08）：**A 必須在 C 之前**——Daily 一旦不印，APP 就得每天被 materialize，
     否則使用者打開看到的是幾天前的數字（L13：管子只接一頭）。
  → Post-MVP Alpha Edge（A–F）
  → Portfolio／Risk later
```

**Step 5 ✅ 2026-09-07 交付並結案（原判 CONDITIONAL GO → 條件同日解除 → GO）**，報告見
[`docs/reports/2026-09-07-webapp-mvp.md`](reports/2026-09-07-webapp-mvp.md)。
產品 invariant **`LLM changes cognition; APP reads cognition`** 已落地，並由四種互相獨立的機械證明守著；
Coverage Pilot 留給 Step 5 的兩筆語意債（「刻意不主張」vs「還沒寫」、`accounting_basis` 字彙是美式的）**都已修**。

**⚠ 原判 CONDITIONAL 的條件已於同日解除。** 使用者完成 Cloudflare 三步後實測：
`https://stockbot.minatoyukina.uk` **未登入回 302 導向 Access 登入頁**（最重要的一條）、
登入後四檔清單可開；`mcp.`（無 token 回 404）與 `neo4j.` 未受影響。

**Google OAuth 也已生效**——Access event analytics：`Identity provider = google`、
`Users = c3035281@gmail.com`、`Granted 1`。⚠ **不是照文件步驟 0 手動設的**：新版 Cloudflare One
在建立 Access 應用程式的流程中已自動把 Google 加成 identity provider。
**開機自啟已完成**（`stockbotv2-graph-services.vbs` 檔尾追加 `serve`；抽成獨立 vbs 實跑驗證：
APP 起得來、無可見視窗、只綁 `127.0.0.1`；備份 ＋ diff 確認除檔尾 6 行外一個位元組未動）。

⚠ **結案不等於每一格都被驗過**（報告 §16.3）：Access policy 的內容由使用者目視確認、
**未經負面案例測試**（analytics 的「1 user granted」只證明你進得來，不證明別人進不來）；
Session Duration 是否兩層都設 1 個月未確認。

⚠ **Cloudflare 已把 Zero Trust 主控台改名 Cloudflare One 並重排側欄。** 交付當下文件寫的是舊版路徑，
實地確認後已回填 [`deploy/cloudflare/README.md`](../deploy/cloudflare/README.md)：
Access 應用程式＝`Access controls → Applications`（右上 `Create new application` → `Self-hosted and private`
→ **`Public DNS`**）；身分識別提供者＝**`Integrations → Identity providers`**（不再在 Settings 底下）。
沒有實地看過的那幾條（撤銷 session）在文件裡**明確標成「未實地驗證」**——不把猜測寫成事實。

**Step 5 留下的兩件待排事項**（都不在本 Step scope 內，寫在這裡待排程）：
1. **全 cohort materialize 排程**（2026-09-08 併入「呈現責任重切」Phase A，與 state kinds 一起做一次 sandbox review）——今天只有四檔有 artifact，要看別的公司必須先手動跑一次。
   自動化前必須先走 sandbox impact review 五步（`python -m webapp materialize` 是新的 executable surface）。
   驗收條件寫成「哪個數字會變」：`python -m webapp status` 的 artifact 數由 **4** 變成 N，
   且其中有幾檔 readiness 不是 blocked。
2. **IQE.L 的 blocker 分層**——今天停在 `upstream_unavailable`（刻意沒有 operating assumption）。
   pq2 **[486]**（IQE 流動性／going-concern 評估）完成之後它才會往下走到方法層。
   **屆時畫面上的字會自己改變，不需要改任何呈現程式碼**——那正是 `absence_kind` 由模型層宣告的好處。


**Coverage Pilot ✅ 2026-09-07 完成（GO）**，報告見
[`docs/reports/2026-09-07-coverage-pilot.md`](reports/2026-09-07-coverage-pilot.md)。
驗收條件「每加一檔，既有那幾檔的 baseline 數字必須 0 格改變」**已達成**（COHR 核心 10 格未動），
`tests/test_full_chain_acceptance.py` 的 35 條全綠。

~~**Pilot 留給 Step 5 的兩個前置提醒**~~ ✅ **2026-09-07 Step 5 兩條都已修**（都是呈現層問題，APP 會把它們直接放到使用者眼前）：
1. **「刻意不寫估值假設」與「還沒寫」目前是同一句話**（L12 一表兩義）。6324.T 的 blocker 逐字是
   「尚未寫入任何估值假設」，而真實狀態是研究結論「無法錨定倍數，所以不寫」。
   修法是給 ValuationAssumption ledger 增加一種 `declined` 紀錄（帶理由與「什麼證據出現才改寫」），
   **那是新增能力不是修 bug**，需要使用者排程。
2. **`accounting_basis` 字彙是美式的**：Lynas（AASB／IFRS）、HDS（日本基準）、聯亞（台灣 IFRS）
   都被標成 `gaap`。語意正確（as reported vs 公司調整後）但字面是錯的。
   **不得改字彙**——它是三本 private append-only ledger 每一筆紀錄的身分一部分（L10）；
   要做的是加一層呈現別名。

> ✅ **兩條都在 Step 5 修掉了**（見 [`2026-09-07-webapp-mvp.md`](reports/2026-09-07-webapp-mvp.md) §6／§7）：①新增 `alpha/abstention/`（append-only、結構上不可能攜帶數字、`reason` 與 `revisit_when` 必填）＋ `alpha/absence.py` 的 `absence_kind` 封閉字彙；6324.T 已寫入 `ab_fc42c0f42c2df1ce`（pq2 [487] go），缺席語意由 `not_yet_recorded` 變成 `deliberate_abstention`，而 **fair value 仍 `None`、readiness 仍 `blocked`**。②**字彙一個字沒改**（L10），加的是呈現別名 `ACCOUNTING_BASIS_DISPLAY`，`gaap` → 「As reported（法定財報口徑）」，label 一律不含準則名稱。
> ⚠ **①的實作與上面寫的計畫不同，理由要記下來：** 上面說「給 ValuationAssumption ledger 增加一種 `declined` 紀錄」，實際做成**另一本 ledger**。原因是 `ValuationAssumption` 的每一條驗證都圍繞著「有一個數字」建立（`value: float` 必填、單位／上下限／`accounting_basis` 皆為身分的一部分），要塞進「沒有數字」的紀錄就得把 `value` 放寬成 `float | None` 並鬆掉一串檢查——**那會讓真正的估值假設也失去那些檢查**。分開之後兩邊都能更嚴：`Abstention` 在 import 當下被掃描，**結構上不可能長出可裝數字的欄位**（L12 的標準修法：不是放寬也不是收緊，是先分開再各自定規則）。它不是第二份 authority——`ValuationAssumption` 擁有「目標倍數是幾」，`Abstention` 只擁有「我們不主張」。

**另外兩個已記錄、未排程的結構觀察**（各只有一個實例，先不做預防性抽象）：
分部軸沒有宣告（同一家公司的 `segment_revenue_share` 是產品線別、`fiscal_year_results.segment_revenue`
是所在地別）；日本基準的 `経常利益／特別損益` 兩層被壓進一格 `interest_and_other_net`。

⚠ **異地副本仍缺**（`drive: skipped`）。Pilot 已讓 `library/private/alpha/` 由 **7 檔變成 13 檔**
（assumptions ×3、valuation ×2、horizon ×2、judgments ×2，另 4 份 COHR 舊路徑檔），補上 Drive 上傳的價值只增不減。

⚠ **不得因為順手就在別的 Step 裡實作 A–H 的任何一項。**

---

### 明確不排程（理由已量測，勿重開）

- **`coverage_gaps` 走 DEPENDS_ON、以及 🔴 桶再往下分類**（2026-09-10 量測後否決）——五個 🔴 逐節點查邊：沒有一個是因為不走 `DEPENDS_ON` 造成的（`direct` 判定用 `-[]->` 任意型別本來就含它，`indirect` 的 `r2` 也已列出）。`tech:scale_up_cpo` 確實有 `DEPENDS_ON` 邊，但指向另一個 chokepoint 而非公司；順著走兩跳正是模組註解記過的 hub 假連結。**會變色的節點數 0** ＝沒改到 binding constraint（L14-1）。
  再往下分桶同樣否決：原提案以為那三個 `tech:scale_*` 來自 system-decompose，實測**五個 🔴 全部**帶 1–2 個 `source_ids`、全部有 `abstraction_level`、全部沒有 ABOUT 文件與 Claim——它們在圖裡沒有任何可機械分辨的差異。在沒有事實支撐的地方切一刀，得到的是會誤報的分類（L16-4）。已改為帶上 `degree` 與 `abstraction_level` 兩個零誤報的脈絡欄位。

| 項 | 為什麼不做 |
|---|---|
| **peer valuation／`config/peer_groups.json`**（2026-09-11 量測後否決） | 樣本結構上不足，**換一個分組軸也解決不了**。實測最新交易日 `pe_forward`：可映射到 `sector_anchors` 的四組裡，3 組 n≤2（一組 n=1）；最大的「AI 光互連／CPO」9 檔裡 7 個正值，範圍 **9.9x–33.2x（最高是最低的 3.3 倍）**，而且組內混著 IC 設計（AVGO）、晶圓代工（TSM）、化合物半導體基板（AXTI／IQE.L）、綜合電線電纜（5802.T）——**需求同源不代表估值可比**：分組軸是「誰的需求驅動你」，而本益比取決於「你的生意長什麼樣」，兩個軸正交。另有 2 檔本益比為負（IQE.L −224.8x、POET −47.1x），剔除虧損公司本身又是一個判斷。<br>取代它的是 **§B Reverse Bridge**：只用這一檔自己的價格與假設，不需要 peer 樣本。<br>查證：`python -c "import json;print(json.load(open('config/sector_anchors.json',encoding='utf-8'))['sectors'])"` ＋ Engine C `SELECT ticker,pe_forward FROM financial_snapshots WHERE bar_date=(SELECT MAX(bar_date) ...)` |
| `_only_system_internal_blockers` 的空集合分支 | 依 L14：改了 **0 筆**資料會變，且風險不對稱（會把 L7 的火警警報藏掉）。要動必須先出現真實實例 |
| 等待機制三套併入 Event Watch | 2026-09-02 實測三套的「假死」實例全為 **0/0/0**。**重構不得以「統一」為由推翻已量測結論** |
| 待辦池 evidence conflict 類型 | 史上最重度 drain 期 `open_conflicts` 仍為 **0**，滯留 0 |
| ETF 完整 look-through 管線 | 使用者定案：LLM 當下概算即可，明標「概算·未經查證」，不寫進 `issuer_loads` |
| Confidence 五軸重構為三類 | 「賠率類」要解的問題在無尺寸系統裡無載體。⚠ **Phase 4 完成後重評**——`expectation_gap_score` 某種程度就是賠率維度 |
| 技術指標擴充（RSI vs QQQ、ATR） | beta 訊號已整組拔除；新增動能指標違反「不得用動能指標表達水位」 |
| 貸款提款時間表／glide path 公式 | 使用者明確暫緩；要導入 glide path 需先定義總曝險口徑 |
| Parked lead 第二層召回（embedding） | false positive 消耗的是使用者注意力，而降低注意力噪音正是當初重構的目的。要做須先量測目前漏掉多少 |

---

## 研究主題範圍（2026-08-20 使用者定案）

**以 CPO 與 humanoid 兩條為主。** HBM／記憶體軸只做到 Micron 這筆入圖候選為止，
不再往下深挖；SK Hynix／Samsung 不主動 onboarding。使用者原話：HBM「太大了，
資金太瘋狂了，而且太寡占，感覺現在進去太晚了」。

判準仍有效——`tech:hbm` 確實是圖中最大的供給側空白——但**「是個真瓶頸」不等於
「現在該投」**：寡占程度、資金擁擠度與進場時點是使用者的判斷維度。

~~**優先序是主線／備援，不是並列。** 其他非 HBM 的 AI 瓶頸（SerDes、載板與中介層、測試）
**只有 CPO 與 humanoid 當輪沒有可推進的工作時才動**，不得因某節點 chokepoint 分數較高就插隊。~~

**修正（2026-09-09 使用者定案）：分散度優先於主線。** 目標是讓單檔判讀 ready 的檔數與**需求錨的數量**
大到能回答「全部是負的，是方法偏空還是市場太貴」。因此：①**新需求錨的 decompose 提案由系統自動鑄成
pq2（manual 型）**，來源是 lead 逐字點名的實體系統、weekly topic digest、使用者貼入的線索；廣度判準是
機械的——需求錨不在 `config/sector_anchors.json` 既有各組、或頂層節點不在圖裡；②使用者逐題核准後，
該錨與 CPO／humanoid **同級**，不再排在「沒工作時才動」之後；③同時 open 的 decompose 提案最多兩個，
drop 過的系統除非新 lead 再點名否則不重生（「建議只由 pool ground truth 導出」）。
上一段的「只有沒工作時才動」到此**廢止**（安靜消失擋不住下次回填）。HBM 那條不變：
SK Hynix／Samsung 仍不主動 onboarding——那是使用者對擁擠度的判斷，不是分散度問題。

⚠ humanoid 的可投資機會在**零組件供應商**不在整機（Agility 未上市、Boston Dynamics 屬 Hyundai）。

---

## 開工前必讀

### 已撤回的診斷

> **這一節不是自責，是一份檢查清單。** 每一筆都是「已經寫進 commit／ROADMAP／程式註解，
> 事後被推翻」的技術診斷——不是待辦、不是 bug，是**曾經看起來完全正確的錯誤結論**。
>
> **共同形狀：錯誤有方向性——全都朝「產生一個有洞察力的結論」偏**，而且每一個都能用專案
> 自己的 lesson 語言包裝（L12 一表兩義、L15 gate 攔錯東西）。
> **模式匹配是提出假說，不是確認假說。** 一個現象能被套進某條 L，只代表它值得查。
>
> **用法：** 宣稱「找到根因了」之前，先跑一條**試圖讓自己的結論變成假的**命令
> （不是驗證它為真——那是確認偏誤）。專案對每個 thesis 都強制 `disproof_condition`，
> 這一節是把同一個要求套到自己的技術診斷上。

| 日期 | 被推翻的診斷 | 一條就能否證它的命令 |
|---|---|---|
| 2026-08-19 | COHR「Engine C 的 `bar_date` 是憑空生成的、`price` 對不上任何收盤」 | `date(2026,8,17).strftime('%A')` → `Monday`。**一本日曆就能否證** |
| 2026-08-19 | 待辦池 `decision_review` 不退場是因為「空 `blockers` 被判成非純系統」 | `python -m decision_lab card <decision_id>` → `card.blockers` 有 **7 個碼**，不是空的 |
| 2026-08-19 | 「`execution_fx_stale_since_decision` 未登記，掉進泛用 prefix」 | 讀 `config/decision_blockers.json` 的 `_matching`（**最長**匹配，不是第一個）。真相是它早就以 exact prefix 登記 |
| 2026-08-19 | 「`live_choices` 仍為 0 筆，live 路徑從未被走過」——**直接引用自家文件** | `select count(*) from live_choices` → **1** |
| 2026-08-19 | 「`commercial_maturity` 積壓缺的是有人去讀年報附註」 | 逐一看 7 個積壓的 `missing_data` → 6 個是 `research_assessment_missing`。**靠讀年報能下降的是 0 個** |
| 2026-08-28 | 「COHR live reassess 失敗的根因是 `--as-of` 沒給」 | 修好 marker 後**不給 as-of 再跑一次** → marker 沒出現。真因在 `adapters.py::current_holdings` 另一處吞例外 |
| 2026-08-28 | 「`co:lumentum` 有兩個 cohort，重複偵測有漏」 | `sed -n '869,876p' decision_lab/store.py` → 註解逐字記著已檢查過這個確切案例。回空集合是正確行為 |
| 2026-08-28 | 「U2 把 `weakest_axis` 改成 level 排序是**零行為變化**的純重構」 | 改完直接 `pytest tests/test_probe_sizing.py` → `[missing_ref]` 立刻紅 |

**有可執行檢查的診斷活不過幾分鐘；沒有的全靠當下願不願意多查一步。**
（實測：U2「零行為變化」被測試抓到用了 3 分鐘；靠運氣發現的兩筆活到下一輪。）
所以落地前不是把診斷寫得更清楚，是**把診斷寫成一條會紅的檢查再落地**。

⚠ **這一節自己的 disproof：** 若之後仍發生「診斷已落地才被推翻」，代表它沒生效。
屆時該做的是把否證步驟綁進會自己執行的東西（測試、hook、commit 前檢查），
**不是把這張表寫得更長**。

### 看起來像缺口但不是——請勿「修正」

- **人工 runway 觀測寫入後 `financial_runway_manual_required` 仍亮，多半是 100 天鮮度窗，
  不要去改窗。** 那個窗刻意對齊財報節奏，**正解是用最新一季財報刷新觀測**。
  ⚠ runway 觀測的 `as_of` 應填**資產負債表日**，不是申報日。
- **5 個 cohort 的最新 `expiry` 仍是 `+72h` 預設值，不要去清。** 它們的 lifecycle 全部已
  `expired` 且 `catalyst_watch` 根本不顯示它們；依 L14，修它們會讓 **0 筆**下游資料變化。
  根因已由 `300b8e0` 修復並有測試防迴歸。
- **Beta 例行成交不進 Engine D 的 `record-fill`，這是設計正確。** `record_live_fill` 要求
  一整條責任鏈（decision → choice → fill），目的是回答「Engine D 的建議準不準」。
  beta 例行投入沒有 decision、沒有接受動作——**它是時間表不是決策**。硬塞會讓
  outcome attribution 變成把 QQQ 漲跌歸因給「今天是 15 號」。
  正確分工：beta → `library/trades/trade_log.jsonl`；alpha thesis 驅動 → 同時進 trade_log 與 Engine D fill。
- **`_bar_identity()` 的 ETL 不得加「info 與 history 不一致就 quarantine」的交叉驗證。**
  它會把完全正確的資料 quarantine 掉，正是 L15 說的「gate 攔下的不是它想攔的東西」。

---

## 想法怎麼變成程式

```
ROADMAP「開放 backlog」  →  docs/brainstorms/  →  docs/plans/  →  實作
     （還沒決定要做）        （需求與盲點審查）     （規格與驗收）
```

四階不是每次都要走完。判準是**改錯的成本**：小工作直接做；需要先想清楚需求與反面的走
brainstorm；範圍大到需要驗收條件才開 plan。`docs/plans/` 已轉純歷史
（見 [`plans/README.md`](plans/README.md)）。

## 什麼值得開發 / 什麼交給 Claude

**值得開發：** 知識累積（更多公司 onboarding、更多高品質文件——這是**研究方向**非開發項，
已由 `research-drain` 的閉包語意涵蓋）｜Skill 介面｜高槓桿 fetcher｜資料品質檢查。

**不值得自己開發：** 長文解讀｜Text2Cypher｜自動選文件頁面｜節點重要性評分｜公司識別
（Claude 做得更好）｜**自動代替使用者做最終投資決定或送單**（Engine D 可提出有邊界的建議，
但 live 接受、覆寫與 broker 下單永遠需要人工）。
